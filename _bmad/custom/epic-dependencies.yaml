# Epic dependency graph for the parallel /epic-cycle orchestrator (range 4-12, plus Epics 13 and 15 added 2026-09-19, Epic 23 chartered and Epics 14 and 16 added by the owner 2026-09-26; never 17).
# depends_on is the EFFECTIVE graph the dispatch loop reads: the owner-approved graph plus the
# edges the orchestrator's analysis added, per the owner's standing instruction (2026-09-16):
# "If your analysis adds an edge, take the more conservative graph and note the difference in the
# parallel log; if it would remove one of these, keep mine."
epics_md_hash: "8e8c90e4006ec71648837692b8a3bc63551bf83265dc8266590e242a49ffc12b"
analyzed_by: claude-opus-5
analyzed_at: "2026-09-16T15:02:34Z"
approved: true
approved_by: owner (graph pre-approved in the /epic-cycle 4-12 invocation, conservative additions pre-authorized)
amended_by: "owner (2026-09-16, epics.md 'Parallel-run amendments': 6.7 UJ-6 replay -> 7.6, 10.2 demo-prompt check -> 17.7, 8.7 -> 7.11; the four edges those references carried are removed below); owner (2026-09-17, epics.md 'Parallel-run amendments, 2026-09-17': 12.1 SSL test -> 9.5, prompts per descriptor, 7.7/7.9/9.4/9.6/11.5/11.6 -> 16.11-16.16; edges 11<-9, 12<-9, 12<-11 removed below); owner (2026-09-20, epics.md 'Owner amendment, 2026-09-20 (contest field)': Story 11.8 added, 17.6 and 7.6 amended; no dependency change)"

owner_graph:  # as approved, never reduced
  epic-4: [epic-3]
  epic-5: [epic-4]
  epic-6: [epic-2]
  epic-7: [epic-5, epic-6]
  epic-8: [epic-5, epic-6]
  epic-9: [epic-8]
  epic-10: [epic-3]
  epic-11: [epic-4, epic-5]
  epic-12: [epic-6, epic-8]
  epic-13: [epic-1]   # added 2026-09-19; Story 13.3 held until the owner releases
  epic-15: [epic-1]   # added 2026-09-19
  epic-23: [epic-11, epic-12]   # chartered 2026-09-26 by the orchestrator under Rule 27, after the submission cut
  epic-14: [epic-4, epic-11, epic-12]   # owner 2026-09-26: run extended to Epics 14 and 16 (never 17) after the submission cut
  epic-16: [epic-6, epic-11, epic-12]   # owner 2026-09-26: same
  epic-18: [epic-11, epic-12]   # owner 2026-09-26 (c242eac4): the whole epic joins the voting-week run on slot B after Epic 14; 18.1 on slot A ahead of 16.4 unless slot B reaches Epic 18 first

added_edges:
  hard:
    - {epic: 10, depends_on: 4, evidence: "epics.md:4573 Story 10.2 'a turn completes with tool calls' (Epic 4 turn + tool registry); 10.3 edits the context chip from Story 4.4"}
    - {epic: 11, depends_on: 10, evidence: "epics.md:4759-4761 Story 11.7 ships only if build step 7 (Epic 10) finished; epics.md:823"}
    - {epic: 11, depends_on: 6, evidence: "epics.md:4651 Story 11.2 explains rows of the log viewers built by Stories 6.13/6.14"}
    - {epic: 12, depends_on: 7, evidence: "epics.md:3951 Story 7.3 'So that the area has a write before its editors ship'; 12.2 revoke sits on 7.2's Users row menu"}
    - {epic: 9, depends_on: 7, evidence: "epics.md:3932 Story 7.2 actions available from both the row menu and the 9.1 editor (also implied transitively via 8->7)"}
  removed_by_owner_amendment_2026-09-16:  # the story text that carried each edge moved (see epics.md, Parallel-run amendments)
    - {epic: 6, depends_on: 5, evidence: "epics.md:3739-3741 Story 6.7 re-points 5.11's navigation target and replays UJ-6 end to end; epics.md 5.11 says 'Task details does not exist until Story 6.7'; 6.7 AC2 pauses refresh while a proposal (Epic 5) is live"}
    - {epic: 8, depends_on: 7, evidence: "epics.md:4265-4267 Story 8.7 acts on rows of the system-event list that Story 7.4 (epics.md:3981) first builds; no earlier epic builds the Auditing configuration screen"}
    - {epic: 10, depends_on: 5, evidence: "epics.md:4576-4577 Story 10.2 runs the UJ-3 demo prompt (Story 5.8's confirmed write) against each provider"}
    - {epic: 10, depends_on: 6, evidence: "epics.md:4576-4577 Story 10.2 runs the UJ-6 demo prompt, completed by Story 6.7"}
  removed_by_owner_amendment_2026-09-17:  # epics.md 'Parallel-run amendments, 2026-09-17': 12.1 SSL test -> 9.5, LDAP test follows 9.6 to 16.14, prompts declared per descriptor (11.3), tab component shared by whichever of 9.1/12.4 lands first
    - {epic: 12, depends_on: 9, evidence: "epics.md:4455 Story 9.6 defers the LDAP test action to 12.1; epics.md:4785 Epic 12 editors use the form-page tabs contract Story 9.1 sets"}
    - {epic: 11, depends_on: 9, pairs: "7-11 0.34, 8-11 0.32, 9-11 0.33", why: "Story 11.3 declares suggested prompts in every screen descriptor that 7/8/9 edit or add; 11.2 and 7.10 both land on the error-log drill-down"}
    - {epic: 12, depends_on: 11, pairs: "11-12 0.31", why: "11.3 prompts go into 12's editor descriptors; 12.3 and 11.2 both land on the audit viewer; order follows the Epic List's polish-week ranking (12 first since the owner's re-sequence of 2026-09-21)"}
  soft:  # footprint_overlap_threshold 0.20 exceeded with semantic overlap (estimates, inference)

# SHARED-CREATE DIRECTORIES (orchestrator correction 2026-09-20).
# Two paths below appear in one epic's paths_hint but are structurally shared by every epic, and
# listing them as if exclusive was an orchestrator error caught by Epic 5:
#
#   src/OcuPilot/Test/**   -- the only home for %UnitTest classes in this project (282 files on
#                             feature; Epic 5 alone added 28 across stories 5.0-5.3). Treating it
#                             as Epic 13's exclusive footprint would make Rule 19 IMPOSSIBLE for
#                             every ObjectScript AC in every other epic: the code could be written
#                             but not pinned, which the project's rules treat as worse than not
#                             shipping. Story 13.2's charter is the suite IN CI -- ui/tools/ci*.mjs,
#                             .github/workflows/**, scripts/ -- not the per-story classes each epic
#                             writes.
#   ui/src/app/core/**     -- Epic 15 holds it for the shell conveniences, but Epic 5 created
#                             core/proposal-view.ts and writes core/turn.ts's proposal publisher.
#
# The rule for both: every epic CREATES files there freely (a path that did not exist cannot
# collide). Modifying a file another epic created or has modified is a Clarification. This is the
# same rule already used elsewhere; the error was never writing it down for these two paths.
epics:
  epic-4:
    depends_on: [epic-3]
    submodules: []
    paths_hint: ["src/OcuPilot/Kernel/Agent/**", "src/OcuPilot/Kernel/State/**", "src/OcuPilot/Kernel/Audit/**", "src/OcuPilot/Kernel/Restraint.cls", "src/OcuPilot/Kernel/Utils.cls", "src/OcuPilot/Kernel/Egress.cls", "src/OcuPilot/Kernel/Secret/Ladder.cls", "src/OcuPilot/Kernel/Provider/**", "src/OcuPilot/Port/ProviderPort.cls", "src/OcuPilot/Port/AdminPort.cls", "src/OcuPilot/Screen/Tool/**", "src/OcuPilot/Screen/Read.cls", "src/OcuPilot/Screen/Registry.cls", "src/OcuPilot/Screen/Descriptor/Base.cls", "src/OcuPilot/Screen/Descriptor/Home.cls", "src/OcuPilot/Api/Turn*.cls", "src/OcuPilot/Api/Router.cls", "src/OcuPilot/Api/Error.cls", "src/OcuPilot/Api/Definitions.cls", "src/OcuPilot/Install/Installer.cls", "src/OcuPilot/Install/Smoke.cls", "src/OcuPilot/Test/**", "scripts/check-objectscript.py", "scripts/test_check_objectscript.py", "ui/src/app/shell/**", "ui/src/app/app.ts", "ui/src/app/app.spec.ts", "ui/src/app/core/**", "ui/src/app/areas/home/**", "ui/src/styles/**", "ui/package.json", "ui/package-lock.json", "ui/angular.json", "ui/tools/**", "ui/browser/**", "ui/src/app/app.ts"]
  # CARVE, narrowed 2026-09-20 (orchestrator correction, third of its kind this run).
  # Epic 5 holds the PROPOSAL AND TURN SURFACE, named as files rather than as a glob:
  #   ui/src/app/shell/{panel.ts,panel.spec.ts,proposal-card*,reply*,tool-call-card*}
  #   ui/src/app/core/{proposal-view.ts,turn.ts}
  # RELEASED from the carve: ui/src/app/shell/panel-resize-handle* -- it matched the old
  # "shell/panel*" glob BY NAME ONLY. Epic 4 created it (Story 4.3) and has merged, and Epic 5
  # never touched it across six stories, so it is trunk under the ordinary rules.
  # The glob caught a file the reason behind the carve does not reach. Three carves of mine this
  # run forbade something they were never meant to: Test/** would have made Rule 19 impossible
  # for every ObjectScript AC; ui/browser/** left an Integration AC with no verification; this
  # one would have forced an AC amendment or a localStorage copy AD-50 says FAILS the criterion.
  # Draw a carve from the files a reason reaches, not from a name they happen to share.
  epic-5:
    depends_on: [epic-4]
    submodules: []
    paths_hint: ["src/OcuPilot/Kernel/Proposal/**", "src/OcuPilot/Kernel/State/**", "src/OcuPilot/Kernel/Agent/**", "src/OcuPilot/Kernel/Audit/**", "src/OcuPilot/Kernel/Restraint.cls", "src/OcuPilot/Screen/Tool/**", "src/OcuPilot/Screen/Read.cls", "src/OcuPilot/Screen/Registry.cls", "src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Port/AdminPort.cls", "src/OcuPilot/Port/LogSourcePort.cls", "src/OcuPilot/Api/**", "src/OcuPilot/Install/**", "src/OcuPilot/Test/**", "ui/tools/**", "ui/src/app/shell/**", "ui/src/app/core/**", "ui/src/app/areas/logs/**", "ui/src/styles/**", "ui/browser/**"]
  epic-6:
    depends_on: [epic-2]
    submodules: []
    paths_hint: ["src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Screen/Read.cls", "src/OcuPilot/Screen/Registry.cls", "src/OcuPilot/Screen/Area.cls", "src/OcuPilot/Port/MonitorPort.cls", "src/OcuPilot/Port/MgmntPort.cls", "src/OcuPilot/Port/AdminPort.cls", "src/OcuPilot/Port/LogSourcePort.cls", "src/OcuPilot/Api/**", "src/OcuPilot/Install/**", "src/OcuPilot/Test/**", "ui/src/app/areas/**", "ui/src/app/shell/**", "ui/src/app/core/**", "ui/tools/**", "ui/src/styles/**", "ui/browser/**"]
  # Owner amendment 1d31fe2 (2026-09-20): Story 7.6 carries a vendor-quirk note that MUST reach
  # Epic 7's runner prompt at dispatch. Recorded as prompt_notes below rather than only in
  # epics.md, because Epic 7 dispatches after Epic 5 merges -- possibly in a later session --
  # and a note that lives only in an orchestrator's context does not survive that.
  epic-7:
    depends_on: [epic-5, epic-6]
    prompt_notes:
      - story: "7.6"
        source: "owner amendment 1d31fe2, 2026-09-20; from another entry's validation notes, IRIS 2026.2"
        note: >-
          The task list's `Suspended` field does not reflect a suspend or resume that has just been
          applied, while the task's own info read does. So the in-place row update and the write's
          VERIFICATION must both read task info, never the list's field. A verification that reads
          the list field sees stale data -- which fails a correct write, or worse passes a wrong one.
    submodules: []
    paths_hint: ["src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Screen/Tool/**", "src/OcuPilot/Kernel/Proposal/**", "src/OcuPilot/Kernel/Restraint.cls", "src/OcuPilot/Port/AdminPort.cls", "src/OcuPilot/Port/LogSourcePort.cls", "src/OcuPilot/Api/**", "src/OcuPilot/Install/Smoke.cls", "src/OcuPilot/Test/**", "ui/src/app/areas/security/**", "ui/src/app/areas/permissions/**", "ui/src/app/areas/tasks/**", "ui/src/app/areas/os-management/**", "ui/src/app/areas/logs/**", "ui/src/app/shell/**", "ui/src/app/core/**", "ui/tools/**", "ui/src/styles/**", "ui/browser/**"]
  epic-8:
    depends_on: [epic-5, epic-6]
    submodules: []
    paths_hint: ["src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Screen/Tool/**", "ui/tools/field-lists.mjs", "ui/tools/field-lists.test.mjs", "scripts/field-lists.sh", "src/OcuPilot/Port/AdminPort.cls", "src/OcuPilot/Api/**", "src/OcuPilot/Install/**", "src/OcuPilot/Kernel/Secret/Ladder.cls", ".github/workflows/ci.yml", "scripts/ci-*.sh", "README.md", "src/OcuPilot/Test/**", "ui/src/app/areas/web-applications/**", "ui/src/app/areas/permissions/**", "ui/src/app/areas/security/**", "ui/src/app/areas/os-management/**", "ui/src/app/shell/**", "ui/src/app/core/**", "ui/tools/**", "ui/src/styles/**", "ui/browser/**"]
  epic-9:
    depends_on: [epic-7, epic-8]
    submodules: []
    paths_hint: ["src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Screen/Tool/**", "ui/tools/field-lists.mjs", "scripts/check-objectscript.py", "src/OcuPilot/Port/AdminPort.cls", "src/OcuPilot/Api/**", "src/OcuPilot/Install/Smoke.cls", "src/OcuPilot/Test/**", "ui/src/app/shell/**", "ui/src/app/areas/permissions/**", "ui/src/app/areas/web-applications/**", "ui/src/app/areas/tasks/**", "ui/src/app/areas/security/**", "ui/src/app/core/**", "ui/tools/**", "ui/src/styles/**", "ui/browser/**"]
  # 2026-09-19: context-chip corrected from "ui/src/app/shell/panel/context-chip*" -- shell/panel/ is not a
  # directory; the component is the flat file shell/context-chip.ts. Keep notes OUT of the flow sequence
  # below: a `#` inside a single-line [...] comments out the rest of the line, closing bracket included.
  epic-10:
    depends_on: [epic-3, epic-4]
    submodules: []
    deferred_stories:   # owner triage 2026-09-23 (providers), HIGH PRIORITY
      - story: "10-4-sampling-parameters-left-to-the-provider"
        after: epic-8        # released now (epic-8 merged); one runner with 10.5 on the FIRST slot to free, ahead of epic-12, story-15-8, epic-11
        reason: >-
          The canonical temperature 0 is refused by the default model of Anthropic (sampling parameters removed) and of
          OpenAI (only 1 accepted), found with live keys on slot C. Send none unless set; never to Anthropic.
      - story: "10-5-a-connection-test-that-answers-before-the-gateway-does"
        after: epic-8        # same runner, after 10.4
        reason: >-
          Test connection on a cold local model is cut off by the Web Gateway's 60 s timeout with an empty 504.
      - story: "10-6-a-turn-survives-a-dropped-connection-and-a-create-says-creat"
        after: epic-8        # owner triage 2026-09-24, HIGH: runs ALONE on slot B at epic-12's next story boundary, then epic-12 resumes
        reason: >-
          Live Opus 5 turns failed 3 in ~10 on a transport <READ> within 1 s, never retried; and a confirmed create
          reported "was updated". Retry a model call once on a fresh connection; SocketTimeout 0; fix the create sentence.
      - story: "10-7-claude-opus-5-5-as-the-anthropic-default"
        after: epic-8        # owner triage 2026-09-25, HIGH: first slot to free; the submission cut waits for it
        reason: >-
          Claude Opus 5.5 released; the demo runs on it. The catalog's Anthropic defaultModel becomes claude-opus-5-5
          (suggestions keep the others); tests pinning the row and the README's provider table follow. No live key needed.
    paths_hint: ["src/OcuPilot/Kernel/Provider/**", "src/OcuPilot/Port/ProviderPort.cls", "src/OcuPilot/Kernel/AgentRules.cls", "src/OcuPilot/Kernel/Egress.cls", "src/OcuPilot/Kernel/State/Agent.cls", "src/OcuPilot/Api/Definitions.cls", "src/OcuPilot/Api/Error.cls", "src/OcuPilot/Screen/Tool/Registry.cls", "src/OcuPilot/Test/OpenAI*.cls", "src/OcuPilot/Test/Gemini*.cls", "src/OcuPilot/Test/Compatible*.cls", "src/OcuPilot/Test/Adapter*.cls", "src/OcuPilot/Test/CatalogProbe*.cls", "src/OcuPilot/Test/AgentRules*.cls", "src/OcuPilot/Test/Provider*.cls", "src/OcuPilot/Test/Egress*.cls", "src/OcuPilot/Test/Anthropic*.cls", "src/OcuPilot/Test/AgentConnection*.cls", "src/OcuPilot/Test/AgentWire*.cls", "src/OcuPilot/Kernel/Provider/Catalog.cls", "ui/src/app/app.routes.guard.wire.spec.ts", "ui/src/app/areas/agent/definition-form.*", "ui/src/app/shell/context-chip.ts", "ui/src/app/core/strings.ts", "ui/tools/strings.test.mjs", "ui/browser/definitions.browser-spec.mjs", "README.md", "ui/src/app/shell/panel*", "ui/src/app/shell/proposal-card*", "ui/src/app/core/toasts.ts"]
  epic-11:
    depends_on: [epic-4, epic-5, epic-6, epic-10]
    submodules: []
    deferred_stories:   # owner request 2026-09-24 (agent grounding), HIGH PRIORITY
      - story: "11-9-the-agent-knows-the-screen-it-is-on"
        after: epic-10       # released now; runs FIRST and ALONE on slot B -- epic-12 hands back at its first boundary and resumes after 11.9 merges
        reason: >-
          The prompt never mentions the screen context, every tool is offered on every screen, and the model is not
          told it is read-only. Screen/Registry.cls is shared with Epic 9 (read the other branch first, append only).
      - story: "11-10-a-judge-succeeds-the-first-time"
        after: epic-10       # released now; runs ALONE on slot B at epic-12's next story boundary, then epic-12 resumes
        reason: >-
          New definitions start read-only, so a judge's first change request is declined; and the agent does not open
          the screen under discussion. Read/write default (Kernel/State/Agent.cls, definition-form store) + a prompt statement.
      - story: "11-11-the-screen-shows-what-the-agent-is-talking-about"
        after: epic-10       # owner triage 2026-09-26, HIGH: ALONE on slot A now; release 1.0.1 is cut from feature at this story's merge
        reason: >-
          The audit database and all-task history open on an empty criteria form, so the agent describes rows the person
          cannot see. Default search on open (24 h / 7 days) and agent navigation that carries its read's criteria.
    paths_hint: ["ui/src/app/shell/**", "ui/src/app/core/**", "ui/src/app/areas/logs/**", "ui/src/app/areas/tasks/**", "src/OcuPilot/Screen/Descriptor/AuditList.cls", "src/OcuPilot/Screen/Descriptor/TaskHistoryList.cls", "src/OcuPilot/Kernel/State/Agent.cls", "ui/src/app/areas/agent/definition-form.*", "src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Screen/Registry.cls", "ui/tools/**", "src/OcuPilot/Api/**", "src/OcuPilot/Kernel/Audit/**", "src/OcuPilot/Kernel/Agent/**", "src/OcuPilot/Kernel/Provider/**", "src/OcuPilot/Port/ProviderPort.cls", "src/OcuPilot/Kernel/Egress.cls", "src/OcuPilot/Install/Smoke.cls", "src/OcuPilot/Test/**", "ui/src/styles/**", "ui/browser/**"]
  epic-12:
    depends_on: [epic-6, epic-7, epic-8]
    submodules: []
    paths_hint: ["src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Screen/Tool/**", "ui/tools/field-lists.mjs", "ui/tools/field-lists.test.mjs", "ui/tools/classic-links.mjs", "ui/tools/classic-links.test.mjs", "src/OcuPilot/Port/AdminPort.cls", "src/OcuPilot/Api/**", "src/OcuPilot/Install/Smoke.cls", "src/OcuPilot/Test/**", "ui/src/app/areas/security/**", "ui/src/app/areas/permissions/**", "ui/src/app/areas/logs/**", "ui/src/app/shell/**", "ui/src/app/core/**", "ui/tools/**", "ui/src/styles/**", "ui/browser/**"]
  # 2026-09-19: dropped a bogus paths_hint entry "ui/src/**/*.browser.spec.ts" -- wrong directory,
  # wrong extension and wrong separator, so it matched nothing. The real specs are the 34
  # ui/browser/*.browser-spec.mjs files, which are Epic 5's. Epic 13 therefore has NO browser-spec
  # footprint, by design rather than by accident: DW-1015, DW-1146 and DW-1223 are Epic 5's to fix.
  epic-13:
    depends_on: [epic-1]
    submodules: []
    hold_stories: ["13-3-publish-the-package-to-the-community-registry"]
    # HARD HOLD, strengthened 2026-09-19 on the owner's instruction. Plan and implement everything up
    # to the publish; do NOT publish. The owner releases by hand. Story 13.3's acceptance criteria in
    # epics.md were rewritten so none of them can be closed by publishing (the archive is proven by a
    # dry-run build plus a local install), because the earlier wording -- "when it is published to the
    # community registry, then it installs from there" -- was satisfiable ONLY by publishing, and a
    # conscientious runner would have closed its own gate by doing the irreversible thing.
    # Every Epic 13 spawn prompt must carry, verbatim: the publish path may be exercised only in its
    # dry-run or local form; no registry credential or token may be read, configured, requested or
    # used; 13.3 may not be reported done by having published; and a gate closable only by publishing
    # is an UNMET gate, to be escalated as a Clarification rather than closed.
    paths_hint: ["src/OcuPilot/Install/**", ".github/workflows/ci.yml", "scripts/ci-*.sh", "scripts/smoke.sh", "ui/tools/ci*.mjs", "ui/tools/client-lint.mjs", "ui/browser.config.mjs", "src/OcuPilot/Test/**", "module.xml", "spec/**"]
  epic-15:
    depends_on: [epic-1]
    submodules: []
    deferred_stories:
      - story: "15-6-the-light-and-dark-theme"
        after: epic-5        # released automatically once epic-5 MERGES -- not an owner hold
        reason: >-
          15.6 edits ui/src/styles/** token files that Epic 5 is editing to style the proposal card,
          and its contrast guards run in both modes. Owner decision 2026-09-19: sequence it after
          Epic 5 lands. Epic 15 ships 15.1-15.5 and reports done with 15.6 left in backlog; the
          orchestrator dispatches 15.6 afterwards. It is NOT dropped -- see pending_units in
          .worktrees/.coordination/dispatch.yaml, which is what stops it being forgotten.
      - story: "15-7-the-rail-s-icons"
        after: epic-5        # owner triage 2026-09-23: dispatched with 15.6 in the window after the FIRST of epic-7/epic-8 merges
        reason: >-
          The rail and Home tiles take the mockup's eight icons (UX-DR15 as amended). ui/src/app/shell/rail* and
          ui/src/app/areas/home/** are touched by no floor epic.
      - story: "15-8-columns-you-can-read"
        after: epic-7        # owner triage 2026-09-23: Epic 7 edits ui/src/app/shell/data-table*; ranked after epic-12, before epic-11
        reason: >-
          Resizable, content-sized columns, horizontal scroll inside the table and a full-value tooltip change
          the shared data table every list uses.
      - story: "15-9-sign-out-where-people-look-and-no-filter-where-there-is-noth"
        after: epic-7        # owner triage 2026-09-24: same runner as 15.8, after it
        reason: >-
          Sign-out only in the status bar's account menu and no Cmd+K sign-out; the command bar's filter renders on
          screens with no read. ui/src/app/shell/command-bar* is shared (Epics 9 and 12 add actions): append only.
      - story: "15-10-the-header-logo-on-a-white-tile"
        after: epic-12       # owner triage 2026-09-25, HIGH: ALONE on slot B now (free since Epic 12 merged); the submission cut waits for it
        reason: >-
          The header's reversed lockup fringes against the navy chrome; the navy-wordmark lockup goes on a white rounded
          tile. Touches ui/src/app/shell/header*, ui/src/styles/_components.scss and ui/src/assets/lockup/ only.
    # Owner carve 2026-09-19: Epic 15 works OUTSIDE ui/src/app/shell/panel, proposal-card, reply and
    # tool-call-card, which Epic 5 owns. Its own shell surfaces are header*, account-menu*, side-bar*
    # and command-box*.
    paths_hint: ["ui/src/app/shell/header*", "ui/src/app/shell/account-menu*", "ui/src/app/shell/side-bar*", "ui/src/app/shell/command-box*", "ui/src/app/core/**", "ui/src/styles/**", "src/OcuPilot/Screen/Descriptor/Home.cls", "src/OcuPilot/Api/Account*.cls", "src/OcuPilot/Api/Ui*.cls", "ui/src/app/shell/rail*", "ui/src/app/areas/home/**", "ui/src/app/shell/data-table*", "ui/src/app/shell/status-bar*", "ui/src/app/shell/command-bar*", "ui/src/app/app.ts", "ui/src/assets/**"]
  epic-23:
    depends_on: [epic-11, epic-12]   # Rule 27: chartered after Epic 12 merges; runs after the submission cut (owner 2026-09-25)
    submodules: []
    paths_hint: ["src/OcuPilot/**", "ui/src/**", "ui/tools/**", "ui/browser/**", "scripts/**", "docs/**", "_bmad-output/planning-artifacts/**"]
    note: >-
      One range-end cleanup story over the ledger slice owner=range-end-cleanup (178 entries at the charter).
      It spans the range, so its paths_hint is broad; a concurrent epic's files are contended-edit for it.
  epic-14:
    depends_on: [epic-4, epic-11, epic-12]
    submodules: []
    story_order: ["14-1", "14-8 BEFORE 14-3 (14.3 AC2 needs 14.8, epics.md:5709-5711)", "14-3", "14-5", "14-4", "14-2 (released 1756c380: shipped post-freeze write keys join the baseline, enabled; a registered write key missing from the list fails a test)", "14-6 (released 1756c380: strings authored in the story; default no limit)"]
    paths_hint: ["src/OcuPilot/Kernel/Governance/**", "src/OcuPilot/Kernel/Agent/**", "src/OcuPilot/Kernel/Proposal/**", "src/OcuPilot/Kernel/Audit/**", "src/OcuPilot/Kernel/State/**", "src/OcuPilot/Kernel/Restraint.cls", "src/OcuPilot/Api/Conversation.cls", "src/OcuPilot/Api/Turn*.cls", "src/OcuPilot/Api/Switches.cls", "src/OcuPilot/Api/Ledger.cls", "src/OcuPilot/Screen/Tool/**", "src/OcuPilot/Install/Installer.cls", "src/OcuPilot/Test/**", "ui/src/app/shell/**", "ui/src/app/areas/agent/**", "ui/src/app/core/**"]
  epic-16:
    depends_on: [epic-6, epic-11, epic-12]
    submodules: []
    story_order: ["16-1", "16-17", "16-18", "16-19", "16-20", "16-21", "16-22", "16-8 BEFORE 16-9 (16.9 lists 16.8's sources, epics.md:6265)", "16-9", "16-3", "16-16", "rest of 16", "16-4 AFTER Story 18.1 merges (owner 2026-09-26: 18.1 brought into the voting week ahead of 16.4; epics.md:6175)"]
    paths_hint: ["src/OcuPilot/Screen/Descriptor/**", "src/OcuPilot/Screen/Tool/**", "src/OcuPilot/Screen/Read.cls", "src/OcuPilot/Screen/Registry.cls", "src/OcuPilot/Port/**", "src/OcuPilot/Api/**", "src/OcuPilot/Kernel/Proposal/**", "src/OcuPilot/Install/**", "src/OcuPilot/Test/**", "ui/src/app/areas/**", "ui/src/app/shell/**", "ui/src/app/core/**", "ui/tools/**", "ui/src/styles/**", "ui/browser/**"]
    note: >-
      Measured 2026-09-26 (orchestrator subagent, read-only): no Epic 16 write needs 14.2 first - Kernel/Governance/Gate.cls
      Decide allows every registered tool today (lines 17-23), so each write can be built and tested before 14.2.
  epic-18:
    depends_on: [epic-11, epic-12]
    submodules: []
    in_run: all   # owner 2026-09-26 (c242eac4): the whole epic, in epics.md story order
    story_order: ["18-1 (slot A ahead of 16-4, unless slot B reaches Epic 18 first)", "18-2", "18-3", "18-4 (destructive keys default DISABLED under 14.2's baseline rule)", "18-5", "18-6", "18-7", "18-8", "18-9", "18-10", "18-11", "18-12", "18-13"]
    slot: "B after Epic 14 (14.1, 14.8, 14.3, 14.5, 14.4, 14.2, 14.6)"
    gate: "Stage 2 gate amended at source (c242eac4): build against the pinned 2026.2 image; re-derive the inventory fixture when 2027.1 ships"
    paths_hint: ["src/OcuPilot/Kernel/**", "src/OcuPilot/Api/**", "src/OcuPilot/Port/**", "src/OcuPilot/Screen/**", "src/OcuPilot/Install/**", "src/OcuPilot/Test/**", "ui/src/app/areas/**", "ui/src/app/shell/**", "ui/src/app/core/**", "ui/tools/**", "ui/browser/**", "ui/src/styles/**"]
