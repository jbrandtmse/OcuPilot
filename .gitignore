# macOS
.DS_Store

# Keep the iris-data folder in the repo, but ignore everything inside it
iris-data/*
!iris-data/.gitkeep

# Exported IRIS source pulled from the HSCUSTOM namespace (regenerate via iris_doc_export)
irislib/

# Exported IRIS source pulled from the %SYS namespace (regenerate via iris_doc_export)
irissys/

# Angular front-end bundles extracted from the IRIS System Management Portal (see irisui/README.md)
irisui/

# Offline mirror of the official InterSystems %Api documentation (regenerate: python3 scripts/fetch-irisdocs.py)
irisdocs/

# Local secrets — never commit
.env
.env.*
!.env.example

# Angular build outputs — keep them out of the 385-file tracked tree
ui/node_modules/
ui/dist/
ui/.angular/
ui/out-tsc/

# Python bytecode left behind by scripts/*.py (check-objectscript, check-prose). An
# untracked __pycache__/ dirties the working tree, and the epic cycle refuses to
# dispatch on a dirty tree.
__pycache__/
*.pyc

# Parallel /epic-cycle epic worktrees (orchestrator mode)
.worktrees/
