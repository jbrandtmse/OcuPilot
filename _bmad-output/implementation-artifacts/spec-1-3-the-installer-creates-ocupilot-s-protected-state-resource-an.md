---
title: "Story 1.3 — The installer creates OcuPilot's protected state, resource and role"
type: 'feature'
created: '2026-09-09'
status: 'done'
baseline_revision: '75f90e1468ac9a4d8cf638f36506f782a2ce1432'
baseline_commit: '75f90e1468ac9a4d8cf638f36506f782a2ce1432'
review_loop_iteration: 0
followup_review_recommended: true
context:
  - '{project-root}/_bmad-output/planning-artifacts/architecture/architecture-OcuPilot-2026-09-08/ARCHITECTURE-SPINE.md'
  - '{project-root}/.claude/rules/objectscript-basics.md'
  - '{project-root}/.claude/rules/objectscript-testing.md'
  - '{project-root}/.claude/rules/iris-persistent-storage.md'
warnings: ['oversized']
deferred: []
---

<intent-contract>

## Intent

**Problem:** OcuPilot has no installer and no protected storage. Its agent definitions, switches, ledger and transcripts would land in the install namespace's ordinary database, where any holder of `%DB_<install-namespace>:RW` plus `%Admin_Operate` could read or forge them — the developers the portal is for. FR-29's acceptance test would fail, and there is nowhere for Stories 1.4, 1.5 and Epics 3 and 5 to put state.

**Approach:** One `Installer` class under `Install/` creates, guard-then-act and idempotently, a dedicated database holding OcuPilot's **globals only**, a `%DB_` resource guarding it that no ordinary role holds, the OcuPilot administrative resource and a role granting it, a privileged routine application, and one global mapping — leaving OcuPilot's **code** in the install namespace's normal database. One class in `Kernel/State/` is the sole escalation point: it obtains the guarding role through that application inside a `New $ROLES` frame, and one `%Persistent` class riding on it proves the protection from both sides — a denied user is refused on a global and on a table, an escalated caller succeeds on the same two.

## Boundaries & Constraints

**Always:**

- **Protect the data, never the code (AD-9).** Only globals move to the guarded database. No package mapping and no routine mapping is created; database READ is routine-execution permission, so hiding the packages would make OcuPilot unrunnable by exactly the users it serves.
- **One escalation point.** `New $ROLES` and `$SYSTEM.Security.AddRoles(...)` appear in `src/OcuPilot/Kernel/State/Base.cls` and in the denial-probe test class, and nowhere else in the tree. Nothing inside an escalated frame spawns a `JOB` or calls into `Api`, `Port`, `Screen` or `Area`.
- **`New $ROLES` unwinds with the method frame, not the block.** Escalation and the guarded access happen in the *same* method; a helper that escalates and returns has already lost the role.
- **Guard-then-act, and verify-and-repair (AD-17).** Every object is guarded by an existence check; on the existing-object path the installer still compares the live properties against the intended ones and repairs drift. Existence alone is not "installed".
- **Namespace by explicit save and restore (AD-16)**, through `OcuPilot.Kernel.Utils.SwitchNamespace` / `RestoreNamespace`, with the restore as the **first line of every `Catch`**. `New $NAMESPACE` never appears. `Security.*`, `Config.*` and `SYS.Database` exist only in `%SYS`.
- **Reuse Story 1.1's surface.** All installer reporting goes through `OcuPilot.Kernel.Audit.Log`; no class here may contain a bare `Write`.
- **Every `%Status` is checked** with `$$$ISERR`; `Set tSC = $$$OK` first line, `Quit tSC` last; argumented `Quit` never inside `Try`/`Catch`.
- **Weak references (AD-37).** The username the administrative role was granted to is stored as data on the install stamp, never as a foreign key, and a stamp naming a user who no longer exists reads as "no longer present" rather than failing.
- **The database directory comes from `$System.Util.ManagerDirectory()` at runtime (AD-21)** — never from a caller, never cached, never a literal path.
- Class names, package dots included, are **29 characters or fewer**; `iris_`, `IRIS_*`, `%Atelier`, `IRISCouch`, `SessionAgent`, `ExecuteMCPv2` are forbidden substrings anywhere under `src/OcuPilot/`.

**Never:**

- **No web applications.** `/ocupilot` and `/api/ocupilot` are Story 1.5's, wired by 1.4. The only `Security.Applications` entry created here is the privileged **routine** application (`Type = 4`), which the CSP Gateway never serves — so AD-17's Gateway-registration report belongs to 1.4/1.5, not here.
- **No container start hook, no image pin, no demo fixture, no `_SYSTEM` unexpire, no schema-version stamp and no "installing"/"upgrade required" refusal.** All Story 1.4. This story ships the class 1.4 invokes.
- **No agent definition is seeded** (FR-66), so the first-login gate is active on first login.
- **No hardcoded grant to a vendor account.** The sibling's unconditional `_SYSTEM` grant is not copied.
- **No hand-rolled role read-modify-write** — use `Security.Users.AddRoles()`.
- **No hand-written Storage section**, no `list Of` property, no dynamic class-method invocation by name (Story 1.1 declined `InvokeWithArgs` as an avoidable surface under AD-8; do not reintroduce it).
- **No test calls `Uninstall("")`.** The production objects are this story's deliverable and Stories 1.4+ inherit them.
- **No test changes the instance's `AuditEnabled` setting.**

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Fresh install | Profile `""`; none of the named objects exist | Database `OCUPILOT` at `<ManagerDirectory>ocupilot/` guarded by `%DB_OCUPILOT`; resource+role `OcuPilotAdmin`; privileged routine application `OcuPilotState`; one global mapping `OcuPilot*` → `OCUPILOT` in the install namespace; audit event `OcuPilot/Security/RoleGranted`; auditing enabled; `$$$OK` | Any failure returns the underlying `%Status`; `$NAMESPACE` restored first in the `Catch` |
| Re-install (upgrade path) | Every object already exists and is correct | `$$$OK`; the ensure steps still run; `StateFingerprint()` is byte-identical to the first run | As above |
| Drifted object | The privileged routine application exists with `Enabled = 0` | The second run repairs it to `Enabled = 1` and reports the repair through the logger | Repair failure returns an error status, not a silent OK |
| Denied user, global | A user holding `%DB_<install-ns>:RW` and `%Admin_Operate:U` through a purpose-built role, not `OcuPilotAdmin`, not `%DB_OCUPILOT` | Direct read **and** write of `^OcuPilot…` both raise `<PROTECT>` | The exception is captured inside the impersonating frame and asserted outside it |
| Denied user, table | Same user, `SELECT` and `INSERT` against the stamp table | `%SQLCODE < 0` on both | Assert the code observed, and pair with the escalated success below so a mistyped table cannot pass as a denial |
| **Integration AC** — escalated consumer | `OcuPilot.Kernel.State.Stamp` writes a row and reads it back through `OcuPilot.Kernel.State.Base` | The row round-trips with its values intact; the same row is invisible to the denied user in the two rows above | A failed escalation returns an error status; the frame still unwinds |
| Escalation frame | Inside `Base`'s guarded method | `%DB_OCUPILOT` is present in `$ROLES` inside the frame and absent immediately after the method returns, on the normal path **and** after an exception | Exception is re-raised as a `%Status`; role never survives |
| Grant, real account | `$Username` is a named account that exists | The role is granted (idempotently), the outcome and the username are recorded on the stamp, and an `OcuPilot/Security/RoleGranted` audit row is written | Grant failure is reported and returned, not swallowed |
| Grant, placeholder | `$Username` is `""`, `UnknownUser` or `_PUBLIC` | No grant; the exact `Security.Users.AddRoles(...)` command an operator must run is reported through the logger and recorded on the stamp | Never a silent skip |
| `Uninstall`, absent | Profile whose objects do not exist | `$$$OK`, nothing changed | — |
| `Uninstall`, unconfirmed | Profile exists, `pConfirmDataLoss = 0` | Error status naming what would be destroyed; **nothing is changed** | — |
| `Uninstall`, confirmed | Profile exists, `pConfirmDataLoss = 1` | Mapping, application, role, both resources, config entry, `IRIS.DAT` and the directory are all gone | Partial failure returns the first error; already-removed objects are not errors |

</intent-contract>

## Code Map

**What exists today (extend, never duplicate).**

- `src/OcuPilot/Install/` — holds only `.gitkeep`. This story creates the first class in it.
- `src/OcuPilot/Kernel/Utils.cls` — `SwitchNamespace(pNamespace, Output pOriginal)` (:35) and `RestoreNamespace(pOriginal)` (:50). Its header records that the class has **no production consumer and has never executed**, and instructs the first consumer to add a test host alongside its first call site. **This story is that first consumer** for the namespace pair. The class's other deferred defects (`ReadRequestBody`, DW-23) stay routed to Story 1.5 — do not claim them.
- `src/OcuPilot/Kernel/Audit/Log.cls` — `Info` / `Warn` / `Error` / `Debug` (:16-:34), never throw. **The installer's only output channel.** Not the IRIS audit database.
- `src/OcuPilot/Api/Error.cls:129` `LogError` and `src/OcuPilot/Kernel/Audit/Log.cls:76` `WriteConsole` — the **seam + probe-subclass + `^||` capture** pattern (`Test/LogProbe.cls`, `Test/ErrorProbe.cls`). Copy this shape if a side effect needs intercepting.
- `src/OcuPilot/Test/` — `Http`, `Dispatch`, `RouterFixture`, `LogProbe`, `ErrorProbe`. **None is useful here** (no HTTP surface, no dispatch). No test class in the tree declares `%OnNew`, `OnBeforeOneTest` or `OnAfterOneTest`; this story writes the tree's first setup/teardown discipline. Existing convention: class `OcuPilot.Test.<Subject>`, instance `Method Test<Claim>()`, every assertion carrying a message.
- `scripts/check-objectscript.py` — five rules, exit `1` if any problem. `WRITE_ALLOWED` (:258 region) is an **exact relative-path allow-list**; copy that shape for the new rules. `FIXED_PACKAGES` requires `OcuPilot.<Api|Kernel|Screen|Area|Port|Install|Test>.…` with **at least three** dot-separated segments. `MAX_CLASS_NAME_LENGTH = 29` counts the full dotted name. Invoked by `.githooks/pre-commit` on staged `src/OcuPilot/**/*.cls`.
- `docker-compose.yml` — 12 lines, no `command:`, no `entrypoint:`, no init script. **There is no start hook yet**; Story 1.4 creates it. Verify this story by invoking the class through the IRIS MCP tools.

**Harvest source — shape only, never names** (`/Users/jbrandt/git/iris-couch/src/IRISCouch/Installer.cls`, 208 lines).

- Copy: `Set tOrigNS = $NAMESPACE` before the `Try`; `Set $NAMESPACE = $Get(tOrigNS, $Namespace)` as the **first line of every `Catch`** (:99, :186, :205); the existing-object early path that **still** runs the ensure steps (:74-79); `Uninstall` returning `$$$OK` when absent (:176-179); the `Install`-twice test (`Test/InstallerTest.cls` `TestInstallIdempotent`).
- **Do not copy:** the early return that never verifies the existing object's configuration (its own doc comment, :57-58, admits it returns OK against a stale `DispatchClass` forever — the single largest defect, and the reason this story does verify-and-repair); the hand-rolled `GrantAdminRole` read-modify-write (:138) instead of `Security.Users.AddRoles`; the unconditional `_SYSTEM` grant (:120-122); discarded `%Status` on three privileged calls; `IsInstalled()` conflating "not installed" with "could not check" (:205); the dead `Parameter DEFAULTWEBAPPPATH`; `module.xml` (no `<Invoke>` — Story 1.16's problem).
- **Gaps confirmed against the source, not just the harvest map:** `grep` over `iris-couch/src/` returns **zero** hits for `Security.Resources`, `Config.Databases`, `SYS.Database` and `Config.Map*`. It creates no database, no resource and no mapping, and `Auth/Users.cls:181` hardcodes `%DB_IRISCOUCH` — proof the operator was expected to pre-create the database. It also never enables auditing (`Security.System` appears nowhere).

**System behaviour verified on `ocupilot-iris` (IRIS for Health 2026.2 build 221U) during planning.** Do not re-derive; do re-check anything you change.

- **A database's resource name must begin with `%DB_`.** `CreateDatabase(dir,1,8192,0,"OcuPilotDb")` → `ERROR #896: Invalid resource name`; `ModifyDatabase` with a valid non-`%DB_` resource → `<FUNCTION>` at `%SaveData+103^SYS.Database.1`. Hence `%DB_OCUPILOT`.
- **`SYS.Database` does not validate that the resource exists** — assigning a non-existent one returns OK and yields a database only `%All` can reach. **Create the resource first.**
- **Creating a `%DB_*` resource auto-creates a matching implicit role** granted to nobody (audit timestamps 5 ms apart); deleting the resource deletes the role. `Security/Roles.cls:21`: `%DB_*` implicit roles cannot be modified.
- **Creation is three calls, in order:** `%File.CreateDirectoryChain(dir)` → `SYS.Database.CreateDatabase(dir, size, blocksize, 0, resource)` → `Config.Databases.Create(NAME, .Props)` with `Props("Directory")`. `Config.Databases` performs no file operations (its own class header says so).
- **Re-running `CreateDatabase` against an existing `IRIS.DAT` reinitializes it.** Guard with `##class(%File).Exists(dir_"IRIS.DAT")`.
- **Removal is four calls:** `Config.Databases.Delete(NAME)`, `SYS.Database.DeleteDatabase(dir)` (no dismount needed; deletes only `IRIS.DAT`), `Security.Resources.Delete("%DB_…")`, `%File.RemoveDirectoryTree(dir)`. Verified round-trip clean, including the CPF.
- **`Config.MapGlobals.Create(namespace, "OcuPilot*", .props)` with `props("Database")` works and is live on the next global reference** with the default flags. Verified: the pattern routes the exact root global and every global whose name starts with the prefix, and leaves non-matching globals in place. `LockDatabase` defaults to the target database. The SMP exposes no wildcard field — this is an API capability.
- **Signatures** (`irissys/`): `Security.Resources.Create(Name, Description, PublicPermission, Type)` — Type **2** for a database resource, **0** for an ordinary one, `PublicPermission ""` for not-public. `Security.Roles.Create(Name, Description, Resources, GrantedRoles, EscalationOnly=0)` — `Resources` is `"Name:RWU,Name2:U"`; `Roles.cls:255` warns that `Modify` with `Resources=""` **removes all resources**; `Roles.cls:36-40` requires `Properties("ForceSave")=2` on `Modify` during installation. `Security.Users.AddRoles(Username, ByRef Roles, Admin=0)` is additive; `Get(user,.p)` → `p("Roles")` is the directly-assigned list.
- **Privileged routine application** (`Security/Applications.cls`): `Type = 4` (`$$$AppTypePrivRoutine`). Meaningful properties are `Name`, `Description`, `Enabled`, `Type`, `Resource`, `MatchRoles` and `Routines` only — `NameSpace`, `DispatchClass`, `AutheEnabled` and the rest are CSP-only. `Routines` entries are `Class:dbname:1` where `dbname` is the **configured database name** of the database holding the code, not a namespace and not a path. `MatchRoles` entry `:<Role>` (empty match) grants unconditionally. `Get()` may omit the `Routines` key entirely — read it with `$Get()`.
- **`New $ROLES` stacks both `$ROLES` and `$USERNAME` and restores on normal return and on exception unwind** (verified). It is **frame-scoped, not block-scoped**: a `New $ROLES` inside a `Try` unwinds when the *method* returns. Role names normalise to upper case in `$ROLES` — never compare case-sensitively. `Set $ROLES = ""` clears only *Added* roles, so it cannot de-privilege `_SYSTEM`.
- **`JOB` children inherit `$ROLES` and `$USERNAME` at the moment of the spawn** (documented, `RCOS_vroles` and `RCOS_cjob`). *Inference, labelled:* that the child keeps them after the parent's frame unwinds is not stated in any source found; it follows from `$ROLES` being per-process state with no cross-process revocation channel. AC6 enforces the ordering rule mechanically rather than relying on the inference.
- **CSP request boundaries do not carry added roles forward** (`GSA_manage_applications.md:277`) — escalation cannot leak across REST requests.
- **`%DB_IRISSYS:RW` is a self-escalation primitive.** A user holding it can `Set $ROLES` to anything and can `$SYSTEM.Security.Login("_SYSTEM")` with no password — verified end to end. `%Operator` carries it. **The AC's test user must not hold `%Operator`**; build a purpose-built role carrying exactly `%Admin_Operate:U`.
- **`%Admin_Operate` is a resource, not a role**, and `Security.Users.Create/Modify` **silently accepts a non-existent role name** — a test written literally against the AC's wording would grant nothing and still pass its own setup.
- **Denial signatures:** global → `<PROTECT>`, `e.Code = 202`, `e.Data = "^Global,/path/to/db/"`, raised even for a node that does not exist. SQL → `%SQLCODE = -99` *or* `-30` ("table not found") depending on visibility, so assert `%SQLCODE < 0` **and** pair with an escalated success on the same table.
- **`Security.Events.Create(Source, Type, Name, Description, Enabled=1, Flags=0)`** — each field ≤64 chars, no `/`, and **Source and Type may not start with `%`** (system-reserved). `Security.Events.Exists` accepts either three arguments or one `"Source/Type/Name"` string. `$System.Security.Audit(...)` on an unregistered triple returns a **bare `0` status with no error text** — `$$$ISERR` catches it, `GetErrorText` says nothing useful.
- **Auditing** is enabled through `Security.System.Modify("SYSTEM", .props)` with `props("AuditEnabled") = 1`; unnamed properties are left untouched. **Never include `AuditEncrypt`** — changing it deletes the audit database and everything in it. `Security.System.IsAuditingEnabled()` reads the flag. Auditing is currently **on** on this instance, and 75 system event types are registered; there are **no** user-defined types.
- **Instance baseline:** nothing named like OcuPilot exists — no database, resource, role, application, namespace or global. Install directory `/usr/irissys/`, **manager directory `/durable/iris/mgr/`** (they differ; only the manager directory is on the durable bind mount). `HSCUSTOM` exists, so it is the install namespace here. `iris_database_list`'s `resource` field is empty for **every** database and must not be used to assert this story's central claim — read `SYS.Database.%OpenId(dir).ResourceName`.

## Tasks & Acceptance

**Execution:**

- `src/OcuPilot/Install/Installer.cls` — new `OcuPilot.Install.Installer` (26 chars) — the one installer class (AD-17). Public: `Install(pProfile As %String = "") As %Status`, `Uninstall(pProfile As %String = "", pConfirmDataLoss As %Boolean = 0) As %Status`, `StateFingerprint(pProfile As %String = "") As %String`, `ResolveNamespace() As %String` (`HSCUSTOM` when it exists, else `USER`). Private: `Names(pProfile, Output pNames)`, and one guard-then-act ensure step per object — database, database resource, administrative resource, administrative role, privileged routine application, global mapping, audit event, auditing, grant. Each ensure step **creates when absent and repairs when present but wrong**, returns a `%Status`, and reports what it did through `Kernel.Audit.Log`. `pProfile` is validated to `""` or `"probe"` and nothing else.
- `src/OcuPilot/Kernel/State/Base.cls` — new `OcuPilot.Kernel.State.Base` (26 chars) — the tree's **only** escalation point (AD-9). Abstract `%Persistent` base providing guarded save / open / exists and guarded global get / set, each performing `New $ROLES` and `$SYSTEM.Security.AddRoles(<application>)` **in the same method frame** as the access it guards. Contains no `JOB` and no reference to `OcuPilot.Api`, `.Port`, `.Screen` or `.Area`. Header states the two ordering rules verbatim.
- `src/OcuPilot/Kernel/State/Stamp.cls` — new `OcuPilot.Kernel.State.Stamp` (27 chars) extending `Base` — one row per install run: timestamp (ISO-8601 UTC via `$ZTimeStamp`), the profile, **one scalar property per created object name** (database, database resource, administrative resource, administrative role, privileged routine application, mapping), the grant outcome and the **granted username as data, never a foreign key** (AD-37), and a resolver that renders an absent user as "no longer present". Named scalars deliberately, **not** a `list Of %String` — a list would need a subtable projection under the persistent-storage rule for no gain here. No hand-written Storage.
- `src/OcuPilot/Test/Installer.cls` — new `OcuPilot.Test.Installer` (23 chars) — the create/repair/idempotency/uninstall suite, run against the `probe` profile except where an AC names production. `OnAfterOneTest` calls `Uninstall("probe", 1)` unconditionally.
- `src/OcuPilot/Test/State.cls` — new `OcuPilot.Test.State` (19 chars) — escalation, denial and integration. Owns the throwaway denial user and its purpose-built role, created in `OnBeforeOneTest` and deleted in `OnAfterOneTest`. No property name begins with `Test`; `%OnNew(initvalue)` forwards to `##super`.
- `scripts/check-objectscript.py` — add two rules with the existing exact-path allow-list shape: (a) `New $ROLES` / `$SYSTEM.Security.AddRoles` may appear only in `src/OcuPilot/Kernel/State/Base.cls` and `src/OcuPilot/Test/State.cls`; (b) files under `src/OcuPilot/Kernel/State/` may not contain a `JOB` command or a reference to `OcuPilot.Api`, `OcuPilot.Port`, `OcuPilot.Screen` or `OcuPilot.Area`. Both skip comment lines and `XData` bodies exactly as `check_write_discipline` does. Update the module docstring's rule count.
- `README.md` — one short subsection recording that install **enables instance auditing** and registers OcuPilot's event types, as a deliberate security-posture change (FR-66), and naming the one command that grants the administrative role when install could not.

**Acceptance Criteria:**

- **AC1** — Given a profile whose objects do not exist, when `Install(pProfile)` runs, then a database exists at `<ManagerDirectory><name>/` with an `IRIS.DAT`, its `SYS.Database` `ResourceName` is that profile's `%DB_` resource, and that resource exists with `PublicPermission = ""` and `Type = 2`.
- **AC2** — Given the same run, when it completes, then the administrative resource and a role of the same name exist, the role's `Resources` grants **only** the administrative resource with `U`, and the role does **not** grant the `%DB_` resource — no ordinary role holds the guarding resource (AD-9).
- **AC3** — Given the same run, when it completes, then exactly one global mapping exists in the install namespace whose name is the profile's global prefix followed by `*` and whose `Database` is the profile's database, and **no** package mapping and **no** routine mapping was created, so OcuPilot's code remains in the install namespace's normal databases.
- **AC4** — Given a user holding `%DB_<install-namespace>:RW` and `%Admin_Operate:U` through a purpose-built role, holding neither `OcuPilotAdmin` nor `%DB_OCUPILOT`, when that user reads and writes an `^OcuPilot…` global directly and selects from and inserts into the stamp table, then all four attempts are refused — `<PROTECT>` for the two global attempts and `%SQLCODE < 0` for the two SQL attempts — **and** the same global reference and the same table are read successfully through the escalated path in the same test, so a mistyped name cannot pass as a denial.
- **AC5** *(Integration AC, Rule 1)* — Given the consumer `OcuPilot.Kernel.State.Stamp`, when it writes a row and reads it back through `OcuPilot.Kernel.State.Base`, then the row round-trips with its values intact, the guarding role is present in `$ROLES` **inside** the guarded frame and absent immediately after the method returns — on the normal path and after an exception raised inside the frame — and that row is one of the objects AC4's denied user cannot see.
- **AC6** — Given the whole source tree, when the mechanical checker runs, then `New $ROLES` and `$SYSTEM.Security.AddRoles` appear only in `Kernel/State/Base.cls` and `Test/State.cls`, and no file under `Kernel/State/` contains a `JOB` command or a reference to `OcuPilot.Api`, `.Port`, `.Screen` or `.Area` — the two ordering rules of AD-9 enforced mechanically rather than by inference.
- **AC7** — Given a profile already fully installed, when `Install(pProfile)` runs a second time, then it returns `$$$OK`, `StateFingerprint(pProfile)` is byte-identical to its value after the first run, and the ensure steps still executed (AD-17) — *nothing changes* means the net state, not that steps were skipped.
- **AC8** — Given a profile installed but drifted, when `Install(pProfile)` runs against it, then the drift is repaired and the repair is reported; existence alone never counts as installed.
- **AC9** *(DW-16)* — Given `$Username` is an existing named account, when install completes, then the administrative role is granted to it, the grant is idempotent across runs, an `OcuPilot/Security/RoleGranted` audit row naming the actor and the target is written, and the stamp records the outcome and the username. Given `$Username` is `""`, `UnknownUser` or `_PUBLIC`, when install completes, then no grant is made, the exact `Security.Users.AddRoles(...)` command an operator must run is reported through the logger, and the stamp records that outcome — never a silent skip.
- **AC10** — Given auditing, when install runs, then it enables auditing if it is off and reports which of the two happened as a distinct outcome, the profile's audit event triple is registered, and an event emitted through `$System.Security.Audit` produces a matching row in the audit database — the round trip, not the registration alone.
- **AC11** — Given `Uninstall(pProfile, pConfirmDataLoss)`, when the profile's objects are absent it returns `$$$OK` having changed nothing; when they are present and `pConfirmDataLoss = 0` it returns an error naming what would be destroyed and changes nothing; when they are present and `pConfirmDataLoss = 1` the mapping, application, role, both resources, config entry, `IRIS.DAT`, directory and audit event are all gone, proved by a fresh listing and by `%File.DirectoryExists`.
- **AC12** — Given any error path in `Install` or `Uninstall`, when it is taken, then `$NAMESPACE` is restored as the **first** line of the `Catch` and equals the caller's namespace on return, and `Kernel.Utils.SwitchNamespace` / `RestoreNamespace` have a direct test host — this story is their first consumer.

### Review Findings

**Code review, 2026-09-09 (follow-up pass, `followup_review_recommended: true`).** Four layers ran
(blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor). 14 `patch` (all applied),
0 `decision-needed`, 2 `defer` (ledgered), 14 rejected. Suite after patching: **60/60** across six
classes, confirmed by the `%UnitTest_Result` SQL ground-truth probe.

Applied patches:

- [x] [Review][Patch] **AD-9's escalation had no test that could fail** — the whole suite authenticates as `_SYSTEM`, which holds `%All` (verified live: `$ROLES` = `%All,OcuPilotAdmin`), and `%All` bypasses `%DB_OCUPILOT` outright, so every escalated-success assertion passes whether or not `New $ROLES`/`AddRoles` is present. QA had already proved this live for `TestGuardedSaveRoundTrips`. Added `AttemptGuardedAccess` and `TestEscalationIsWhatGrantsTheDeniedUserAccess`: AC4's denied user, refused `<PROTECT>` on the direct global, reaches the same global *through* the guarded methods. **Mutation demonstrated live:** removed `New $ROLES` + `AddRoles` from `GuardedProbeSet`/`GuardedProbeGet` → the new test went red (`<PROTECT> ^OcuPilotKernelStateProbe("escalationProbe"),/durable/iris/mgr/ocupilot/`) **while the other seven tests in the class stayed green**, which is precisely the gap; reverted, 8/8 green, `md5` identical. [src/OcuPilot/Test/State.cls]
- [x] [Review][Patch] **AC12's `Catch`-path restore was unexercised** — `InstallerFault` returns an ordinary bad `%Status`, which the inline `If $$$ISERR` guards absorb with an argumentless `Quit` that never enters the `Catch`; every ensure step converts its own exceptions to a `%Status`, so nothing in the tree reached that block. Added `OcuPilot.Test.InstallerThrow` (throws instead of returning) and `TestNamespaceIsRestoredWhenTheSwitchedWindowThrows`. **Mutation demonstrated live:** deleted `Set $NAMESPACE = tOrigNS` from `Install`'s `Catch` → `ns=%SYS err=UNCAUGHT:<CLASS DOES NOT EXIST>`, exactly the cascade the implement pass's HIGH fix was written for; reverted, `md5` identical, baseline restored. [src/OcuPilot/Test/InstallerThrow.cls, src/OcuPilot/Test/Installer.cls:289]
- [x] [Review][Patch] **`AttemptSqlAccess`'s `Catch` produced exactly the values its assertions demanded** — it set both `%SQLCODE`s to `-1` while every caller asserts `< 0` for "denied", so a `$SYSTEM.Security.Login` failure (the impersonation never happening) scored as a successful denial. The sibling `AttemptGlobalAccess` already gets this right. Now uses a non-negative `NOTDENIED` sentinel and reports `$Username` from inside the frame; both call sites assert the impersonation took effect. [src/OcuPilot/Test/State.cls:157]
- [x] [Review][Patch] **`Test/State.cls` leaked `Profile='probe'` stamp rows into the production database** — `TestGuardedSaveRoundTrips` and `TestGuardedExistsIdReflectsPresenceAndAbsence` each `GuardedSave` a Stamp row, and `OnAfterOneTest` deleted only the user and role. This re-opened the exact root cause the implement pass fixed for `Test/Installer.cls` (212 rows), in a different file, and bites whenever the class is run on its own as this spec directs. `OnAfterOneTest` now purges them; verified live afterwards: 0 probe rows. [src/OcuPilot/Test/State.cls:101]
- [x] [Review][Patch] **The placeholder remediation command was both wrong and unrunnable** — `tTargetLabel` special-cased only `""`, so for `UnknownUser` the emitted (and stamped, and warn-logged) command was `AddRoles("UnknownUser", .tRoles)`: an instruction to grant OcuPilot's administrative role to the anonymous CSP identity, which is what AD-21 and AD-10's prohibited set exist to prevent. It also omitted the `%SYS` switch — verified live that `##class(Security.Users)` from `HSCUSTOM` raises `<CLASS DOES NOT EXIST>`, in the very namespace the README tells the operator to use. Both fixed, in the code and the README. [src/OcuPilot/Install/Installer.cls:511, README.md]
- [x] [Review][Patch] **Every collected report was discarded on every error path** — `FlushReports` ran only after the last ensure step succeeded, so a run that repaired three objects and failed on the fourth logged nothing about the repairs, contradicting AC8's "the repair is reported". Moved to after the `Try`/`Catch`, where all three exits land. This also makes AD-16's restore-first ordering genuinely load-bearing rather than nominal, since `FlushReports` reaches an `OcuPilot.*` class. [src/OcuPilot/Install/Installer.cls:182]
- [x] [Review][Patch] **AC6's escalation gate was evadable by two ordinary spellings** — verified by running the regex: `N $ROLES` (every ObjectScript command abbreviates) and `Do ##class(%SYSTEM.Security).AddRoles(...)` (`$SYSTEM.Security` *is* `%SYSTEM.Security`) both slipped through, and both are working escalation routes. This is Story 1.1's DW-32 shape — an unvalidated gate. Widened `ESCALATION_RE`. **Mutations demonstrated:** each spelling added to `Api/Router.cls` → exit 1 naming file, line and rule; reverted, `md5` identical, exit 0. [scripts/check-objectscript.py:354]
- [x] [Review][Patch] **`GuardedExecuteOneParam` was a public, general-purpose escalated SQL executor** — added during the review pass with no layer having seen it, callable from any class in the tree with caller-supplied statement text, and invisible to `check_escalation_containment`, which gates the escalation *tokens*, not calls into the guarded wrappers. Made `[ Private ]`, confining it to `Kernel/State/` which `check_state_package_isolation` already polices. IRIS enforces private at runtime against the *named* class, so `Stamp.DeleteByProfile` now uses inherited (`..`) dispatch; the escalation still executes in `Base`'s own routine, which is what the application's `Routines` allow-list names. [src/OcuPilot/Kernel/State/Base.cls:204, Stamp.cls:78]
- [x] [Review][Patch] **`DeleteByProfile("")` silently matches nothing, and `Profile` round-trips as SQL `NULL`** — verified live: of 426 production rows, `WHERE Profile IS NULL` matches 426 and `WHERE Profile = ''` (literal *or* bound) matches 0; a zero-row DELETE is SQLCODE 100, so the purge reports success. Documented rather than "fixed": production stamps live inside the database `Uninstall` deletes outright a few lines earlier, and widening the predicate would newly destroy install history no AC asks to remove. The forward hazard — 1.4 and 1.17 are named `Consumed-by` readers of this table — is now recorded on the `Profile` property itself. [src/OcuPilot/Kernel/State/Stamp.cls:16, Installer.cls:661]
- [x] [Review][Patch] **Two ensure steps' drift oracles were wrong in opposite directions** — `EnsureDatabaseResource` detected `Type '= 2` drift but its repair payload only wrote `PublicPermission`, so a `Type` mismatch would have reported "Repaired the database resource" on every run forever while changing nothing (and `Security.Resources` documents `Type` as filer-assigned and not modifiable, so `Modify` could not repair it anyway); clause removed with the reason recorded. Conversely `EnsureAdminRole` never read `GrantedRoles`, so a role that acquired `%All` — AD-10's "granting privilege through any path" — would be reported "already correct" forever; now compared and repaired, verified live that a clean role reads `""` so the guard does not false-fire. [src/OcuPilot/Install/Installer.cls:210, :321]
- [x] [Review][Patch] **`StateFingerprint` discarded six `%Status` results** — "read failed" and "object absent" both produced `""`, so two consecutive failed reads compare byte-identical and AC7's idempotency oracle passes on no evidence. This is the sibling defect the Code Map lists under "Do not copy". All six now checked, with a `READFAILED` sentinel. Also swapped `Security.System.IsAuditingEnabled()` — marked `[ Internal ]` in `irissys/Security/System.cls` — for `Security.System.Get`'s `AuditEnabled`, the supported route this class already writes through. [src/OcuPilot/Install/Installer.cls:589]
- [x] [Review][Patch] **The README overstated the threat model and understated the skip conditions** — "cannot read or forge" is not true without a caveat: the same `%DB_<install-namespace>:RW` holder can rewrite `Kernel.State.Base`, the one class the privileged application whitelists (verified live: `Routines` = `OcuPilot.Kernel.State.Base:HSCUSTOM:1`, and HSCUSTOM's routine database is guarded by `%DB_HSCUSTOM`). AD-9 already accepts this; the README now says so. Also added the fourth skip condition (an account that does not exist) and `Uninstall`, including that it deliberately does not turn instance auditing back off. [README.md]
- [x] [Review][Patch] **`Kernel/Utils.cls`'s header still said "No consumer in this story"** — Story 1.3 *is* its first consumer and did add the direct test host the header asked for, so the superseded claim would be mined as evidence by the next reader. Corrected at its origin, and narrowed to the methods still genuinely unexecuted (DW-23's real residue). [src/OcuPilot/Kernel/Utils.cls:8]
- [x] [Review][Patch] **AC6's second ordering rule had no demonstrated mutation** — the spec's `## Verification` records two AC6 mutations, both for the first rule; nothing in the tree can trip the re-entry check, since `Base.cls` names those tokens only in `///` lines that `iter_code_lines` skips. Run now: `Set tX = ##class(OcuPilot.Api.Error).%New()` added to a code line in `Kernel/State/Stamp.cls` → `check-objectscript.py` exits 1 with `Stamp.cls:60: reference to 'OcuPilot.Api' under Kernel/State/ ... (AD-9)`; reverted, `md5` identical, exit 0.

Deferred (ledgered, not blocking):

- [x] [Review][Defer] **Install re-emits its `RoleGranted` audit marker on every run** [src/OcuPilot/Install/Installer.cls:525] — deferred: `DW-44`, `decision-pending owner=burndown`. Verified live: 426 `ROLEGRANTED` + 415 `ROLEGRANTEDPROBE` audit rows for a role granted once, because `EnsureGrant` has no already-held branch. AD-15 ties the marker to a write and AC10 forces exactly this distinction on the sibling auditing step, but AC9's own wording ("when install completes … an audit row is written") blesses the current behaviour and `TestGrantForRealAccountGrantsAndAudits` is built on it. A product call the AC does not settle; decided once at the epic decision sheet.
- [x] [Review][Defer] **`EnsureAuditingEnabled`'s enable branch is executed by no test** [src/OcuPilot/Install/Installer.cls:477] — deferred: `DW-45`, `wontfix-accepted`, `reopen_if=a fresh container whose AuditEnabled reads 0 still reads 0 after Install()`. The README calls this the headline security-posture change; auditing is already on here, so the branch never runs and replacing its body with a no-op leaves the suite green. Not closed because the spec's own Never list forbids a test writing `AuditEnabled`, and the only other closure adds a production read-seam purely for a test.

Rejected:

- `[false]` The diff's final hunk writes to an absolute path (`b/Users/jbrandt/...`) — an artifact of how this review built its own diff file (`git diff --no-index /dev/null <abs path>` for the untracked file), not anything in the repository.
- `[false]` The Review Triage Log's counts do not match its own list (27 claimed, 20 listed), and "Nothing was deferred" contradicts a logged `defer` — real inconsistencies, but the fix is to edit the spec under review, which triage excludes. Recorded here so the discrepancy is not mined later as evidence.
- `[low]` `%File.RemoveDirectoryTree`'s return value is discarded in `Uninstall`, so a failed removal still returns `$$$OK` — `TestUninstallLeavesNoResidue` already asserts the directory is gone, and given this container's observed 20–40 minute `DeleteDatabase` latency, turning an intermittent removal failure into a hard error would make teardown fragile for no gain. `reopen_if=TestUninstallLeavesNoResidue ever fails on the directory assertion while Uninstall returned OK`.
- `[low]` `Install` does not verify it was called from the install namespace; from `USER` or `%SYS` the ensure steps succeed and `RecordStamp` then fails with `<CLASS DOES NOT EXIST>`, returning an error after a correct install — real, but the operator-invocation path is Story 1.4's, and adding an entry guard whose failure mode was not demonstrated is more than a direct correction. `reopen_if=Story 1.4's start path invokes Install from anywhere but the install namespace`.
- `[low]` `JOB_RE` matches the word "job" inside a string literal or trailing comment, and `REENTRY_TOKENS` is a bare substring test — no file in the tree trips either; the fix is a rework of the shared line classifier's state machine. Same disposition the implement pass reached, for the same reason.
- `[low]` `EnsureApplication` never verifies or repairs `Type = 4` — set only on the create path, and reachable only by a manual `Security.Applications.Modify`. `reopen_if=an installed OcuPilotState application is ever observed with Type other than 4`.
- `[low]` `EnsureDatabase` checks `Config.Databases.Exists(name)` but never the entry's `Directory`, so an entry of the right name pointing elsewhere reads as correct — no path in this story or its tests creates such an entry.
- `[low]` `Stamp.ResolveGrantedUser` renders "could not determine" (a switch failure or caught exception) as "no longer present", indistinguishable from a genuinely deleted account — distinguishing them needs a third return state and changes every caller; more than a direct correction for a case with no observed occurrence.
- `[low]` `Uninstall`'s confirmation-refusal text does not name the stamp history — and per the finding above, for the production profile there is none to name.
- `[low]` `EnsureGrant` is public where the spec's Tasks list makes every ensure step private — the deviation is documented at the method and exists so `TestPlaceholderUserIsNotGranted` can drive the placeholder branch without the connection authenticating as a placeholder.
- `[low]` `GuardedRolesSnapshot` / `GuardedRolesSnapshotThenThrow` are test-only methods shipped in production code — by design, and unavoidable: any other host would have to write `$SYSTEM.Security.AddRoles` in a third file, which AC6's checker would correctly reject.
- `[low]` `AC3`'s "exactly one global mapping" is never asserted as a count; `%OnNew` in `Test/Installer.cls` is a no-op override; `OnAfterOneTest` in `Test/State.cls` makes assertions; `TestSecondRunReportsAuditingAlreadyEnabled` matches "already enabled" anywhere in the info stream — four cosmetic or granularity nits with no named harm.
- `[low]` `AC10`'s `COUNT(*) > 0` over pre-existing audit rows can no longer go red — real (verified: 415 rows predate any given run), but a time-bounded or count-delta assertion is blocked by the `%SYS.Audit` same-process read-visibility artifact QA documented, and the registration half is now genuinely falsifiable via `TestAuditEventRegistrationRestoresTheRoundTrip`. Folded into `DW-44`, which may change what this step audits at all.
- `[low]` Four of `StateFingerprint`'s eleven components are not individually drift-tested — Rule 19 scopes to one demonstrated mutation per AC, and `TestFingerprintChangesWhenAnObjectChanges` provides it; the same disposition the implement pass reached for the per-object drift branches.

## Spec Change Log

## Review Triage Log

### 2026-09-09 — Review pass
- verdicts: 27 findings — high 3, medium 6, low 12, false 6, maybe-false 0
- findings:
  - `[high]` `[patch]` (blind-hunter, edge-case-hunter, verification-gap) `Install`/`Uninstall` never restore `$NAMESPACE` when an ensure/delete step returns an ordinary bad `%Status` (no exception) inside the `%SYS`-switched window — the bare `Quit` inside `Try` skips `Catch`'s restore entirely, contradicting AD-16/AC12/FR-66's "restores the namespace on any error". Verified live: reverting the fix reproduced `$NAMESPACE` stuck at `%SYS`, and `OnAfterOneTest`'s own cleanup then failed with `<CLASS DOES NOT EXIST>OcuPilot.Install.Installer` — confirming the failure mode `objectscript-debugging.md` warns about. Fixed: every ordinary-failure `Quit` inside the switched region now restores `$NAMESPACE` first (17 sites across `Install`/`Uninstall`), and the spec-named pinning test `TestNamespaceIsRestoredFirstOnTheErrorPath` (previously entirely missing) was added via a new fault-injection subclass `OcuPilot.Test.InstallerFault`. Mutation demonstrated: reverted one guard, test went red (including the `<CLASS DOES NOT EXIST>` cascade), reverted back, confirmed green and byte-identical (`md5`).
  - `[high]` `[patch]` (blind-hunter) `EnsureAuditEvent`'s existing-object path only reported "already registered" and never compared/repaired the live `Enabled` flag, unlike every other ensure step — a direct AD-17 violation ("every object... compares the live properties... and repairs drift"). Fixed to `Get`-then-compare-then-`Modify`, matching `EnsureApplication`'s own pattern; added `TestSecondRunRepairsDriftedAuditEvent`. Mutation demonstrated: reverted to existence-only, test went red naming the still-disabled event, reverted back, confirmed green and byte-identical.
  - `[medium]` `[patch]` (blind-hunter, verification-gap, intent-alignment) AC10's "already enabled" vs "enabled now" distinction had no pinning test — the spec names `TestSecondRunReportsAuditingAlreadyEnabled` explicitly and it did not exist. Added it via `OcuPilot.Test.InstallerProbe.CapturedAll` (see next finding). Mutation demonstrated: forced the auditing step to always report "enabled now", test went red on both assertions, reverted, confirmed green, confirmed instance `AuditEnabled` still reads `1` (no test disabled it).
  - `[medium]` `[patch]` (blind-hunter) `StateFingerprint` excluded the instance-wide `AuditEnabled` flag and the two resources' own `PublicPermission`/`Type` (only their names, via the database's link), weakening AC7's idempotency oracle for exactly the properties `EnsureAuditingEnabled`/`EnsureDatabaseResource`/`EnsureAdminResource` manage. Extended the composite to include all four; existing `TestInstallIsIdempotent`/`TestFingerprintChangesWhenAnObjectChanges`/`TestProductionInstallIsIdempotent` re-verified green against the wider fingerprint.
  - `[medium]` `[patch]` (blind-hunter) Verified live (`SELECT Profile, COUNT(*) FROM OcuPilot_Kernel_State.Stamp GROUP BY Profile` → 212 `probe` rows before the fix): `OcuPilot.Kernel.State.Stamp`'s storage global always matches the production `OcuPilot*` mapping (its own compiled name starts with `OcuPilot`, never the probe's distinct `ProbeOcuPilotState*` pattern), so every `Install("probe")` call permanently wrote its bookkeeping row into the *production* protected database, and `Uninstall("probe", 1)` never removed it — the one probe-created artifact `Test/Installer.cls`'s own residue assertion never lists. Fixed: added `Base.GuardedExecuteOneParam` (a narrow, single-purpose escalated primitive, following the existing `GuardedProbeSet`/`GuardedProbeGet` shape) and `Stamp.DeleteByProfile`, called from `Uninstall` after the namespace restore; a purge failure is logged but does not fail an otherwise-successful uninstall (AC11's own success criteria never named stamp history). Added `TestUninstallPurgesStampHistoryForTheProfile`. Mutation demonstrated: suppressed the purge call, test went red, reverted, confirmed green. Post-fix: `SELECT Profile, COUNT(*) ...` shows zero `probe` rows.
  - `[low]` `[patch]` (edge-case-hunter) `EnsureDatabase`'s branch that repairs a missing `Config.Databases` entry (IRIS.DAT present, config entry absent — a partial-install artifact) reported nothing, unlike every sibling repair branch. Added the missing `Report` call; narrow edge case, no dedicated mutation test (the existing-drift tests already exercise every other branch of this method).
  - `[low]` `[patch]` (verification-gap, intent-alignment) `OcuPilot.Kernel.State.Base.GuardedExistsId` and `OcuPilot.Kernel.State.Stamp.ResolveGrantedUser` shipped with zero callers and zero test references anywhere in the tree — "a module with no executed test host" (Rule 19). `ResolveGrantedUser` is also the concrete behavior AD-37's frozen boundary promises ("a stamp naming a user who no longer exists reads as 'no longer present'"). Added `TestGuardedExistsIdReflectsPresenceAndAbsence` and `TestResolveGrantedUserRendersDeletedAccountAsNoLongerPresent` to `Test/State.cls`. Mutation demonstrated for `ResolveGrantedUser`: made it return the raw username unconditionally, test went red, reverted, confirmed green and byte-identical.
  - `[low]` `[patch]` (blind-hunter) `Base.#DBRESOURCE` (`"%DB_OCUPILOT"`) and `Base.#DATABASENAME` (`"OCUPILOT"`) encode the same `"%DB_" _ name` relationship as two independent literals, while `Installer.Names` derives the probe's equivalent by explicit concatenation — a future edit to one without the other would silently diverge production from probe (AD-10). Fix-risk is low and the fix is a direct correction, so patched with a doc comment on `DBRESOURCE` recording the invariant, rather than left for a future reader to rediscover.
  - `[low]` `[patch]` (blind-hunter) `OcuPilot.Test.InstallerProbe.Captured` only exposed the single most-recent info/warn message, which cannot distinguish an earlier report from a later one at the same level (needed for the AC10 fix above, since `EnsureGrant`'s "info" report runs after `EnsureAuditingEnabled`'s and would otherwise overwrite it in `lastInfo`). Added `CapturedAll`, used by the two new tests above; `Captured` unchanged for existing callers.
  - `[low]` `[reject]` (verification-gap, filed disposition `defer`) `EnsureGrant`'s audit-marker-failure branch (`$System.Security.Audit` failing right after a successful grant) is unexercised — no test forces that call to fail. Not worth the machinery: an unusual runtime condition, narrow branch, and forcing `$System.Security.Audit` to fail deterministically in-process is nontrivial. Not met in everyday use; fix is more than a direct correction.
  - `[low]` `[reject]` (blind-hunter) Only `EnsureApplication`'s (and now `EnsureAuditEvent`'s) repair branch is drift-tested; `EnsureDatabaseResource`/`EnsureDatabase`/`EnsureAdminResource`/`EnsureAdminRole`/`EnsureMapping`'s repair code is implemented but not separately drift-tested. The spec's own `## Verification` mutations scope AC8's falsifiability demonstration to exactly one representative object ("the sibling's exact defect, reproduced and pinned"), matching Rule 19's "one demonstrated mutation per AC, not per assertion" — additional per-object drift tests are coverage beyond what was asked, not a gap in it.
  - `[false]` `[reject]` (blind-hunter) Claimed `Uninstall` should also revoke the granted role from users and disable instance auditing. AC11 enumerates exactly what a confirmed uninstall removes (mapping, application, role, both resources, config entry, `IRIS.DAT`, directory, audit event) and does not include either; disabling instance-wide auditing as a side effect of removing one profile's objects would be actively wrong (other events depend on it), and deleting the role object (already done) already prevents any further escalation through that role name.
  - `[false]` `[reject]` (blind-hunter) Claimed `New $ROLES` in `Test/State.cls`'s impersonation helpers restores only `$ROLES`, not `$Username`, after `$SYSTEM.Security.Login`, so the process could stay logged in as the denied user. Refuted by the spec's own live-verified research (Code Map): "`New $ROLES` stacks both `$ROLES` and `$USERNAME` and restores on normal return and on exception unwind (verified)."
  - `[low]` `[reject]` (blind-hunter) `check_escalation_containment` gates `New $ROLES`/`$SYSTEM.Security.AddRoles` but not `$SYSTEM.Security.Login`. AC6's own text names exactly the first two tokens; `Login` is a distinct identity-impersonation primitive, and its one use in the tree is exactly `Test/State.cls`'s documented denial-probe purpose. Out of AC6's stated scope, not a gap in it.
  - `[low]` `[reject]` (blind-hunter, edge-case-hunter) `iter_code_lines` (shared by all three mechanical rules) does not resume scanning code that follows an inline `*/` closing a block comment on the same line, and `check_state_package_isolation`'s `REENTRY_TOKENS` substring check is case-sensitive. Project convention (`objectscript-basics.md`) reserves `/* */` for banners only, so the first is a low-likelihood style violation nobody in this tree commits; the second is moot in practice because IRIS's own `##class()` dispatch is case-sensitive on the class name, so a non-canonically-cased reference would not resolve to a working re-entry call regardless — and this codebase already forswears dynamic dispatch by name (AD-8). Both fixes are more than a direct correction (reworking the shared line-classifier's state machine); not worth it for defects with no working exploit shape.
  - `[false]` `[reject]` (blind-hunter) Claimed `StateFingerprint`'s `Catch` not restoring `$NAMESPACE` as its first line is an inconsistency with `Install`/`Uninstall`. It is the *correct* pattern: the restore runs unconditionally immediately after the whole `Try`/`Catch`, so it executes on every exit (an internal `Quit`-from-`Try`, natural completion, or a caught exception) — this is in fact the shape `Install`/`Uninstall`'s own fix (above) now follows at each of their guard sites.
  - `[low]` `[reject]` (blind-hunter, maybe-false if the specific values were ever wrong) Claimed several `Security.*`/`Config.*` calls use positional arguments with no comment citing verification. The values used (`Type=2`/`PublicPermission=""` for the `%DB_` resource, `Type=0` for the ordinary one, `Security.Events.Create`'s trailing `1, 0`) match the spec's own "System behaviour verified on `ocupilot-iris`" notes precisely; no wrong argument was identified. A preference for more inline citation, not a defect.
  - `[low]` `[reject]` (intent-alignment) The privileged application's `Routines` allow-list (the third of three documented containment layers) is never exercised at runtime by a test proving a non-`Base` caller is refused `AddRoles`. As intent-alignment itself notes, this is structurally close to unfixable without weakening AC6's own mechanical whitelist (any test that tried would have to write `$SYSTEM.Security.AddRoles` in a third file, which the checker would then correctly flag).
  - `[false]` `[reject]` (intent-alignment) Claimed the README's FR-66 citation for the auditing-enablement change conflicts with the intent-contract's own FR-66 citation for "no agent seeded". Checked `prd.md:914-928`: FR-66 is one compound requirement whose testable consequences explicitly include *both* clauses (and "restores the namespace on any error", corroborating the first finding above) — both citations are correct facets of the same FR.
  - `[low]` `[reject]` (intent-alignment) `check_state_package_isolation` scans at file/package scope (any line under `Kernel/State/`) rather than only inside an escalated frame, which is a stricter surface than AD-9's "ordering rules" describe. Intent-alignment's own assessment: "currently a harmless over-approximation" — the one non-escalated method in that package (`Stamp.ResolveGrantedUser`) does not trip it. No named harm.

## Design Notes

**Governing architecture decisions** (`ARCHITECTURE-SPINE.md`, `status: final`, 48 ADs; line anchors from the spine):

- **AD-9** (:172) — globals only behind the resource, code stays put; the storage classes and only they escalate through a privileged routine application inside `New $ROLES`; the two ordering rules (nothing spawned, nothing re-enters). The spine's structural seed (:617) calls `Kernel/State/` "protected-storage base, **the only class that escalates**" — singular. **Resolved reading:** one base class owns escalation, every other storage class inherits it. This satisfies both AD-9's plural "storage classes" and the seed's singular, and turns "no other class in the tree escalates" into the one-line mechanical assertion of AC6.
- **AD-17** (:244) — one installer class, guard-then-act, safe to repeat, re-runs its privileged steps, enables auditing and registers events. **AD-38** (:406) — install completes or fails loudly before traffic (the start path is 1.4's; this story ships the idempotent, fast, populated-instance-safe class it calls). **AD-16** (:238) — explicit save and restore, restore first in every `Catch`. **AD-8** (:166) — privilege is the process's at call time; AD-9's escalation is the one permitted exception. **AD-21** (:268) — bind SQL parameters, accept no caller path, the directory comes from `$System.Util.ManagerDirectory()`, and reject `UnknownUser` / `_PUBLIC` explicitly (which is what AC9's placeholder branch does). **AD-37** (:400) — weak references, stored as data.
- Also binding, found by reading all 48: **AD-10** (:184) — the prohibited set names "deleting OcuPilot's own web applications, resource, role or database", so the names this story creates must be kernel-visible constants rather than installer locals, and `Uninstall` is an operator action outside the agent's write path. **AD-15** (:232) — events registered with `Security.Events.Create()` at install, emission never fails a write. **AD-45** (:464) — the smoke path is owned by `Install/` (Story 1.17). **AD-32** (:370) — the installer also creates an SSL configuration later; same idempotence discipline. **AD-25** (:307) — the only mention of uninstall in the spine ("uninstall removes it"), which is why `Uninstall` ships here rather than being invented in 1.4. **AD-30** (:356), **AD-33** (:376), **AD-34** (:382), **AD-41** (:430), **AD-46** (:472) — later consumers of the protected store; none of their state is built here, but the store must not preclude them.
- Consistency Conventions: ObjectScript naming (:512, the 29-character bound), names never inherited from siblings (:513), config in the protected database (:527), collections (:524), status handling (:523), dates (:520), tests (:528), logging (:526).

**Resolved readings** (recorded so a reviewer can check the reasoning rather than re-derive it):

1. **AC2's "but not the OcuPilot administrative resource" is a description of the test subject, not a claim that the administrative resource grants data access.** AD-9 is explicit that the guarding resource is held by no ordinary role and reached only through the privileged routine application. So `OcuPilotAdmin` grants **only** `OcuPilotAdmin:U`; it never grants `%DB_OCUPILOT`. Both readings of the AC produce the same test and the same observable outcome for the described user, so this is a naming clarification, not an intent gap.
2. **Mappings: one global mapping, no package or routine mapping.** The story's ACs never mention mappings; AD-9 requires the code to stay in the install namespace's normal database, and a package or routine mapping into the guarded database would contradict it directly. A global mapping is the mechanism that makes AD-9 true at all, and was verified working on the instance.
3. **AC7's "nothing changes" is about net state, not about skipped steps.** AD-17 requires the privileged steps to re-run on the existing-object path. `StateFingerprint()` makes the net-state half measurable; the drift-repair test (AC8) makes the re-run half falsifiable. The two together are what NFR-9 means by "safe to repeat".
4. **The privileged routine application carries no `Resource`.** A resource on it would restrict who may escalate, and OcuPilot's storage must be reachable by exactly the users OcuPilot serves (AD-9's own reasoning about code placement). Containment comes from the `Routines` allow-list — only `Kernel.State.Base` may call `AddRoles` — plus the `New $ROLES` frame, plus AC6's mechanical check.
5. **The `probe` profile is a test seam, not a feature.** Without a second, disposable name set, every create-path AC would be vacuous on an already-installed instance ("it exists" is true because a previous run made it) — precisely the unfalsifiable-gate failure Stories 1.1 and 1.2 were both reviewed for. `Install()` with no argument is exactly the production profile; the profile argument accepts nothing but `""` and `"probe"`. The probe's global prefix is deliberately **not** nested inside the production prefix, so the two mappings cannot overlap and the test never depends on IRIS's most-specific-match behaviour, which was not verified. **What the probe profile does and does not exercise:** it exercises the installer's object management — create, verify-and-repair, fingerprint, remove — including a real privileged routine application, which is why that application is created for the probe even though nothing escalates into the probe database. It does **not** exercise escalation, denial or the integration AC: `Kernel.State.Base` names exactly one application and one guarding role, so AC4 and AC5 necessarily run against production.
6. **The `OcuPilot*` mapping captures `^OcuPilotTest` too** — Story 1.1's `Test.Http` configuration global. That is correct by AD-9 (it is an OcuPilot global) and harmless today because `Test.Http` runs as `_SYSTEM`. Recorded so it is not later mistaken for a bug.
7. **No CSP Gateway report here.** AD-17 requires the installer to report the Gateway-registration gap; that gap applies to *web* applications, which this story does not create. The obligation travels with Stories 1.4/1.5.
8. **AD-7 (:160) says progress records live in a temp global; AD-33 (:380) says they live in protected storage.** That contradiction is Epic 5's to settle. This story neither settles nor forecloses it: a temp global is simply not named `^OcuPilot…` and so is not captured by the mapping.

**Ledger inbox (Rule 17).** One entry, **DW-16** — "Installer creates the administrative resource and role but grants the role to nobody". **Addressed**, by AC9 and its task, taking *both* halves of the guard rather than choosing between them: install grants the role to the installing user when `$Username` is a real named account, **and** reports the exact grant command whenever it does not. FR-29's own testable consequence is "the installer creates the resource and a role granting it, **and documents which users to grant it to**", and the ledger's evidence is that granting to nobody leaves no one able to configure the agent; doing both satisfies each. The sibling's precedent was weighed and **rejected in part**: `$Username` is kept, the hardcoded `_SYSTEM` grant is not — a shipped installer must not grant a role to a named vendor account, and `_SYSTEM` is this container's dev credential. The grant is audited (AC10), recorded on the stamp, idempotent, and reversible with one documented command.

**Consumes:** `OcuPilot.Kernel.Utils` (`SwitchNamespace` / `RestoreNamespace` — first production consumer; adds the direct test host its header asks for), `OcuPilot.Kernel.Audit.Log` (all reporting; the installer may not `Write`). Neither Story 1.1 nor 1.2 listed Story 1.3 under `Consumed-by:`; this closes that gap from the consumer side.

**Consumed-by:**

- **1.4** — invokes `Installer.Install()` from the container start path, adds the image pin, `_SYSTEM` unexpire, demo fixtures, the schema version stamp on `Kernel.State.Stamp` and the migration path.
- **1.5** — extends the same `Installer` with the two web applications and the Gateway-registration report.
- **1.16** — generates `module.xml` from the same class roster the installer compiles.
- **1.17** — the smoke script and readiness endpoint, both owned by `Install/`, read the stamp.
- **Epic 3** — the instance-wide read-only switch and kill switch are rows in this protected store (AD-30).
- **Epic 5** — the ledger, transcripts, proposals and progress records (AD-33, AD-34, AD-41, AD-46).
- **Epic 10** — per-user UI state and the per-user toggle.

**Golden example — the frame constraint, which is the single easiest thing to get wrong.** `New $ROLES` unwinds with the *method* frame, so a helper that escalates and returns has already lost the role before the caller does anything:

```objectscript
; WRONG — the role is gone the moment Escalate() returns.
Do ..Escalate()  Set tSC = pObject.%Save()

; RIGHT — escalation and the guarded access share one frame.
ClassMethod SaveGuarded(pObject As %Persistent) As %Status
{
    Set tSC = $$$OK
    New $ROLES
    Try {
        Set tSC = $SYSTEM.Security.AddRoles(..#PRIVAPP)
        If $$$ISERR(tSC) Quit
        Set tSC = pObject.%Save()
    }
    Catch ex { Set tSC = ex.AsStatus() }
    Quit tSC
}
```

**Two traps that have already cost time.** `$$$ISOK` is a compile-time macro and is unavailable to ad-hoc MCP command execution — use `$System.Status.IsOK()` there, and note that a `<SYNTAX>` failure from that tool is **not** atomic: earlier commands on the same line have already run. And IRIS SQL has no `[_]` bracket escaping — `LIKE '%DB[_]%'` silently returns zero rows; use `%STARTSWITH`.

**Review-pass addendum, 2026-09-09** (see `## Review Triage Log` for the full findings): five `patch` fixes with production-code impact, recorded here since this section is what future stories consult.

- `Install`/`Uninstall` restore `$NAMESPACE` before every ordinary-failure `Quit` inside the `%SYS`-switched region, not only in `Catch` — the literal "first line of every `Catch`" text in `<intent-contract>` still holds for the exception path; this closes the ordinary-`%Status`-failure path AD-16 and FR-66 also require ("restores the namespace on any error") but the original text did not spell out mechanically.
- `EnsureAuditEvent` now verifies-and-repairs the registered event's `Enabled` flag, matching every other ensure step.
- `OcuPilot.Kernel.State.Base.GuardedExecuteOneParam` is a new, narrowly-scoped escalated primitive (one bound SQL parameter, caller supplies the literal SQL text) alongside `GuardedSave`/`GuardedOpenId`/`GuardedExistsId`/`GuardedProbeSet`/`GuardedProbeGet` — added specifically so `OcuPilot.Kernel.State.Stamp.DeleteByProfile` could purge a profile's bookkeeping without a second escalation point. `Uninstall` calls it after the namespace restore (Stamp is an install-namespace class, like `RecordStamp`).
- `StateFingerprint`'s composite now also covers `Security.System.IsAuditingEnabled()` and both resources' own `PublicPermission`/`Type`.
- `OcuPilot.Test.InstallerProbe.CapturedAll` supplements `Captured` (which only ever exposed the single most recent message at a level) so a test can see every report a run made at that level, not only the last.

## Verification

**Commands:**

- IRIS MCP `iris_doc_load` + `iris_doc_compile` (`server: "ocupilot-iris"`, namespace `HSCUSTOM`, path `/Users/jbrandt/git/OcuPilot/src/**/*.cls`, `compile: true`, flags `cku`) -- expected: every class compiles with no errors. Use the `src/**` form, not `src/OcuPilot/*.cls`, which swallows the package into the base directory and loads the class unqualified. Read the error text; a clean local file is not evidence.
- IRIS MCP `iris_execute_classmethod` on `OcuPilot.Install.Installer` `Install` with no argument (`server: "ocupilot-iris"`) -- expected: `$$$OK`, and the production objects exist afterwards. There is no container start hook yet; this is how install is invoked until Story 1.4.
- IRIS MCP `iris_execute_tests` **per class** (`level: "class"`) for `OcuPilot.Test.Installer`, `OcuPilot.Test.State`, then the four Story 1.1 classes (`Routing`, `Envelope`, `Log`, `EntityId`) -- expected: zero failures each, and the epic total rises from 30 by exactly the number of new `Test*` methods. Aggregate the totals yourself.
- The `%UnitTest_Result` SQL probe from `.claude/rules/objectscript-testing.md` -- expected: the per-class totals match the aggregate above. **Mandatory before claiming the suite green** — the MCP runner envelope truncates per-class result lists and can under-report.
- `uv run scripts/check-objectscript.py` -- expected: exit 0 over `src/OcuPilot/**` and `ui/**`, including the two new rules. A bare `python3` runs outside the project environment.
- `bash scripts/lint-docs.sh` -- expected: exit 0 over the authored Markdown, including the new README subsection.
- `grep -rn "New \$ROLES\|AddRoles" src/OcuPilot/ --include=*.cls` -- expected: matches only in `Kernel/State/Base.cls` and `Test/State.cls`. This is the human-readable form of AC6; the checker is the enforced form.

**Mutations (Rule 19 — one per acceptance criterion and per pinning test; after each, revert and confirm `git status --short` and `git diff --stat` are unchanged):**

**QA independent re-verification (this section), 2026-09-09.** Every mutation below was
re-applied live by the QA pass — not merely re-read from the implementation pass's own
claim — via `iris_doc_load`+`iris_doc_compile` (`server: "ocupilot-iris"`), observed red,
reverted via `git checkout`, recompiled, and confirmed green, with `git status --short` /
`git diff --stat` empty after every revert. `mutations_demonstrated=12` (one per AC).
Three of the twelve mutations **as originally worded** did not actually falsify their
named test on this instance; each is corrected below with the mutation that does, and the
gap is reported rather than silently patched over, per this story's own "look for that
same shape elsewhere" instruction.

- **AC1** — mutation: in `Installer.cls`'s ensure-database step, pass `"%DB_%DEFAULT"` instead of the profile's `%DB_` resource to `SYS.Database.CreateDatabase` -> `TestCreatesGuardedDatabase` in `Test/Installer.cls` goes red naming the observed `ResourceName` against the expected one. Read the resource with `SYS.Database.%OpenId(dir).ResourceName`; `iris_database_list` reports it empty for every database and would make this assertion vacuous. **QA-confirmed live**: red (`AssertEquals: the database's ResourceName is the profile's %DB_ resource`), reverted, green, byte-identical.
- **AC2** — mutation: create the administrative role with `Resources = ""` -> `TestAdminRoleGrantsOnlyTheAdminResource` goes red naming the empty resource list. **QA-confirmed live**: red (`AssertEquals: the role grants only the administrative resource, with U`), reverted, green, byte-identical.
- **AC3** — mutation: change the mapping's `Database` property to a wrong literal (e.g. `IRISSYS`) -> `TestGlobalMappingTargetsTheGuardedDatabase` goes red naming the wrong target. **QA-confirmed live**: red (`AssertEquals: the mapping targets the probe's own database`), reverted, green, byte-identical. (Second mutation — adding a `Config.MapPackages.Create` call for `OcuPilot`, pinned by `TestNoPackageOrRoutineMappingIsCreated` — not independently re-run this pass; one demonstrated mutation per AC is the Rule 19 bar.)
- **AC4** — **corrected**: `"%DB_%DEFAULT"` does not work as the drift target on this instance — its `PublicPermission` is `0` (verified: `SELECT PublicPermission, Type FROM Security.Resources WHERE Name='%DB_%DEFAULT'` → `0, 3`) and the denial-probe user holds no grant on it either, so the denied reads would still raise `<PROTECT>`, just against a different (still-denying) resource — the test would stay green and falsely look like a pass. Corrected mutation: drift the production database's guarding resource to **`%DB_HSCUSTOM`** instead — the exact resource `Test/State.cls`'s own denial role legitimately grants `RW` on (verified: `HSCUSTOM`'s globals database `SYS.Database.ResourceName` = `%DB_HSCUSTOM`), so a mis-guarded `OCUPILOT` genuinely becomes reachable through the denied user's existing, unrelated grant — the real shape of "drift lets the wrong role in". Mutation: in the ensure-database repair path, wrap the drift-repair branch in `If 0 { ... }` so it never fires, then directly `SYS.Database.ModifyDatabase` production's `OCUPILOT` resource to `%DB_HSCUSTOM`, then re-run `Install("")` (mutated — does not repair) -> `TestDeniedUserCannotReachTheGlobal` in `Test/State.cls` goes red on all three assertions (both `<PROTECT>` codes and the escalated-read value check). **QA-confirmed live**: red, then reverted (`git checkout`, recompile) and repaired by re-running the real `Install("")`, confirmed `SYS.Database.%OpenId(dir).ResourceName` = `%DB_OCUPILOT` again (matching the pre-mutation `Size=1, GlobalJournalState=3`), full `OcuPilot.Test.State` class re-run 7/7 green. This is the one mutation that temporarily leaves the guarded database unprotected; done last, confirmed via direct `SYS.Database` reads before and after, not merely by trusting a subsequent green run.
- **AC5** — **corrected**: the spec's first mutation ("move `New $ROLES` out of `GuardedSave` into a helper the method calls -> `TestGuardedSaveRoundTrips` goes red with `<PROTECT>`") **does not falsify on this instance** — QA applied it live and the test stayed green, because the MCP connection this whole suite runs under authenticates as `_SYSTEM`, which holds `%All` (verified: `iris_user_get` on `_SYSTEM` → `roles: "%All,OcuPilotAdmin"`), and `%All` bypasses `%DB_OCUPILOT`'s resource check entirely regardless of whether `GuardedSave` escalates. `TestGuardedSaveRoundTrips` therefore cannot serve as AC5's pinning test under this connection; AC4's denial-user tests (a deliberately non-`%All` identity) remain the real proof that the resource check itself works. Second mutation, unaffected by the `%All` caveat (it inspects `$ROLES` content directly rather than relying on an actual permission check): delete the `New $ROLES` line from `GuardedRolesSnapshot` -> `TestRoleIsAbsentAfterTheFrameReturns` goes red naming the surviving role in `$ROLES` on both the normal-return and post-exception assertions. **QA-confirmed live** for the second mutation: red, reverted, green (full `OcuPilot.Test.State` class re-run 7/7), byte-identical. The first mutation's failure to falsify is reported here rather than silently dropped — same shape as AC7's already-flagged `TestInstallIsIdempotent` gap: a claimed mutation→test pairing that, checked live, does not actually fire.
- **AC6** — mutation: add `New $ROLES` to `src/OcuPilot/Api/Router.cls` (inside `ResolvedUsername`) -> `uv run scripts/check-objectscript.py` exits 1 naming file, line and rule. Second mutation: add a `Job` command to `src/OcuPilot/Kernel/State/Base.cls` (inside `GuardedExistsId`) -> the same command exits 1 naming the spawn rule. Both must be demonstrated, because Story 1.1's escalated DW-32 recorded that authoring an unvalidated gate is itself the failure mode. **QA-confirmed live, both**: each mutation alone produced exactly one problem naming the correct file/line/rule, reverted, checker back to `0 problem(s)` after each. No IRIS compile needed — `check-objectscript.py` runs against the working tree directly.
- **AC7** — the pinning test is **not** `TestInstallIsIdempotent`: a fingerprint compared against itself stays equal however wrong the fingerprint is, so that test alone cannot go red. It is pinned by `TestFingerprintChangesWhenAnObjectChanges` in `Test/Installer.cls`, which installs the `probe` profile, disables its privileged routine application, takes the fingerprint, re-installs (repairing it), takes it again, and asserts the two differ. mutation: drop `tAppEnabled` from `StateFingerprint()`'s composition -> `TestFingerprintChangesWhenAnObjectChanges` goes red because the two fingerprints are equal. **QA-confirmed live**: red (`AssertNotEquals: the fingerprint after repair differs from the fingerprint taken while drifted`), reverted, green, byte-identical. `Uninstall("probe", 1)` in `OnAfterOneTest` removes everything the test created.
- **AC8** — mutation: force `tNeedsRepair = 0` unconditionally in the ensure-application step's existing-object branch (equivalent to a bare `Exists()` early return — the drift-detect logic runs but its result is discarded) -> `TestSecondRunRepairsDriftedApplication` goes red naming the still-disabled application and the missing repair report. This is the sibling's exact defect, reproduced and pinned. **QA-confirmed live**: red on both assertions, reverted, green, byte-identical.
- **AC9** — mutation: remove the explicit `(pUsername = "UnknownUser")` clause from the placeholder guard -> `TestPlaceholderUserIsNotGranted` goes red naming the granted placeholder. **Verified this is a real, load-bearing check, not dead code**: `UnknownUser` and `_PUBLIC` are genuine stock `Security.Users` accounts on this instance (`Security.Users.Exists` returns `1` for both) — removing the explicit name check does not fall through to the "does not exist" fallback clause, because the fallback is false for an account that actually exists. **QA-confirmed live**: red (`AssertEquals: UnknownUser is treated as a placeholder, not granted`); the mutation, before revert, actually called `Security.Users.AddRoles("UnknownUser", "OcuPilotAdminProbe")` for real — confirmed via `iris_user_get` before/after that `UnknownUser`'s `roles` stayed `""` throughout (the granted role was removed along with `OcuPilotAdminProbe` itself by `OnAfterOneTest`'s `Uninstall("probe", 1)`), then reverted the code, recompiled, confirmed green and byte-identical. (Second mutation — removing the reported grant command from the skip branch, pinned by `TestSkippedGrantReportsTheRequiredCommand` — not independently re-run this pass.)
- **AC10** — **gap found and closed**: the spec names `TestAuditEventRoundTrips` as this AC's first pinning test, but **no method of that name exists anywhere in the tree**, and the nearest candidate (`TestGrantForRealAccountGrantsAndAudits`) cannot stand in for it — QA confirmed live that removing `EnsureAuditEvent`'s `Security.Events.Create` call leaves that test **green**, because production's `RoleGranted` audit event was registered by an earlier run and stays registered regardless of what a later `Install()` does; that test checks only that the round trip currently succeeds, which it does independent of the mutation. Closed by adding **`TestAuditEventRegistrationRestoresTheRoundTrip`** (QA) to `Test/Installer.cls`: deletes the probe's own `RoleGrantedProbe` registration first (making "not yet registered" a real, current fact), confirms `$System.Security.Audit` fails while it is absent, re-installs (the real, unmutated `EnsureAuditEvent`), and confirms the same call now succeeds. mutation: remove the `Security.Events.Create` call from the ensure-events step's absent-object branch -> `TestAuditEventRegistrationRestoresTheRoundTrip` goes red (`AssertTrue: the audit event is registered again after re-install; AssertStatusOK: ... ERROR #00`). **QA-confirmed live**: red, reverted, green, byte-identical (source files; the new test itself is a permanent addition, see file list below). A same-process `%SQL.Statement` count query taken immediately after `$System.Security.Audit`, in the same test, does **not** reflect the just-written row (verified live: `before=350` / `after=350` across two calls with a 2-second `Hang` between them; a later, separate SQL probe **did** see the row) — a `%SYS.Audit` SQL-visibility artifact, not a defect in this story's code, and the reason the new test asserts on `$System.Security.Audit`'s own `%Status` rather than a row count. Second mutation (make the auditing ensure step always report `enabled-now`, pinned by `TestSecondRunReportsAuditingAlreadyEnabled`) — not independently re-run this pass. **No mutation disables instance auditing**, and no test writes `AuditEnabled`.
- **AC11** — mutation: change the confirmation guard to `If 0 { ... }` (never fires regardless of `pConfirmDataLoss`) -> `TestUninstallRefusesWithoutConfirmation` goes red on all three assertions because the probe objects are actually destroyed when the test asserts they remain. **QA-confirmed live**: red, reverted, green, byte-identical; `OnBeforeOneTest`'s fresh probe re-install on the next method-level run absorbed the real deletion this mutation caused with no residue. (Second mutation — dropping `%File.RemoveDirectoryTree`, pinned by `TestUninstallLeavesNoResidue` — not independently re-run this pass.)
- **AC12** — **corrected**: the spec's first mutation ("move the `$NAMESPACE` restore below `Set tSC = ex.AsStatus()` in `Install`'s `Catch`, force a failure inside `%SYS` -> `TestNamespaceIsRestoredFirstOnTheErrorPath` goes red") **does not falsify on this instance** — QA applied the `Catch`-block reorder live and `TestNamespaceIsRestoredFirstOnTheErrorPath` stayed green, because that test's own induced failure (`Test/InstallerFault.cls`'s `EnsureGrant` override) returns an ordinary bad `%Status`, never throws, so it is caught by `Install`'s inline `If $$$ISERR(tSC) { Set $NAMESPACE = tOrigNS  Quit }` guards and never reaches the `Catch` block whose reordering was just mutated. Same shape as AC5 and AC7's flagged gaps: a claimed mutation→test pairing that does not actually fire. AC12's own second mutation is the one that does: make `Kernel.Utils.RestoreNamespace` a no-op -> `TestSwitchAndRestoreNamespaceRoundTrip` goes red (`AssertEquals: RestoreNamespace actually switches to the namespace it is given`). **QA-confirmed live**: red, reverted, green, byte-identical. (No test in the tree currently forces a genuine thrown exception, as opposed to an ordinary bad `%Status`, inside `Install`'s `%SYS`-switched window — the `Catch` block's own restore-first ordering is therefore unexercised by any pinning test; recorded as a residual gap rather than closed, since the two mutations above already meet Rule 19's one-per-AC bar and a third fault-injection subclass was judged out of proportion for this pass.)

**QA-added test files (Rule 8, discoverable in the default suite — `%UnitTest.TestCase` subclass under `src/OcuPilot/Test/`, method name starts with `Test`):**

- `src/OcuPilot/Test/Installer.cls` (QA) — one new method, `TestAuditEventRegistrationRestoresTheRoundTrip`, added to the existing class (AC10 gap closure above). Cleans up through the class's own `OnBeforeOneTest` / `OnAfterOneTest` unconditional `Install("probe")` / `Uninstall("probe", 1)` pair, same as every other method in this class; leaves no additional residue.

**How each test cleans up, and what is deliberately not reversible:**

- **`Test/Installer.cls`** runs every create, repair and uninstall assertion against the **`probe`** profile. `OnAfterOneTest` calls `Uninstall("probe", 1)` unconditionally, and the residue assertion re-lists databases, resources, roles, applications, mappings and audit events plus `%File.DirectoryExists` on the probe directory. Only two of its methods touch production: the idempotency pair, which calls `Install("")` twice — safe by definition, since a second install is a no-op.
- **`Test/State.cls`** creates one throwaway user and one purpose-built role in `OnBeforeOneTest` and deletes both in `OnAfterOneTest`, checking `Exists()` on each afterwards. It must **not** grant `%Operator`: that role carries `%DB_IRISSYS:RW`, which is a self-escalation primitive — the "denied" user could then `Set $ROLES` to anything and log in as `_SYSTEM`, and the denial assertions would fail for a reason unrelated to this story. Grant `%Admin_Operate:U` through the purpose-built role instead, and assert with `$SYSTEM.Security.CheckUserPermission` in setup that the user actually holds it — `Security.Users.Create` silently accepts a role name that does not exist.
- **Impersonation is confined to one method frame:** `New $ROLES` then `$SYSTEM.Security.Login(<user>)`, the guarded access inside a `Try`, the exception captured, and the assertion made **after** the method returns. `New $ROLES` restores both `$ROLES` and `$USERNAME` on the normal path and on the exception path (verified). Do the absolute minimum inside the frame — a de-privileged process cannot necessarily execute further code. Never rehearse this at a terminal: `NEW` in direct mode raises a stack level that persists until `QUIT`, so it will look broken when it is not.
- **Deliberately not reversible, by design:** the production install itself — the `OCUPILOT` database, `%DB_OCUPILOT`, `OcuPilotAdmin` (resource and role), the `OcuPilotState` application, the `OcuPilot*` mapping, the registered audit event, the role grant, and `AuditEnabled = 1`. These are the story's deliverable and Stories 1.4, 1.5, 1.16, 1.17 and Epics 3, 5 and 10 inherit them. **No test calls `Uninstall("")`**, and no test writes `AuditEnabled`.
- **Residue that survives every cleanup and cannot be removed:** audit rows. `Security.Users.Delete()` does not touch `%SYS.Audit`, so the throwaway user's denial rows and the security-change rows for every created and deleted object remain permanently. That is correct — erasing audit history to tidy a test run would be worse than the noise — and it is also useful evidence when reading a failed run.
- Any agent probing this instance concurrently should use a per-agent name suffix, not a bare shared prefix: IRIS role and resource names are case-insensitive (`NameLowerCase` is the IdKey), so two agents using the same prefix in different cases will delete each other's objects and each report a clean sweep.

**Manual checks:**

- Confirm the compiled `%Persistent` classes were given their **natural** data globals (`^OcuPilot.Kernel.State.StampD`, not a hashed name) and that those globals resolve to the guarded database — read `^|"^^<manager-dir>ocupilot/"|…` directly. A hashed global name would silently escape the `OcuPilot*` mapping and defeat the whole story; this is what the 29-character class-name rule exists to prevent.
- Confirm no `.cls` under `src/OcuPilot/` contains the substring `iris_` — the checker forbids it, and installer doc comments naturally want to name the MCP tools that load and compile them. Keep tool names in this spec, not in the source.
- Confirm `.vscode/settings.json` still carries `objectscript.conn.active: false`; loading and compiling go through the MCP tools only.

## Auto Run Result

**Summary of implemented change.** `OcuPilot.Install.Installer` creates, guard-then-act and idempotently, OcuPilot's protected state: the `OCUPILOT` database guarded by `%DB_OCUPILOT` (held by no ordinary role), the `OcuPilotAdmin` resource and role, the `OcuPilotState` privileged routine application, one `OcuPilot*` global mapping, the `OcuPilot/Security/RoleGranted` audit event, instance auditing, and an idempotent role grant with a documented fallback command. `OcuPilot.Kernel.State.Base` is the tree's one escalation point (guarded save/open/exists/global-get/global-set, plus a narrow escalated SQL primitive added during review); `OcuPilot.Kernel.State.Stamp` is the one row-per-run record proving the protection from both sides. A review pass (below) found and fixed two HIGH-severity defects (namespace not restored on an ordinary `%Status` failure inside the escalated window; the audit event's drift-repair path missing) and several real MEDIUM/LOW gaps before finalizing.

**Files changed:**
- `src/OcuPilot/Install/Installer.cls` (new) — the installer: `Install`, `Uninstall`, `StateFingerprint`, `ResolveNamespace`, nine ensure steps, deferred-report/flush seam.
- `src/OcuPilot/Kernel/State/Base.cls` (new) — the one escalation point: `GuardedSave`, `GuardedOpenId`, `GuardedExistsId`, `GuardedProbeSet`/`GuardedProbeGet`, `GuardedExecuteOneParam`, test-only role-snapshot methods.
- `src/OcuPilot/Kernel/State/Stamp.cls` (new) — one row per install run; `ResolveGrantedUser` (AD-37 weak reference); `DeleteByProfile`.
- `src/OcuPilot/Test/Installer.cls` (new) — 20 test methods covering AC1–AC12 and the review pass's fixes.
- `src/OcuPilot/Test/State.cls` (new) — 7 test methods covering AC4/AC5/AD-9 and the review pass's fixes.
- `src/OcuPilot/Test/InstallerProbe.cls` (new) — log-capture probe subclass (`Captured`, `CapturedAll`).
- `src/OcuPilot/Test/InstallerFault.cls` (new) — fault-injection probe subclass pinning AC12's ordinary-failure namespace-restore path.
- `scripts/check-objectscript.py` — two new rules (escalation containment, state-package isolation) sharing a refactored `iter_code_lines` helper.
- `README.md` — new "Installer: protected state and auditing" subsection.

**Review findings breakdown** (full detail and evidence in `## Review Triage Log` above): 27 findings from four independent review layers (blind-hunter, edge-case-hunter, verification-gap, intent-alignment) plus two more found by this agent while reading the diff before dispatching review. High 3 (all patched), medium 6 (all patched), low 12 (9 patched, 3 rejected as out-of-scope/non-trivial-for-low-value), false 6 (all rejected on refutation), maybe-false 0. Nothing was deferred — every real finding's fix turned out proportionate to apply directly; `deferred:` frontmatter remains `[]`.

**Follow-up review recommendation: `true`.** This pass patched HIGH-severity findings (two independent root causes) and multiple MEDIUM findings, all self-verified by this same agent via live mutation-observe-revert cycles rather than by a fresh reviewer. Specific unverified risk to check on a follow-up pass: (1) `Base.GuardedExecuteOneParam` and `Stamp.DeleteByProfile` are new escalated surface added during review, not present when the four review layers ran — a fresh pass should confirm the SQL text is always a call-site literal (never caller-influenced) and that purging stamps for the production profile (`""`) if an operator ever legitimately runs `Uninstall("")` is the intended behavior, not merely convenient for `"probe"`; (2) the extended `StateFingerprint` composite's completeness (whether any other live property this story's installer manages is still excluded).

**Verification performed:**
- `iris_doc_load` + `iris_doc_compile` (`server: "ocupilot-iris"`, `HSCUSTOM`, `src/**/*.cls`, `cku`) — all 22 project classes compile clean on every pass, including after each mutation and each revert.
- `iris_execute_classmethod` `OcuPilot.Install.Installer.Install()` with no argument — `$$$OK`; production objects confirmed present.
- `iris_execute_tests` per class: `OcuPilot.Test.Installer` 20/20, `OcuPilot.Test.State` 7/7, `OcuPilot.Test.Routing` 12/12, `OcuPilot.Test.Envelope` 10/10, `OcuPilot.Test.Log` 5/5, `OcuPilot.Test.EntityId` 3/3 — 57/57 total (epic total rose from 30 by 27 new `Test*` methods).
- `%UnitTest_Result` SQL ground-truth probe — 57/57, matching the per-class totals.
- `uv run scripts/check-objectscript.py` — exit 0 throughout, including the two new rules.
- `bash scripts/lint-docs.sh` — exit 0, including this section and the new README subsection.
- `grep -rn 'New \$ROLES\|\$SYSTEM\.Security\.AddRoles' src/OcuPilot/ --include='*.cls'` (code lines only) — matches only in `Kernel/State/Base.cls` and `Test/State.cls`.
- Manual checks: `^OcuPilot.Kernel.State.BaseD` (Base and Stamp's shared, natural — not hashed — data global) confirmed routed by the `OcuPilot*` mapping into the `OCUPILOT` database (`Config.MapGlobals.Get("HSCUSTOM","OcuPilot*",...)` → `Database="OCUPILOT"`); no `.cls` under `src/OcuPilot/` contains `iris_`; `.vscode/settings.json` still carries `"active": false`.
- Rule 19 mutations demonstrated live (apply → observe red → revert → confirm green and `md5`-identical) for: AC6 (two, by the implementation subagent), the namespace-restore fix (`TestNamespaceIsRestoredFirstOnTheErrorPath`), the audit-event repair fix (`TestSecondRunRepairsDriftedAuditEvent`), the auditing-outcome fix (`TestSecondRunReportsAuditingAlreadyEnabled`), `ResolveGrantedUser` (`TestResolveGrantedUserRendersDeletedAccountAsNoLongerPresent`), and the Stamp-purge fix (`TestUninstallPurgesStampHistoryForTheProfile`). Not separately mutation-demonstrated: the five other ensure steps' drift-repair branches (AC8's own spec-named mutation scopes to one representative object) and AC10's first mutation / AC1/AC2/AC3/AC5/AC7/AC9/AC11's mutations beyond what their own tests already drive as a live drift-detect-repair cycle.

**Residual risks:**
- **Container-level `<PROTECT>`/latency anomaly on `SYS.Database` delete, confirmed independently.** Repeated `Install("probe")`/`Uninstall("probe", 1)` cycling within one long-lived session intermittently raises `<PROTECT>Delete+9^SYS.Database.1` on the probe database and, even without that error, makes `Uninstall` calls that touch `SYS.Database.DeleteDatabase` take 20–40 minutes instead of ~1 second — reproduced three times in this session alone, always cleared immediately by `docker compose restart iris`, never reproduced on a freshly-restarted container's first few cycles. Tests remain correct (they pass once the slowness/lock resolves), so this was not treated as a code defect, but it is a real operational characteristic of this container/build worth flagging to whoever owns `Install/`'s smoke script (Story 1.17) and CI timeouts.
- Production `OcuPilot_Kernel_State.Stamp` accumulated 273 rows with `Profile=''` during this story's and this review's repeated `Install("")` testing — expected and by-design (FR-66/AC7: "the ensure steps still run" every time, and `RecordStamp` deliberately records one row per run), not cleaned by this story (`Uninstall("")` is never called by any test, per the spec's own boundary), and not itself a defect; a real retention/rotation policy for this table, if ever needed, is a decision for whichever story starts reading it (1.4+).
- The two items named under "Follow-up review recommendation" above.

Status: done
Blocking condition: none
