diff --git a/_bmad-output/planning-artifacts/ux-designs/ux-OcuPilot-2026-09-08/EXPERIENCE.md b/_bmad-output/planning-artifacts/ux-designs/ux-OcuPilot-2026-09-08/EXPERIENCE.md
index ed0e201..32d7972 100644
--- a/_bmad-output/planning-artifacts/ux-designs/ux-OcuPilot-2026-09-08/EXPERIENCE.md
+++ b/_bmad-output/planning-artifacts/ux-designs/ux-OcuPilot-2026-09-08/EXPERIENCE.md
@@ -394,6 +394,10 @@ Microcopy. Brand voice and aesthetic posture live in `DESIGN.md`.
 | "Fill in every masked field to confirm." | the published reason a proposal card's Confirm is `aria-disabled` while a secret the write sends is still empty (Story 5.10, `:427`), reached through `aria-describedby` so the reason is announced rather than left to the disabled state to imply |
 | "create a web application for a REST API" | the Web applications list's agent invitation (Story 8.1, FR-30): the list declares a primary action now, so its empty state invites the agent instead of naming a next step, and this is the phrase the shared write-capable empty state resolves [ADDED 2026-09-22 - see the story change log] |
 | "New web application" · "Application type" · "CSP/ZEN" · "REST" · "Python (WSGI or ASGI)" · "Python protocol type" · "Application file" · "Callable name" · "Application directory" · "Include subdirectories" · "create a web application" | the create-a-web-application form (Story 8.1, FR-30): its screen title, the type control and its three options, the five fields a Python application adds, and the action a privilege refusal on this form names in the published "You need &lt;resource&gt; to &lt;action&gt;." sentence (`:236`). Every other label it draws is a key that already exists - the name, description, namespace, enabled, dispatch class and resource columns (`:316`), the Services list's own "Authentication methods" heading (`:319`), the required-fields legend (`:341`), Save, Cancel and the saved confirmation. Each authentication method is named by the instance itself, in the words the classic editor draws it with, and is never republished here [ADDED 2026-09-22 - see the story change log] |
+| "create a user account" | the Users list's agent invitation (Story 8.2, FR-30): the list declares a primary action now, so its empty state invites the agent instead of naming a next step, and this is the phrase the shared write-capable empty state resolves [ADDED 2026-09-23 - see the story change log] |
+| "New user" · "Account expiration date" · "Startup namespace" · "Startup tag^routine" · "create a user" | the create-a-user form (Story 8.2, FR-30): its screen title, the three labels the classic editor carries that no existing key holds, and the action a privilege refusal on this form names in the published "You need &lt;resource&gt; to &lt;action&gt;." sentence (`:236`). Every other label it draws is a key that already exists - the name, full name and roles columns (`:316`), the password field, the reveal toggle's two names, the stored-secret caption, the required-fields legend (`:341`), Save, Cancel and the saved confirmation. Each role is named by the instance itself, and a role the server refuses to grant is marked with the server's own sentence, never one published here [ADDED 2026-09-23 - see the story change log] |
+| "Resolved directory on this instance" | the read-only line under a Python web application's Application directory field (Story 8.2, DW-1495, AD-21): it labels the directory the typed name resolves to under the instance's fixed WSGI root, which the server computes at the call; the form displays it and never sends it [ADDED 2026-09-23 - see the story change log] |
+| "Anyone who can reach this address can use the application without signing in." | the effect of the Unauthenticated method on a web application (Story 8.2, DW-1489): shown at the create form's authentication-method field while that method is ticked, and as a line on the proposal card of a create the kernel marks with that consequence. It warns and refuses nothing [ADDED 2026-09-23 - see the story change log] |
 
 ## Component Patterns
 
diff --git a/scripts/ci-throwaway.sh b/scripts/ci-throwaway.sh
index e6ec7bb..8bb1889 100755
--- a/scripts/ci-throwaway.sh
+++ b/scripts/ci-throwaway.sh
@@ -201,6 +201,7 @@ services:
       # classes: ToolSetFull
       # classes: ToolWire, TurnContext, TurnConversation, TurnLong, TurnProviderFault, TurnWire
       # classes: WebAppWire
+      # classes: UserCreateWire
       # classes: TurnWireFixture, UnexpireScope, UserUpdate, Version, Wire, WireOAuthRead, WireSecurityRead
       OCUPILOT_ALLOW_PRINCIPALS: "1"
       # Writes an application error to a namespace's own ^ERRORS. Same reasoning again, and one
diff --git a/src/OcuPilot/Api/Error.cls b/src/OcuPilot/Api/Error.cls
index e2cb426..e712dec 100644
--- a/src/OcuPilot/Api/Error.cls
+++ b/src/OcuPilot/Api/Error.cls
@@ -1090,6 +1090,19 @@ ClassMethod ReasonForViolation(pCode As %String) As %String
     If pCode = ..#WEBAPPWSGITYPE Quit ..#REASONWEBAPPWSGITYPE
     If pCode = ..#WEBAPPTYPECONFLICT Quit ..#REASONWEBAPPTYPECONFLICT
     If pCode = ..#WEBAPPFIELDLENGTH Quit ..#REASONWEBAPPFIELDLENGTH
+    If pCode = ..#WEBAPPWSGIAPPLOCATIONSHAPE Quit ..#REASONWEBAPPWSGIAPPLOCATIONSHAPE
+    If pCode = ..#USERNAMEREQUIRED Quit ..#REASONUSERNAMEREQUIRED
+    If pCode = ..#USERNAMESHAPE Quit ..#REASONUSERNAMESHAPE
+    If pCode = ..#USERNAMETAKEN Quit ..#REASONUSERNAMETAKEN
+    If pCode = ..#USERPASSWORDREQUIRED Quit ..#REASONUSERPASSWORDREQUIRED
+    If pCode = ..#USERPASSWORDPOLICY Quit ..#REASONUSERPASSWORDPOLICY
+    If pCode = ..#USERFULLNAMELENGTH Quit ..#REASONUSERFULLNAMELENGTH
+    If pCode = ..#USEREXPIRATIONDATESHAPE Quit ..#REASONUSEREXPIRATIONDATESHAPE
+    If pCode = ..#USERNAMESPACEUNKNOWN Quit ..#REASONUSERNAMESPACEUNKNOWN
+    If pCode = ..#USERROUTINENAMESPACE Quit ..#REASONUSERROUTINENAMESPACE
+    If pCode = ..#USERROUTINELENGTH Quit ..#REASONUSERROUTINELENGTH
+    If pCode = ..#USERROLESUNKNOWN Quit ..#REASONUSERROLESUNKNOWN
+    If pCode = ..#USERROLESSHAPE Quit ..#REASONUSERROLESSHAPE
     If pCode = ..#PORTFIELDREQUIRED Quit ..#REASONPORTFIELDREQUIRED
     If pCode = ..#PORTFIELDUNEXPECTED Quit ..#REASONPORTFIELDUNEXPECTED
     If pCode = ..#PORTFIELDSHAPE Quit ..#REASONPORTFIELDSHAPE
@@ -1102,7 +1115,16 @@ ClassMethod ReasonForViolation(pCode As %String) As %String
 /// added with no sentence behind it reddens.</p>
 ClassMethod WebAppViolationCodes() As %List
 {
-    Quit $ListBuild(..#WEBAPPNAMEREQUIRED, ..#WEBAPPNAMESHAPE, ..#WEBAPPNAMETAKEN, ..#WEBAPPNAMESPACEREQUIRED, ..#WEBAPPAUTHEREQUIRED, ..#WEBAPPAUTHEUNKNOWN, ..#WEBAPPDISPATCHREQUIRED, ..#WEBAPPWSGIAPPNAME, ..#WEBAPPWSGILOCATION, ..#WEBAPPWSGITYPE, ..#WEBAPPTYPECONFLICT, ..#WEBAPPFIELDLENGTH, ..#PORTFIELDREQUIRED, ..#PORTFIELDUNEXPECTED, ..#PORTFIELDSHAPE)
+    Quit $ListBuild(..#WEBAPPNAMEREQUIRED, ..#WEBAPPNAMESHAPE, ..#WEBAPPNAMETAKEN, ..#WEBAPPNAMESPACEREQUIRED, ..#WEBAPPAUTHEREQUIRED, ..#WEBAPPAUTHEUNKNOWN, ..#WEBAPPDISPATCHREQUIRED, ..#WEBAPPWSGIAPPNAME, ..#WEBAPPWSGILOCATION, ..#WEBAPPWSGITYPE, ..#WEBAPPTYPECONFLICT, ..#WEBAPPFIELDLENGTH, ..#WEBAPPWSGIAPPLOCATIONSHAPE, ..#PORTFIELDREQUIRED, ..#PORTFIELDUNEXPECTED, ..#PORTFIELDSHAPE)
+}
+
+/// <p>Every <code>USER.*</code> field-level violation code, as a <class>%List</class>, for the
+/// reason <method>WebAppViolationCodes</method> exists: a test holds
+/// <method>ReasonForViolation</method> to the vocabulary, so a code added with no sentence behind
+/// it reddens.</p>
+ClassMethod UserViolationCodes() As %List
+{
+    Quit $ListBuild(..#USERNAMEREQUIRED, ..#USERNAMESHAPE, ..#USERNAMETAKEN, ..#USERPASSWORDREQUIRED, ..#USERPASSWORDPOLICY, ..#USERFULLNAMELENGTH, ..#USEREXPIRATIONDATESHAPE, ..#USERNAMESPACEUNKNOWN, ..#USERROUTINENAMESPACE, ..#USERROUTINELENGTH, ..#USERROLESUNKNOWN, ..#USERROLESSHAPE)
 }
 
 /// <p>Every <code>AGENT.*</code> field-level violation code that carries a sentence, as a
@@ -1584,6 +1606,98 @@ Parameter WEBAPPFIELDLENGTH = "WEBAPP.FIELD.LENGTH";
 
 Parameter REASONWEBAPPFIELDLENGTH = "That value is longer than this instance stores for the field.";
 
+/// <p>Violation code: a WSGI or ASGI application's directory is not one directory name that
+/// resolves under this instance's own WSGI root (AD-21, <class>OcuPilot.Area.WebApp.Location</class>).
+/// A path, a <code>..</code> and a name outside the pattern are each refused rather than sent.</p>
+Parameter WEBAPPWSGIAPPLOCATIONSHAPE = "WEBAPP.WSGIAPPLOCATION.SHAPE";
+
+Parameter REASONWEBAPPWSGIAPPLOCATIONSHAPE = "Name one directory of letters, digits, dots, dashes and underscores, at most 64 characters; it is placed in this instance's own WSGI directory. Any other location is set in the classic portal.";
+
+/// <p>Machine code (AD-12, AD-39): the request to create a user broke one or more of the rules
+/// <class>OcuPilot.Area.Permissions.UserCreateRules</class> applies before the port is touched.
+/// Rendered on the <parameter>VALIDATIONFAILED</parameter> slug with every violation in
+/// <code>detail.violations</code>, the shape <parameter>WEBAPPVALIDATION</parameter> takes.</p>
+Parameter USERVALIDATION = "USER.VALIDATION";
+
+/// <p>The envelope's <code>reason</code> for <parameter>USERVALIDATION</parameter>, which the
+/// screen renders only where no violation named a field.</p>
+Parameter REASONUSERVALIDATION = "The user was refused.";
+
+/// <p>Violation code: no name was supplied. A user's name is the vendor's <code>name</code> query
+/// parameter rather than a body field.</p>
+Parameter USERNAMEREQUIRED = "USER.NAME.REQUIRED";
+
+Parameter REASONUSERNAMEREQUIRED = "Give the account a name.";
+
+/// <p>Violation code: the name is not one this instance can hold -- longer than
+/// <class>%Library.Username</class> stores, or carrying a control character or an <code>@</code>.</p>
+Parameter USERNAMESHAPE = "USER.NAME.SHAPE";
+
+Parameter REASONUSERNAMESHAPE = "A user name is at most 160 characters, with no control character and no @ sign.";
+
+/// <p>Violation code: an account already carries that name. <class>Security.Users</class> keys on
+/// the lower-cased name, so a name taken in any case is taken. A read rather than a validation --
+/// <code>GET /users/name</code> answers it on blur.</p>
+Parameter USERNAMETAKEN = "USER.NAME.TAKEN";
+
+Parameter REASONUSERNAMETAKEN = "This instance already has a user with that name. Choose a different one.";
+
+/// <p>Violation code: no password was supplied. The vendor's create requires one.</p>
+Parameter USERPASSWORDREQUIRED = "USER.PASSWORD.REQUIRED";
+
+Parameter REASONUSERPASSWORDREQUIRED = "Give the account a password.";
+
+/// <p>Violation code: the password does not match <property>Security.System.PasswordPattern</property>.
+/// The sentence names the policy, never the value (AD-35).</p>
+Parameter USERPASSWORDPOLICY = "USER.PASSWORD.POLICY";
+
+Parameter REASONUSERPASSWORDPOLICY = "The password does not meet this instance's password policy.";
+
+/// <p>Violation code: the full name is longer than <property>Security.Users.FullName</property>
+/// stores.</p>
+Parameter USERFULLNAMELENGTH = "USER.FULLNAME.LENGTH";
+
+Parameter REASONUSERFULLNAMELENGTH = "That full name is longer than this instance stores.";
+
+/// <p>Violation code: the expiration date is not a real date in the <code>YYYY-MM-DD</code> form
+/// <property>Security.Users.ExpirationDate</property> is set from.</p>
+Parameter USEREXPIRATIONDATESHAPE = "USER.EXPIRATIONDATE.SHAPE";
+
+Parameter REASONUSEREXPIRATIONDATESHAPE = "An expiration date is a real date, written YYYY-MM-DD.";
+
+/// <p>Violation code: the startup namespace is not a namespace this instance holds.</p>
+Parameter USERNAMESPACEUNKNOWN = "USER.NAMESPACE.UNKNOWN";
+
+Parameter REASONUSERNAMESPACEUNKNOWN = "That is not a namespace on this instance.";
+
+/// <p>Violation code: a startup routine was named with no startup namespace to run it in.</p>
+Parameter USERROUTINENAMESPACE = "USER.ROUTINE.NAMESPACE";
+
+Parameter REASONUSERROUTINENAMESPACE = "A startup routine runs in a startup namespace, so choose one as well.";
+
+/// <p>Violation code: the startup routine is longer than <property>Security.Users.Routine</property>
+/// stores.</p>
+Parameter USERROUTINELENGTH = "USER.ROUTINE.LENGTH";
+
+Parameter REASONUSERROUTINELENGTH = "A startup tag^routine is at most 64 characters.";
+
+/// <p>Violation code: a role named is not one this instance holds.
+/// <method>Security.Users.Create</method> accepts an unknown role silently, so the rule is
+/// OcuPilot's.</p>
+Parameter USERROLESUNKNOWN = "USER.ROLES.UNKNOWN";
+
+Parameter REASONUSERROLESUNKNOWN = "One of those roles does not exist on this instance.";
+
+/// <p>Violation code: the roles are not a list of role names.</p>
+Parameter USERROLESSHAPE = "USER.ROLES.SHAPE";
+
+Parameter REASONUSERROLESSHAPE = "Roles are a list of role names.";
+
+/// <p>The sentence a privileged role carries on the Roles field (AD-10, AD-39): the reason of the
+/// privilege-grant violation the screen's create answers, and the sentence
+/// the form's bootstrap read ships for the picker's pre-mark, so the two are one wording.</p>
+Parameter REASONUSERROLESPRIVILEGED = "This role grants %All or an administrative privilege, which OcuPilot does not grant. Assign it in the classic portal.";
+
 /// <p>Violation code (AD-39): the vendor refused the request because a body field it requires was
 /// absent. Produced at the port boundary by <method>ViolationsFromVendor</method>, on the field the
 /// vendor's own status names, so a refusal lands on the field it is about.</p>
diff --git a/src/OcuPilot/Api/Router.cls b/src/OcuPilot/Api/Router.cls
index f833b64..7fd8fad 100644
--- a/src/OcuPilot/Api/Router.cls
+++ b/src/OcuPilot/Api/Router.cls
@@ -94,6 +94,9 @@ XData UrlMap [ XMLNamespace = "http://www.intersystems.com/urlmap" ]
   <Route Url="/web-applications/form" Method="GET" Call="WebAppFormRules"/>
   <Route Url="/web-applications/name" Method="GET" Call="WebAppNameCheck"/>
   <Route Url="/web-applications" Method="POST" Call="WebAppCreate"/>
+  <Route Url="/users/form" Method="GET" Call="UserFormRules"/>
+  <Route Url="/users/name" Method="GET" Call="UserNameCheck"/>
+  <Route Url="/users" Method="POST" Call="UserCreate"/>
   <Route Url="/screens/:screen/read" Method="GET" Call="ScreenRead"/>
   <Route Url="/logs/errors/namespaces" Method="GET" Call="LogErrorNamespaces"/>
   <Route Url="/logs/errors/dates" Method="GET" Call="LogErrorDates"/>
@@ -279,6 +282,28 @@ ClassMethod WebAppCreate() As %Status
     Quit ##class(OcuPilot.Area.WebApp.Create).HandleCreate()
 }
 
+/// <p>Thin <code>Call=</code> target for <code>GET /users/form</code> (Story 8.2): delegates to
+/// <class>OcuPilot.Area.Permissions.UserCreateRules</class> and carries no logic of its own. The
+/// two sub-resources precede <code>POST /users</code>, the order the REST convention requires.</p>
+ClassMethod UserFormRules() As %Status
+{
+    Quit ##class(OcuPilot.Area.Permissions.UserCreateRules).HandleForm()
+}
+
+/// <p>Thin <code>Call=</code> target for <code>GET /users/name</code>: the User form's blur check
+/// of whether a name is already taken.</p>
+ClassMethod UserNameCheck() As %Status
+{
+    Quit ##class(OcuPilot.Area.Permissions.UserCreateRules).HandleName()
+}
+
+/// <p>Thin <code>Call=</code> target for <code>POST /users</code>: the User form's Save, which
+/// resolves the write through the one tool the agent's confirm uses (AD-55).</p>
+ClassMethod UserCreate() As %Status
+{
+    Quit ##class(OcuPilot.Area.Permissions.UserCreate).HandleCreate()
+}
+
 /// <p>Thin <code>Call=</code> target for <code>GET /screens/:screen/read</code>: delegates to
 /// <class>OcuPilot.Api.ScreenRead</class> and carries no logic of its own.</p>
 ClassMethod ScreenRead(pScreen As %String) As %Status
diff --git a/src/OcuPilot/Area/Permissions/UserCreate.cls b/src/OcuPilot/Area/Permissions/UserCreate.cls
new file mode 100644
index 0000000..1f53a94
--- /dev/null
+++ b/src/OcuPilot/Area/Permissions/UserCreate.cls
@@ -0,0 +1,233 @@
+/// <p>The screen's half of a user create (AD-55), over <code>POST /users</code>. The agent's half
+/// is the same tool class (<class>OcuPilot.Screen.Tool.UserCreate</class>) read by
+/// <class>OcuPilot.Kernel.Proposal.Mint</class> and <class>OcuPilot.Kernel.Proposal.Confirm</class>,
+/// and its mint applies <method>OcuPilot.Area.Permissions.UserCreateRules.Validate</method>'s rules.</p>
+/// <p><b>It resolves everything about the write from the tool class</b>: the endpoint, the request
+/// type, the port, the settable field list and the payload composition. It mints no proposal and
+/// takes no confirm token. In order: the caller's own pairs (AD-8); the name's shape; AD-10's
+/// prohibited set over the caller's own fields minus the declared secrets, a privileged role
+/// answered as a violation on the Roles field; the form's rules; the composed payload with the
+/// password merged in after that verdict, as <method>OcuPilot.Kernel.Proposal.Confirm</method>
+/// merges a supplied secret; and only then the port, which builds the vendor's wrapper.</p>
+/// <p><b>The password is a secret</b> (AD-35): it is never logged, echoed or returned, and the
+/// created account the route answers is the vendor's own read, which carries none.</p>
+/// <p>Seams a subclass may override: <method>PortClass</method> and <method>ProhibitedClass</method>.
+/// Production overrides neither. The name look-up goes through
+/// <method>OcuPilot.Area.Permissions.UserCreateRules.Taken</method>, which resolves the tool's own
+/// port directly.</p>
+Class OcuPilot.Area.Permissions.UserCreate Extends %RegisteredObject
+{
+
+/// <p>The tool class both callers resolve the write through (AD-55).</p>
+Parameter TOOLCLASS = "OcuPilot.Screen.Tool.UserCreate";
+
+/// <p>The form screen whose declared pairs gate this route (AD-8).</p>
+Parameter DESCRIPTORCLASS = "OcuPilot.Screen.Descriptor.UserForm";
+
+/// <p>The input property that names the target: the vendor's <code>name</code> query parameter.</p>
+Parameter IDARGUMENT = "Name";
+
+/// <p>The field a privileged role's refusal lands on.</p>
+Parameter FIELDROLES = "Roles";
+
+/// <p>The field whose presence is a privilege grant whatever it holds; its refusal is the
+/// prohibited set's own envelope rather than a Roles violation.</p>
+Parameter FIELDESCALATIONROLES = "EscalationRoles";
+
+ClassMethod ToolClass() As %String
+{
+    Quit ..#TOOLCLASS
+}
+
+/// <p>The port the write goes through, which is the tool's own declaration (AD-52). Overridable so
+/// a fixture can observe what was sent; production never overrides it.</p>
+ClassMethod PortClass() As %String
+{
+    Quit $ClassMethod(..#TOOLCLASS, "PortClass")
+}
+
+/// <p>AD-10's set, asked before the port is touched. Overridable for the reason
+/// <method>PortClass</method> is; production never overrides it.</p>
+ClassMethod ProhibitedClass() As %String
+{
+    Quit "OcuPilot.Kernel.Proposal.Prohibited"
+}
+
+/// <p>Answer <code>POST /users</code>: create one account from the body and answer
+/// <code>{name, user}</code> with 201, the account as the instance then holds it. A broken rule is
+/// one 422 <parameter>OcuPilot.Api.Error.USERVALIDATION</parameter> envelope carrying every
+/// violation; any other refusal is the port's or the prohibited set's own envelope.</p>
+ClassMethod HandleCreate() As %Status
+{
+    Set tSC = $$$OK
+    Try {
+        Set tGateSC = ..Gate(.tRefused)
+        If tRefused || $$$ISERR(tGateSC) {
+            Set tSC = tGateSC
+            Quit
+        }
+        Set tReadSC = ##class(OcuPilot.Kernel.Utils).ReadRequestBody(.tBody, .tStage)
+        If $$$ISERR(tReadSC) || '$IsObject(tBody) || 'tBody.%IsA("%Library.DynamicObject") {
+            Set tSC = ##class(OcuPilot.Api.Error).Render(400, ##class(OcuPilot.Api.Error).#BADREQUEST, ##class(OcuPilot.Api.Error).#REASONAGENTBADBODY, ##class(OcuPilot.Api.Error).#AGENTBADBODY)
+            Quit
+        }
+
+        Set tName = ""
+        If tBody.%GetTypeOf(..#IDARGUMENT) = "string" Set tName = tBody.%Get(..#IDARGUMENT)
+        Set tPerformSC = ..Perform(tName, tBody, .tCreated, .tHttp, .tFault, .tViolations)
+        Set tBody = ""
+        If $Get(tViolations) > 0 {
+            Set tSC = ..RenderViolations(.tViolations)
+            Quit
+        }
+        If $$$ISERR(tPerformSC) {
+            If '$IsObject($Get(tFault)) || (tFault.code = ##class(OcuPilot.Api.Error).#INTERNAL) {
+                Set tSC = ##class(OcuPilot.Api.Error).RenderInternal("An internal error occurred", ##class(%Exception.StatusException).CreateFromStatus(tPerformSC))
+                Quit
+            }
+            Set tSC = ##class(OcuPilot.Api.Error).Render($Select(+$Get(tHttp): +tHttp, 1: 500), tFault.error, tFault.reason, tFault.code, tFault.%Get("detail"))
+            Quit
+        }
+        Set tAnswer = {}
+        Do tAnswer.%Set("name", tName, "string")
+        Do tAnswer.%Set("user", $Select($IsObject($Get(tCreated)): tCreated, 1: {}))
+        Set tSC = ##class(OcuPilot.Api.Response).JSONStatus(201, tAnswer)
+    } Catch ex {
+        Set tSC = ##class(OcuPilot.Api.Error).RenderInternal("An internal error occurred", ex)
+    }
+    Quit tSC
+}
+
+/// <p>Create the account <var>pName</var> from <var>pArgs</var>, and answer the account the
+/// instance then holds in <var>pCreated</var>.</p>
+/// <p><b>Caller contract:</b> <var>pViolations</var> is the violation array
+/// (<code>pViolations(n) = $ListBuild(field, code[, reason])</code>, count in
+/// <code>pViolations</code>); a non-zero count means nothing was written and the other outputs say
+/// nothing. Otherwise an error status carries <var>pHttpStatus</var> and <var>pFault</var>, and
+/// success carries the created account.</p>
+ClassMethod Perform(pName As %String, pArgs As %DynamicObject, Output pCreated As %DynamicObject, Output pHttpStatus As %Integer, Output pFault As %DynamicObject, Output pViolations) As %Status
+{
+    Set pCreated = ""
+    Set pHttpStatus = 0
+    Set pFault = ""
+    Kill pViolations
+    Set pViolations = 0
+    Set tSC = $$$OK
+    Try {
+        Set tRules = "OcuPilot.Area.Permissions.UserCreateRules"
+        Do $ClassMethod(tRules, "NameViolation", pName, .pViolations)
+        If pViolations > 0 Quit
+
+        Set tClass = ..#TOOLCLASS
+        Set tRefSC = ##class(OcuPilot.Kernel.EntityRef).Key($ClassMethod(..#DESCRIPTORCLASS, "PrimaryEntityType"), ##class(OcuPilot.Kernel.EntityRef).ScopeFor($ClassMethod(..#DESCRIPTORCLASS, "Scope")), pName, .tTargetRef)
+        If $$$ISERR(tRefSC) {
+            Set tSC = tRefSC
+            Quit
+        }
+
+        ; AD-10, AD-55. The set the confirm asks, asked before the port and before the field rules,
+        ; over the caller's own fields -- minus the declared secrets, which the confirm merges only
+        ; after its verdict too.
+        Set tSecrets = $ClassMethod(tClass, "SecretArguments")
+        Set tProhibitSC = $ClassMethod(..ProhibitedClass(), "Prohibits", $Parameter(tClass, "TOOLNAME"), tTargetRef, ..CallerFields(pArgs, tSecrets).%ToJSON(), "[]", .tProhibits, .tProhibitCode)
+        If $$$ISERR(tProhibitSC) {
+            Set tSC = tProhibitSC
+            Quit
+        }
+        If tProhibits {
+            If (tProhibitCode = ##class(OcuPilot.Kernel.Proposal.Prohibited).#PRIVILEGEGRANT) && (pArgs.%GetTypeOf(..#FIELDESCALATIONROLES) = "unassigned") {
+                ; AD-39: the refusal lands on the field it concerns, in the sentence the form's
+                ; bootstrap read ships for the picker's pre-mark.
+                Set pViolations($Increment(pViolations)) = $ListBuild(..#FIELDROLES, tProhibitCode, ##class(OcuPilot.Api.Error).#REASONUSERROLESPRIVILEGED)
+                Quit
+            }
+            Set pHttpStatus = 403
+            Set pFault = ##class(OcuPilot.Kernel.Fault).Build(##class(OcuPilot.Api.Error).#FORBIDDEN, tProhibitCode, ##class(OcuPilot.Kernel.Proposal.Prohibited).ReasonFor(tProhibitCode))
+            Set tSC = $$$ERROR($$$GeneralError, tProhibitCode)
+            Quit
+        }
+
+        Set tSC = $ClassMethod(tRules, "Validate", pName, pArgs, .pViolations)
+        If $$$ISERR(tSC) || (pViolations > 0) Quit
+
+        Set tComposeSC = ##class(OcuPilot.Kernel.Proposal.Mint).Compose(pArgs, ..#IDARGUMENT, $ClassMethod(tClass, "SettableFields"), .tPayload, .tDiff, .tUnchanged, .tProblem)
+        If $$$ISERR(tComposeSC) {
+            Set tSC = tComposeSC
+            Quit
+        }
+        If tProblem '= "" {
+            Set tSC = $$$ERROR($$$GeneralError, "the create's payload could not be composed after its rules passed")
+            Quit
+        }
+
+        ; AD-35: the declared secrets join the body here, after the verdict and the rules, and
+        ; nowhere else.
+        For tI = 1:1:$ListLength(tSecrets) {
+            Set tSecret = $List(tSecrets, tI)
+            If pArgs.%GetTypeOf(tSecret) = "string" Do tPayload.%Set(tSecret, pArgs.%Get(tSecret), "string")
+        }
+
+        Kill tQuery
+        Set tQuery($ClassMethod(tClass, "IdParam")) = pName
+        Set tSC = $ClassMethod(..PortClass(), "Invoke", $ClassMethod(tClass, "Endpoint"), $ClassMethod(tClass, "WriteType"), .tQuery, tPayload, .pCreated, .pHttpStatus, .pFault)
+        Set tPayload = ""
+    } Catch ex {
+        Set pCreated = ""
+        Set tSC = ex.AsStatus()
+    }
+    Quit tSC
+}
+
+/// <p>The caller's own body fields, minus the identifying argument and minus every name in
+/// <var>pSecrets</var>, as one <class>%DynamicObject</class>: what AD-10's set is asked about on the
+/// screen path. It is not the composed payload, so a field the tool does not admit reaches the set
+/// rather than being silently dropped.</p>
+ClassMethod CallerFields(pArgs As %DynamicObject, pSecrets As %List = "") As %DynamicObject
+{
+    Set tFields = {}
+    If '$IsObject(pArgs) Quit tFields
+    Set tIterator = pArgs.%GetIterator()
+    While tIterator.%GetNext(.tKey, .tValue) {
+        If tKey = ..#IDARGUMENT Continue
+        If $ListFind(pSecrets, tKey) Continue
+        Set tType = pArgs.%GetTypeOf(tKey)
+        If (tType = "object") || (tType = "array") {
+            Do tFields.%Set(tKey, pArgs.%Get(tKey))
+        } Else {
+            Do tFields.%Set(tKey, pArgs.%Get(tKey), tType)
+        }
+    }
+    Quit tFields
+}
+
+/// <p>Render one <code>(field, code)</code> violation as the 422 envelope.</p>
+ClassMethod RenderOne(pField As %String, pCode As %String) As %Status
+{
+    Kill tViolations
+    Set tViolations = 1
+    Set tViolations(1) = $ListBuild(pField, pCode)
+    Quit ..RenderViolations(.tViolations)
+}
+
+/// <p>Render <var>pViolations</var> as one 422 envelope carrying every broken rule in
+/// <code>detail.violations</code>, each a <code>{field, code, reason}</code> object (AD-39).</p>
+ClassMethod RenderViolations(ByRef pViolations) As %Status
+{
+    Set tDetail = {}
+    Do tDetail.%Set("violations", ##class(OcuPilot.Kernel.AgentRules).ViolationsJson(.pViolations))
+    Quit ##class(OcuPilot.Api.Error).Render(422, ##class(OcuPilot.Api.Error).#VALIDATIONFAILED, ##class(OcuPilot.Api.Error).#REASONUSERVALIDATION, ##class(OcuPilot.Api.Error).#USERVALIDATION, tDetail)
+}
+
+/// <p>Refuse a caller who does not hold <parameter>DESCRIPTORCLASS</parameter>'s declared pairs
+/// (AD-8): <var>pRefused</var> 1 means the envelope is written and the handler writes nothing
+/// else.</p>
+ClassMethod Gate(Output pRefused As %Boolean) As %Status
+{
+    Set pRefused = 0
+    Set tFailedPair = ""
+    If ##class(OcuPilot.Screen.Gate).Evaluate(..#DESCRIPTORCLASS, .tFailedPair) Quit $$$OK
+    Set pRefused = 1
+    Quit ##class(OcuPilot.Kernel.Denial).Envelope(tFailedPair)
+}
+
+}
diff --git a/src/OcuPilot/Area/Permissions/UserCreateRules.cls b/src/OcuPilot/Area/Permissions/UserCreateRules.cls
new file mode 100644
index 0000000..0760373
--- /dev/null
+++ b/src/OcuPilot/Area/Permissions/UserCreateRules.cls
@@ -0,0 +1,469 @@
+/// <p>The User form's rules (AD-39, AD-55): the one rule set both callers of
+/// <class>OcuPilot.Screen.Tool.UserCreate</class> apply (<method>Validate</method>), the form's
+/// bootstrap read (<method>HandleForm</method>), and the name look-up
+/// (<method>HandleName</method>, <method>Taken</method>).</p>
+/// <p><b>Everything the vendor's post-create <code>Modify</code> reads is validated first.</b>
+/// <code>%Api.Admin.Endpoints.Security.User:RunPost</code> calls <method>Security.Users.Create</method>
+/// with the password and only then <code>Modify</code> with the other fields, so a field
+/// <code>Modify</code> refuses would leave an account created with none of them.</p>
+/// <p><b>Every sentence is <method>OcuPilot.Api.Error.ReasonForViolation</method>'s</b>, and the
+/// bootstrap read serves the same sentences, so an inline blur refusal and a failed Save are one
+/// wording. No sentence names a password or any part of one (AD-35).</p>
+Class OcuPilot.Area.Permissions.UserCreateRules Extends %RegisteredObject
+{
+
+/// <p>The tool both callers resolve the write through (AD-55).</p>
+Parameter TOOLCLASS = "OcuPilot.Screen.Tool.UserCreate";
+
+/// <p>The form screen whose declared pairs gate these routes (AD-8).</p>
+Parameter DESCRIPTORCLASS = "OcuPilot.Screen.Descriptor.UserForm";
+
+/// <p>The fields a create must carry, comma-separated.</p>
+Parameter REQUIREDFIELDS = "Name,Password";
+
+/// <p>The vendor class whose property definitions the length bounds are read from.</p>
+Parameter SECURITYCLASS = "Security.Users";
+
+/// <p>The properties whose <code>MAXLEN</code> the form renders, comma-separated.</p>
+Parameter LENGTHFIELDS = "Name,FullName,NameSpace,Routine";
+
+/// <p>The longest a user name may be: <class>%Library.Username</class>'s own <code>MAXLEN</code>,
+/// the type <property>Security.Users.Name</property> is declared with.</p>
+Parameter NAMEMAXLENGTH As INTEGER = 160;
+
+/// <p>The one character a user name may not carry beyond the control characters.</p>
+Parameter NAMEFORBIDDEN = "@";
+
+/// <p>The endpoint and request type the role picker lists through, on the tool's own port.</p>
+Parameter ROLEENDPOINT = "Security.Role";
+
+Parameter ROLELISTTYPE = "LIST";
+
+/// <p>The body fields the rules read, named once.</p>
+Parameter FIELDNAME = "Name";
+
+Parameter FIELDPASSWORD = "Password";
+
+Parameter FIELDFULLNAME = "FullName";
+
+Parameter FIELDEXPIRY = "ExpirationDate";
+
+Parameter FIELDNAMESPACE = "NameSpace";
+
+Parameter FIELDROUTINE = "Routine";
+
+Parameter FIELDROLES = "Roles";
+
+/// <p>Every rule a create applies, in <var>pViolations</var> as
+/// <code>pViolations(n) = $ListBuild(field, code)</code> with the count in
+/// <code>pViolations</code>.</p>
+/// <p><var>pCheckTaken</var> 0 skips the name look-up and <var>pCheckPassword</var> 0 skips the
+/// password rules, for the agent's mint, which has read the target's absence and never holds a
+/// password (<method>OcuPilot.Screen.Tool.UserCreate.ArgumentProblem</method>).</p>
+ClassMethod Validate(pName As %String, pArgs As %DynamicObject, Output pViolations, pCheckTaken As %Boolean = 1, pCheckPassword As %Boolean = 1) As %Status
+{
+    Kill pViolations
+    Set pViolations = 0
+    Set tSC = $$$OK
+    Try {
+        Do ..NameViolation(pName, .pViolations)
+        If (pViolations = 0) && pCheckTaken {
+            Set tTakenSC = ..Taken(pName, .tTaken)
+            If $$$ISERR(tTakenSC) {
+                Set tSC = tTakenSC
+                Quit
+            }
+            If tTaken Do ..Add(.pViolations, ..#FIELDNAME, ##class(OcuPilot.Api.Error).#USERNAMETAKEN)
+        }
+
+        If pCheckPassword {
+            Set tPassword = ""
+            If $IsObject(pArgs) && (pArgs.%GetTypeOf(..#FIELDPASSWORD) = "string") Set tPassword = pArgs.%Get(..#FIELDPASSWORD)
+            If tPassword = "" {
+                Do ..Add(.pViolations, ..#FIELDPASSWORD, ##class(OcuPilot.Api.Error).#USERPASSWORDREQUIRED)
+            } ElseIf '..MeetsPolicy(tPassword) {
+                Do ..Add(.pViolations, ..#FIELDPASSWORD, ##class(OcuPilot.Api.Error).#USERPASSWORDPOLICY)
+            }
+            Set tPassword = ""
+        }
+
+        For tField = ..#FIELDFULLNAME, ..#FIELDEXPIRY, ..#FIELDNAMESPACE, ..#FIELDROUTINE {
+            If '..IsScalar(pArgs, tField) Do ..Add(.pViolations, tField, ##class(OcuPilot.Api.Error).#PORTFIELDSHAPE)
+        }
+
+        Set tMax = ..Lengths()
+        Set tFullNameMax = +tMax.%Get(..#FIELDFULLNAME)
+        If (tFullNameMax > 0) && ($Length(..Text(pArgs, ..#FIELDFULLNAME)) > tFullNameMax) Do ..Add(.pViolations, ..#FIELDFULLNAME, ##class(OcuPilot.Api.Error).#USERFULLNAMELENGTH)
+
+        Set tExpiry = ..Text(pArgs, ..#FIELDEXPIRY)
+        If (tExpiry '= "") && '..IsDate(tExpiry) Do ..Add(.pViolations, ..#FIELDEXPIRY, ##class(OcuPilot.Api.Error).#USEREXPIRATIONDATESHAPE)
+
+        Set tNamespace = ..Text(pArgs, ..#FIELDNAMESPACE)
+        If (tNamespace '= "") && '..NamespaceExists(tNamespace) Do ..Add(.pViolations, ..#FIELDNAMESPACE, ##class(OcuPilot.Api.Error).#USERNAMESPACEUNKNOWN)
+
+        Set tRoutine = ..Text(pArgs, ..#FIELDROUTINE)
+        If tRoutine '= "" {
+            If tNamespace = "" Do ..Add(.pViolations, ..#FIELDROUTINE, ##class(OcuPilot.Api.Error).#USERROUTINENAMESPACE)
+            Set tRoutineMax = +tMax.%Get(..#FIELDROUTINE)
+            If (tRoutineMax > 0) && ($Length(tRoutine) > tRoutineMax) Do ..Add(.pViolations, ..#FIELDROUTINE, ##class(OcuPilot.Api.Error).#USERROUTINELENGTH)
+        }
+
+        Set tRolesSC = ..RoleViolation(pArgs, .pViolations)
+        If $$$ISERR(tRolesSC) Set tSC = tRolesSC
+    } Catch ex {
+        Set tSC = ex.AsStatus()
+    }
+    Quit tSC
+}
+
+/// <p>Add the name's own shape violation to <var>pViolations</var>, or none: an absent name, and
+/// one longer than <parameter>NAMEMAXLENGTH</parameter> or carrying a control character or
+/// <parameter>NAMEFORBIDDEN</parameter>. Run first by the create route, because everything after it
+/// addresses the target by this name.</p>
+ClassMethod NameViolation(pName As %String, Output pViolations)
+{
+    If pName = "" {
+        Do ..Add(.pViolations, ..#FIELDNAME, ##class(OcuPilot.Api.Error).#USERNAMEREQUIRED)
+        Quit
+    }
+    If '..NameIsUsable(pName) Do ..Add(.pViolations, ..#FIELDNAME, ##class(OcuPilot.Api.Error).#USERNAMESHAPE)
+}
+
+/// <p>Whether <var>pName</var> is a name this instance can hold.</p>
+ClassMethod NameIsUsable(pName As %String) As %Boolean
+{
+    If $Length(pName) > ..#NAMEMAXLENGTH Quit 0
+    If pName [ ..#NAMEFORBIDDEN Quit 0
+    Set tUsable = 1
+    For tI = 1:1:$Length(pName) {
+        Set tCode = $Ascii(pName, tI)
+        If (tCode < 32) || (tCode = 127) Set tUsable = 0 Quit
+    }
+    Quit tUsable
+}
+
+/// <p>Whether <var>pPassword</var> matches <property>Security.System.PasswordPattern</property>,
+/// read in <code>%SYS</code> by AD-16's explicit save and restore and matched with the pattern
+/// operator. An instance with no pattern accepts any non-empty password. The value is never
+/// logged, returned or placed in a status (AD-35).</p>
+ClassMethod MeetsPolicy(pPassword As %String) As %Boolean
+{
+    Set tMeets = 0
+    Set tOrigNS = $NAMESPACE
+    Try {
+        Set $NAMESPACE = "%SYS"
+        Set tPattern = ""
+        Set tSystem = ##class(Security.System).%OpenId("SYSTEM")
+        If $IsObject(tSystem) Set tPattern = tSystem.PasswordPattern
+        Set tSystem = ""
+        Set $NAMESPACE = tOrigNS
+        If tPattern = "" {
+            Set tMeets = 1
+        } Else {
+            Set tMeets = ''(pPassword?@tPattern)
+        }
+    } Catch {
+        Set $NAMESPACE = tOrigNS
+        Set tMeets = 0
+    }
+    Quit tMeets
+}
+
+/// <p>Whether <var>pValue</var> is a real calendar date written <code>YYYY-MM-DD</code>: the
+/// shape, and a round trip through the instance's own date conversion, so
+/// <code>2027-02-30</code> is refused.</p>
+ClassMethod IsDate(pValue As %String) As %Boolean
+{
+    If '$Match(pValue, "^\d{4}-\d{2}-\d{2}$") Quit 0
+    Set tDay = $ZDateH(pValue, 3, , , , , , , "")
+    If tDay = "" Quit 0
+    Quit ($ZDate(tDay, 3) = pValue)
+}
+
+/// <p>Whether <var>pNamespace</var> is a namespace this instance holds.</p>
+ClassMethod NamespaceExists(pNamespace As %String) As %Boolean
+{
+    Set tExists = 0
+    Try {
+        Set tExists = ''##class(%SYS.Namespace).Exists(pNamespace)
+    } Catch {
+        Set tExists = 0
+    }
+    Quit tExists
+}
+
+/// <p>Add the roles' violation to <var>pViolations</var>, or none: a <code>Roles</code> member that
+/// is not an array of strings, or one naming a role the instance does not hold
+/// (<method>Security.Users.Create</method> would accept it silently). An absent member names no
+/// role.</p>
+ClassMethod RoleViolation(pArgs As %DynamicObject, Output pViolations) As %Status
+{
+    Set tSC = $$$OK
+    If '$IsObject(pArgs) Quit tSC
+    Set tType = pArgs.%GetTypeOf(..#FIELDROLES)
+    If tType = "unassigned" Quit tSC
+    If tType '= "array" {
+        Do ..Add(.pViolations, ..#FIELDROLES, ##class(OcuPilot.Api.Error).#USERROLESSHAPE)
+        Quit tSC
+    }
+    Set tNames = ""
+    Set tIterator = pArgs.%Get(..#FIELDROLES).%GetIterator()
+    While tIterator.%GetNext(.tIndex, .tRole) {
+        If $IsObject(tRole) || (tRole = "") {
+            Set tNames = ""
+            Do ..Add(.pViolations, ..#FIELDROLES, ##class(OcuPilot.Api.Error).#USERROLESSHAPE)
+            Quit
+        }
+        Set tNames = tNames _ $ListBuild(tRole)
+    }
+    If tNames = "" Quit tSC
+    Set tOrigNS = $NAMESPACE
+    Try {
+        Set tUnknown = 0
+        Set $NAMESPACE = "%SYS"
+        For tI = 1:1:$ListLength(tNames) {
+            If '##class(Security.Roles).Exists($List(tNames, tI)) Set tUnknown = 1 Quit
+        }
+        Set $NAMESPACE = tOrigNS
+        If tUnknown Do ..Add(.pViolations, ..#FIELDROLES, ##class(OcuPilot.Api.Error).#USERROLESUNKNOWN)
+    } Catch ex {
+        Set $NAMESPACE = tOrigNS
+        Set tSC = ex.AsStatus()
+    }
+    Quit tSC
+}
+
+/// <p>Answer <code>GET /users/form</code>: everything the create form needs before it can draw a
+/// field (AD-39): <code>{requiredFields, maxLengths, rules, roles, rolesReason}</code>.
+/// <code>rules</code> is one <code>{field, code, reason}</code> per rule, each field's required
+/// rule first; <code>roles</code> is <code>[{name, privileged}]</code>, listed through the tool's
+/// own port and each marked by
+/// <method>OcuPilot.Kernel.Proposal.Prohibited.RoleGrantsAdministrativePrivilege</method>; and
+/// <code>rolesReason</code> is the sentence a privileged role's refusal carries on the Roles
+/// field, so the picker's pre-mark and the refusal are one wording.</p>
+ClassMethod HandleForm() As %Status
+{
+    Set tSC = $$$OK
+    Try {
+        Set tGateSC = ..Gate(.tRefused)
+        If tRefused || $$$ISERR(tGateSC) {
+            Set tSC = tGateSC
+            Quit
+        }
+        Set tRolesSC = ..Roles(.tRoles)
+        If $$$ISERR(tRolesSC) {
+            Set tSC = ##class(OcuPilot.Api.Error).RenderInternal("An internal error occurred", ##class(%Exception.StatusException).CreateFromStatus(tRolesSC))
+            Quit
+        }
+        Set tBody = {}
+        Do tBody.%Set("requiredFields", ..Names(..#REQUIREDFIELDS))
+        Do tBody.%Set("maxLengths", ..Lengths())
+        Do tBody.%Set("rules", ..Rules())
+        Do tBody.%Set("roles", tRoles)
+        Do tBody.%Set("rolesReason", ##class(OcuPilot.Api.Error).#REASONUSERROLESPRIVILEGED, "string")
+        Set tSC = ##class(OcuPilot.Api.Response).JSON(tBody)
+    } Catch ex {
+        Set tSC = ##class(OcuPilot.Api.Error).RenderInternal("An internal error occurred", ex)
+    }
+    Quit tSC
+}
+
+/// <p>Answer <code>GET /users/name?name=&lt;n&gt;</code>: whether that name is free, in
+/// <code>{name, available, reason}</code>. A look-up that cannot be made is a refusal envelope,
+/// never an assertion that the name is free.</p>
+ClassMethod HandleName() As %Status
+{
+    Set tSC = $$$OK
+    Try {
+        Set tGateSC = ..Gate(.tRefused)
+        If tRefused || $$$ISERR(tGateSC) {
+            Set tSC = tGateSC
+            Quit
+        }
+        Set tName = $Get(%request.Data("name", 1))
+        If tName = "" {
+            Set tSC = ##class(OcuPilot.Area.Permissions.UserCreate).RenderOne(..#FIELDNAME, ##class(OcuPilot.Api.Error).#USERNAMEREQUIRED)
+            Quit
+        }
+        Set tTakenSC = ..Taken(tName, .tTaken)
+        If $$$ISERR(tTakenSC) {
+            Set tSC = ##class(OcuPilot.Api.Error).RenderInternal("An internal error occurred", ##class(%Exception.StatusException).CreateFromStatus(tTakenSC))
+            Quit
+        }
+        Set tBody = {}
+        Do tBody.%Set("name", tName, "string")
+        Do tBody.%Set("available", 'tTaken, "boolean")
+        Do tBody.%Set("reason", $Select(tTaken: ##class(OcuPilot.Api.Error).ReasonForViolation(##class(OcuPilot.Api.Error).#USERNAMETAKEN), 1: ""), "string")
+        Set tSC = ##class(OcuPilot.Api.Response).JSON(tBody)
+    } Catch ex {
+        Set tSC = ##class(OcuPilot.Api.Error).RenderInternal("An internal error occurred", ex)
+    }
+    Quit tSC
+}
+
+/// <p>Whether an account already carries <var>pName</var>, in <var>pTaken</var>, read through the
+/// create tool's own port, endpoint and read type (AD-52). <class>Security.Users</class> keys on
+/// the lower-cased name, so the read answers for a name in any case.</p>
+/// <p><b>Caller contract:</b> an error status means the question was not answered; only a 404 is
+/// "free".</p>
+ClassMethod Taken(pName As %String, Output pTaken As %Boolean) As %Status
+{
+    Set pTaken = 0
+    Set tSC = $$$OK
+    Try {
+        Set tClass = ..#TOOLCLASS
+        Kill tQuery
+        Set tQuery($ClassMethod(tClass, "IdParam")) = pName
+        Set tReadSC = $ClassMethod($ClassMethod(tClass, "PortClass"), "Invoke", $ClassMethod(tClass, "Endpoint"), $ClassMethod(tClass, "ReadType"), .tQuery, "", .tFresh, .tHttp, .tFault)
+        If $$$ISERR(tReadSC) {
+            If +$Get(tHttp) = 404 Quit
+            Set tSC = tReadSC
+            Quit
+        }
+        Set pTaken = ($IsObject(tFresh) && tFresh.%IsA("%Library.DynamicObject"))
+    } Catch ex {
+        Set pTaken = 0
+        Set tSC = ex.AsStatus()
+    }
+    Quit tSC
+}
+
+/// <p>The roles this instance holds, as <code>[{name, privileged}]</code> in the order the vendor
+/// lists them, read through the create tool's own port (AD-52).</p>
+ClassMethod Roles(Output pRoles As %DynamicArray) As %Status
+{
+    Set pRoles = []
+    Set tSC = $$$OK
+    Try {
+        Set tClass = ..#TOOLCLASS
+        Kill tQuery
+        Set tSC = $ClassMethod($ClassMethod(tClass, "PortClass"), "Invoke", ..#ROLEENDPOINT, ..#ROLELISTTYPE, .tQuery, "", .tList, .tHttp, .tFault)
+        If $$$ISERR(tSC) Quit
+        If '$IsObject(tList) || 'tList.%IsA("%Library.DynamicArray") Quit
+        Set tIterator = tList.%GetIterator()
+        While tIterator.%GetNext(.tIndex, .tRow) {
+            If '$IsObject(tRow) Continue
+            Set tName = tRow.%Get("Name")
+            If tName = "" Continue
+            Set tEntry = {}
+            Do tEntry.%Set("name", tName, "string")
+            Do tEntry.%Set("privileged", ##class(OcuPilot.Kernel.Proposal.Prohibited).RoleGrantsAdministrativePrivilege(tName), "boolean")
+            Do pRoles.%Push(tEntry)
+        }
+    } Catch ex {
+        Set pRoles = []
+        Set tSC = ex.AsStatus()
+    }
+    Quit tSC
+}
+
+/// <p>The length each field stores, as <code>{field: length}</code>, read from
+/// <parameter>SECURITYCLASS</parameter>'s compiled property definitions in <code>%SYS</code>; the
+/// name's bound is <parameter>NAMEMAXLENGTH</parameter>, its datatype's own.</p>
+ClassMethod Lengths() As %DynamicObject
+{
+    Set tObject = {}
+    Set tOrigNS = $NAMESPACE
+    Try {
+        Set $NAMESPACE = "%SYS"
+        For tI = 1:1:$Length(..#LENGTHFIELDS, ",") {
+            Set tField = $Piece(..#LENGTHFIELDS, ",", tI)
+            If tField = ..#FIELDNAME {
+                Do tObject.%Set(tField, ..#NAMEMAXLENGTH, "number")
+                Continue
+            }
+            Set tProperty = ##class(%Dictionary.CompiledProperty).%OpenId(..#SECURITYCLASS _ "||" _ tField)
+            If '$IsObject(tProperty) Continue
+            Set tMax = tProperty.Parameters.GetAt("MAXLEN")
+            Set tProperty = ""
+            If tMax = "" Continue
+            Do tObject.%Set(tField, +tMax, "number")
+        }
+        Set $NAMESPACE = tOrigNS
+    } Catch {
+        Set $NAMESPACE = tOrigNS
+        Set tObject = {}
+    }
+    Quit tObject
+}
+
+/// <p>Every rule this form applies, as <code>[{field, code, reason}]</code>, each field's
+/// required-ness rule first. The sentences are
+/// <method>OcuPilot.Api.Error.ReasonForViolation</method>'s; a code with no sentence is absent.</p>
+ClassMethod Rules() As %DynamicArray
+{
+    Set tRows = []
+    Set tSpec = $ListBuild($ListBuild(..#FIELDNAME, ##class(OcuPilot.Api.Error).#USERNAMEREQUIRED))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDNAME, ##class(OcuPilot.Api.Error).#USERNAMESHAPE))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDNAME, ##class(OcuPilot.Api.Error).#USERNAMETAKEN))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDPASSWORD, ##class(OcuPilot.Api.Error).#USERPASSWORDREQUIRED))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDPASSWORD, ##class(OcuPilot.Api.Error).#USERPASSWORDPOLICY))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDFULLNAME, ##class(OcuPilot.Api.Error).#USERFULLNAMELENGTH))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDEXPIRY, ##class(OcuPilot.Api.Error).#USEREXPIRATIONDATESHAPE))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDNAMESPACE, ##class(OcuPilot.Api.Error).#USERNAMESPACEUNKNOWN))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDROUTINE, ##class(OcuPilot.Api.Error).#USERROUTINENAMESPACE))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDROUTINE, ##class(OcuPilot.Api.Error).#USERROUTINELENGTH))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDROLES, ##class(OcuPilot.Api.Error).#USERROLESUNKNOWN))
+    Set tSpec = tSpec _ $ListBuild($ListBuild(..#FIELDROLES, ##class(OcuPilot.Api.Error).#USERROLESSHAPE))
+    For tI = 1:1:$ListLength(tSpec) {
+        Set tPair = $List(tSpec, tI)
+        Set tCode = $List(tPair, 2)
+        Set tReason = ##class(OcuPilot.Api.Error).ReasonForViolation(tCode)
+        If tReason = "" Continue
+        Set tRow = {}
+        Do tRow.%Set("field", $List(tPair, 1), "string")
+        Do tRow.%Set("code", tCode, "string")
+        Do tRow.%Set("reason", tReason, "string")
+        Do tRows.%Push(tRow)
+    }
+    Quit tRows
+}
+
+/// <p>Refuse a caller who does not hold the form screen's declared pairs (AD-8):
+/// <var>pRefused</var> 1 means the envelope is written and the caller writes nothing else.</p>
+ClassMethod Gate(Output pRefused As %Boolean) As %Status
+{
+    Set pRefused = 0
+    Set tFailedPair = ""
+    If ##class(OcuPilot.Screen.Gate).Evaluate(..#DESCRIPTORCLASS, .tFailedPair) Quit $$$OK
+    Set pRefused = 1
+    Quit ##class(OcuPilot.Kernel.Denial).Envelope(tFailedPair)
+}
+
+/// <p>Whether <var>pArgs</var>'s member <var>pName</var> is absent or a literal, never an object or
+/// an array.</p>
+ClassMethod IsScalar(pArgs As %DynamicObject, pName As %String) As %Boolean [ Private ]
+{
+    If '$IsObject(pArgs) Quit 1
+    Set tType = pArgs.%GetTypeOf(pName)
+    Quit '((tType = "object") || (tType = "array"))
+}
+
+/// <p><var>pArgs</var>'s member <var>pName</var> as the rules read it: a literal verbatim and
+/// anything else <code>""</code>.</p>
+ClassMethod Text(pArgs As %DynamicObject, pName As %String) As %String [ Private ]
+{
+    If '$IsObject(pArgs) Quit ""
+    Set tType = pArgs.%GetTypeOf(pName)
+    If (tType = "unassigned") || (tType = "null") || (tType = "object") || (tType = "array") Quit ""
+    Quit ##class(OcuPilot.Kernel.Proposal.Mint).Display(pArgs.%Get(pName), tType)
+}
+
+/// <p>A comma-separated list as a <class>%DynamicArray</class> of strings.</p>
+ClassMethod Names(pList As %String) As %DynamicArray [ Private ]
+{
+    Set tRows = []
+    For tI = 1:1:$Length(pList, ",") {
+        Set tName = $Piece(pList, ",", tI)
+        If tName '= "" Do tRows.%Push(tName)
+    }
+    Quit tRows
+}
+
+/// <p>Append one <code>(field, code)</code> violation.</p>
+ClassMethod Add(Output pViolations, pField As %String, pCode As %String) [ Private ]
+{
+    Set pViolations($Increment(pViolations)) = $ListBuild(pField, pCode)
+}
+
+}
diff --git a/src/OcuPilot/Area/WebApp/Create.cls b/src/OcuPilot/Area/WebApp/Create.cls
index cb3bc7f..4908d47 100644
--- a/src/OcuPilot/Area/WebApp/Create.cls
+++ b/src/OcuPilot/Area/WebApp/Create.cls
@@ -259,7 +259,13 @@ ClassMethod Validate(pName As %String, pArgs As %DynamicObject, Output pViolatio
         Set tIsWsgi = ((tWsgiName '= "") || (tWsgiLocation '= "") || (tWsgiType '= "") || (..Text(pArgs, ..#FIELDWSGICALLABLE) '= ""))
         If tIsWsgi {
             If tWsgiName = "" Do ..Add(.pViolations, ..#FIELDWSGINAME, ##class(OcuPilot.Api.Error).#WEBAPPWSGIAPPNAME)
-            If tWsgiLocation = "" Do ..Add(.pViolations, ..#FIELDWSGILOCATION, ##class(OcuPilot.Api.Error).#WEBAPPWSGILOCATION)
+            ; AD-21: the location is one directory name resolved under the fixed WSGI root, never a
+            ; path; OcuPilot.Area.WebApp.Location is the one resolver both callers pass.
+            If tWsgiLocation = "" {
+                Do ..Add(.pViolations, ..#FIELDWSGILOCATION, ##class(OcuPilot.Api.Error).#WEBAPPWSGILOCATION)
+            } ElseIf '##class(OcuPilot.Area.WebApp.Location).Resolve(tWsgiLocation, .tResolvedLocation) {
+                Do ..Add(.pViolations, ..#FIELDWSGILOCATION, ##class(OcuPilot.Api.Error).#WEBAPPWSGIAPPLOCATIONSHAPE)
+            }
             If '..IsWsgiType(tWsgiType) Do ..Add(.pViolations, ..#FIELDWSGITYPE, ##class(OcuPilot.Api.Error).#WEBAPPWSGITYPE)
             If ..Text(pArgs, ..#FIELDDISPATCH) '= "" Do ..Add(.pViolations, ..#FIELDDISPATCH, ##class(OcuPilot.Api.Error).#WEBAPPTYPECONFLICT)
         } ElseIf (pArgs.%GetTypeOf(..#FIELDDISPATCH) = "string") && (..Text(pArgs, ..#FIELDDISPATCH) = "") {
diff --git a/src/OcuPilot/Area/WebApp/FormRules.cls b/src/OcuPilot/Area/WebApp/FormRules.cls
index ac7b560..84dbc36 100644
--- a/src/OcuPilot/Area/WebApp/FormRules.cls
+++ b/src/OcuPilot/Area/WebApp/FormRules.cls
@@ -75,7 +75,7 @@ Parameter NAMEMAXLENGTH As INTEGER = 64;
 /// <p>Answer <code>GET /web-applications/form</code>: everything the create form needs before it
 /// can draw a field (AD-39, DW-376).</p>
 /// <p>The body is
-/// <code>{authenticationMethods, requiredFields, conditionalFields, wsgiTypes, maxLengths, rules}</code>.
+/// <code>{authenticationMethods, defaultMethod, requiredFields, conditionalFields, wsgiTypes, maxLengths, rules, wsgiRoot}</code>.
 /// <code>rules</code> is one <code>{field, code, reason}</code> per rule the server applies, in the
 /// same shape a refusal's own <code>detail.violations</code> row takes -- so the client renders an
 /// inline blur refusal and a failed Save through one path and holds no copy of its own.</p>
@@ -96,6 +96,9 @@ ClassMethod HandleForm() As %Status
         Do tBody.%Set("wsgiTypes", ..Names(..#WSGITYPES))
         Do tBody.%Set("maxLengths", ..Lengths())
         Do tBody.%Set("rules", ..Rules())
+        ; AD-21: the root a WSGI or ASGI directory name resolves under, computed for this read, so
+        ; the form can show the resolved path read-only. The server resolves again at the write.
+        Do tBody.%Set("wsgiRoot", ##class(OcuPilot.Area.WebApp.Location).Root(), "string")
         Set tSC = ##class(OcuPilot.Api.Response).JSON(tBody)
     } Catch ex {
         Set tSC = ##class(OcuPilot.Api.Error).RenderInternal("An internal error occurred", ex)
@@ -309,6 +312,7 @@ ClassMethod Rules() As %DynamicArray
     Set tSpec = tSpec _ ",DispatchClass:" _ ##class(OcuPilot.Api.Error).#WEBAPPDISPATCHREQUIRED
     Set tSpec = tSpec _ ",WSGIAppName:" _ ##class(OcuPilot.Api.Error).#WEBAPPWSGIAPPNAME
     Set tSpec = tSpec _ ",WSGIAppLocation:" _ ##class(OcuPilot.Api.Error).#WEBAPPWSGILOCATION
+    Set tSpec = tSpec _ ",WSGIAppLocation:" _ ##class(OcuPilot.Api.Error).#WEBAPPWSGIAPPLOCATIONSHAPE
     Set tSpec = tSpec _ ",WSGIType:" _ ##class(OcuPilot.Api.Error).#WEBAPPWSGITYPE
     For tI = 1:1:$Length(tSpec, ",") {
         Set tPair = $Piece(tSpec, ",", tI)
diff --git a/src/OcuPilot/Area/WebApp/Location.cls b/src/OcuPilot/Area/WebApp/Location.cls
new file mode 100644
index 0000000..02ecbc9
--- /dev/null
+++ b/src/OcuPilot/Area/WebApp/Location.cls
@@ -0,0 +1,84 @@
+/// <p>Where a WSGI or ASGI web application's directory lives (AD-21): one directory name,
+/// resolved under a fixed root on this instance. It is the one place a web-application create
+/// turns a caller's text into a filesystem directory, and both callers of the create tool reach it
+/// through <method>OcuPilot.Area.WebApp.Create.Validate</method> and
+/// <method>OcuPilot.Screen.Tool.WebAppCreate.DerivedFields</method> (AD-55).</p>
+/// <p><b>The caller names a directory, never a path.</b> The name is a single segment matching
+/// <parameter>NAMEPATTERN</parameter> and carrying no literal <code>..</code>; it is resolved under
+/// <method>Root</method>, computed on every call and never cached, by lexical normalization of the
+/// root and the name together; and the resolved directory is
+/// accepted only when it lies strictly inside that root, the comparison keeping the root's trailing
+/// separator so a sibling such as <code>&lt;mgr&gt;/wsgiprobe/</code> is outside. Nothing here
+/// creates a directory. Any other location stays a classic-portal action.</p>
+Class OcuPilot.Area.WebApp.Location Extends %RegisteredObject
+{
+
+/// <p>The one directory name a caller may give: a letter or digit, then up to 63 letters, digits,
+/// dots, dashes or underscores.</p>
+Parameter NAMEPATTERN = "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$";
+
+/// <p>The root's own directory name under the manager directory.</p>
+Parameter ROOTNAME = "wsgi";
+
+/// <p>The fixed root every application directory resolves under:
+/// <code>&lt;ManagerDirectory&gt;wsgi/</code>, normalized with its trailing separator
+/// (<code>/durable/iris/mgr/wsgi/</code> on this build). Computed on every call, because the manager
+/// directory is the instance's and not a constant; <code>""</code> when it cannot be computed,
+/// which every caller reads as "nothing resolves".</p>
+ClassMethod Root() As %String
+{
+    Set tRoot = ""
+    Try {
+        Set tManager = $System.Util.ManagerDirectory()
+        If tManager = "" Quit
+        Set tRoot = ##class(%File).NormalizeDirectory(..#ROOTNAME, tManager)
+    } Catch {
+        Set tRoot = ""
+    }
+    Quit tRoot
+}
+
+/// <p>Whether <var>pName</var> resolves to an application directory, with that directory in
+/// <var>pPath</var> (normalized, trailing separator kept) or <code>""</code>.</p>
+/// <p><b>Caller contract:</b> <var>pPath</var> is what is sent as both <code>WSGIAppLocation</code>
+/// and <code>Path</code>; the caller's own text is never sent. Refused: a name outside
+/// <parameter>NAMEPATTERN</parameter>, any name carrying <code>..</code>, a root that cannot be
+/// computed, and a resolution that is the root itself or lies outside it.</p>
+ClassMethod Resolve(pName As %String, Output pPath As %String) As %Boolean
+{
+    Set pPath = ""
+    Set tResolved = 0
+    Try {
+        If '$Match(pName, ..#NAMEPATTERN) Quit
+        If pName [ ".." Quit
+        Set tRoot = ..Root()
+        If tRoot = "" Quit
+        ; The one-argument form: with a base argument %File changes into that directory first
+        ; and answers "" when it does not exist yet, which a root no application has used is.
+        Set tCandidate = ##class(%File).NormalizeDirectory(tRoot _ pName)
+        If tCandidate = "" Quit
+        If '..IsInsideRoot(tRoot, tCandidate) Quit
+        Set pPath = tCandidate
+        Set tResolved = 1
+    } Catch {
+        Set pPath = ""
+        Set tResolved = 0
+    }
+    Quit tResolved
+}
+
+/// <p>Whether the normalized directory <var>pCandidate</var> lies strictly inside
+/// <var>pRoot</var>: it starts with the root, trailing separator included, and is not the root
+/// itself. Separate from <method>Resolve</method> so the prefix check can be shown on its own --
+/// inside <method>Resolve</method> the pattern and the <code>..</code> refusal already refuse
+/// every input that could reach it.</p>
+ClassMethod IsInsideRoot(pRoot As %String, pCandidate As %String) As %Boolean
+{
+    If (pRoot = "") || (pCandidate = "") Quit 0
+    Set tRoot = ##class(%File).NormalizeDirectory(pRoot)
+    If tRoot = "" Quit 0
+    If pCandidate = tRoot Quit 0
+    Quit ($Extract(pCandidate, 1, $Length(tRoot)) = tRoot)
+}
+
+}
diff --git a/src/OcuPilot/Kernel/Proposal/Mint.cls b/src/OcuPilot/Kernel/Proposal/Mint.cls
index 1a8968e..9de6f9f 100644
--- a/src/OcuPilot/Kernel/Proposal/Mint.cls
+++ b/src/OcuPilot/Kernel/Proposal/Mint.cls
@@ -178,6 +178,10 @@ ClassMethod Mint(pDescriptor As %String, pToolName As %String, pUser As %String,
 
         If tCreates {
             Set tSC = ..Compose(pArgs, pIdArgument, pSettable, .tPayload, .tDiff, .tUnchanged, .tProblem)
+            ; AD-6, AD-35: a create whose body carries a secret the user supplies at confirm shows
+            ; one row per such name, empty on both sides, so the card asks for it beside the diff.
+            ; The payload does not carry it and unchangedCount stays 0.
+            If $$$ISOK(tSC) && (tProblem = "") Do ..AppendComposedSecrets(pToolClass, tDiff)
         } Else {
             Set tSC = ..Merge(tFresh, pArgs, pIdArgument, pSettable, .tPayload, .tDiff, .tUnchanged, .tProblem)
         }
@@ -284,6 +288,7 @@ ClassMethod Mint(pDescriptor As %String, pToolName As %String, pUser As %String,
             Quit
         }
         Set tValues("auditWarning") = ..WarnsAuditingOff(pToolName, tType, tScope, pArgs)
+        Set tValues("consequence") = ..ConsequenceOf(pToolClass, tPayload)
         Set tValues("destructive") = ..DestructiveTool(pToolName)
         ; FR-17's definition condition is answered at confirm against the definition that was in
         ; force here. Read, never written: the definition store is Epic 10's.
@@ -559,6 +564,46 @@ ClassMethod CreatesOf(pToolClass As %String) As %Boolean
     Quit tCreates
 }
 
+/// <p>Append to <var>pDiff</var> one <code>{field, before, after}</code> row, both sides empty, per
+/// name <var>pToolClass</var>'s <code>ComposedSecrets()</code> answers (AD-6, AD-35). A class that
+/// names none, or cannot be asked, adds nothing.</p>
+/// <p><b>Caller contract:</b> called for a create only, after <method>Compose</method>. The rows
+/// carry no value: the secret is supplied at confirm and never stored.</p>
+ClassMethod AppendComposedSecrets(pToolClass As %String, pDiff As %DynamicArray) [ Private ]
+{
+    Set tNames = ""
+    Try {
+        If pToolClass '= "" Set tNames = $ClassMethod(pToolClass, "ComposedSecrets")
+    } Catch {
+        Set tNames = ""
+    }
+    If '$ListValid(tNames) Quit
+    For tI = 1:1:$ListLength(tNames) {
+        Set tName = $ListGet(tNames, tI)
+        If tName = "" Continue
+        Set tRow = {}
+        Do tRow.%Set("field", tName, "string")
+        Do tRow.%Set("before", "", "string")
+        Do tRow.%Set("after", "", "string")
+        Do pDiff.%Push(tRow)
+    }
+}
+
+/// <p>The kernel-owned consequence code <var>pToolClass</var> answers for the payload
+/// <var>pPayload</var> (<code>Consequence(payload)</code>), or <code>""</code> where the class
+/// names none or cannot be asked. The card renders the code's sentence; it decides nothing about
+/// whether the write may happen.</p>
+ClassMethod ConsequenceOf(pToolClass As %String, pPayload As %DynamicObject) As %String [ Private ]
+{
+    Set tCode = ""
+    Try {
+        If pToolClass '= "" Set tCode = $ClassMethod(pToolClass, "Consequence", pPayload)
+    } Catch {
+        Set tCode = ""
+    }
+    Quit tCode
+}
+
 /// <p>The agent definition in force and the version it carries, for the proposal's row.</p>
 /// <p><var>pVersion</var> is <method>OcuPilot.Kernel.State.Agent.GuardedVersion</method>'s answer,
 /// where <code>""</code> is "no row" and is never conflated with <code>0</code>. No definition in
diff --git a/src/OcuPilot/Kernel/Proposal/Prohibited.cls b/src/OcuPilot/Kernel/Proposal/Prohibited.cls
index f3b43f6..2f1d5f7 100644
--- a/src/OcuPilot/Kernel/Proposal/Prohibited.cls
+++ b/src/OcuPilot/Kernel/Proposal/Prohibited.cls
@@ -311,9 +311,14 @@ ClassMethod AlwaysProhibitedFields(pType As %String) As %List
 /// them and is never settable by a caller: the server derives it from
 /// <code>WSGIAppLocation</code> (<method>OcuPilot.Screen.Tool.Write.DerivedFields</method>), and
 /// <method>Security.Applications.Create</method> creates the directory a CSP application names.</p>
+/// <p>A user account's five are the settings the create form captures beside the name and the
+/// password: its expiry, full name, startup namespace and routine, and its roles -- the last
+/// permitted except where <method>Created</method> finds a role that grants privilege. The password
+/// is a declared secret and never part of the payload this set is asked about.</p>
 ClassMethod PermittedCreateFields(pType As %String) As %List
 {
     If pType = ..#TYPEWEBAPPLICATION Quit $ListBuild("AutheEnabled", "Description", "DispatchClass", "Enabled", "NameSpace", "Recurse", "Resource", "WSGIAppLocation", "WSGIAppName", "WSGICallable", "WSGIType")
+    If pType = ..#TYPEUSER Quit $ListBuild("ExpirationDate", "FullName", "NameSpace", "Routine", ..#FIELDROLES)
     Quit ""
 }
 
@@ -324,10 +329,12 @@ ClassMethod PermittedCreateFields(pType As %String) As %List
 /// sentence of its own rather than the reviewed-few one. Setting application roles on any web
 /// application is a privilege grant through a path, and creating the application is no exception:
 /// <code>MatchRoles</code> is refused here exactly as
-/// <method>AlwaysProhibitedFields</method> refuses it on a change.</p>
+/// <method>AlwaysProhibitedFields</method> refuses it on a change, and an account's
+/// <code>EscalationRoles</code> for the same reason.</p>
 ClassMethod AlwaysProhibitedCreateFields(pType As %String) As %List
 {
     If pType = ..#TYPEWEBAPPLICATION Quit $ListBuild("MatchRoles")
+    If pType = ..#TYPEUSER Quit $ListBuild(..#FIELDESCALATIONROLES)
     Quit ""
 }
 
@@ -486,8 +493,9 @@ ClassMethod UncoveredWriteTools(Output pTools As %List) As %Status
 }
 
 /// <p>The <b>create</b> predicates, in declared order, first hit wins: a create at a path that
-/// serves OcuPilot, a field AD-10 forbids unconditionally, and any field the release has not
-/// reviewed for a create of this type (AD-54).</p>
+/// serves OcuPilot, a field AD-10 forbids unconditionally, for a user account a role that grants
+/// an administrative privilege (<method>GrantsPrivilege</method> against no held role), and any
+/// field the release has not reviewed for a create of this type (AD-54).</p>
 /// <p><b>The set here is the payload's own keys, not a diff against a live target</b>, and that is
 /// the whole difference. A create has no prior state to compare against, so
 /// <method>Changed</method> has no subject: every property the composed body carries is a property
@@ -524,6 +532,16 @@ ClassMethod Created(pType As %String, pServes As %Boolean, pPayload As %DynamicO
             }
         }
         If pProhibits Quit
+        ; AD-10: a new account holds no role yet, so every role the create names is a grant, and
+        ; one that reaches %All or an %Admin_* resource refuses the create.
+        If (pType = ..#TYPEUSER) && (pPayload.%GetTypeOf(..#FIELDROLES) '= "unassigned") {
+            Set tSC = ..GrantsPrivilege(pPayload, {}, .tGrants)
+            If $$$ISERR(tSC) Quit
+            If tGrants {
+                Do ..Refuse(..#PRIVILEGEGRANT, .pProhibits, .pCode)
+                Quit
+            }
+        }
         Set tIterator = pPayload.%GetIterator()
         While tIterator.%GetNext(.tKey, .tValue) {
             If '$ListFind(tPermitted, tKey) {
@@ -1047,6 +1065,23 @@ ClassMethod RoleEscalates(pRole As %String, Output pEscalates As %Boolean) As %S
     Quit tSC
 }
 
+/// <p>Whether granting <var>pRole</var> would grant <code>%All</code> or an administrative
+/// privilege, by name, through its nested roles, or through a resource it carries
+/// (<method>RoleEscalates</method>). For a picker's pre-mark: the refusal itself is
+/// <method>Prohibits</method>'s, asked at the write. A role whose answer cannot be read is reported
+/// as granting, the fail-closed direction for a mark that disables a choice.</p>
+ClassMethod RoleGrantsAdministrativePrivilege(pRole As %String) As %Boolean
+{
+    Set tEscalates = 1
+    Try {
+        Set tSC = ..RoleEscalates(pRole, .tEscalates)
+        If $$$ISERR(tSC) Set tEscalates = 1
+    } Catch {
+        Set tEscalates = 1
+    }
+    Quit ''tEscalates
+}
+
 /// <p>Whether role <var>pRole</var> carries a privileged <b>resource</b>, in <var>pGrants</var>:
 /// one named <parameter>ALLROLE</parameter> or carrying <parameter>ADMINROLEPREFIX</parameter>, at
 /// any permission.</p>
diff --git a/src/OcuPilot/Kernel/State/Propose.cls b/src/OcuPilot/Kernel/State/Propose.cls
index 326382c..9ce9598 100644
--- a/src/OcuPilot/Kernel/State/Propose.cls
+++ b/src/OcuPilot/Kernel/State/Propose.cls
@@ -130,6 +130,16 @@ Property AuditWarning As %Boolean [ InitialExpression = 0 ];
 /// declaration answers for every tool that shipped before this property existed.</p>
 Property Destructive As %Boolean [ InitialExpression = 0 ];
 
+/// <p>The kernel-owned consequence code the write's tool answered for its payload at mint time
+/// (<code>Consequence(payload)</code>), such as <code>WEBAPP.UNAUTHENTICATED</code>, or
+/// <code>""</code>. The card renders the code's sentence beside the audit warning; it refuses
+/// nothing.</p>
+/// <p><b>Added with no <code>SCHEMAVERSION</code> move</b>, for the reason
+/// <property>AuditWarning</property> records: every pre-existing row reads <code>""</code>, which
+/// is what every tool that shipped before this property existed answers, so no older row means
+/// anything different once a newer OcuPilot reads it.</p>
+Property Consequence As %String(MAXLEN = 64);
+
 /// <p>When the confirmed write was committed, ISO-8601 UTC; <code>""</code> while the row is not
 /// <parameter>STATECONFIRMED</parameter>. Written inside the claim's own transition and read by the
 /// card's confirmed status line.</p>
@@ -180,8 +190,8 @@ Index ProposalTargetIdx On (TargetRef As Exact, State);
 /// <code>turnKey</code>, <code>toolName</code>, <code>targetRef</code>, <code>fingerprint</code>,
 /// <code>arguments</code>, <code>payload</code>, <code>diff</code>, <code>unchangedCount</code>,
 /// <code>rationale</code>, <code>expectedImpact</code>, <code>reverse</code>,
-/// <code>auditWarning</code>, <code>definitionId</code> and <code>definitionVersion</code>, every
-/// one already
+/// <code>auditWarning</code>, <code>consequence</code>, <code>definitionId</code> and
+/// <code>definitionVersion</code>, every one already
 /// built and already stripped of secret-declared keys by
 /// <method>OcuPilot.Kernel.Proposal.Mint.Mint</method>; nothing here strips anything.
 /// <var>pLimitsClass</var> is the class <code>PROPOSALEXPIRYSECONDS</code> is read from, so a test
@@ -220,6 +230,7 @@ ClassMethod GuardedMint(ByRef pValues, pLimitsClass As %String, Output pKey As %
         Set tRow.Reverse = ..Cut($Get(pValues("reverse")), tSummaryMax)
         Set tRow.AuditWarning = ''$Get(pValues("auditWarning"))
         Set tRow.Destructive = ''$Get(pValues("destructive"))
+        Set tRow.Consequence = ..Cut($Get(pValues("consequence")), 64)
         Set tRow.DefinitionId = ..Cut($Get(pValues("definitionId")), 64)
         Set tRow.DefinitionVersion = ..Cut($Get(pValues("definitionVersion")), 32)
         Set tRow.CreatedAt = ..Stamp(tStamp)
@@ -492,7 +503,7 @@ ClassMethod ProjectedState(pStored As %String, pExpiresAt As %String) As %String
 /// <p>Turn <var>pTurnKey</var>'s proposals owned by <var>pUser</var>, oldest first, as the wire
 /// array the turn's progress payload carries:
 /// <code>{proposalId, target: {type, scope, id}, expiresAt, tool, changed, unchangedCount,
-/// unchanged, rationale, expectedImpact, reverse, state, auditWarning}</code>.</p>
+/// unchanged, rationale, expectedImpact, reverse, state, auditWarning, consequence}</code>.</p>
 /// <p><b>Every value is the row's own.</b> The target triple is
 /// <method>OcuPilot.Kernel.EntityRef.Parse</method>'s reading of the stored key, so a reader
 /// cannot be handed a triple this store never wrote; <code>changed</code> is the stored diff,
@@ -607,6 +618,7 @@ ClassMethod RowValues(pRow As OcuPilot.Kernel.State.Propose, Output pValues) As
     Set pValues("definitionVersion") = ..Text(pRow.DefinitionVersion)
     Set pValues("auditWarning") = ''pRow.AuditWarning
     Set pValues("destructive") = ''pRow.Destructive
+    Set pValues("consequence") = ..Text(pRow.Consequence)
     Set tSC = ..GuardedStreamText(pRow, "Arguments", .tArguments)
     If $$$ISERR(tSC) Quit tSC
     Set pValues("arguments") = tArguments
@@ -650,6 +662,7 @@ ClassMethod WireRow(ByRef pValues, Output pOut As %DynamicObject) As %Status [ P
     Do tOut.%Set("confirmedAt", $Get(pValues("confirmedAt")), "string")
     Do tOut.%Set("auditWarning", ''$Get(pValues("auditWarning")), "boolean")
     Do tOut.%Set("destructive", ''$Get(pValues("destructive")), "boolean")
+    Do tOut.%Set("consequence", $Get(pValues("consequence")), "string")
     Set pOut = tOut
     Quit $$$OK
 }
diff --git a/src/OcuPilot/Port/AdminPort.cls b/src/OcuPilot/Port/AdminPort.cls
index e713d38..db4fdaf 100644
--- a/src/OcuPilot/Port/AdminPort.cls
+++ b/src/OcuPilot/Port/AdminPort.cls
@@ -163,7 +163,7 @@ Parameter TYPESUFFIXES = "GET,LIST,INFO,CERTINFO,UPCOMING,HISTORY,VOLUMELIST";
 /// one-line delegate to <code>RunTerminate</code> -- and AD-10 names terminating an IRIS system
 /// process in the Release 1 prohibited set, so neither pairs with <code>Process</code> here and
 /// <method>EndpointType</method> answers <code>""</code> for either.</p>
-Parameter MUTATINGTYPES = "WebApp.App/PUT,Security.User/PUT,Security.Audit.Enabled/PUT,Task.CRUD/RESUME,Process/SUSPEND,Process/RESUME";
+Parameter MUTATINGTYPES = "WebApp.App/PUT,Security.User/PUT,Security.Audit.Enabled/PUT,Task.CRUD/RESUME,Process/SUSPEND,Process/RESUME,Security.User/POST";
 
 /// <p>The <parameter>MUTATINGTYPES</parameter> entries the vendor declares need no request body,
 /// so <method>Invoke</method>'s object-body refusal does not apply to them (AD-51). Comma-separated
@@ -181,6 +181,22 @@ Parameter MUTATINGTYPES = "WebApp.App/PUT,Security.User/PUT,Security.Audit.Enabl
 /// the silent success this refusal exists to prevent.</p>
 Parameter BODYLESSTYPES = "Task.CRUD/RESUME,Process/SUSPEND,Process/RESUME";
 
+/// <p>The <parameter>MUTATINGTYPES</parameter> entries whose vendor body is a <b>wrapper</b>, as
+/// comma-separated <code>&lt;endpoint&gt;/&lt;type&gt;=&lt;wrapper&gt;:&lt;outer&gt;[:&lt;outer&gt;...]</code>
+/// entries. <method>Invoke</method> applies it before the vendor's <code>ValidateRequest</code>:
+/// every key of the caller's flat body except the listed outer keys moves under the wrapper key,
+/// and an outer key the body does not carry stays missing, so the vendor's own required-field
+/// refusal answers for it.</p>
+/// <p><code>Security.User/POST</code> is <code>%Api.Admin.Endpoints.Security.User</code>'s create
+/// (read on this build): <code>TYPEPOST</code> 5, <code>NeedsRequestBody()</code> true, a
+/// <code>ValidateRequest</code> requiring <code>{User:{...}, Password}</code>, and a
+/// <code>RunPost</code> that calls <method>Security.Users.Create</method> with the password and then
+/// <code>Modify</code> with <code>User</code>'s fields. The wrapper is a wire fact, so it lives in
+/// the port (AD-27) and the rest of the product reads <code>Security.User</code>'s flat derived
+/// field list. <method>Fail</method> strips the wrapper's leading segment from a field a vendor
+/// refusal names, so the refusal lands on the flat field the caller sent.</p>
+Parameter WRAPPEDTYPES = "Security.User/POST=User:Password";
+
 /// <p>The pair set the <b>backing query or class</b> of an endpoint requires, beyond the
 /// endpoint's own <code>ResourcesOR()</code> gate (AD-29). Comma-separated entries of the form
 /// <code>&lt;endpoint&gt;/&lt;type&gt;=&lt;pair&gt;|&lt;pair&gt;</code>, each pair
@@ -456,7 +472,11 @@ ClassMethod Invoke(pEndpoint As %String, pType As %String, ByRef pQuery, pBody A
             Quit
         }
 
-        Set tSeqSC = ..RunSequence(pEndpoint, tClass, tType, .pQuery, pBody, .tAnswer, .tHttp, .tLocation, ..HttpMethodFor(pEndpoint, pType))
+        ; WRAPPEDTYPES: a flat body becomes the vendor's wrapper here, before ValidateRequest reads
+        ; it, so no caller above the port composes the wire shape.
+        Set tBody = pBody
+        If $IsObject(pBody) Set tBody = ..Wrapped(pEndpoint, pType, pBody)
+        Set tSeqSC = ..RunSequence(pEndpoint, tClass, tType, .pQuery, tBody, .tAnswer, .tHttp, .tLocation, ..HttpMethodFor(pEndpoint, pType))
         If $$$ISOK(tSeqSC) && (tHttp = 202) && (tLocation [ ..#ASYNCLOCATION) {
             Set tAnswer = ""
             Set tGuid = $Piece($Piece(tLocation, ..#ASYNCLOCATION, 2), "&", 1)
@@ -479,7 +499,7 @@ ClassMethod Invoke(pEndpoint As %String, pType As %String, ByRef pQuery, pBody A
             Set tSC = ..Denied(pEndpoint, tFailedPair, tSeqSC, .pHttpStatus, .pFault)
             Quit
         }
-        Set tSC = ..Fail(pEndpoint, tHttp, tSeqSC, .pHttpStatus, .pFault)
+        Set tSC = ..Fail(pEndpoint, tHttp, tSeqSC, .pHttpStatus, .pFault, ..WrapperKey(pEndpoint, pType))
     } Catch ex {
         Set pResult = ""
         Set pHttpStatus = 500
@@ -798,6 +818,58 @@ ClassMethod IsBodyless(pEndpoint As %String, pSuffix As %String) As %Boolean
     Quit ''$ListFind($ListFromString(..#BODYLESSTYPES), pEndpoint _ "/" _ pSuffix)
 }
 
+/// <p>The wrapper key <parameter>WRAPPEDTYPES</parameter> declares for <var>pEndpoint</var>'s
+/// request type <var>pSuffix</var>, or <code>""</code> for a type whose body is sent as it is.</p>
+ClassMethod WrapperKey(pEndpoint As %String, pSuffix As %String) As %String
+{
+    Set tKey = ""
+    Set tEntries = $ListFromString(..#WRAPPEDTYPES)
+    For tI = 1:1:$ListLength(tEntries) {
+        Set tEntry = $List(tEntries, tI)
+        If $Piece(tEntry, "=", 1) '= (pEndpoint _ "/" _ pSuffix) Continue
+        Set tKey = $Piece($Piece(tEntry, "=", 2), ":", 1)
+        Quit
+    }
+    Quit tKey
+}
+
+/// <p><var>pBody</var> in the vendor's wrapper shape for <var>pEndpoint</var>'s request type
+/// <var>pSuffix</var>, or <var>pBody</var> itself where <parameter>WRAPPEDTYPES</parameter> declares
+/// none. Every key except the declared outer keys moves under the wrapper key, each keeping its own
+/// JSON type; an outer key <var>pBody</var> does not carry stays missing. <var>pBody</var> is not
+/// modified.</p>
+ClassMethod Wrapped(pEndpoint As %String, pSuffix As %String, pBody As %DynamicObject) As %DynamicObject
+{
+    Set tWrapper = ..WrapperKey(pEndpoint, pSuffix)
+    If tWrapper = "" Quit pBody
+    If '$IsObject(pBody) || 'pBody.%IsA("%Library.DynamicObject") Quit pBody
+    Set tOuter = ""
+    Set tEntries = $ListFromString(..#WRAPPEDTYPES)
+    For tI = 1:1:$ListLength(tEntries) {
+        Set tEntry = $List(tEntries, tI)
+        If $Piece(tEntry, "=", 1) '= (pEndpoint _ "/" _ pSuffix) Continue
+        Set tSpec = $Piece(tEntry, "=", 2)
+        For tJ = 2:1:$Length(tSpec, ":") {
+            If $Piece(tSpec, ":", tJ) '= "" Set tOuter = tOuter _ $ListBuild($Piece(tSpec, ":", tJ))
+        }
+        Quit
+    }
+    Set tOut = {}
+    Set tInner = {}
+    Set tIterator = pBody.%GetIterator()
+    While tIterator.%GetNext(.tKey, .tValue) {
+        Set tType = pBody.%GetTypeOf(tKey)
+        Set tTarget = $Select($ListFind(tOuter, tKey): tOut, 1: tInner)
+        If (tType = "object") || (tType = "array") {
+            Do tTarget.%Set(tKey, tValue)
+        } Else {
+            Do tTarget.%Set(tKey, tValue, tType)
+        }
+    }
+    Do tOut.%Set(tWrapper, tInner)
+    Quit tOut
+}
+
 /// <p>Whether <var>pEndpoint</var>'s request type <var>pSuffix</var> is one
 /// <parameter>MUTATINGTYPES</parameter> admits (DW-1473). Every read of that parameter is this
 /// method, so admitting a type on one endpoint can never be read as admitting it on another.</p>
@@ -853,6 +925,7 @@ ClassMethod ImplementsRead(pClass As %String, pSuffix As %String) As %Boolean [
 {
     If ..MethodOrigin(pClass, "Run") '= ..#ENDPOINTBASECLASS Quit 1
     Set tRunner = $Case(pSuffix, "GET": "RunGet", "LIST": "RunList", "PUT": "RunPut", : "")
+    If pSuffix = "POST" Set tRunner = "RunPost"
     If tRunner = "" Quit 0
     Quit ..MethodOrigin(pClass, tRunner) '= ..#ENDPOINTBASECLASS
 }
@@ -1047,8 +1120,10 @@ ClassMethod AwaitTask(pEndpoint As %String, pGuid As %String, Output pResult, Ou
 }
 
 /// <p>A failed vendor outcome: the fault from <method>OcuPilot.Kernel.Fault.Outcome</method>,
-/// the vendor status to the log, and an error status carrying only the fault's own text.</p>
-ClassMethod Fail(pEndpoint As %String, pHttp As %Integer, pVendorSC As %Status, Output pHttpStatus As %Integer, Output pFault As %DynamicObject) As %Status [ Private ]
+/// the vendor status to the log, and an error status carrying only the fault's own text.
+/// <var>pWrapper</var> is the request's <parameter>WRAPPEDTYPES</parameter> wrapper key, or
+/// <code>""</code>; a violation field that begins with it and a dot loses that segment.</p>
+ClassMethod Fail(pEndpoint As %String, pHttp As %Integer, pVendorSC As %Status, Output pHttpStatus As %Integer, Output pFault As %DynamicObject, pWrapper As %String = "") As %Status [ Private ]
 {
     Set pHttpStatus = $Select(+pHttp: +pHttp, 1: 500)
     Set tOutcomeSC = ##class(OcuPilot.Kernel.Fault).Outcome(pHttpStatus, pVendorSC, .pFault, .tDetail)
@@ -1058,6 +1133,14 @@ ClassMethod Fail(pEndpoint As %String, pHttp As %Integer, pVendorSC As %Status,
     ; own sentence -- so a screen renders it on the field it is about and a tool result renders the
     ; code. The vendor's own text still reaches the log alone, on the line below.
     Set tViolations = ##class(OcuPilot.Api.Error).ViolationsFromVendor(pVendorSC)
+    If (pWrapper '= "") && $IsObject(tViolations) {
+        Set tRowIterator = tViolations.%GetIterator()
+        While tRowIterator.%GetNext(.tRowIndex, .tRow) {
+            If '$IsObject(tRow) Continue
+            Set tField = tRow.%Get("field")
+            If $Extract(tField, 1, $Length(pWrapper) + 1) = (pWrapper _ ".") Do tRow.%Set("field", $Extract(tField, $Length(pWrapper) + 2, *), "string")
+        }
+    }
     If $IsObject(pFault) && $IsObject(tViolations) && (tViolations.%Size() > 0) {
         Set tFieldDetail = {}
         Do tFieldDetail.%Set("violations", tViolations)
diff --git a/src/OcuPilot/Screen/Descriptor/UserForm.cls b/src/OcuPilot/Screen/Descriptor/UserForm.cls
new file mode 100644
index 0000000..cb13ae2
--- /dev/null
+++ b/src/OcuPilot/Screen/Descriptor/UserForm.cls
@@ -0,0 +1,48 @@
+/// <p>The User form: one user account, created here, rendered by the <code>form-page</code>
+/// archetype (AD-5). Its Save and the agent's confirmed <code>permissions.users.create</code> are two
+/// callers of one tool (AD-55).</p>
+/// <p><b>It is built and routable, and it takes no side-bar position</b> (<code>sideBarPosition</code>
+/// 0): it is reached from <class>OcuPilot.Screen.Descriptor.UserList</class>'s Create, at that list's
+/// own route with <code>/edit</code> appended.</p>
+/// <p><b>It declares no <code>read</code> and no <code>table</code></b> (AD-36): the create route
+/// answers the account it made, and the list is where the declared read lives.</p>
+/// <p><b>Its privilege set is the list's own</b> -- <code>%Admin_Secure:USE</code> plus
+/// <code>%DB_IRISSYS:READ</code> -- because the form writes the endpoint the list reads (AD-8).</p>
+/// <p><b>Its classic equivalent is <code>%CSP.UI.Portal.User</code></b>, in the class's own case
+/// (AD-44). It declares no exemption and does not refresh (AD-43).</p>
+/// <p><b><code>Password</code> is secret-typed in its context</b> (AD-24, AD-35), so a screen-context
+/// capture of this form never carries the value being typed.</p>
+Class OcuPilot.Screen.Descriptor.UserForm Extends OcuPilot.Screen.Descriptor.Base
+{
+
+XData Declaration
+{
+{
+  "route": "permissions/users/edit",
+  "area": "permissions",
+  "labelKey": "userFormLabel",
+  "sideBarPosition": 0,
+  "archetype": "form-page",
+  "built": true,
+  "refreshes": false,
+  "refreshRates": [],
+  "privileges": [{"resource": "%Admin_Secure", "permission": "USE"}, {"resource": "%DB_IRISSYS", "permission": "READ"}],
+  "entityType": "user",
+  "secondaryEntityTypes": [],
+  "scope": "instance",
+  "parentScope": "",
+  "id": {"kind": "single", "parts": []},
+  "primaryAction": {"id": "", "selfProtection": ""},
+  "rowActions": [],
+  "context": {"fields": [], "secretFields": ["Password"]},
+  "secretArguments": [],
+  "fingerprintExcludes": [],
+  "emptyStateKey": "",
+  "commandAliases": [],
+  "classicPage": "%CSP.UI.Portal.User",
+  "classicLinkExemption": {"exempt": false, "reason": "", "label": "", "href": ""},
+  "toolIdentifier": "permissions.userform"
+}
+}
+
+}
diff --git a/src/OcuPilot/Screen/Descriptor/UserList.cls b/src/OcuPilot/Screen/Descriptor/UserList.cls
index a86cd55..fe6b621 100644
--- a/src/OcuPilot/Screen/Descriptor/UserList.cls
+++ b/src/OcuPilot/Screen/Descriptor/UserList.cls
@@ -19,8 +19,13 @@
 /// account in the healthy colour, so the word carries the state.</p>
 /// <p><b>The classic page is <code>%CSP.UI.Portal.Users</code></b>, in the class's own case
 /// (AD-44). As a list archetype it links out to nothing. The context carries no secret field.</p>
-/// <p>It declares no primary or row action, so its empty state names what to do next, and it does
+/// <p><b>Its primary action is Create</b>, which opens <class>OcuPilot.Screen.Descriptor.UserForm</class>;
+/// being write-capable, its empty state invites the agent rather than naming a next step. It does
 /// not refresh (AD-43).</p>
+/// <p><b><code>Password</code> is its one secret argument</b> (AD-3, AD-6): the create tool's
+/// authored wrapper field, supplied on the screen once and at confirm on the card, and never
+/// accepted from the model. The declaration opens the update tool's confirm channel to the same
+/// name, which that tool's card never asks for because no update diff names it.</p>
 Class OcuPilot.Screen.Descriptor.UserList Extends OcuPilot.Screen.Descriptor.Base
 {
 
@@ -41,9 +46,10 @@ XData Declaration
   "scope": "instance",
   "parentScope": "",
   "id": {"kind": "single", "parts": []},
-  "primaryAction": {"id": "", "selfProtection": ""},
+  "primaryAction": {"id": "create", "selfProtection": ""},
   "rowActions": [],
   "context": {"fields": ["Name", "FullName", "Enabled", "Type", "Roles", "ExpirationDate", "Expired"], "secretFields": []},
+  "secretArguments": ["Password"],
   "emptyStateKey": "userListEmpty",
   "commandAliases": ["accounts"],
   "classicPage": "%CSP.UI.Portal.Users",
@@ -74,8 +80,8 @@ XData Declaration
       {"field": "Type", "labelKey": "tableColumnType", "kind": "text"},
       {"field": "Roles", "labelKey": "userColumnRoles", "kind": "identifier"}
     ],
-    "emptyNextKey": "tableReadOnlyEmptyNext",
-    "emptyAgentKey": ""
+    "emptyNextKey": "",
+    "emptyAgentKey": "userListEmptyAgent"
   },
   "toolIdentifier": "permissions.users"
 }
diff --git a/src/OcuPilot/Screen/Registry.cls b/src/OcuPilot/Screen/Registry.cls
index 81ac360..170400c 100644
--- a/src/OcuPilot/Screen/Registry.cls
+++ b/src/OcuPilot/Screen/Registry.cls
@@ -1975,6 +1975,11 @@ ClassMethod NameArrayProblem(pParent As %DynamicObject, pKey As %String, pAllowe
 /// spelling nobody sends. Both entries are now checked against one set,
 /// <method>DeclaredNames</method>'s, so the two sibling declarations cannot be validated against
 /// different sets, which is the cause rather than the symptom.</p>
+/// <p><b>A <code>secretArguments</code> entry also qualifies when it is a top-level
+/// <code>secret</code> literal row of the screen's write tools</b> (<method>SecretRowNames</method>):
+/// a derived credential, or a wrapper field the tool authors (AD-3). The source is additive --
+/// every name the two sources above admit is still admitted -- and
+/// <file>ui/tools/screen-mirror.mjs</file>'s <code>secretRowNames</code> applies the same rule.</p>
 /// <p><b>Both directions moved, and both are pinned.</b> Folding the declared criteria into the
 /// set <b>loosens</b> <code>fingerprintExcludes</code> -- a criterion name was refused before and
 /// is a name the read declares. Scoping the tool half to the rows a write tool may actually set
@@ -2010,9 +2015,10 @@ ClassMethod ConfirmChannelProblem(pDeclaration As %DynamicObject) As %String
         }
         If tProblem '= "" Quit
 
+        Set tSecretRows = ..SecretRowNames(.tRows)
         For tI = 1:1:$ListLength(tSecrets) {
             Set tName = $List(tSecrets, tI)
-            If '$ListFind(tNames("settable"), tName) && '$ListFind(tNames("read"), tName) {
+            If '$ListFind(tNames("settable"), tName) && '$ListFind(tNames("read"), tName) && '$ListFind(tSecretRows, tName) {
                 Set tProblem = "secretArguments names '" _ tName _ "', which is neither a settable field of this screen's write tool nor one its read declares (AD-6)"
                 Quit
             }
@@ -2373,7 +2379,7 @@ ClassMethod LastSegment(pPath As %String) As %String [ Private ]
 }
 
 /// <p>The classified field rows of the write tool <var>pIdentifier</var> owns, as
-/// <code>pRows(path) = $ListBuild(isSettable, strippedName, isSettableStringLiteral)</code>;
+/// <code>pRows(path) = $ListBuild(isSettable, strippedName, isSettableStringLiteral, isTopLevelSecret)</code>;
 /// empty when the identifier owns no <class>OcuPilot.Screen.Tool.ToolFields</class> entry. A
 /// refusal is returned when that generated block cannot be read, because an unreadable field list
 /// is not a negative result.</p>
@@ -2387,8 +2393,8 @@ ClassMethod LastSegment(pPath As %String) As %String [ Private ]
 /// which is the circularity this shape avoids. The consequence is stated rather than hidden: a
 /// derived field the tool does not permit is still a nameable secret here, and closing that is
 /// this spec's <code>deferred:</code> item. The third element narrows the rows to a string
-/// placeholder, which is the credential-name rule's own qualification and nothing else's. One
-/// reader of the generated block, three questions asked of its rows.</p>
+/// placeholder, which is the credential-name rule's own qualification and nothing else's; the fourth marks a top-level <code>secret</code> literal (<method>SecretRowNames</method>). One
+/// reader of the generated block, four questions asked of its rows.</p>
 ClassMethod ToolFieldRows(pIdentifier As %String, Output pRows) As %String [ Private ]
 {
     Kill pRows
@@ -2418,7 +2424,8 @@ ClassMethod ToolFieldRows(pIdentifier As %String, Output pRows) As %String [ Pri
                 Set tLiteral = (tField.%Get("class") = "ordinary") && (tField.%Get("shape") = "literal")
                 Set tName = $Select($Extract(tPath, *-1, *) = "[]": $Extract(tPath, 1, *-2), 1: tPath)
                 If (tPath [ ".") || (tName [ "[") Set tName = ""
-                Set pRows(tPath) = $ListBuild((tLiteral && (tName '= "")), tName, (tLiteral && (tField.%Get("templateType") = "string")))
+                Set tTopSecret = (tField.%Get("class") = "secret") && (tField.%Get("shape") = "literal") && '(tPath [ ".") && '(tPath [ "[")
+                Set pRows(tPath) = $ListBuild((tLiteral && (tName '= "")), tName, (tLiteral && (tField.%Get("templateType") = "string")), tTopSecret)
             }
         }
     } Catch ex {
@@ -2428,6 +2435,25 @@ ClassMethod ToolFieldRows(pIdentifier As %String, Output pRows) As %String [ Pri
     Quit tProblem
 }
 
+/// <p>The names of the top-level <code>secret</code> literal rows in <var>pRows</var>
+/// (<method>ToolFieldRows</method>'s answer), as a <class>%List</class>: the third source a
+/// <code>secretArguments</code> entry may name. A nested path, an array element and a row of any
+/// other class contribute nothing.</p>
+ClassMethod SecretRowNames(ByRef pRows) As %List [ Private ]
+{
+    Set tNames = ""
+    Set tPath = ""
+    For {
+        Set tPath = $Order(pRows(tPath))
+        If tPath = "" Quit
+        Set tRow = pRows(tPath)
+        If '$ListValid(tRow) || ($ListLength(tRow) < 4) Continue
+        If '$List(tRow, 4) Continue
+        If '$ListFind(tNames, tPath) Set tNames = tNames _ $ListBuild(tPath)
+    }
+    Quit tNames
+}
+
 /// <p>A refusal naming the first key of <var>pObject</var> outside <var>pAllowed</var>, or
 /// <code>""</code>.</p>
 ClassMethod UnknownKeyProblem(pObject As %DynamicObject, pWhere As %String, pAllowed As %List) As %String [ Private ]
diff --git a/src/OcuPilot/Screen/Tool/Classification.cls b/src/OcuPilot/Screen/Tool/Classification.cls
index d1e4619..68f2b8d 100644
--- a/src/OcuPilot/Screen/Tool/Classification.cls
+++ b/src/OcuPilot/Screen/Tool/Classification.cls
@@ -7,9 +7,12 @@
 /// (<code>&lt;area&gt;.&lt;screen&gt;.&lt;verb&gt;</code>, lower case, dots only). Each entry holds
 /// <code>fieldList</code>, a list key <class>OcuPilot.Screen.Tool.FieldLists</class> carries, and
 /// <code>classification</code>, an object mapping a classifiable path of that list (a row no other
-/// row's path extends) to <code>ordinary</code>, <code>secret</code> or <code>opaque</code>. The
-/// keys <code>required</code>, <code>enum</code> and <code>description</code> are reserved for the
-/// semantic half of a tool's schema and accept no content yet; no other key is accepted.</p>
+/// row's path extends) to <code>ordinary</code>, <code>secret</code> or <code>opaque</code>. An
+/// optional <code>authored</code> object maps a top-level name the endpoint's request wrapper
+/// carries and its template does not to <code>secret</code>, the only class it accepts; such a name
+/// may not collide with a derived path. The keys <code>required</code>, <code>enum</code> and
+/// <code>description</code> are reserved for the semantic half of a tool's schema and accept no
+/// content yet; no other key is accepted.</p>
 /// <p><b>It fails closed.</b> A classifiable path the entry does not name is emitted
 /// <code>secret</code>. <code>opaque</code> is for a member-less object or array only, and a
 /// member-less object or array may not be <code>ordinary</code>. A string literal whose last path
@@ -48,6 +51,11 @@ Class OcuPilot.Screen.Tool.Classification Extends %RegisteredObject
 /// <code>ordinary</code> on a member-less array, so <code>opaque</code> is also the only class
 /// either row can carry. The tool's one authored array argument is its semantic half
 /// (<class>OcuPilot.Screen.Tool.UserUpdate</class>), not a derived field.</p>
+/// <p><b><code>permissions.users.create</code></b> classifies the same <code>Security.User</code>
+/// list as the update entry does, and authors <code>Password</code> as <code>secret</code>: the
+/// vendor's POST body is <code>{User, Password}</code>, whose outer <code>Password</code> the
+/// template never shows, so AD-3's "the write tool authors [wrapper fields] as secret fields of its
+/// own" is stated here, where the generator can check it.</p>
 XData Entries
 {
 {
@@ -177,6 +185,30 @@ XData Entries
       "WSGIDebug": "ordinary",
       "WSGIType": "ordinary"
     }
+  },
+  "permissions.users.create": {
+    "fieldList": "Security.User",
+    "classification": {
+      "AccountNeverExpires": "ordinary",
+      "AutheEnabled": "ordinary",
+      "ChangePassword": "ordinary",
+      "Comment": "ordinary",
+      "EmailAddress": "ordinary",
+      "Enabled": "ordinary",
+      "ExpirationDate": "ordinary",
+      "FullName": "ordinary",
+      "HOTPKeyDisplay": "ordinary",
+      "NameSpace": "ordinary",
+      "PasswordNeverExpires": "ordinary",
+      "PhoneNumber": "ordinary",
+      "PhoneProvider": "ordinary",
+      "Roles": "opaque",
+      "EscalationRoles": "opaque",
+      "Routine": "ordinary"
+    },
+    "authored": {
+      "Password": "secret"
+    }
   }
 }
 }
diff --git a/src/OcuPilot/Screen/Tool/ToolFields.cls b/src/OcuPilot/Screen/Tool/ToolFields.cls
index dad9508..d30bf55 100644
--- a/src/OcuPilot/Screen/Tool/ToolFields.cls
+++ b/src/OcuPilot/Screen/Tool/ToolFields.cls
@@ -10,6 +10,25 @@ Class OcuPilot.Screen.Tool.ToolFields Extends %RegisteredObject
 XData Tools
 {
 {
+"permissions.users.create": {"fieldList":"Security.User","fields":[
+  {"path":"AccountNeverExpires","shape":"literal","templateType":"boolean","itemType":"","class":"ordinary"},
+  {"path":"AutheEnabled","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"ChangePassword","shape":"literal","templateType":"boolean","itemType":"","class":"ordinary"},
+  {"path":"Comment","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"EmailAddress","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"Enabled","shape":"literal","templateType":"boolean","itemType":"","class":"ordinary"},
+  {"path":"ExpirationDate","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"FullName","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"HOTPKeyDisplay","shape":"literal","templateType":"boolean","itemType":"","class":"ordinary"},
+  {"path":"NameSpace","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"PasswordNeverExpires","shape":"literal","templateType":"boolean","itemType":"","class":"ordinary"},
+  {"path":"PhoneNumber","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"PhoneProvider","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"Roles","shape":"array","templateType":"array","itemType":"","class":"opaque"},
+  {"path":"EscalationRoles","shape":"array","templateType":"array","itemType":"","class":"opaque"},
+  {"path":"Routine","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
+  {"path":"Password","shape":"literal","templateType":"string","class":"secret","authored":true}
+]},
 "permissions.users.update": {"fieldList":"Security.User","fields":[
   {"path":"AccountNeverExpires","shape":"literal","templateType":"boolean","itemType":"","class":"ordinary"},
   {"path":"AutheEnabled","shape":"literal","templateType":"string","itemType":"","class":"ordinary"},
diff --git a/src/OcuPilot/Screen/Tool/UserCreate.cls b/src/OcuPilot/Screen/Tool/UserCreate.cls
new file mode 100644
index 0000000..4668402
--- /dev/null
+++ b/src/OcuPilot/Screen/Tool/UserCreate.cls
@@ -0,0 +1,135 @@
+/// <p>The Users list's create tool (AD-54, AD-55): propose a new user account on this instance. It
+/// reads the intended name fresh through the vendor's own <code>Security.User</code>
+/// <code>GET</code>, requires that read to answer <b>nothing</b>, composes the payload from its
+/// arguments and mints a proposal the user confirms. It writes nothing.</p>
+/// <p><b>The password is a secret end to end</b> (AD-3, AD-6, AD-35). It is the create's one
+/// authored wrapper field (<class>OcuPilot.Screen.Tool.Classification</class>), declared in
+/// <class>OcuPilot.Screen.Descriptor.UserList</class>'s <code>secretArguments</code>, so it is
+/// absent from this schema, refused from the model at the mint, never stored, and supplied only at
+/// confirm on the card beside the one masked row <method>ComposedSecrets</method> names.</p>
+/// <p><b>The vendor body is a wrapper</b>, <code>{User:{...}, Password}</code>; the port builds it
+/// (<parameter>OcuPilot.Port.AdminPort.WRAPPEDTYPES</parameter>), so this tool and the screen send
+/// the flat field set.</p>
+/// <p><b>It admits the fields the create form captures</b>: the four derived literals
+/// <parameter>PERMITTEDFIELDS</parameter> names and the authored <code>Roles</code> array, which are
+/// exactly the ones <method>OcuPilot.Kernel.Proposal.Prohibited.PermittedCreateFields</method> has
+/// reviewed for a user create. A role that grants <code>%All</code> or an administrative privilege
+/// is refused at the write, whatever the caller. <code>EscalationRoles</code> is excluded by
+/// name.</p>
+Class OcuPilot.Screen.Tool.UserCreate Extends OcuPilot.Screen.Tool.Write
+{
+
+Parameter TOOLNAME = "permissions.users.create";
+
+Parameter DESCRIPTORCLASS = "OcuPilot.Screen.Descriptor.UserList";
+
+Parameter DESCRIPTION = "Propose a new user account on this instance. It creates nothing by itself: it answers a proposal the user reviews and confirms, and the user types the account's password on the card at that point -- never send a password. Name the account, and optionally its full name, expiration date, startup namespace and routine, and roles.";
+
+/// <p>This tool creates its target (AD-54).</p>
+Parameter CREATES As BOOLEAN = 1;
+
+/// <p>The AD-14 action a confirmed create performs.</p>
+Parameter CHANGEACTION = "created";
+
+/// <p>The vendor request type a create is issued with: <code>Security.User</code>'s
+/// <code>POST</code>. Its <code>PUT</code> refuses an absent account with 404, so it is not an
+/// upsert and cannot create.</p>
+Parameter WRITETYPE = "POST";
+
+/// <p>The derived top-level fields this tool admits, comma-separated: the account settings whose
+/// creation AD-10's prohibited set has reviewed
+/// (<method>OcuPilot.Kernel.Proposal.Prohibited.PermittedCreateFields</method> names these and
+/// <parameter>ROLESARGUMENT</parameter>). A test asserts the two agree.</p>
+Parameter PERMITTEDFIELDS = "ExpirationDate,FullName,NameSpace,Routine";
+
+/// <p>The one authored argument: the complete list of roles the new account holds.</p>
+Parameter ROLESARGUMENT = "Roles";
+
+/// <p>The subtree AD-10 forbids on every path: an account's escalation roles.</p>
+Parameter EXCLUDEDFIELD = "EscalationRoles";
+
+/// <p>The secret the confirm supplies and the payload never carries.</p>
+Parameter PASSWORDFIELD = "Password";
+
+/// <p>The pair a create requires: <code>%Admin_Secure:USE</code>, the vendor endpoint's own
+/// <code>ResourcesOR()</code> resource and the pair the Users list already declares (AD-8).</p>
+Parameter WRITERESOURCE = "%Admin_Secure";
+
+Parameter WRITEPERMISSION = "USE";
+
+ClassMethod Endpoint() As %String
+{
+    Quit "Security.User"
+}
+
+/// <p>The field names the composed payload may set: the <code>ordinary</code> top-level literals
+/// this tool admits, derived rather than transcribed (AD-3), plus
+/// <parameter>ROLESARGUMENT</parameter>.</p>
+ClassMethod SettableFields() As %List
+{
+    Quit ..AdmittedFields() _ $ListBuild(..#ROLESARGUMENT)
+}
+
+/// <p><parameter>EXCLUDEDFIELD</parameter>, so no path of this tool admits an escalation-role
+/// grant.</p>
+ClassMethod ExcludedFields() As %List
+{
+    Quit $ListBuild(..#EXCLUDEDFIELD)
+}
+
+/// <p><parameter>PERMITTEDFIELDS</parameter>, so a field the prohibited set has not reviewed for a
+/// create is absent from the schema and from <method>SettableFields</method>.</p>
+ClassMethod PermittedFields() As %List
+{
+    Quit $ListFromString(..#PERMITTEDFIELDS)
+}
+
+/// <p>The declared secrets a create's body carries and its payload does not, for the mint's masked
+/// diff rows (AD-6): <parameter>PASSWORDFIELD</parameter>.</p>
+ClassMethod ComposedSecrets() As %List
+{
+    Quit $ListBuild(..#PASSWORDFIELD)
+}
+
+/// <p>The derived schema with <parameter>ROLESARGUMENT</parameter> added as an array of role
+/// names, closed with <code>additionalProperties: false</code>.</p>
+ClassMethod InputSchema(pDescriptor As %String = "") As %DynamicObject
+{
+    Set tSchema = ##super(pDescriptor)
+    If '$IsObject(tSchema) Quit tSchema
+    If tSchema.%GetTypeOf("properties") '= "object" Quit tSchema
+    Set tRoles = {"type": "array", "description": "The roles the new account holds, each named exactly as this instance's own roles list reports it. A role that would grant an administrative privilege is refused, whether it grants it by name, through another role, or as a resource it carries -- %All, %Manager, %Operator and %SecurityAdministrator are all refused."}
+    Do tRoles.%Set("items", {"type": "string"})
+    Do tSchema.%Get("properties").%Set(..#ROLESARGUMENT, tRoles)
+    Quit tSchema
+}
+
+/// <p>The screen's own field rules (<method>OcuPilot.Area.Permissions.UserCreateRules.Validate</method>)
+/// over the agent's arguments, each broken rule as its field and published sentence (AD-55). The
+/// name look-up and the password rules are skipped: the mint's absence read has answered the first,
+/// and the password is supplied at confirm.</p>
+ClassMethod ArgumentProblem(pIdValue As %String, pArgs As %DynamicObject, Output pProblem As %String) As %Status
+{
+    Set pProblem = ""
+    Set tSC = ##class(OcuPilot.Area.Permissions.UserCreateRules).Validate(pIdValue, pArgs, .tViolations, 0, 0)
+    If $$$ISERR(tSC) Quit tSC
+    For tI = 1:1:$Get(tViolations) {
+        Set tField = $List(tViolations(tI), 1)
+        Set tReason = ##class(OcuPilot.Api.Error).ReasonForViolation($List(tViolations(tI), 2))
+        Set pProblem = pProblem _ $Select(pProblem = "": "", 1: " ") _ "'" _ tField _ "': " _ tReason
+    }
+    Quit $$$OK
+}
+
+/// <p>What the caller must hold: the screen's own pairs together with
+/// <parameter>WRITERESOURCE</parameter>:<parameter>WRITEPERMISSION</parameter>, each pair once.</p>
+ClassMethod PrivilegePairs(pDescriptor As %String = "", Output pResolved As %Boolean) As %List
+{
+    Set tPairs = ##class(OcuPilot.Screen.Gate).RequiredPairs(..#DESCRIPTORCLASS, .pResolved)
+    If 'pResolved Quit ""
+    Set tOwn = $ListBuild(..#WRITERESOURCE, ..#WRITEPERMISSION)
+    If $ListFind(tPairs, tOwn) Quit tPairs
+    Quit tPairs _ $ListBuild(tOwn)
+}
+
+}
diff --git a/src/OcuPilot/Screen/Tool/WebAppCreate.cls b/src/OcuPilot/Screen/Tool/WebAppCreate.cls
index df1810e..3c876ac 100644
--- a/src/OcuPilot/Screen/Tool/WebAppCreate.cls
+++ b/src/OcuPilot/Screen/Tool/WebAppCreate.cls
@@ -26,9 +26,12 @@
 /// <method>OcuPilot.Kernel.Proposal.Prohibited.AlwaysProhibitedCreateFields</method>. <code>Name</code>
 /// is the vendor's <code>name</code> query parameter and a 400 in the body
 /// (<code>UnexpectedRequestBodyField</code>). And <code>Path</code> is never settable by a caller:
-/// <method>DerivedFields</method> derives it from <code>WSGIAppLocation</code> for a WSGI or ASGI
-/// application, and <method>Security.Applications.Create</method> creates the directory a CSP
-/// application names.</p>
+/// <method>DerivedFields</method> sets it, and <code>WSGIAppLocation</code>, to the directory the
+/// caller's one directory name resolves to under the instance's fixed WSGI root (AD-21), and
+/// <method>Security.Applications.Create</method> creates the directory a CSP application names.</p>
+/// <p><b>Choosing Unauthenticated is permitted and named</b> (DW-1489): <method>Consequence</method>
+/// carries <parameter>CONSEQUENCEUNAUTHENTICATED</parameter> onto the proposal, and the card states
+/// it.</p>
 /// <p><b><code>Type</code> is never sent.</b> <code>MergeJsonAndProperties</code> hard-codes it to
 /// <code>$$$AppTypeCSP</code> and <method>RequestBodySchema</method> does not carry it, so sending
 /// it is a 400. The CSP, REST, WSGI and ASGI choice a caller makes is a combination of
@@ -63,12 +66,21 @@ Parameter PERMITTEDFIELDS = "AutheEnabled,Description,DispatchClass,Enabled,Name
 
 /// <p>The vendor property the server derives, and the property it derives it from. Not settable by
 /// a caller and absent from <parameter>PERMITTEDFIELDS</parameter>: no OcuPilot endpoint accepts a
-/// filesystem path from a caller (AD-21), and a WSGI application's directory is the one the caller
-/// already named.</p>
+/// filesystem path from a caller (AD-21), so both are set to the directory
+/// <class>OcuPilot.Area.WebApp.Location</class> resolves the caller's one directory name to.</p>
 Parameter DERIVEDPATH = "Path";
 
 Parameter WSGILOCATION = "WSGIAppLocation";
 
+/// <p>The authentication mask field, and the bit that makes an application reachable without
+/// signing in (<file>irissys/%sySecurityMacros.inc</file>).</p>
+Parameter AUTHEFIELD = "AutheEnabled";
+
+Parameter UNAUTHENTICATEDBIT As INTEGER = 64;
+
+/// <p>The consequence code a create choosing Unauthenticated carries onto its proposal (DW-1489).</p>
+Parameter CONSEQUENCEUNAUTHENTICATED = "WEBAPP.UNAUTHENTICATED";
+
 /// <p>The pair a create requires: <code>%Admin_Secure:USE</code>, which is exactly what
 /// <method>%CSP.Portal.Application.CheckSecurity</method> checks before the classic editor saves,
 /// and exactly the pair <class>OcuPilot.Screen.Descriptor.WebAppList</class> already declares for
@@ -103,24 +115,56 @@ ClassMethod PermittedFields() As %List
     Quit $ListFromString(..#PERMITTEDFIELDS)
 }
 
-/// <p>Derive <code>Path</code> from <code>WSGIAppLocation</code> for a WSGI or ASGI application,
-/// and leave the body alone otherwise (AD-55).</p>
+/// <p>Set <code>WSGIAppLocation</code> and <code>Path</code> to the directory the caller's
+/// location name resolves to under the fixed WSGI root, or remove both where it does not resolve
+/// (AD-21, AD-55). A body that names no location is left alone.</p>
 /// <p><b>Caller contract:</b> <class>OcuPilot.Kernel.Proposal.Confirm</class> and
 /// <class>OcuPilot.Area.WebApp.Create</class> each call it once, on the body about to be sent,
-/// after the prohibited set has reached its verdict. Both callers resolve it here, which is what
-/// makes the screen's Save and the agent's confirm one composition rather than two.</p>
-/// <p>A WSGI or ASGI application serves from the directory holding its callable, so the vendor's
-/// own physical path is that directory; a CSP or REST application's is left unset, and
-/// <method>Security.Applications.Create</method> creates the directory it then defaults to. An
-/// empty location derives nothing, so a body that names a protocol and no directory reaches the
-/// vendor's own refusal rather than a path this method invented.</p>
+/// after the prohibited set has reached its verdict. The root is computed at this call by
+/// <method>OcuPilot.Area.WebApp.Location.Resolve</method>, so the directory sent is the one the
+/// instance answers now; the caller's own text never reaches the vendor. A name that no longer
+/// resolves at confirm drops both keys rather than passing the text through.</p>
 ClassMethod DerivedFields(pPayload As %DynamicObject)
 {
     If '$IsObject(pPayload) Quit
-    If pPayload.%GetTypeOf(..#WSGILOCATION) '= "string" Quit
-    Set tLocation = pPayload.%Get(..#WSGILOCATION)
-    If tLocation = "" Quit
-    Do pPayload.%Set(..#DERIVEDPATH, tLocation, "string")
+    If pPayload.%GetTypeOf(..#WSGILOCATION) = "unassigned" Quit
+    Set tLocation = ""
+    If pPayload.%GetTypeOf(..#WSGILOCATION) = "string" Set tLocation = pPayload.%Get(..#WSGILOCATION)
+    If (tLocation '= "") && ##class(OcuPilot.Area.WebApp.Location).Resolve(tLocation, .tPath) {
+        Do pPayload.%Set(..#WSGILOCATION, tPath, "string")
+        Do pPayload.%Set(..#DERIVEDPATH, tPath, "string")
+        Quit
+    }
+    Do pPayload.%Remove(..#WSGILOCATION)
+    Do pPayload.%Remove(..#DERIVEDPATH)
+}
+
+/// <p>The kernel-owned consequence code this create's payload <var>pPayload</var> carries, or
+/// <code>""</code> (DW-1489): <parameter>CONSEQUENCEUNAUTHENTICATED</parameter> when
+/// <code>AutheEnabled</code> includes the unauthenticated bit. The mint stores it on the proposal
+/// and the card states its sentence; the create is not refused.</p>
+ClassMethod Consequence(pPayload As %DynamicObject) As %String
+{
+    If '$IsObject(pPayload) Quit ""
+    Set tType = pPayload.%GetTypeOf(..#AUTHEFIELD)
+    If (tType '= "number") && (tType '= "string") Quit ""
+    Set tMask = pPayload.%Get(..#AUTHEFIELD)
+    If '$IsValidNum(tMask) Quit ""
+    If ((tMask \ 1) \ ..#UNAUTHENTICATEDBIT) # 2 Quit ..#CONSEQUENCEUNAUTHENTICATED
+    Quit ""
+}
+
+/// <p>The derived schema with <code>WSGIAppLocation</code> described as the one directory name it
+/// is (AD-21): a name, not a path, placed under the instance's own WSGI directory.</p>
+ClassMethod InputSchema(pDescriptor As %String = "") As %DynamicObject
+{
+    Set tSchema = ##super(pDescriptor)
+    If '$IsObject(tSchema) Quit tSchema
+    If tSchema.%GetTypeOf("properties") '= "object" Quit tSchema
+    Set tProperties = tSchema.%Get("properties")
+    If tProperties.%GetTypeOf(..#WSGILOCATION) '= "object" Quit tSchema
+    Do tProperties.%Get(..#WSGILOCATION).%Set("description", "For a WSGI or ASGI application only: the name of one directory, such as myapp -- letters, digits, dots, dashes and underscores, at most 64 characters. It is placed in this instance's own WSGI directory. A path, or a name containing .., is refused.")
+    Quit tSchema
 }
 
 /// <p>The screen's own field rules (<method>OcuPilot.Area.WebApp.Create.Validate</method>) over the
diff --git a/src/OcuPilot/Test/EndpointCoverage.cls b/src/OcuPilot/Test/EndpointCoverage.cls
index 6f85b6c..28173b8 100644
--- a/src/OcuPilot/Test/EndpointCoverage.cls
+++ b/src/OcuPilot/Test/EndpointCoverage.cls
@@ -125,6 +125,11 @@ XData Probes
      OcuPilot.Test.UiAboutWire's. -->
 <probe class="Api.Router" method="GET" url="/ui/help" path="/api/ocupilot/ui/help" disposition="envelope"/>
 <probe class="Api.Router" method="GET" url="/ui/system" path="/api/ocupilot/ui/system" disposition="envelope"/>
+<probe class="Api.Router" method="GET" url="/users/form" path="/api/ocupilot/users/form" disposition="envelope"/>
+<!-- /users/name carries no query string here, for /web-applications/name's reason: it draws the
+     documented 422 USER.NAME.REQUIRED envelope. Its own answers are OcuPilot.Test.UserCreateWire's. -->
+<probe class="Api.Router" method="GET" url="/users/name" path="/api/ocupilot/users/name" disposition="envelope"/>
+<probe class="Api.Router" method="POST" url="/users" path="/api/ocupilot/users" disposition="envelope" refusal="1"/>
 <probe class="Api.Readiness" method="GET" url="/" path="/api/ocupilot/readiness/" disposition="envelope"/>
 <probe class="Api.StaticHandler" method="GET" url="/(.*)" path="/ocupilot/index.html" disposition="bytes"/>
 <probe class="Api.StaticHandler" method="HEAD" url="/(.*)" path="/ocupilot/index.html" disposition="bytes"/>
diff --git a/src/OcuPilot/Test/Prohibited.cls b/src/OcuPilot/Test/Prohibited.cls
index 2412686..bd36358 100644
--- a/src/OcuPilot/Test/Prohibited.cls
+++ b/src/OcuPilot/Test/Prohibited.cls
@@ -322,6 +322,10 @@ Method TestNoWriteToolAdmitsAnAlwaysProhibitedField()
             ; the prohibited verdict is reached, so a tool must not declare one of these a secret.
             Do $$$AssertEquals($ListFind(tSecrets, tField), 0, "and does not declare it a secret argument")
         }
+        ; AD-3: a wrapper secret one of the screen's tools authors -- Security.User's POST
+        ; Password -- is no field of the endpoint's template, so no predicate reviews it and the
+        ; reviewed-few floor below is asked about the declared secrets that are fields.
+        Set tSecrets = ..WithoutAuthoredSecrets(tDescriptor, tSecrets)
         ; The other half of AD-10's title, and the one that covers a field nobody has named: a tool
         ; advertises nothing outside the set's reviewed few, so a vendor property added to the
         ; endpoint's template reaches no schema until the set has a predicate for it.
@@ -639,4 +643,34 @@ Method Sources(Output pSources) As %Status
     Quit tSC
 }
 
+/// <p><var>pSecrets</var> minus every name a write tool of descriptor <var>pDescriptor</var>'s
+/// screen authors as a <code>secret</code> wrapper field in
+/// <class>OcuPilot.Screen.Tool.ToolFields</class> (a row marked <code>authored</code>): a name the
+/// endpoint's template does not carry, which the confirm merges into the body and the vendor reads
+/// only as its wrapper's outer key.</p>
+ClassMethod WithoutAuthoredSecrets(pDescriptor As %String, pSecrets As %List) As %List
+{
+    Set tIdentifier = ""
+    If pDescriptor '= "" Set tIdentifier = $ClassMethod(pDescriptor, "ToolIdentifier")
+    If tIdentifier = "" Quit pSecrets
+    Set tXData = ##class(%Dictionary.XDataDefinition).IDKEYOpen("OcuPilot.Screen.Tool.ToolFields", "Tools")
+    If '$IsObject(tXData) Quit pSecrets
+    Set tTools = ##class(%DynamicAbstractObject).%FromJSON(tXData.Data)
+    Kill tAuthored
+    Set tIterator = tTools.%GetIterator()
+    While tIterator.%GetNext(.tName, .tEntry) {
+        If $Piece(tName, ".", 1, 2) '= tIdentifier Continue
+        Set tFields = tEntry.%Get("fields").%GetIterator()
+        While tFields.%GetNext(.tIndex, .tField) {
+            If (tField.%Get("authored") = 1) && (tField.%Get("class") = "secret") Set tAuthored(tField.%Get("path")) = ""
+        }
+    }
+    Set tOut = ""
+    For tI = 1:1:$ListLength(pSecrets) {
+        If $Data(tAuthored($List(pSecrets, tI))) Continue
+        Set tOut = tOut _ $ListBuild($List(pSecrets, tI))
+    }
+    Quit tOut
+}
+
 }
diff --git a/src/OcuPilot/Test/ProposalCreate.cls b/src/OcuPilot/Test/ProposalCreate.cls
index 083a7de..996396b 100644
--- a/src/OcuPilot/Test/ProposalCreate.cls
+++ b/src/OcuPilot/Test/ProposalCreate.cls
@@ -39,10 +39,10 @@ Parameter OTHERSPELLING = "/CSP/OcuPilotProbeCreate/";
 /// create's inverted acceptance refuses.</p>
 Parameter PRESENT = "{""Name"":""/csp/ocupilotprobecreate"",""Enabled"":true,""NameSpace"":""HSCUSTOM""}";
 
-/// <p>The vendor property the server derives, and the directory it derives it from.</p>
+/// <p>The vendor property the server derives, and the directory name it resolves it from.</p>
 Parameter DERIVEDFIELD = "Path";
 
-Parameter WSGILOCATION = "/tmp/ocupilot-probe-wsgi";
+Parameter WSGILOCATION = "ocupilot-probe-wsgi";
 
 Method OnBeforeOneTest() As %Status
 {
@@ -307,7 +307,7 @@ Method TestTheConfirmedBodyCarriesTheServerDerivedPath()
     Do $$$AssertStatusOK(tSC, "and confirmed")
     If $$$ISERR(tSC) Quit
     Set tSent = ##class(%DynamicAbstractObject).%FromJSON(##class(OcuPilot.Test.ProposalFixture).LastWriteBody())
-    Do $$$AssertEquals(tSent.%Get(..#DERIVEDFIELD), ..#WSGILOCATION, "the body the port received carries the path the tool derived from the WSGI directory")
+    Do $$$AssertEquals(tSent.%Get(..#DERIVEDFIELD), ##class(OcuPilot.Area.WebApp.Location).Root() _ ..#WSGILOCATION _ "/", "the body the port received carries the path the tool resolved from the WSGI directory name")
 }
 
 /// <p>AD-10 and AD-54 inside the atomic transition: a stored create payload carrying
diff --git a/src/OcuPilot/Test/ReadTool.cls b/src/OcuPilot/Test/ReadTool.cls
index 5c5d5c3..e6edf39 100644
--- a/src/OcuPilot/Test/ReadTool.cls
+++ b/src/OcuPilot/Test/ReadTool.cls
@@ -90,8 +90,8 @@ Method TestTheRegistryListsDescriptorReadsAndInheritedKinds()
         Set tNames = tNames _ $Select(tNames = "": "", 1: ",") _ tName
         Set tName = $Order(tProductionByName(tName))
     }
-    Do $$$AssertEquals(tProduction.%Size(), 49, "the production registry holds forty-nine tools, fixtures excluded: Epic 6's thirty-seven reads, Epic 4's three shell reads and its navigation tool, and the eight write tools Stories 5.5, 5.9, 5.10, 5.11, 5.12, 5.13 and 8.1 landed")
-    Do $$$AssertEquals(tNames, "agent.definitions.read,logs.alerts.read,logs.applicationerrors.delete,logs.applicationerrors.read,logs.audit.read,logs.messages.read,osmgmt.databasedetails.read,osmgmt.databasefreespace.read,osmgmt.databases.read,osmgmt.databasevolumes.read,osmgmt.devices.read,osmgmt.locks.read,osmgmt.processdetails.read,osmgmt.processes.read,osmgmt.processes.resume,osmgmt.processes.suspend,osmgmt.systemusage.read,permissions.resources.read,permissions.roles.read,permissions.services.read,permissions.users.read,permissions.users.update,security.auditing.update,security.ldap.read,security.oauthclients.read,security.oauthresourceservers.read,security.oauthserver.read,security.oauthserverclients.read,security.oauthserverdescriptions.read,security.secrets.read,security.ssl.read,security.wallet.read,security.x509.read,shell.instance.read,shell.namespaces.read,shell.privileges.read,shell.screen.open,tasks.history.read,tasks.ondemand.read,tasks.schedule.read,tasks.schedule.resume,tasks.taskdetails.read,tasks.taskhistory.read,tasks.upcoming.read,webapp.list.create,webapp.list.read,webapp.list.update,webapp.openapi.read,webapp.restapis.read", "every descriptor-declared read of the six areas, the application error log's own class tool, the three shell reads, the navigation tool and the eight write tools -- the last thirteen class tools of their own, with no descriptor-derived read")
+    Do $$$AssertEquals(tProduction.%Size(), 50, "the production registry holds fifty tools, fixtures excluded: Epic 6's thirty-seven reads, Epic 4's three shell reads and its navigation tool, and the nine write tools Stories 5.5, 5.9, 5.10, 5.11, 5.12, 5.13, 8.1 and 8.2 landed")
+    Do $$$AssertEquals(tNames, "agent.definitions.read,logs.alerts.read,logs.applicationerrors.delete,logs.applicationerrors.read,logs.audit.read,logs.messages.read,osmgmt.databasedetails.read,osmgmt.databasefreespace.read,osmgmt.databases.read,osmgmt.databasevolumes.read,osmgmt.devices.read,osmgmt.locks.read,osmgmt.processdetails.read,osmgmt.processes.read,osmgmt.processes.resume,osmgmt.processes.suspend,osmgmt.systemusage.read,permissions.resources.read,permissions.roles.read,permissions.services.read,permissions.users.create,permissions.users.read,permissions.users.update,security.auditing.update,security.ldap.read,security.oauthclients.read,security.oauthresourceservers.read,security.oauthserver.read,security.oauthserverclients.read,security.oauthserverdescriptions.read,security.secrets.read,security.ssl.read,security.wallet.read,security.x509.read,shell.instance.read,shell.namespaces.read,shell.privileges.read,shell.screen.open,tasks.history.read,tasks.ondemand.read,tasks.schedule.read,tasks.schedule.resume,tasks.taskdetails.read,tasks.taskhistory.read,tasks.upcoming.read,webapp.list.create,webapp.list.read,webapp.list.update,webapp.openapi.read,webapp.restapis.read", "every descriptor-declared read of the six areas, the application error log's own class tool, the three shell reads, the navigation tool and the nine write tools -- the last fourteen class tools of their own, with no descriptor-derived read")
     For tPair = $ListBuild("shell.instance.read", "OcuPilot.Kernel.Shell.InstanceRead"), $ListBuild("shell.namespaces.read", "OcuPilot.Kernel.Shell.NamespacesRead"), $ListBuild("shell.privileges.read", "OcuPilot.Kernel.Shell.PrivilegesRead") {
         Set tName = $List(tPair, 1)
         If '$Data(tProductionByName(tName)) Continue
diff --git a/src/OcuPilot/Test/SurfaceCoverage.cls b/src/OcuPilot/Test/SurfaceCoverage.cls
index 6cefbdf..a88c0a6 100644
--- a/src/OcuPilot/Test/SurfaceCoverage.cls
+++ b/src/OcuPilot/Test/SurfaceCoverage.cls
@@ -92,12 +92,14 @@ XData Coverage
 <screen name="TaskRunList" class="OcuPilot.Test.ReadTool" method="TestTheRegistryListsDescriptorReadsAndInheritedKinds" corpus="named inside the production read-tool roster, which asserts every descriptor-derived read in one loop"/>
 <screen name="TaskScheduleList" class="OcuPilot.Test.Descriptor" method="TestTheTaskScheduleListValidatesAndNamesItsClassicPageInItsOwnCase"/>
 <screen name="TaskUpcomingList" class="OcuPilot.Test.ReadTool" method="TestTheRegistryListsDescriptorReadsAndInheritedKinds" corpus="named inside the production read-tool roster, which asserts every descriptor-derived read in one loop"/>
+<screen name="UserForm" class="OcuPilot.Test.UserCreate" method="TestTheFormDescriptorIsBuiltUnlistedAndCarriesTheListsOwnPairs"/>
 <screen name="UserList" class="OcuPilot.Test.Descriptor" method="TestTheUsersListValidatesAndNamesItsClassicPageInItsOwnCase"/>
 <screen name="WalletCollectionList" class="OcuPilot.Test.ReadTool" method="TestTheRegistryListsDescriptorReadsAndInheritedKinds" corpus="named inside the production read-tool roster, which asserts every descriptor-derived read in one loop"/>
 <screen name="WalletSecretList" class="OcuPilot.Test.ReadTool" method="TestTheRegistryListsDescriptorReadsAndInheritedKinds" corpus="named inside the production read-tool roster, which asserts every descriptor-derived read in one loop"/>
 <screen name="WebAppForm" class="OcuPilot.Test.WebAppCreate" method="TestTheFormDescriptorIsBuiltUnlistedAndCarriesTheListsOwnPairs"/>
 <screen name="WebAppList" class="OcuPilot.Test.Descriptor" method="TestTheWebApplicationsListValidatesAndNamesItsClassicPageInItsOwnCase"/>
 <screen name="X509CredentialList" class="OcuPilot.Test.ReadTool" method="TestTheRegistryListsDescriptorReadsAndInheritedKinds" corpus="named inside the production read-tool roster, which asserts every descriptor-derived read in one loop"/>
+<tool name="permissions.users.create" class="OcuPilot.Test.UserCreate" method="TestTheSchemaCarriesNoSecretAndThePermittedSetIsTheSets"/>
 <tool name="logs.applicationerrors.delete" class="OcuPilot.Test.ToolWrite" method="TestTheErrorDeleteToolIsAnActionWriteOverItsOwnPort"/>
 <tool name="osmgmt.processes.resume" class="OcuPilot.Test.ToolWrite" method="TestTheProcessResumeToolIsAnActionWriteOverTheScreensOwnPairs"/>
 <tool name="osmgmt.processes.suspend" class="OcuPilot.Test.ToolWrite" method="TestTheProcessSuspendToolIsAnActionWriteOverTheScreensOwnPairs"/>
diff --git a/src/OcuPilot/Test/ToolRoundTrip.cls b/src/OcuPilot/Test/ToolRoundTrip.cls
index f181633..8f38ebe 100644
--- a/src/OcuPilot/Test/ToolRoundTrip.cls
+++ b/src/OcuPilot/Test/ToolRoundTrip.cls
@@ -27,7 +27,7 @@ Parameter PROBEPREFIX = "OcuPilotProbeAgent";
 /// still requires it (AD-13, AD-34). It holds for the task resume as well, which admits no field
 /// at all and whose schema is therefore the identifying property and the agent's own words
 /// (AD-51), and for the two process writes, whose schemas are the pid and those same words.</p>
-Parameter REFUSEEMPTY = "logs.applicationerrors.read:TOOL.ARGUMENTS,logs.applicationerrors.delete:TOOL.ARGUMENTS,shell.screen.open:NAV.NOTINSTANCE,osmgmt.databasedetails.read:READ.CRITERION,osmgmt.processdetails.read:READ.CRITERION,tasks.taskdetails.read:READ.CRITERION,osmgmt.databasevolumes.read:PORT.VALIDATION,security.secrets.read:PORT.VALIDATION,webapp.openapi.read:PORT.VALIDATION,webapp.list.create:TOOL.ARGUMENTS,webapp.list.update:TOOL.ARGUMENTS,permissions.users.update:TOOL.ARGUMENTS,security.auditing.update:TOOL.ARGUMENTS,tasks.schedule.resume:TOOL.ARGUMENTS,osmgmt.processes.suspend:TOOL.ARGUMENTS,osmgmt.processes.resume:TOOL.ARGUMENTS";
+Parameter REFUSEEMPTY = "logs.applicationerrors.read:TOOL.ARGUMENTS,logs.applicationerrors.delete:TOOL.ARGUMENTS,shell.screen.open:NAV.NOTINSTANCE,osmgmt.databasedetails.read:READ.CRITERION,osmgmt.processdetails.read:READ.CRITERION,tasks.taskdetails.read:READ.CRITERION,osmgmt.databasevolumes.read:PORT.VALIDATION,security.secrets.read:PORT.VALIDATION,webapp.openapi.read:PORT.VALIDATION,webapp.list.create:TOOL.ARGUMENTS,webapp.list.update:TOOL.ARGUMENTS,permissions.users.update:TOOL.ARGUMENTS,security.auditing.update:TOOL.ARGUMENTS,tasks.schedule.resume:TOOL.ARGUMENTS,osmgmt.processes.suspend:TOOL.ARGUMENTS,osmgmt.processes.resume:TOOL.ARGUMENTS,permissions.users.create:TOOL.ARGUMENTS";
 
 Method OnBeforeOneTest() As %Status
 {
diff --git a/src/OcuPilot/Test/UserCreate.cls b/src/OcuPilot/Test/UserCreate.cls
new file mode 100644
index 0000000..d59c7c8
--- /dev/null
+++ b/src/OcuPilot/Test/UserCreate.cls
@@ -0,0 +1,461 @@
+/// <p>Pins Story 8.2's user create (AD-3, AD-6, AD-10, AD-35, AD-54, AD-55): what
+/// <class>OcuPilot.Screen.Tool.UserCreate</class> advertises, the one rule set both callers apply,
+/// the privileged-role refusal on both callers, the password's absence from every stored surface,
+/// the one body the screen and the confirm send, and the port's wrapper.</p>
+/// <p><b>What it needs from the environment:</b> OcuPilot installed and its protected state
+/// readable. Every write goes to <class>OcuPilot.Test.ProposalFixture</class>, so nothing here
+/// creates an account; the name-taken read is live, so <parameter>PROBEUSER</parameter> must be a
+/// name the instance does not hold. No arming variable is required.</p>
+Class OcuPilot.Test.UserCreate Extends %UnitTest.TestCase
+{
+
+Parameter TOOL = "OcuPilot.Screen.Tool.UserCreate";
+
+Parameter LIST = "OcuPilot.Screen.Descriptor.UserList";
+
+Parameter SET = "OcuPilot.Kernel.Proposal.Prohibited";
+
+/// <p>The shipped screen route with only its port replaced.</p>
+Parameter SCREEN = "OcuPilot.Test.UserCreateFixture";
+
+/// <p>The shipped mint and confirm with the port replaced.</p>
+Parameter MINT = "OcuPilot.Test.ProposalMint";
+
+Parameter CONFIRM = "OcuPilot.Test.ConfirmFixture";
+
+Parameter STORE = "OcuPilot.Kernel.State.Propose";
+
+Parameter LIMITS = "OcuPilot.Kernel.Agent.Limits";
+
+/// <p>The name every create here proposes; nothing on the instance holds it.</p>
+Parameter PROBEUSER = "ocupilotprobeusercreate";
+
+/// <p>A password that meets this build's policy (<code>3.255ANP</code>) and appears nowhere else,
+/// so its absence from a stored surface is a search for exactly this text.</p>
+Parameter PROBEPASSWORD = "Ocu8Probe-Secret";
+
+Method OnBeforeOneTest() As %Status
+{
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Do ##class(OcuPilot.Kernel.Proposal.Caller).Clear()
+    Quit $$$OK
+}
+
+Method OnAfterOneTest() As %Status
+{
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Do ##class(OcuPilot.Kernel.Proposal.Caller).Clear()
+    Set tSC = ##class(OcuPilot.Test.ProposalFixture).RemoveSeeded(.tRemaining)
+    Do $$$AssertStatusOK(tSC, "the probe proposals and turns are removed")
+    Do $$$AssertEquals(tRemaining, 0, "and none survives the removal")
+    Quit $$$OK
+}
+
+/// <p>AD-3 and AD-10: the schema carries no <code>Password</code> and no
+/// <code>EscalationRoles</code>, and the tool's settable set equals the prohibited set's reviewed
+/// create list for a user.</p>
+/// <p>Mutation: add <code>Comment</code> to <parameter>OcuPilot.Screen.Tool.UserCreate.PERMITTEDFIELDS</parameter>
+/// -> the set equality goes red.</p>
+Method TestTheSchemaCarriesNoSecretAndThePermittedSetIsTheSets()
+{
+    Set tSchema = $ClassMethod(..#TOOL, "InputSchema")
+    Set tProperties = tSchema.%Get("properties")
+    Do $$$AssertEquals(tProperties.%GetTypeOf("Password"), "unassigned", "the schema never offers the model a password")
+    Do $$$AssertEquals(tProperties.%GetTypeOf("EscalationRoles"), "unassigned", "nor escalation roles")
+    Do $$$AssertEquals(tProperties.%GetTypeOf("Roles"), "object", "and it offers the roles")
+    Do $$$AssertEquals(tSchema.%Get("additionalProperties"), 0, "closed to everything else")
+    Do $$$AssertEquals(..Sorted($ClassMethod(..#TOOL, "SettableFields")), ..Sorted($ClassMethod(..#SET, "PermittedCreateFields", "user")), "the settable set is the prohibited set's reviewed create list")
+    Do $$$AssertTrue($ListFind($ClassMethod(..#SET, "AlwaysProhibitedCreateFields", "user"), "EscalationRoles"), "and escalation roles are always prohibited on a create")
+    Do $$$AssertEquals($ClassMethod(..#TOOL, "WriteType"), "POST", "the create is the vendor's POST")
+    Do $$$AssertEquals($ClassMethod(..#TOOL, "Creates"), 1, "and a create (AD-54)")
+    Do $$$AssertEquals($ListToString($ClassMethod(..#TOOL, "ComposedSecrets")), "Password", "whose one composed secret is the password")
+    Do $$$AssertTrue($ListFind($ClassMethod(..#LIST, "SecretArguments"), "Password"), "which the Users list declares secret")
+}
+
+/// <p>AD-55: every field rule refuses on the screen route and at the agent's mint alike, on the
+/// field it names; the two password rules are the screen's alone, because the agent never holds a
+/// password.</p>
+/// <p>Mutation: drop the namespace leg from
+/// <method>OcuPilot.Area.Permissions.UserCreateRules.Validate</method> -> the namespace case goes
+/// red on both callers.</p>
+Method TestEveryFieldRuleRefusesOnBothCallers()
+{
+    Set tLong = $Translate($Justify("", 161), " ", "a")
+    For tCase = 1:1:8 {
+        Set tName = ..#PROBEUSER
+        Set tArgs = ..Args()
+        If tCase = 1 Set tName = "probe@user", tField = "Name", tCode = "USERNAMESHAPE"
+        If tCase = 2 Set tName = tLong, tField = "Name", tCode = "USERNAMESHAPE"
+        If tCase = 3 Set tName = "probe" _ $Char(1), tField = "Name", tCode = "USERNAMESHAPE"
+        If tCase = 4 Do tArgs.%Set("ExpirationDate", "2027-02-30", "string") Set tField = "ExpirationDate", tCode = "USEREXPIRATIONDATESHAPE"
+        If tCase = 5 Do tArgs.%Set("NameSpace", "OCUPILOTNOSUCHNS", "string") Set tField = "NameSpace", tCode = "USERNAMESPACEUNKNOWN"
+        If tCase = 6 Do tArgs.%Remove("NameSpace") Do tArgs.%Set("Routine", "^probe", "string") Set tField = "Routine", tCode = "USERROUTINENAMESPACE"
+        If tCase = 7 Do tArgs.%Set("Routine", $Translate($Justify("", 65), " ", "r"), "string") Set tField = "Routine", tCode = "USERROUTINELENGTH"
+        If tCase = 8 Do tArgs.%Set("Roles", ["OcuPilotNoSuchRole"]) Set tField = "Roles", tCode = "USERROLESUNKNOWN"
+        Set tExpected = $Parameter("OcuPilot.Api.Error", tCode)
+
+        Set tScreenArgs = ##class(%DynamicAbstractObject).%FromJSON(tArgs.%ToJSON())
+        Do tScreenArgs.%Set("Password", ..#PROBEPASSWORD, "string")
+        Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+        Set tSC = $ClassMethod(..#SCREEN, "Perform", tName, tScreenArgs, .tCreated, .tHttp, .tFault, .tViolations)
+        Do $$$AssertEquals(..CodeFor(.tViolations, tField), tExpected, "case " _ tCase _ ": the screen refuses on " _ tField)
+        Do $$$AssertEquals(##class(OcuPilot.Test.ProposalFixture).WriteCount(), 0, "case " _ tCase _ ": and nothing reaches the port")
+
+        Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+        Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+        Set tSC = ..Mint(tName, tArgs, .tProposal, .tHttp, .tFault)
+        Do $$$AssertStatusNotOK(tSC, "case " _ tCase _ ": the agent's mint refuses it too")
+        Do $$$AssertEquals(tHttp, 400, "case " _ tCase _ ": at 400")
+        Set tProblem = ..Problem(tFault)
+        Do $$$AssertTrue(tProblem [ ##class(OcuPilot.Api.Error).ReasonForViolation(tExpected), "case " _ tCase _ ": with the same sentence: " _ tProblem)
+    }
+
+    ; The two password rules, the screen's alone.
+    For tPassword = "", "ab" {
+        Set tArgs = ..Args()
+        If tPassword '= "" Do tArgs.%Set("Password", tPassword, "string")
+        Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+        Set tSC = $ClassMethod(..#SCREEN, "Perform", ..#PROBEUSER, tArgs, .tCreated, .tHttp, .tFault, .tViolations)
+        Set tCode = $Select(tPassword = "": ##class(OcuPilot.Api.Error).#USERPASSWORDREQUIRED, 1: ##class(OcuPilot.Api.Error).#USERPASSWORDPOLICY)
+        Do $$$AssertEquals(..CodeFor(.tViolations, "Password"), tCode, "a password of '" _ $Length(tPassword) _ "' characters is refused on the password field")
+        Do $$$AssertEquals(##class(OcuPilot.Test.ProposalFixture).WriteCount(), 0, "and nothing reaches the port")
+    }
+}
+
+/// <p>AD-55: the screen's Save and the agent's confirm send one body from the same arguments --
+/// the composed fields, then the password merged in after the verdict.</p>
+/// <p>Mutation: replace <code>$ClassMethod(tClass, "SettableFields")</code> in
+/// <method>OcuPilot.Area.Permissions.UserCreate.Perform</method> with a hand-typed list missing
+/// <code>FullName</code> -> the bodies differ and this goes red.</p>
+Method TestTheScreenAndTheConfirmSendOneBody()
+{
+    Set tArgs = ..Args()
+    Set tScreenArgs = ##class(%DynamicAbstractObject).%FromJSON(tArgs.%ToJSON())
+    Do tScreenArgs.%Set("Password", ..#PROBEPASSWORD, "string")
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Set tSC = $ClassMethod(..#SCREEN, "Perform", ..#PROBEUSER, tScreenArgs, .tCreated, .tHttp, .tFault, .tViolations)
+    Do $$$AssertStatusOK(tSC, "the screen accepts the create")
+    Do $$$AssertEquals(+$Get(tViolations), 0, "with no violation")
+    Set tScreenBody = ##class(OcuPilot.Test.ProposalFixture).LastWriteBody()
+    Do $$$AssertEquals(##class(OcuPilot.Test.ProposalFixture).LastType(), "POST", "at the tool's own write type")
+
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Mint(..#PROBEUSER, tArgs, .tProposal, .tHttp, .tFault)
+    Do $$$AssertStatusOK(tSC, "the agent mints the same create")
+    If $$$ISERR(tSC) Quit
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmWrite("{}")
+    Set tSC = ..Confirm(tProposal.%Get("proposalId"), .tHttp, .tCode, .tResult, ..Secrets())
+    Do $$$AssertStatusOK(tSC, "and its confirm, supplied the password on the card, applies it")
+    Set tAgentBody = ##class(OcuPilot.Test.ProposalFixture).LastWriteBody()
+
+    Do $$$AssertNotEquals(tScreenBody, "", "a body reached the port from the screen")
+    Do $$$AssertEquals(tScreenBody, tAgentBody, "the screen's Save and the agent's confirm send the identical body")
+    Set tBody = ##class(%DynamicAbstractObject).%FromJSON(tScreenBody)
+    Do $$$AssertEquals(tBody.%Get("FullName"), tArgs.%Get("FullName"), "carrying the full name")
+    Do $$$AssertEquals(tBody.%GetTypeOf("Roles"), "array", "the roles")
+    Do $$$AssertEquals(tBody.%Get("Password"), ..#PROBEPASSWORD, "and the password, which reaches the port and nothing else")
+    Do $$$AssertEquals(tBody.%GetTypeOf("Name"), "unassigned", "and never the name, which is the vendor's query parameter")
+}
+
+/// <p>AD-10 on both callers: a role that grants <code>%All</code> by name, or an administrative
+/// resource it carries, is refused -- on the screen as the Roles violation carrying the server's
+/// sentence, at the agent's confirm inside the transition -- and neither reaches the port. An
+/// ordinary role is not refused. A seeded row carrying escalation roles is refused inside the
+/// transition too.</p>
+/// <p>Mutation: remove the <code>user</code> step from
+/// <method>OcuPilot.Kernel.Proposal.Prohibited.Created</method> -> every privileged leg goes red on
+/// both callers.</p>
+Method TestAPrivilegedRoleIsRefusedOnBothCallers()
+{
+    For tRole = "%All", "%Manager" {
+        Set tArgs = ..Args()
+        Do tArgs.%Set("Roles", [(tRole)])
+        Set tScreenArgs = ##class(%DynamicAbstractObject).%FromJSON(tArgs.%ToJSON())
+        Do tScreenArgs.%Set("Password", ..#PROBEPASSWORD, "string")
+        Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+        Set tSC = $ClassMethod(..#SCREEN, "Perform", ..#PROBEUSER, tScreenArgs, .tCreated, .tHttp, .tFault, .tViolations)
+        Do $$$AssertEquals(+$Get(tViolations), 1, tRole _ ": the screen answers one violation")
+        Do $$$AssertEquals(..CodeFor(.tViolations, "Roles"), $Parameter(..#SET, "PRIVILEGEGRANT"), tRole _ ": on the Roles field, as a privilege grant")
+        Do $$$AssertEquals($ListGet($Get(tViolations(1)), 3), ##class(OcuPilot.Api.Error).#REASONUSERROLESPRIVILEGED, tRole _ ": carrying the server's sentence")
+        Do $$$AssertEquals(##class(OcuPilot.Test.ProposalFixture).WriteCount(), 0, tRole _ ": and nothing reaches the port")
+
+        Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+        Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+        Set tSC = ..Mint(..#PROBEUSER, tArgs, .tProposal, .tHttp, .tFault)
+        Do $$$AssertStatusOK(tSC, tRole _ ": the agent's mint proposes it")
+        If $$$ISERR(tSC) Continue
+        Do ##class(OcuPilot.Test.ProposalFixture).ArmWrite("{}")
+        Set tSC = ..Confirm(tProposal.%Get("proposalId"), .tHttp, .tCode, .tResult, ..Secrets())
+        Do $$$AssertStatusNotOK(tSC, tRole _ ": and its confirm refuses it")
+        Do $$$AssertEquals(tHttp, 403, tRole _ ": at 403")
+        Do $$$AssertEquals(tCode, $Parameter(..#SET, "PRIVILEGEGRANT"), tRole _ ": on the privilege-grant predicate")
+        Do $$$AssertEquals(##class(OcuPilot.Test.ProposalFixture).WriteCount(), 0, tRole _ ": and nothing reaches the port")
+    }
+
+    ; An ordinary role grants nothing AD-10 names.
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Set tSC = $ClassMethod(..#SET, "Prohibits", $Parameter(..#TOOL, "TOOLNAME"), ..TargetRef(), {"Roles": ["%Developer"]}.%ToJSON(), "[]", .tProhibits, .tCode)
+    Do $$$AssertStatusOK(tSC, "the set answers for an ordinary role")
+    Do $$$AssertEquals(tProhibits, 0, "and does not refuse it")
+    Do $$$AssertEquals(##class(OcuPilot.Kernel.Proposal.Prohibited).RoleGrantsAdministrativePrivilege("%Developer"), 0, "the picker leaves an ordinary role available")
+    Do $$$AssertEquals(##class(OcuPilot.Kernel.Proposal.Prohibited).RoleGrantsAdministrativePrivilege("%Manager"), 1, "and pre-marks one that carries an administrative resource")
+
+    ; A seeded row the shipped mint cannot produce: escalation roles, refused inside the transition.
+    Set tSC = ##class(OcuPilot.Test.ProposalFixture).SeedTurn($Username, "completed", .tTurnKey)
+    Do $$$AssertStatusOK(tSC, "a probe turn is seeded")
+    If $$$ISERR(tSC) Quit
+    Set tFpSC = ##class(OcuPilot.Kernel.Proposal.Fingerprint).Of(##class(OcuPilot.Kernel.Proposal.Mint).AbsenceState(0), "", .tFingerprint)
+    Do $$$AssertStatusOK(tFpSC, "the absence digest is taken")
+    Set tPayload = {"FullName": "probe", "EscalationRoles": ["%Developer"]}
+    Kill tValues
+    Set tValues("user") = $Username
+    Set tValues("turnKey") = tTurnKey
+    Set tValues("toolName") = $Parameter(..#TOOL, "TOOLNAME")
+    Set tValues("targetRef") = ..TargetRef()
+    Set tValues("fingerprint") = tFingerprint
+    Set tValues("arguments") = tPayload.%ToJSON()
+    Set tValues("payload") = tPayload.%ToJSON()
+    Set tValues("diff") = "[]"
+    Set tValues("unchangedCount") = 0
+    Set tSC = ##class(OcuPilot.Test.ProposalFixture).SeedProposal(.tValues, .tKey, .tToken)
+    Do $$$AssertStatusOK(tSC, "a create proposal carrying escalation roles is seeded")
+    If $$$ISERR(tSC) Quit
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Confirm(tKey, .tHttp, .tCode, .tResult, ..Secrets())
+    Do $$$AssertEquals(tHttp, 403, "the seeded row is refused at 403")
+    Do $$$AssertEquals(tCode, $Parameter(..#SET, "PRIVILEGEGRANT"), "as a privilege grant")
+    Do $$$AssertEquals(##class(OcuPilot.Test.ProposalFixture).WriteCount(), 0, "with no write")
+}
+
+/// <p>AD-6 and AD-35: a password among the mint's arguments is refused, and a minted create's
+/// stored arguments, payload and diff carry no password -- only one <code>Password</code> diff row,
+/// empty on both sides -- and its ledger row names every sent field except the password.</p>
+/// <p>Mutation: store <code>Password</code> in
+/// <method>OcuPilot.Kernel.Proposal.Mint.StoredArguments</method> (set it on the stored text) -> the
+/// stored-arguments assertion goes red.</p>
+Method TestThePasswordIsAbsentFromEveryStoredSurface()
+{
+    Set tArgs = ..Args()
+    Do tArgs.%Set("Password", ..#PROBEPASSWORD, "string")
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Mint(..#PROBEUSER, tArgs, .tProposal, .tHttp, .tFault)
+    Do $$$AssertStatusNotOK(tSC, "a password from the agent is refused")
+    Do $$$AssertEquals(tHttp, 400, "at 400")
+    Do $$$AssertTrue(..Problem(tFault) [ "'Password' is a secret field", "as a secret the agent never supplies")
+
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Mint(..#PROBEUSER, ..Args(), .tProposal, .tHttp, .tFault, .tTurnKey)
+    Do $$$AssertStatusOK(tSC, "a create with no password is minted")
+    If $$$ISERR(tSC) Quit
+    Do $$$AssertEquals(tProposal.%Get("unchangedCount"), 0, "with nothing unchanged")
+    Set tChanged = tProposal.%Get("changed")
+    Set tLast = tChanged.%Get(tChanged.%Size() - 1)
+    Do $$$AssertEquals(tLast.%Get("field"), "Password", "and one Password row, last")
+    Do $$$AssertEquals(tLast.%Get("before") _ "|" _ tLast.%Get("after"), "|", "empty on both sides")
+
+    Set tSC = $ClassMethod(..#STORE, "GuardedByKey", tProposal.%Get("proposalId"), .tFound, .tValues)
+    Do $$$AssertStatusOK(tSC, "the stored row reads back")
+    If 'tFound Quit
+    Set tStoredArgs = ##class(%DynamicAbstractObject).%FromJSON($Get(tValues("arguments")))
+    Set tStoredPayload = ##class(%DynamicAbstractObject).%FromJSON($Get(tValues("payload")))
+    Do $$$AssertEquals(tStoredArgs.%GetTypeOf("Password"), "unassigned", "the stored arguments carry no password")
+    Do $$$AssertEquals(tStoredPayload.%GetTypeOf("Password"), "unassigned", "nor the stored payload")
+
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmWrite("{}")
+    Set tSC = ..Confirm(tProposal.%Get("proposalId"), .tHttp, .tCode, .tResult, ..Secrets())
+    Do $$$AssertStatusOK(tSC, "the confirm supplies the password and applies the create")
+    If $$$ISERR(tSC) Quit
+    Set tStored = $Get(tValues("arguments")) _ $Get(tValues("payload")) _ $Get(tValues("diff"))
+    Set tSC = $ClassMethod(..#STORE, "GuardedByKey", tProposal.%Get("proposalId"), .tFound, .tAfter)
+    Set tStored = tStored _ $Get(tAfter("arguments")) _ $Get(tAfter("payload")) _ $Get(tAfter("diff"))
+    Do $$$AssertTrue('(tStored [ ..#PROBEPASSWORD), "no stored surface carries the password, before or after the confirm")
+    Do $$$AssertEquals(##class(OcuPilot.Test.AuditMarker).WriteRow(tTurnKey, .tRow), 1, "the turn holds one write-kind ledger row")
+    Set tFields = $Get(tRow("Fields"))
+    Do $$$AssertTrue(tFields [ "FullName", "which names the sent fields: " _ tFields)
+    Do $$$AssertTrue('$ListFind($ListFromString(tFields), "Password"), "and never the password: " _ tFields)
+    Do $$$AssertTrue('(tFields [ ..#PROBEPASSWORD), "or its value")
+}
+
+/// <p>AD-5, AD-15 and AD-14: a confirmed create is marked, answers the <code>created</code> action,
+/// and the body the port receives becomes the vendor's wrapper -- the account's fields under
+/// <code>User</code> and the password beside it.</p>
+/// <p>Mutation: remove <code>Security.User/POST</code> from
+/// <parameter>OcuPilot.Port.AdminPort.WRAPPEDTYPES</parameter> -> the wrapper assertions go red.</p>
+Method TestAConfirmedCreateIsMarkedAndItsBodyIsWrapped()
+{
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Mint(..#PROBEUSER, ..Args(), .tProposal, .tHttp, .tFault, .tTurnKey)
+    Do $$$AssertStatusOK(tSC, "the create is minted")
+    If $$$ISERR(tSC) Quit
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmWrite("{}")
+    Set tSC = ..Confirm(tProposal.%Get("proposalId"), .tHttp, .tCode, .tResult, ..Secrets())
+    Do $$$AssertStatusOK(tSC, "and confirmed")
+    If $$$ISERR(tSC) Quit
+    Do $$$AssertEquals($Select($IsObject($Get(tResult)): tResult.%Get("action"), 1: ""), "created", "the answer carries the created action")
+    Do $$$AssertEquals(##class(OcuPilot.Test.AuditMarker).WriteRow(tTurnKey, .tRow), 1, "one write-kind ledger row")
+    Do $$$AssertEquals($Get(tRow("AuditMarked")), $Parameter("OcuPilot.Kernel.State.Ledger", "MARKEDYES"), "and the agent marker landed on it")
+
+    Set tSent = ##class(%DynamicAbstractObject).%FromJSON(##class(OcuPilot.Test.ProposalFixture).LastWriteBody())
+    Set tWire = ##class(OcuPilot.Port.AdminPort).Wrapped("Security.User", "POST", tSent)
+    Do $$$AssertEquals(tWire.%GetTypeOf("User"), "object", "the port wraps the account's fields under User")
+    If tWire.%GetTypeOf("User") '= "object" Quit
+    Do $$$AssertEquals(tWire.%Get("User").%Get("FullName"), ..Args().%Get("FullName"), "carrying the full name")
+    Do $$$AssertEquals(tWire.%Get("User").%GetTypeOf("Roles"), "array", "and the roles, as an array")
+    Do $$$AssertEquals(tWire.%Get("User").%GetTypeOf("Password"), "unassigned", "with the password outside the wrapper")
+    Do $$$AssertEquals(tWire.%Get("Password"), ..#PROBEPASSWORD, "beside it")
+    ; A missing outer key stays missing, so the vendor's own refusal answers for it.
+    Set tBare = ##class(OcuPilot.Port.AdminPort).Wrapped("Security.User", "POST", {"FullName": "x"})
+    Do $$$AssertEquals(tBare.%GetTypeOf("Password"), "unassigned", "a password the body lacks stays missing")
+    Do $$$AssertEquals(##class(OcuPilot.Port.AdminPort).WrapperKey("Security.User", "PUT"), "", "and the update's PUT is not wrapped")
+}
+
+/// <p>The User form is built, unlisted, gated by the list's own pairs and secret-typed on the
+/// password in its context; the list is write-capable and declares the password secret (AD-5,
+/// AD-8, AD-24).</p>
+/// <p>Mutation: give the form a non-zero <code>sideBarPosition</code> -> the unlisted assertion goes
+/// red.</p>
+Method TestTheFormDescriptorIsBuiltUnlistedAndCarriesTheListsOwnPairs()
+{
+    Set tForm = "OcuPilot.Screen.Descriptor.UserForm"
+    Do $$$AssertEquals($ClassMethod(tForm, "Route"), $ClassMethod(..#LIST, "Route") _ "/edit", "the form's route is the list's own with the editor suffix")
+    Do $$$AssertEquals(''$ClassMethod(tForm, "IsBuilt"), 1, "it is built")
+    Do $$$AssertEquals(+$ClassMethod(tForm, "SideBarPosition"), 0, "and unlisted")
+    Do $$$AssertEquals($ClassMethod(tForm, "Archetype"), "form-page", "it is a form page")
+    Do $$$AssertEquals($ClassMethod(tForm, "PrimaryEntityType"), "user", "over the user entity type")
+    Do $$$AssertEquals($ClassMethod(tForm, "ToolIdentifier"), "permissions.userform", "with its own tool identifier")
+    Do $$$AssertEquals($IsObject($ClassMethod(tForm, "Read")), 0, "and no declared read")
+    Do $$$AssertEquals($ListToString($ClassMethod(tForm, "PrivilegePairs")), $ListToString($ClassMethod(..#LIST, "PrivilegePairs")), "the form requires exactly what the list requires")
+    Do $$$AssertEquals($Parameter("OcuPilot.Area.Permissions.UserCreate", "DESCRIPTORCLASS"), tForm, "and the create route gates on the form's declaration")
+    Set tSC = $ClassMethod(tForm, "DeclarationJson", .tFormDeclaration)
+    Do $$$AssertStatusOK(tSC, "the form's declaration parses")
+    If $$$ISOK(tSC) Do $$$AssertEquals(tFormDeclaration.%Get("context").%Get("secretFields").%ToJSON(), "[""Password""]", "its context never carries the password being typed")
+    Set tSC = $ClassMethod(..#LIST, "DeclarationJson", .tDeclaration)
+    Do $$$AssertStatusOK(tSC, "the list's declaration parses")
+    If $$$ISERR(tSC) Quit
+    Do $$$AssertEquals(tDeclaration.%Get("primaryAction").%Get("id"), "create", "the list declares Create as its primary action")
+    Do $$$AssertEquals(tDeclaration.%Get("table").%Get("emptyNextKey"), "", "so its empty state names no next step")
+    Do $$$AssertEquals(tDeclaration.%Get("table").%Get("emptyAgentKey"), "userListEmptyAgent", "and invites the agent instead")
+}
+
+/// <p>AD-3, AD-6: a <code>secretArguments</code> name qualifies when it is a top-level
+/// <code>secret</code> row of the screen's write tools -- the create's authored
+/// <code>Password</code> -- while a name no source carries, and a nested secret path, are still
+/// refused, and a settable field still qualifies. The twin of
+/// <file>ui/tools/screen-mirror.test.mjs</file>'s secret-row test.</p>
+/// <p>Mutation: drop the <method>OcuPilot.Screen.Registry.SecretRowNames</method> clause from
+/// <method>OcuPilot.Screen.Registry.ConfirmChannelProblem</method> -> the Users list's own
+/// declaration is refused and this goes red.</p>
+Method TestTheConfirmChannelAdmitsATopLevelSecretRowAndNothingElseNew()
+{
+    Set tSC = $ClassMethod(..#LIST, "DeclarationJson", .tUsers)
+    Do $$$AssertStatusOK(tSC, "the Users list's declaration parses")
+    If $$$ISERR(tSC) Quit
+    Do $$$AssertEquals(##class(OcuPilot.Screen.Registry).ConfirmChannelProblem(tUsers), "", "its authored Password qualifies")
+    Do tUsers.%Set("secretArguments", ["Pasword"])
+    Do $$$AssertTrue(##class(OcuPilot.Screen.Registry).ConfirmChannelProblem(tUsers) [ "'Pasword'", "a name no source carries is still refused")
+    Set tSC = ##class(OcuPilot.Screen.Descriptor.WebAppList).DeclarationJson(.tApps)
+    Do $$$AssertStatusOK(tSC, "the Web applications list's declaration parses")
+    If $$$ISERR(tSC) Quit
+    Do tApps.%Set("secretArguments", ["Timeout"])
+    Do $$$AssertEquals(##class(OcuPilot.Screen.Registry).ConfirmChannelProblem(tApps), "", "a settable field still qualifies")
+    Do tApps.%Set("secretArguments", ["MatchRoles[].MatchRole"])
+    Do $$$AssertNotEquals(##class(OcuPilot.Screen.Registry).ConfirmChannelProblem(tApps), "", "and a nested secret path does not")
+}
+
+/// <p>AD-39: every <code>USER.*</code> field code carries a written sentence, and the form's
+/// bootstrap rules carry exactly those sentences.</p>
+/// <p>Mutation: add a code to <method>OcuPilot.Api.Error.UserViolationCodes</method> with no row in
+/// <method>OcuPilot.Api.Error.ReasonForViolation</method> -> this goes red naming it.</p>
+Method TestEveryUserFieldCodeCarriesItsOwnSentence()
+{
+    Set tCodes = ##class(OcuPilot.Api.Error).UserViolationCodes()
+    Do $$$AssertTrue($ListLength(tCodes) > 0, "the story publishes user field codes")
+    For tI = 1:1:$ListLength(tCodes) {
+        Do $$$AssertNotEquals(##class(OcuPilot.Api.Error).ReasonForViolation($List(tCodes, tI)), "", $List(tCodes, tI) _ " carries a written sentence")
+    }
+    Set tRules = ##class(OcuPilot.Area.Permissions.UserCreateRules).Rules()
+    Do $$$AssertEquals(tRules.%Size(), $ListLength(tCodes), "and the bootstrap publishes one rule per code")
+    Set tIterator = tRules.%GetIterator()
+    While tIterator.%GetNext(.tIndex, .tRule) {
+        Do $$$AssertEquals(tRule.%Get("reason"), ##class(OcuPilot.Api.Error).ReasonForViolation(tRule.%Get("code")), "the rule on " _ tRule.%Get("field") _ " carries the server's sentence")
+    }
+}
+
+/// <p>The arguments every create here proposes.</p>
+ClassMethod Args() As %DynamicObject
+{
+    Set tArgs = {}
+    Do tArgs.%Set("FullName", "OcuPilot create probe", "string")
+    Do tArgs.%Set("ExpirationDate", "2099-12-31", "string")
+    Do tArgs.%Set("NameSpace", $Namespace, "string")
+    Do tArgs.%Set("Roles", ["%Developer"])
+    Quit tArgs
+}
+
+/// <p>The confirm body: the password, the one key the channel is open to.</p>
+ClassMethod Secrets() As %DynamicObject
+{
+    Set tSecrets = {}
+    Do tSecrets.%Set("Password", ..#PROBEPASSWORD, "string")
+    Quit tSecrets
+}
+
+/// <p>The canonical reference of <parameter>PROBEUSER</parameter>.</p>
+ClassMethod TargetRef() As %String
+{
+    Do ##class(OcuPilot.Kernel.EntityRef).Key("user", "instance", ..#PROBEUSER, .tRef)
+    Quit $Get(tRef)
+}
+
+/// <p>Mint one create of <var>pName</var> through the fixture mint, as
+/// <method>OcuPilot.Screen.Tool.Write.View</method> would.</p>
+Method Mint(pName As %String, pArgs As %DynamicObject, Output pProposal As %DynamicObject, Output pHttpStatus As %Integer, Output pFault As %DynamicObject, Output pTurnKey As %String) As %Status
+{
+    Set pTurnKey = ""
+    Set tSC = ##class(OcuPilot.Test.ProposalFixture).SeedTurn($Username, "completed", .pTurnKey)
+    If $$$ISERR(tSC) Quit tSC
+    Quit $ClassMethod(..#MINT, "Mint", ..#LIST, $Parameter(..#TOOL, "TOOLNAME"), $Username, "", pTurnKey, pArgs, ..#LIMITS, .pProposal, .pHttpStatus, .pFault, $ClassMethod(..#TOOL, "Endpoint"), $ClassMethod(..#TOOL, "IdParam"), pName, $ClassMethod(..#TOOL, "IdArgument"), $ClassMethod(..#TOOL, "SettableFields"), ..#TOOL)
+}
+
+/// <p>Confirm <var>pKey</var> through the shipped transition, supplying <var>pSecrets</var>.</p>
+Method Confirm(pKey As %String, Output pHttp As %Integer, Output pCode As %String, Output pResult As %DynamicObject, pSecrets As %DynamicObject = "") As %Status
+{
+    Set pResult = ""
+    Quit $ClassMethod(..#CONFIRM, "Confirm", pKey, pSecrets, $Username, .pResult, .pHttp, .pCode, .tDetail)
+}
+
+/// <p>The problem a 400 <code>TOOL.ARGUMENTS</code> refusal carries, or <code>""</code>.</p>
+ClassMethod Problem(pFault As %DynamicObject) As %String
+{
+    If '$IsObject(pFault) Quit ""
+    If '$IsObject(pFault.%Get("detail")) Quit ""
+    Quit pFault.%Get("detail").%Get("problem")
+}
+
+/// <p>The code <var>pViolations</var> carries for <var>pField</var>, or <code>""</code>.</p>
+ClassMethod CodeFor(ByRef pViolations, pField As %String) As %String
+{
+    Set tCode = ""
+    For tI = 1:1:+$Get(pViolations) {
+        If $List(pViolations(tI), 1) = pField Set tCode = $List(pViolations(tI), 2) Quit
+    }
+    Quit tCode
+}
+
+/// <p><var>pList</var> sorted, as a comma-separated string.</p>
+ClassMethod Sorted(pList As %List) As %String
+{
+    Kill tSeen
+    For tI = 1:1:$ListLength(pList) Set tSeen($List(pList, tI)) = ""
+    Set tOut = ""
+    Set tKey = ""
+    For {
+        Set tKey = $Order(tSeen(tKey))
+        If tKey = "" Quit
+        Set tOut = tOut _ $Select(tOut = "": "", 1: ",") _ tKey
+    }
+    Quit tOut
+}
+
+}
diff --git a/src/OcuPilot/Test/UserCreateFixture.cls b/src/OcuPilot/Test/UserCreateFixture.cls
new file mode 100644
index 0000000..e6f88a1
--- /dev/null
+++ b/src/OcuPilot/Test/UserCreateFixture.cls
@@ -0,0 +1,14 @@
+/// <p>Test-only <class>OcuPilot.Area.Permissions.UserCreate</class> subclass with one replaced
+/// seam: <method>PortClass</method> answers <class>OcuPilot.Test.ProposalFixture</class>, so
+/// <method>Perform</method> runs for real and the write it would send is recorded instead of
+/// creating an account. The name-taken read is not this seam and still reads the live instance.</p>
+/// <p><b>Not a <class>%UnitTest.TestCase</class></b>: it holds no assertions and runs no tests.</p>
+Class OcuPilot.Test.UserCreateFixture Extends OcuPilot.Area.Permissions.UserCreate
+{
+
+ClassMethod PortClass() As %String
+{
+    Quit "OcuPilot.Test.ProposalFixture"
+}
+
+}
diff --git a/src/OcuPilot/Test/UserCreateWire.cls b/src/OcuPilot/Test/UserCreateWire.cls
new file mode 100644
index 0000000..34d016b
--- /dev/null
+++ b/src/OcuPilot/Test/UserCreateWire.cls
@@ -0,0 +1,256 @@
+/// <p>The three user-create routes over the wire (AD-12, AD-35, AD-39, AD-55): the form's bootstrap
+/// read, the name look-up and the create, and the port's wrapper against the vendor itself.</p>
+/// <p><b>What it needs from the environment:</b> OcuPilot installed and serving, an account holding
+/// the screen's own pairs, and <parameter>ARMINGVARIABLE</parameter> reading <code>1</code> --
+/// because the create leg creates a real account. <method>OnBeforeAllTests</method> refuses anywhere
+/// it does not, and refuses to adopt a probe name the instance already holds;
+/// <method>OnAfterOneTest</method> deletes the probe names by exact name, never by prefix.</p>
+Class OcuPilot.Test.UserCreateWire Extends %UnitTest.TestCase
+{
+
+/// <p>The environment variable that arms this class, set by <file>scripts/ci-throwaway.sh</file>
+/// alone.</p>
+Parameter ARMINGVARIABLE = "OCUPILOT_ALLOW_PRINCIPALS";
+
+Parameter FORMPATH = "/users/form";
+
+Parameter NAMEPATH = "/users/name";
+
+Parameter CREATEPATH = "/users";
+
+/// <p>The account the create leg makes, and the one the port legs name and never create.</p>
+Parameter PROBEUSER = "ocupilotprobeuserwire";
+
+Parameter PORTPROBE = "ocupilotprobeuserport";
+
+/// <p>A password that meets this build's policy and appears nowhere else.</p>
+Parameter PROBEPASSWORD = "Ocu8Wire-Secret";
+
+Method OnBeforeAllTests() As %Status
+{
+    If $System.Util.GetEnviron(..#ARMINGVARIABLE) '= 1 {
+        Quit $$$ERROR($$$GeneralError, "OcuPilot.Test.UserCreateWire creates and deletes an account on this instance and runs on a throwaway container only. " _ ..#ARMINGVARIABLE _ " does not read 1 here, so nothing was created. Bring one up with scripts/ci-throwaway.sh, which sets it.")
+    }
+    If ..Exists(..#PROBEUSER) || ..Exists(..#PORTPROBE) {
+        Quit $$$ERROR($$$GeneralError, "a probe account of OcuPilot.Test.UserCreateWire already exists on this instance, and this class deletes what it creates -- it will not adopt one it did not make.")
+    }
+    Quit $$$OK
+}
+
+Method OnAfterOneTest() As %Status
+{
+    Do ..Remove(..#PROBEUSER)
+    Do ..Remove(..#PORTPROBE)
+    Do $$$AssertEquals(..Exists(..#PROBEUSER) + ..Exists(..#PORTPROBE), 0, "the probe accounts are gone")
+    Quit $$$OK
+}
+
+/// <p><code>GET /users/form</code> publishes the rules with the server's own sentences, and the
+/// roles with the privileged ones pre-marked by the sentence the refusal carries.</p>
+/// <p>Mutation: make <method>OcuPilot.Kernel.Proposal.Prohibited.RoleGrantsAdministrativePrivilege</method>
+/// answer 0 -> the pre-mark assertions go red.</p>
+Method TestTheFormReadPublishesTheRulesAndMarksThePrivilegedRoles()
+{
+    Set tSC = ##class(OcuPilot.Test.Http).MakeRequest("GET", ..#FORMPATH, .tStatus, .tBody)
+    Do $$$AssertStatusOK(tSC, "the form read answers")
+    Do $$$AssertEquals(tStatus, 200, "at 200")
+    If '$IsObject($Get(tBody)) Quit
+    Do $$$AssertEquals(tBody.%Get("rolesReason"), ##class(OcuPilot.Api.Error).#REASONUSERROLESPRIVILEGED, "it ships the Roles sentence")
+    Do $$$AssertTrue(tBody.%Get("requiredFields").%Size() = 2, "and the two required fields")
+    Set tIterator = tBody.%Get("rules").%GetIterator()
+    Set tRules = 0
+    While tIterator.%GetNext(.tIndex, .tRule) {
+        Set tRules = tRules + 1
+        Do $$$AssertEquals(tRule.%Get("reason"), ##class(OcuPilot.Api.Error).ReasonForViolation(tRule.%Get("code")), "the rule on " _ tRule.%Get("field") _ " carries the server's sentence")
+    }
+    Do $$$AssertTrue(tRules > 0, "it publishes rules")
+    Kill tMarked
+    Set tIterator = tBody.%Get("roles").%GetIterator()
+    While tIterator.%GetNext(.tIndex, .tRole) {
+        Set tMarked(tRole.%Get("name")) = tRole.%Get("privileged")
+    }
+    Do $$$AssertEquals($Get(tMarked("%All"), "absent"), 1, "%All is pre-marked")
+    Do $$$AssertEquals($Get(tMarked("%Manager"), "absent"), 1, "and %Manager, which carries an administrative resource")
+    Do $$$AssertEquals($Get(tMarked("%Developer"), "absent"), 0, "and an ordinary role is not")
+}
+
+/// <p><code>POST /users</code> creates the account with the sent fields, the name look-up then
+/// reads it as taken, and neither the Users read nor the vendor's <code>GET</code> carries the
+/// password.</p>
+/// <p>Mutation: remove <code>Security.User/POST</code> from
+/// <parameter>OcuPilot.Port.AdminPort.WRAPPEDTYPES</parameter> -> the vendor refuses the flat body
+/// and the create leg goes red.</p>
+Method TestTheCreateMakesTheAccountAndNoReadCarriesThePassword()
+{
+    Set tSC = ##class(OcuPilot.Test.Http).MakeRequest("GET", ..#NAMEPATH _ "?name=" _ ..#PROBEUSER, .tStatus, .tBody)
+    Do $$$AssertEquals(tStatus, 200, "the name look-up answers")
+    If $IsObject($Get(tBody)) Do $$$AssertEquals(tBody.%Get("available"), 1, "and the probe name is free")
+
+    Set tRequest = {"Name": (..#PROBEUSER), "Password": (..#PROBEPASSWORD), "FullName": "OcuPilot wire probe", "NameSpace": ($Namespace), "Roles": ["%Developer"]}
+    Set tSC = ##class(OcuPilot.Test.Http).MakeRequest("POST", ..#CREATEPATH, .tStatus, .tBody, tRequest.%ToJSON(), "application/json")
+    Do $$$AssertStatusOK(tSC, "the create answers")
+    Do $$$AssertEquals(tStatus, 201, "at 201")
+    If tStatus '= 201 Quit
+    Do $$$AssertEquals(..Exists(..#PROBEUSER), 1, "and the instance holds the account")
+    Do $$$AssertEquals(..Property(..#PROBEUSER, "FullName"), "OcuPilot wire probe", "with the full name sent")
+    Do $$$AssertTrue(("," _ ..Property(..#PROBEUSER, "Roles") _ ",") [ ",%Developer,", "and the role sent")
+    Do $$$AssertTrue('(tBody.%ToJSON() [ ..#PROBEPASSWORD), "the create's own answer does not echo the password")
+
+    Set tSC = ##class(OcuPilot.Test.Http).MakeRequest("GET", ..#NAMEPATH _ "?name=" _ $ZConvert(..#PROBEUSER, "U"), .tStatus, .tBody)
+    If $IsObject($Get(tBody)) {
+        Do $$$AssertEquals(tBody.%Get("available"), 0, "the name is taken now, in any case")
+        Do $$$AssertEquals(tBody.%Get("reason"), ##class(OcuPilot.Api.Error).ReasonForViolation(##class(OcuPilot.Api.Error).#USERNAMETAKEN), "with the server's sentence")
+    }
+
+    Set tSC = ##class(OcuPilot.Test.Http).MakeRequest("GET", "/screens/permissions.users/read?maxRows=1000", .tStatus, .tBody)
+    Do $$$AssertEquals(tStatus, 200, "the Users read answers")
+    If $IsObject($Get(tBody)) {
+        Set tText = tBody.%ToJSON()
+        Do $$$AssertTrue(tText [ ..#PROBEUSER, "and lists the new account")
+        Do $$$AssertTrue('(tText [ ..#PROBEPASSWORD), "and carries no password value")
+        Do $$$AssertTrue('(tText [ """Password"":"), "and no Password key")
+    }
+
+    Kill tQuery
+    Set tQuery("name") = ..#PROBEUSER
+    Set tSC = ##class(OcuPilot.Port.AdminPort).Invoke("Security.User", "GET", .tQuery, "", .tRead, .tHttp, .tFault)
+    Do $$$AssertStatusOK(tSC, "the vendor's GET answers")
+    If $IsObject($Get(tRead)) {
+        Do $$$AssertEquals(tRead.%GetTypeOf("Password"), "unassigned", "with no Password key")
+        Do $$$AssertTrue('(tRead.%ToJSON() [ ..#PROBEPASSWORD), "and no password value")
+    }
+}
+
+/// <p>A vendor refusal of the wrapped body lands on the flat field the caller sent: the wrapper's
+/// <code>User.</code> segment is stripped, and a missing outer password is named as itself. Nothing
+/// is created -- the vendor refuses before it runs.</p>
+/// <p>Mutation: drop the strip from <method>OcuPilot.Port.AdminPort.Fail</method> -> the first
+/// field reads <code>User.OcuPilotNotAField</code> and this goes red.</p>
+Method TestAVendorRefusalOfTheWrappedBodyLandsOnTheFlatField()
+{
+    Kill tQuery
+    Set tQuery("name") = ..#PORTPROBE
+    Set tSC = ##class(OcuPilot.Port.AdminPort).Invoke("Security.User", "POST", .tQuery, {"Password": (..#PROBEPASSWORD), "OcuPilotNotAField": "x"}, .tAnswer, .tHttp, .tFault)
+    Do $$$AssertStatusNotOK(tSC, "the vendor refuses a field its template does not declare")
+    Do $$$AssertEquals(tHttp, 400, "at 400")
+    Do $$$AssertEquals(..FirstViolationField(tFault), "OcuPilotNotAField", "on the flat field the caller sent")
+
+    Set tSC = ##class(OcuPilot.Port.AdminPort).Invoke("Security.User", "POST", .tQuery, {"FullName": "x"}, .tAnswer, .tHttp, .tFault)
+    Do $$$AssertStatusNotOK(tSC, "and refuses a body carrying no password")
+    Do $$$AssertEquals(..FirstViolationField(tFault), "Password", "on the password")
+    Do $$$AssertEquals(..Exists(..#PORTPROBE), 0, "and neither refusal created an account")
+}
+
+/// <p>AD-10 over the wire: a role that grants <code>%All</code> only through its nested roles is
+/// refused on the Roles field, and no account is created.</p>
+/// <p>Mutation: make <method>OcuPilot.Kernel.Proposal.Prohibited.RoleEscalates</method> ignore
+/// the recursed role set -> the nested role passes and this goes red.</p>
+Method TestARoleReachingAllThroughItsNestedRolesIsRefused()
+{
+    Set tSC = ##class(OcuPilot.Test.ProposalFixture).EnsureAllRole(.tCreated)
+    Do $$$AssertStatusOK(tSC, "a probe role granting %All through a nested role is created")
+    If $$$ISERR(tSC) Quit
+    Try {
+        Set tRole = $Parameter("OcuPilot.Test.ProposalFixture", "USERALLROLE")
+        Set tRequest = {"Name": (..#PROBEUSER), "Password": (..#PROBEPASSWORD), "Roles": [(tRole)]}
+        Set tSC = ##class(OcuPilot.Test.Http).MakeRequest("POST", ..#CREATEPATH, .tStatus, .tBody, tRequest.%ToJSON(), "application/json")
+        Do $$$AssertEquals(tStatus, 422, "the create is refused at 422")
+        If $IsObject($Get(tBody)) {
+            Set tRow = tBody.%Get("detail").%Get("violations").%Get(0)
+            Do $$$AssertEquals(tRow.%Get("field"), "Roles", "on the Roles field")
+            Do $$$AssertEquals(tRow.%Get("code"), $Parameter("OcuPilot.Kernel.Proposal.Prohibited", "PRIVILEGEGRANT"), "as a privilege grant")
+            Do $$$AssertEquals(tRow.%Get("reason"), ##class(OcuPilot.Api.Error).#REASONUSERROLESPRIVILEGED, "with the server's sentence")
+        }
+        Do $$$AssertEquals(..Exists(..#PROBEUSER), 0, "and no account was created")
+    } Catch ex {
+        Do $$$AssertStatusOK(ex.AsStatus(), "the nested-role leg ran")
+    }
+    Do ##class(OcuPilot.Test.ProposalFixture).RemoveAllRole()
+}
+
+/// <p>AD-12: each of the three routes answers exactly one JSON envelope, read with its own headers
+/// over a real HTTP request, and a create naming nothing is refused on its own envelope code.</p>
+/// <p>Mutation: make <method>OcuPilot.Api.Response.JSON</method> write the body twice -> the
+/// one-envelope assertion goes red on both reads.</p>
+Method TestEachRouteAnswersOneJsonEnvelopeOverTheWire()
+{
+    Set tUser = ##class(OcuPilot.Test.Http).GetTestUsername()
+    Set tPassword = ##class(OcuPilot.Test.Http).GetTestPassword()
+    Set tBase = $Parameter("OcuPilot.Test.Http", "BASEPATH")
+    For tPath = tBase _ ..#FORMPATH, tBase _ ..#NAMEPATH _ "?name=" _ ..#PROBEUSER {
+        Set tSC = ##class(OcuPilot.Test.Http).AbsoluteRequest("GET", tPath, tUser, tPassword, .tStatus, .tBody, .tHeaders)
+        Do $$$AssertStatusOK(tSC, tPath _ ": the request completes")
+        Do $$$AssertEquals(tStatus, 200, tPath _ ": at 200")
+        Do $$$AssertTrue($Get(tHeaders("CONTENT-TYPE")) [ "application/json", tPath _ ": as application/json")
+        Do $$$AssertEquals((tBody [ "}{"), 0, tPath _ ": exactly one envelope")
+        Do $$$AssertTrue($IsObject(##class(%DynamicObject).%FromJSON(tBody)), tPath _ ": whose body parses as one object")
+    }
+    Set tSC = ##class(OcuPilot.Test.Http).AbsoluteRequest("POST", tBase _ ..#CREATEPATH, tUser, tPassword, .tStatus, .tBody, .tHeaders, "{}", "application/json")
+    Do $$$AssertStatusOK(tSC, "the create request completes")
+    Do $$$AssertEquals(tStatus, 422, "a body naming nothing is refused at 422")
+    Do $$$AssertTrue($Get(tHeaders("CONTENT-TYPE")) [ "application/json", "as application/json")
+    Do $$$AssertEquals((tBody [ "}{"), 0, "exactly one envelope")
+    Set tParsed = ##class(%DynamicObject).%FromJSON(tBody)
+    Do $$$AssertEquals(tParsed.code, ##class(OcuPilot.Api.Error).#USERVALIDATION, "with the create's own envelope code")
+    Do $$$AssertEquals(..Exists(..#PROBEUSER), 0, "and nothing was created")
+}
+
+/// <p>The first <code>detail.violations</code> field <var>pFault</var> carries, or <code>""</code>.</p>
+ClassMethod FirstViolationField(pFault As %DynamicObject) As %String
+{
+    If '$IsObject(pFault) Quit ""
+    Set tDetail = pFault.%Get("detail")
+    If '$IsObject(tDetail) || '$IsObject(tDetail.%Get("violations")) Quit ""
+    Set tRow = tDetail.%Get("violations").%Get(0)
+    If '$IsObject(tRow) Quit ""
+    Quit tRow.%Get("field")
+}
+
+/// <p>Whether the instance holds an account named exactly <var>pName</var>.</p>
+ClassMethod Exists(pName As %String) As %Boolean
+{
+    Set tExists = 0
+    Set tOrigNS = $NAMESPACE
+    Try {
+        Set $NAMESPACE = "%SYS"
+        Set tExists = ''##class(Security.Users).Exists(pName)
+        Set $NAMESPACE = tOrigNS
+    } Catch {
+        Set $NAMESPACE = tOrigNS
+        Set tExists = 0
+    }
+    Quit tExists
+}
+
+/// <p>Account <var>pName</var>'s property <var>pProperty</var> as <method>Security.Users.Get</method>
+/// answers it, or <code>""</code>.</p>
+ClassMethod Property(pName As %String, pProperty As %String) As %String
+{
+    Set tValue = ""
+    Set tOrigNS = $NAMESPACE
+    Try {
+        Set $NAMESPACE = "%SYS"
+        Kill tProps
+        If $$$ISOK(##class(Security.Users).Get(pName, .tProps)) Set tValue = $Get(tProps(pProperty))
+        Set $NAMESPACE = tOrigNS
+    } Catch {
+        Set $NAMESPACE = tOrigNS
+        Set tValue = ""
+    }
+    Quit tValue
+}
+
+/// <p>Delete the account named exactly <var>pName</var>, if there is one.</p>
+ClassMethod Remove(pName As %String)
+{
+    Set tOrigNS = $NAMESPACE
+    Try {
+        Set $NAMESPACE = "%SYS"
+        If ##class(Security.Users).Exists(pName) Do ##class(Security.Users).Delete(pName)
+        Set $NAMESPACE = tOrigNS
+    } Catch {
+        Set $NAMESPACE = tOrigNS
+    }
+}
+
+}
diff --git a/src/OcuPilot/Test/WebAppCreate.cls b/src/OcuPilot/Test/WebAppCreate.cls
index 7fb5c08..c3c435b 100644
--- a/src/OcuPilot/Test/WebAppCreate.cls
+++ b/src/OcuPilot/Test/WebAppCreate.cls
@@ -157,8 +157,9 @@ Method TestTheScreenRouteAndTheToolComposeOnePayloadAndOneDerivedPath()
     ; And the one field the tool derives is derived on both real call sites, not merely in
     ; isolation: a body with no WSGI directory would leave it unset, so the field's presence here
     ; is the derivation actually running rather than the property surviving from the arguments.
-    Do $$$AssertEquals(##class(%DynamicAbstractObject).%FromJSON(tScreenBody).%Get("Path"), tArgs.%Get("WSGIAppLocation"), "the screen's own write carries the tool-derived path")
-    Do $$$AssertEquals(##class(%DynamicAbstractObject).%FromJSON(tAgentBody).%Get("Path"), tArgs.%Get("WSGIAppLocation"), "and so does the agent's")
+    Set tResolved = ##class(OcuPilot.Area.WebApp.Location).Root() _ tArgs.%Get("WSGIAppLocation") _ "/"
+    Do $$$AssertEquals(##class(%DynamicAbstractObject).%FromJSON(tScreenBody).%Get("Path"), tResolved, "the screen's own write carries the tool-derived path")
+    Do $$$AssertEquals(##class(%DynamicAbstractObject).%FromJSON(tAgentBody).%Get("Path"), tResolved, "and so does the agent's")
 
     Do ##class(OcuPilot.Test.ProposalFixture).RemoveSeeded(.tRemaining)
     Do $$$AssertEquals(tRemaining, 0, "the probe proposal and turn are removed")
@@ -186,7 +187,7 @@ ClassMethod PayloadArgs() As %DynamicObject
     Do tArgs.%Set("Recurse", 1, "boolean")
     Do tArgs.%Set("WSGIAppName", "probe.py", "string")
     Do tArgs.%Set("WSGICallable", "app", "string")
-    Do tArgs.%Set("WSGIAppLocation", "/tmp/ocupilot-probe-wsgi-payload", "string")
+    Do tArgs.%Set("WSGIAppLocation", "ocupilot-probe-wsgi-payload", "string")
     Do tArgs.%Set("WSGIType", "WSGI", "string")
     Quit tArgs
 }
@@ -434,7 +435,7 @@ ClassMethod Args() As %DynamicObject
     Do tArgs.%Set("AutheEnabled", 32, "number")
     Do tArgs.%Set("WSGIAppName", "probe.py", "string")
     Do tArgs.%Set("WSGICallable", "app", "string")
-    Do tArgs.%Set("WSGIAppLocation", "/tmp/ocupilot-probe-wsgi", "string")
+    Do tArgs.%Set("WSGIAppLocation", "ocupilot-probe-wsgi", "string")
     Do tArgs.%Set("WSGIType", "WSGI", "string")
     Quit tArgs
 }
diff --git a/src/OcuPilot/Test/WebAppLocation.cls b/src/OcuPilot/Test/WebAppLocation.cls
new file mode 100644
index 0000000..ae0d77f
--- /dev/null
+++ b/src/OcuPilot/Test/WebAppLocation.cls
@@ -0,0 +1,218 @@
+/// <p>Pins AD-21's WSGI directory rule on both callers of the web-application create (AD-55,
+/// DW-1495), and the unauthenticated consequence a create's proposal carries (DW-1489).</p>
+/// <p><b>What it needs from the environment:</b> OcuPilot installed and its protected state
+/// readable. Every write goes to <class>OcuPilot.Test.ProposalFixture</class>, so nothing here
+/// creates a web application; the screen path's name-taken read is live, so
+/// <parameter>PROBEAPP</parameter> must be a name the instance does not hold. No arming variable is
+/// required.</p>
+Class OcuPilot.Test.WebAppLocation Extends %UnitTest.TestCase
+{
+
+Parameter TOOL = "OcuPilot.Screen.Tool.WebAppCreate";
+
+Parameter LIST = "OcuPilot.Screen.Descriptor.WebAppList";
+
+/// <p>The shipped screen route with only its port replaced.</p>
+Parameter SCREEN = "OcuPilot.Test.CreateFixture";
+
+/// <p>The shipped mint and confirm with the port replaced.</p>
+Parameter MINT = "OcuPilot.Test.ProposalMint";
+
+Parameter CONFIRM = "OcuPilot.Test.ConfirmFixture";
+
+Parameter STORE = "OcuPilot.Kernel.State.Propose";
+
+Parameter LIMITS = "OcuPilot.Kernel.Agent.Limits";
+
+/// <p>The name every create here proposes; nothing on the instance holds it.</p>
+Parameter PROBEAPP = "/csp/ocupilotprobelocation";
+
+/// <p>The one valid directory name the resolved-path legs use.</p>
+Parameter VALIDNAME = "probeapp";
+
+Method OnBeforeOneTest() As %Status
+{
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Do ##class(OcuPilot.Kernel.Proposal.Caller).Clear()
+    Quit $$$OK
+}
+
+Method OnAfterOneTest() As %Status
+{
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Do ##class(OcuPilot.Kernel.Proposal.Caller).Clear()
+    Set tSC = ##class(OcuPilot.Test.ProposalFixture).RemoveSeeded(.tRemaining)
+    Do $$$AssertStatusOK(tSC, "the probe proposals and turns are removed")
+    Do $$$AssertEquals(tRemaining, 0, "and none survives the removal")
+    If ##class(OcuPilot.Test.WebAppWire).Exists(..#PROBEAPP) Do ##class(OcuPilot.Test.WebAppWire).Remove(..#PROBEAPP)
+    Quit $$$OK
+}
+
+/// <p>A <code>..</code>, an absolute path and a sibling escape are each refused on the screen route
+/// and on <code>webapp.list.create</code>, and neither reaches the port.</p>
+/// <p>Mutation: make <method>OcuPilot.Area.WebApp.Location.Resolve</method> return 1 with the
+/// caller's text -> every leg below goes red, on both callers.</p>
+Method TestAnEscapingLocationIsRefusedOnBothCallers()
+{
+    For tLocation = "..", "/tmp/ocupilotprobe", "../wsgiprobe" {
+        Set tArgs = ..Args(tLocation)
+
+        Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+        Set tSC = $ClassMethod(..#SCREEN, "Perform", ..#PROBEAPP, tArgs, .tCreated, .tHttp, .tFault, .tViolations)
+        Do $$$AssertEquals(..CodeFor(.tViolations, "WSGIAppLocation"), ##class(OcuPilot.Api.Error).#WEBAPPWSGIAPPLOCATIONSHAPE, "the screen route refuses '" _ tLocation _ "' on the directory field")
+        Do $$$AssertEquals(##class(OcuPilot.Test.ProposalFixture).WriteCount(), 0, "and '" _ tLocation _ "' never reaches the port from the screen")
+
+        Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+        Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+        Set tSC = ..Mint(tArgs, .tProposal, .tHttp, .tFault)
+        Do $$$AssertStatusNotOK(tSC, "the agent's mint refuses '" _ tLocation _ "'")
+        Do $$$AssertEquals(tHttp, 400, "at 400")
+        Set tProblem = $Select($IsObject(tFault) && $IsObject(tFault.%Get("detail")): tFault.%Get("detail").%Get("problem"), 1: "")
+        Do $$$AssertTrue(tProblem [ "'WSGIAppLocation': ", "naming the directory field: " _ tProblem)
+    }
+}
+
+/// <p>A valid name is sent as its directory under the fixed root, computed at the call, as both
+/// <code>WSGIAppLocation</code> and <code>Path</code>, from the screen route and from the agent's
+/// confirm alike -- the caller's own text is sent by neither.</p>
+/// <p>Mutation: make <method>OcuPilot.Area.WebApp.Location.Resolve</method> return 1 with the
+/// caller's text -> both bodies carry <code>probeapp</code> and this goes red on both callers.</p>
+Method TestAValidNameIsSentAsItsDirectoryUnderTheRootOnBothCallers()
+{
+    Set tRoot = ##class(OcuPilot.Area.WebApp.Location).Root()
+    Do $$$AssertTrue($Extract(tRoot, *-4, *) = "wsgi/", "the root is the manager directory's wsgi directory: " _ tRoot)
+    Set tExpected = tRoot _ ..#VALIDNAME _ "/"
+    Set tArgs = ..Args(..#VALIDNAME)
+
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Set tSC = $ClassMethod(..#SCREEN, "Perform", ..#PROBEAPP, tArgs, .tCreated, .tHttp, .tFault, .tViolations)
+    Do $$$AssertStatusOK(tSC, "the screen route accepts a valid directory name")
+    Do $$$AssertEquals(+$Get(tViolations), 0, "with no violation")
+    Set tScreen = ..LastBody()
+    Do $$$AssertEquals(tScreen.%Get("WSGIAppLocation"), tExpected, "the screen sends the resolved directory as WSGIAppLocation")
+    Do $$$AssertEquals(tScreen.%Get("Path"), tExpected, "and as Path")
+
+    Do ##class(OcuPilot.Test.ProposalFixture).Clear()
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Mint(tArgs, .tProposal, .tHttp, .tFault)
+    Do $$$AssertStatusOK(tSC, "the agent's mint accepts it")
+    If $$$ISERR(tSC) Quit
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmWrite("{}")
+    Set tSC = $ClassMethod(..#CONFIRM, "Confirm", tProposal.%Get("proposalId"), "", $Username, .tResult, .tHttp, .tCode, .tDetail)
+    Do $$$AssertStatusOK(tSC, "and its confirm applies it")
+    Set tAgent = ..LastBody()
+    Do $$$AssertEquals(tAgent.%Get("WSGIAppLocation"), tExpected, "the confirm sends the resolved directory as WSGIAppLocation")
+    Do $$$AssertEquals(tAgent.%Get("Path"), tExpected, "and as Path")
+}
+
+/// <p>The prefix check on its own: a normalized sibling of the root, the root itself and an empty
+/// candidate are outside, and a directory beneath the root is inside.</p>
+/// <p>Mutation: drop the root's trailing separator from the comparison in
+/// <method>OcuPilot.Area.WebApp.Location.IsInsideRoot</method> -> the sibling leg goes red.</p>
+Method TestThePrefixCheckRefusesANormalizedSibling()
+{
+    Set tRoot = ##class(OcuPilot.Area.WebApp.Location).Root()
+    Do $$$AssertNotEquals(tRoot, "", "the root is computed")
+    Set tManager = ##class(%File).NormalizeDirectory($System.Util.ManagerDirectory())
+    Do $$$AssertEquals($Extract(tRoot, 1, $Length(tManager)), tManager, "under the manager directory")
+    Set tSibling = ##class(%File).NormalizeDirectory(tManager _ "wsgiprobe")
+    Do $$$AssertEquals(##class(OcuPilot.Area.WebApp.Location).IsInsideRoot(tRoot, tSibling), 0, "a sibling sharing the root's name as a prefix is outside: " _ tSibling)
+    Do $$$AssertEquals(##class(OcuPilot.Area.WebApp.Location).IsInsideRoot(tRoot, tRoot), 0, "the root itself is refused")
+    Do $$$AssertEquals(##class(OcuPilot.Area.WebApp.Location).IsInsideRoot(tRoot, ""), 0, "and so is an empty candidate")
+    Do $$$AssertEquals(##class(OcuPilot.Area.WebApp.Location).IsInsideRoot(tRoot, tRoot _ ..#VALIDNAME _ "/"), 1, "a directory beneath the root is inside")
+    For tName = "a..b", "x/y", "", "-lead", $Translate($Justify("", 65), " ", "a") {
+        Do $$$AssertEquals(##class(OcuPilot.Area.WebApp.Location).Resolve(tName, .tPath), 0, "'" _ tName _ "' does not resolve")
+        Do $$$AssertEquals(tPath, "", "and yields no path")
+    }
+}
+
+/// <p>DW-1489: a create choosing Unauthenticated is minted, not refused, and its proposal carries
+/// <code>WEBAPP.UNAUTHENTICATED</code> on the stored row and on the wire; one that does not carries
+/// <code>""</code>.</p>
+/// <p>Mutation: return <code>""</code> from
+/// <method>OcuPilot.Screen.Tool.WebAppCreate.Consequence</method> -> the first half goes red.</p>
+Method TestTheUnauthenticatedConsequenceIsStoredAndNotRefused()
+{
+    Set tArgs = ..Args(..#VALIDNAME)
+    Do tArgs.%Set("AutheEnabled", 96, "number")
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Mint(tArgs, .tProposal, .tHttp, .tFault, .tTurnKey)
+    Do $$$AssertStatusOK(tSC, "a create choosing Unauthenticated is minted, not refused")
+    If $$$ISERR(tSC) Quit
+    Do $$$AssertEquals(..StoredConsequence(tProposal.%Get("proposalId")), "WEBAPP.UNAUTHENTICATED", "its stored row carries the unauthenticated consequence")
+    Do $$$AssertEquals(..WireConsequence(tTurnKey, tProposal.%Get("proposalId")), "WEBAPP.UNAUTHENTICATED", "and so does its wire row")
+
+    Do tArgs.%Set("AutheEnabled", 32, "number")
+    Do ##class(OcuPilot.Test.ProposalFixture).ArmMissing()
+    Set tSC = ..Mint(tArgs, .tProposal, .tHttp, .tFault, .tTurnKey)
+    Do $$$AssertStatusOK(tSC, "a password-only create is minted")
+    If $$$ISERR(tSC) Quit
+    Do $$$AssertEquals(..StoredConsequence(tProposal.%Get("proposalId")), "", "and carries no consequence")
+    Do $$$AssertEquals(..WireConsequence(tTurnKey, tProposal.%Get("proposalId")), "", "on the wire either")
+}
+
+/// <p>A WSGI create's arguments with <var>pLocation</var> as its directory.</p>
+ClassMethod Args(pLocation As %String) As %DynamicObject
+{
+    Set tArgs = {}
+    Do tArgs.%Set("NameSpace", $Namespace, "string")
+    Do tArgs.%Set("AutheEnabled", 32, "number")
+    Do tArgs.%Set("WSGIAppName", "probe.py", "string")
+    Do tArgs.%Set("WSGICallable", "app", "string")
+    Do tArgs.%Set("WSGIAppLocation", pLocation, "string")
+    Do tArgs.%Set("WSGIType", "WSGI", "string")
+    Quit tArgs
+}
+
+/// <p>Mint one create of <parameter>PROBEAPP</parameter> from <var>pArgs</var> through the fixture
+/// mint, as <method>OcuPilot.Screen.Tool.Write.View</method> would.</p>
+Method Mint(pArgs As %DynamicObject, Output pProposal As %DynamicObject, Output pHttpStatus As %Integer, Output pFault As %DynamicObject, Output pTurnKey As %String) As %Status
+{
+    Set pTurnKey = ""
+    Set tSC = ##class(OcuPilot.Test.ProposalFixture).SeedTurn($Username, "completed", .pTurnKey)
+    If $$$ISERR(tSC) Quit tSC
+    Quit $ClassMethod(..#MINT, "Mint", ..#LIST, $Parameter(..#TOOL, "TOOLNAME"), $Username, "", pTurnKey, pArgs, ..#LIMITS, .pProposal, .pHttpStatus, .pFault, $ClassMethod(..#TOOL, "Endpoint"), $ClassMethod(..#TOOL, "IdParam"), ..#PROBEAPP, $ClassMethod(..#TOOL, "IdArgument"), $ClassMethod(..#TOOL, "SettableFields"), ..#TOOL)
+}
+
+/// <p>The body the fixture port last received, parsed, or <code>{}</code>.</p>
+ClassMethod LastBody() As %DynamicObject
+{
+    Set tText = ##class(OcuPilot.Test.ProposalFixture).LastWriteBody()
+    If tText = "" Quit {}
+    Quit ##class(%DynamicAbstractObject).%FromJSON(tText)
+}
+
+/// <p>The consequence proposal <var>pKey</var>'s stored row carries.</p>
+ClassMethod StoredConsequence(pKey As %String) As %String
+{
+    Set tSC = ##class(OcuPilot.Kernel.State.Propose).GuardedByKey(pKey, .tFound, .tValues)
+    If $$$ISERR(tSC) || 'tFound Quit "<unread>"
+    Quit $Get(tValues("consequence"), "<absent>")
+}
+
+/// <p>The consequence turn <var>pTurnKey</var>'s wire row for <var>pKey</var> carries.</p>
+ClassMethod WireConsequence(pTurnKey As %String, pKey As %String) As %String
+{
+    Set tSC = ##class(OcuPilot.Kernel.State.Propose).GuardedRowsForTurn(pTurnKey, $Username, .tRows)
+    If $$$ISERR(tSC) Quit "<unread>"
+    Set tFound = "<absent>"
+    Set tIterator = tRows.%GetIterator()
+    While tIterator.%GetNext(.tIndex, .tRow) {
+        If tRow.%Get("proposalId") '= pKey Continue
+        If tRow.%GetTypeOf("consequence") '= "string" Quit
+        Set tFound = tRow.%Get("consequence")
+    }
+    Quit tFound
+}
+
+/// <p>The code <var>pViolations</var> carries for <var>pField</var>, or <code>""</code>.</p>
+ClassMethod CodeFor(ByRef pViolations, pField As %String) As %String
+{
+    Set tCode = ""
+    For tI = 1:1:+$Get(pViolations) {
+        If $List(pViolations(tI), 1) = pField Set tCode = $List(pViolations(tI), 2) Quit
+    }
+    Quit tCode
+}
+
+}
diff --git a/src/OcuPilot/Test/WebAppWire.cls b/src/OcuPilot/Test/WebAppWire.cls
index 1c2752f..22efb7c 100644
--- a/src/OcuPilot/Test/WebAppWire.cls
+++ b/src/OcuPilot/Test/WebAppWire.cls
@@ -198,21 +198,21 @@ Method TestAVendorFieldRefusalCrossesThePortOnTheFieldItNames()
 }
 
 /// <p>AD-55 at the <b>screen's</b> own call site: a WSGI create made over the wire leaves the
-/// instance holding a <code>Path</code> equal to the directory the caller named, derived by the
-/// tool rather than accepted from the body (AD-21).</p>
+/// instance holding a <code>Path</code> equal to the directory the caller's name resolves to under
+/// the fixed WSGI root, derived by the tool rather than accepted from the body (AD-21).</p>
 /// <p>Mutation: remove the <method>DerivedFields</method> call from
 /// <method>OcuPilot.Area.WebApp.Create.Perform</method> -> this goes red, and the application
 /// serves from whatever directory <method>Security.Applications.Create</method> defaults to.</p>
 Method TestAWsgiCreateLeavesTheInstanceHoldingTheDerivedPath()
 {
-    Set tLocation = "/tmp/ocupilot-probe-wire-wsgi"
+    Set tLocation = "ocupilot-probe-wire-wsgi"
     Set tBody = "{""Name"":""" _ ..#PROBEAPP _ """,""NameSpace"":""" _ $Namespace _ """,""AutheEnabled"":32,""WSGIAppName"":""probe.py"",""WSGICallable"":""app"",""WSGIAppLocation"":""" _ tLocation _ """,""WSGIType"":""WSGI""}"
     Set tSC = ##class(OcuPilot.Test.Http).MakeRequest("POST", ..#CREATEPATH, .tStatus, .tBody2, tBody, "application/json")
     Do $$$AssertStatusOK(tSC, "a WSGI create answers")
     Do $$$AssertEquals(tStatus, 201, "at 201")
     If tStatus '= 201 Quit
     Do $$$AssertEquals(..Exists(..#PROBEAPP), 1, "and the instance holds it")
-    Do $$$AssertEquals(..PathOf(..#PROBEAPP), tLocation, "with the path the server derived from the directory the caller named")
+    Do $$$AssertEquals(..PathOf(..#PROBEAPP), ##class(OcuPilot.Area.WebApp.Location).Root() _ tLocation _ "/", "with the path the server resolved from the directory name the caller gave")
 }
 
 /// <p><var>pName</var>'s stored physical path, or <code>""</code>.</p>
diff --git a/ui/browser/users-create.browser-spec.mjs b/ui/browser/users-create.browser-spec.mjs
new file mode 100644
index 0000000..a260b54
--- /dev/null
+++ b/ui/browser/users-create.browser-spec.mjs
@@ -0,0 +1,291 @@
+/**
+ * The create-a-user form in a real browser, against the throwaway instance (Story 8.2).
+ *
+ * Four claims, each asserted on rendered DOM, on the real URL or on the instance itself:
+ *
+ * 1. **The form captures the field set in the classic order** (AC1): name, full name, password,
+ *    expiration date, startup namespace, startup routine, then roles.
+ * 2. **The password is never pre-filled or echoed** (AC2): the field is empty on arrival, asks the
+ *    browser for a new password, and is empty and captioned as stored after a Save.
+ * 3. **A role the server refuses to grant is drawn unavailable before any Save** (AC3), described by
+ *    the server's sentence from the bootstrap read.
+ * 4. **A valid Save creates the account** (AC4) with the fields sent, replaces the route with
+ *    `permissions/users/edit/<id>`, reads the saved sentence, and the Users list then shows it. The
+ *    change event itself is pinned in `user-create-form.store.spec.ts`, for the reason
+ *    `web-applications-create.browser-spec.mjs` gives.
+ *
+ * **It refuses the live container.** Every account it creates is named below and removed by that
+ * exact name through the vendor's own `Security.Users.Delete` inside the throwaway, before and after.
+ *
+ * Run: `npm run test:browser` (after `npm run build` and `sh scripts/ci-throwaway.sh up`).
+ */
+
+import { test, before, after } from 'node:test';
+import assert from 'node:assert/strict';
+import { spawnSync } from 'node:child_process';
+import { dirname, join } from 'node:path';
+import { fileURLToPath } from 'node:url';
+import puppeteer from 'puppeteer';
+
+import { LIVE_CONTAINER, READINESS_PATH, browserConfig, launchOptions } from '../browser.config.mjs';
+import { waitForRows } from './list-spec.mjs';
+import { leaveFirstLoginGate } from './shell-entry.mjs';
+import { resetRememberedState } from './preferences-reset.mjs';
+
+const uiRoot = join(dirname(fileURLToPath(import.meta.url)), '..');
+const { STRINGS } = await import(join(uiRoot, 'src', 'app', 'core', 'strings.ts'));
+
+const config = browserConfig();
+const LIST_URL = '/ocupilot/permissions/users?ns=HSCUSTOM';
+const FORM_URL = '/ocupilot/permissions/users/edit?ns=HSCUSTOM';
+
+/** Every account this spec creates, removed by exact name in `before` and `after`. */
+const NAMES = ['OcuPilotProbeUserCreate', 'OcuPilotProbeUserNoEcho'];
+
+/** A password the instance's default pattern (`3.255ANP`) accepts. Never asserted on screen. */
+const PROBE_PASSWORD = 'ProbePass2026';
+
+const PROBE_FULL_NAME = 'OcuPilot probe account';
+
+/** Roles the server refuses to grant: %All by name, %Manager through an administrative resource it carries. */
+const PRIVILEGED = ['%All', '%Manager'];
+
+let browser = null;
+
+before(async () => {
+  assert.notEqual(config.container, LIVE_CONTAINER, 'this spec drives the throwaway, never the live container');
+  assert.notEqual(config.container, '', 'it removes the accounts it creates, so it needs the container that serves the origin');
+  const ready = await (await fetch(`${config.origin}${READINESS_PATH}`)).json();
+  assert.equal(ready.state, 'installed', `the throwaway must be installed, not ${JSON.stringify(ready)}`);
+  removeProbeUsers();
+  browser = await puppeteer.launch(launchOptions(config));
+});
+
+after(async () => {
+  removeProbeUsers();
+  if (browser !== null) await browser.close();
+});
+
+function credentials() {
+  return { user: config.username, password: config.password };
+}
+
+/** Run `lines` in `%SYS` inside the throwaway, returning the combined transcript. */
+function irisSys(lines) {
+  const result = spawnSync('docker', ['exec', '-i', config.container, 'iris', 'session', 'iris', '-U', '%SYS'], {
+    input: `${[...lines, 'Halt'].join('\n')}\n`,
+    encoding: 'utf8',
+    timeout: 600000,
+  });
+  return `${result.stdout ?? ''}${result.stderr ?? ''}`;
+}
+
+/** Remove every account this spec creates, by its exact name -- never a prefix sweep. */
+function removeProbeUsers() {
+  irisSys(NAMES.map((name) => `Do ##class(Security.Users).Delete("${name}")`));
+}
+
+/** The stored account `name` as the vendor reads it, or `null` when it does not exist. */
+function storedUser(name) {
+  const output = irisSys([
+    `Set tOK = ##class(Security.Users).Get("${name}", .tProps)`,
+    'Write "OCU-OK-START:",tOK,":OCU-OK-END",!',
+    'Write "OCU-FULL-START:",$Get(tProps("FullName")),":OCU-FULL-END",!',
+    'Write "OCU-ROLES-START:",$Get(tProps("Roles")),":OCU-ROLES-END",!',
+  ]);
+  const ok = /OCU-OK-START:(.*?):OCU-OK-END/.exec(output);
+  if (ok === null || ok[1].trim() !== '1') return null;
+  const fullName = /OCU-FULL-START:(.*?):OCU-FULL-END/.exec(output);
+  const roles = /OCU-ROLES-START:(.*?):OCU-ROLES-END/.exec(output);
+  return {
+    fullName: fullName === null ? '' : fullName[1],
+    roles: roles === null || roles[1] === '' ? [] : roles[1].split(','),
+  };
+}
+
+/** A fresh context signed in through the shell's own form, landed at `url`. */
+async function signedInAt(url) {
+  const { user, password } = credentials();
+  await resetRememberedState();
+  const context = await browser.createBrowserContext();
+  const page = await context.newPage();
+  page.setDefaultNavigationTimeout(config.navigationTimeoutMs);
+  await page.goto(`${config.origin}${url}`, { waitUntil: 'networkidle2' });
+  await page.waitForSelector('#ocu-signin-user', { visible: true, timeout: config.navigationTimeoutMs });
+  await page.type('#ocu-signin-user', user);
+  await page.type('#ocu-signin-password', password);
+  await page.click('.ocu-signin-card button[type="submit"]');
+  await page.waitForSelector('app-rail .ocu-rail', { timeout: config.navigationTimeoutMs });
+  await leaveFirstLoginGate(page, config.navigationTimeoutMs, url);
+  return { context, page };
+}
+
+/** Type `value` into the form control `id`, replacing whatever is there. */
+async function fill(page, id, value) {
+  await page.waitForSelector(`#${id}`, { visible: true, timeout: config.navigationTimeoutMs });
+  await page.click(`#${id}`, { clickCount: 3 });
+  await page.type(`#${id}`, value);
+}
+
+/** The Save button in the sticky bar. */
+async function saveButton(page) {
+  const buttons = await page.$$('.ocu-form-bar-actions button');
+  const save = buttons.at(-1);
+  assert.ok(save, 'the sticky bar carries a primary action');
+  return save;
+}
+
+/** Every role checkbox the form draws: its role name, whether it is disabled, and its description. */
+async function roleBoxes(page) {
+  return page.$$eval('fieldset.ocu-form-authe .ocu-field-checkbox', (labels) =>
+    labels.map((label) => {
+      const input = label.querySelector('input[type="checkbox"]');
+      const describedBy = input.getAttribute('aria-describedby');
+      const description = describedBy === null ? '' : document.getElementById(describedBy)?.textContent?.trim() ?? '';
+      return { name: label.textContent.trim(), id: input.id, disabled: input.disabled, description };
+    })
+  );
+}
+
+/** Wait for the sticky bar's saved sentence and the route replaced with the new account's URL. */
+async function waitForSaved(page) {
+  await page.waitForFunction(
+    (sentence) => document.querySelector('.ocu-form-bar-status')?.textContent?.includes(sentence) === true,
+    { timeout: config.navigationTimeoutMs },
+    STRINGS.formSaved
+  );
+  await page.waitForFunction(
+    () => /\/permissions\/users\/edit\/[^/]+$/.test(new URL(window.location.href).pathname),
+    { timeout: config.navigationTimeoutMs }
+  );
+}
+
+// AC1. Mutation (Rule 19): swap the Password and Full name blocks in the page template -> this
+// goes red naming the order.
+test('AC1: the form captures name, full name, password, expiry, startup namespace, startup routine and roles, in that order', async () => {
+  const { context, page } = await signedInAt(FORM_URL);
+  try {
+    await page.waitForSelector('#ocu-user-Name', { visible: true, timeout: config.navigationTimeoutMs });
+    const labels = await page.$$eval(
+      [
+        '.ocu-form-fields > .ocu-field:not(.ocu-form-authe) > .ocu-field-label',
+        '.ocu-form-fields > .ocu-form-authe > legend',
+      ].join(', '),
+      (nodes) => nodes.map((node) => node.textContent.trim())
+    );
+    assert.deepEqual(labels, [
+      STRINGS.tableColumnName,
+      STRINGS.userColumnFullName,
+      STRINGS.fieldPassword,
+      STRINGS.userFormExpiry,
+      STRINGS.userFormNamespace,
+      STRINGS.userFormRoutine,
+      STRINGS.userColumnRoles,
+    ]);
+    assert.equal(await page.$eval('#ocu-user-Password', (node) => node.type), 'password', 'the password is masked');
+    assert.equal(await page.$eval('#ocu-user-ExpirationDate', (node) => node.type), 'date', 'the expiry is a date control');
+    const roles = await roleBoxes(page);
+    assert.ok(roles.length > 0, 'the roles are the instance\'s own, and it holds some');
+  } finally {
+    await context.close();
+  }
+});
+
+// AC2. Mutation (Rule 19): keep the password in the store's buffer through a Save and across the
+// route replacement -> the after-save leg goes red with the password still in the field.
+test('AC2: the password is never pre-filled, asks for a new password, and is empty and captioned after a Save', async () => {
+  const { context, page } = await signedInAt(FORM_URL);
+  try {
+    await page.waitForSelector('#ocu-user-Password', { visible: true, timeout: config.navigationTimeoutMs });
+    assert.equal(await page.$eval('#ocu-user-Password', (node) => node.value), '', 'nothing is pre-filled');
+    assert.equal(
+      await page.$eval('#ocu-user-Password', (node) => node.getAttribute('autocomplete')),
+      'new-password',
+      'and the browser is asked for a new password, never a saved one'
+    );
+    assert.equal(await page.$('#ocu-user-Password-caption'), null, 'no stored caption before a Save');
+
+    await fill(page, 'ocu-user-Name', NAMES[1]);
+    await fill(page, 'ocu-user-Password', PROBE_PASSWORD);
+    await (await saveButton(page)).click();
+    await waitForSaved(page);
+
+    await page.waitForSelector('#ocu-user-Password-caption', { visible: true, timeout: config.navigationTimeoutMs });
+    assert.equal(await page.$eval('#ocu-user-Password', (node) => node.value), '', 'the field is empty after the Save');
+    assert.equal(
+      await page.$eval('#ocu-user-Password-caption', (node) => node.textContent.trim()),
+      STRINGS.formSecretStored
+    );
+    const describedBy = await page.$eval('#ocu-user-Password', (node) => node.getAttribute('aria-describedby') ?? '');
+    assert.ok(describedBy.split(' ').includes('ocu-user-Password-caption'), 'and the caption describes the field');
+    const text = await page.evaluate(() => document.body.innerText);
+    assert.ok(!text.includes(PROBE_PASSWORD), 'the password is echoed nowhere on the page');
+    assert.ok(storedUser(NAMES[1]) !== null, 'the account was created');
+  } finally {
+    await context.close();
+  }
+});
+
+// AC3. Mutation (Rule 19): drop `[disabled]` from the role checkbox -> this goes red on %All.
+test('AC3: a role the server refuses to grant is drawn unavailable, described by the server sentence', async () => {
+  const { context, page } = await signedInAt(FORM_URL);
+  try {
+    await page.waitForSelector('fieldset.ocu-form-authe', { visible: true, timeout: config.navigationTimeoutMs });
+    const roles = await roleBoxes(page);
+    for (const name of PRIVILEGED) {
+      const role = roles.find((entry) => entry.name === name);
+      assert.ok(role, `the instance offers ${name}: ${JSON.stringify(roles.map((entry) => entry.name))}`);
+      assert.equal(role.disabled, true, `${name} is unavailable`);
+      assert.ok(role.description.length > 0, `${name} is described by a sentence`);
+    }
+    const sentences = new Set(roles.filter((entry) => entry.disabled).map((entry) => entry.description));
+    assert.equal(sentences.size, 1, `every unavailable role reads the one server sentence: ${JSON.stringify([...sentences])}`);
+    assert.ok(
+      roles.some((entry) => !entry.disabled),
+      'and a role the server grants stays available'
+    );
+  } finally {
+    await context.close();
+  }
+});
+
+// AC4. Mutation (Rule 19): drop the route replacement from `onSave` -> the URL leg goes red.
+test('AC4: a valid Save creates the account with the sent fields, replaces the route, and the Users list shows it', async () => {
+  const { context, page } = await signedInAt(FORM_URL);
+  try {
+    await page.waitForSelector('#ocu-user-Name', { visible: true, timeout: config.navigationTimeoutMs });
+    await fill(page, 'ocu-user-Name', NAMES[0]);
+    await fill(page, 'ocu-user-FullName', PROBE_FULL_NAME);
+    await fill(page, 'ocu-user-Password', PROBE_PASSWORD);
+    const granted = (await roleBoxes(page)).find((entry) => !entry.disabled);
+    assert.ok(granted, 'the instance offers a role the server grants');
+    await page.evaluate((id) => document.getElementById(id).click(), granted.id);
+    await (await saveButton(page)).click();
+    await waitForSaved(page);
+
+    const path = await page.evaluate(() => new URL(window.location.href).pathname);
+    // Case-insensitive: the instance keys an account on its lowercased name (AD-13).
+    assert.ok(
+      path.toLowerCase().endsWith(`/permissions/users/edit/${NAMES[0].toLowerCase()}`),
+      `the URL names the account: ${path}`
+    );
+
+    const stored = storedUser(NAMES[0]);
+    assert.ok(stored !== null, 'the instance holds the account the form created');
+    assert.equal(stored.fullName, PROBE_FULL_NAME, 'with the full name sent');
+    assert.ok(stored.roles.includes(granted.name), `and the role sent: ${JSON.stringify(stored.roles)}`);
+
+    await page.goto(`${config.origin}${LIST_URL}`, { waitUntil: 'networkidle2' });
+    await leaveFirstLoginGate(page, config.navigationTimeoutMs, LIST_URL);
+    await waitForRows(page, config.navigationTimeoutMs);
+    const rows = await page.$$eval('[role="grid"] .ocu-data-table-body [role="row"]', (nodes) =>
+      nodes.map((node) => node.textContent)
+    );
+    assert.ok(
+      rows.some((text) => text.includes(NAMES[0])),
+      `the account the form created is in the Users list: ${JSON.stringify(rows.slice(0, 8))}`
+    );
+  } finally {
+    await context.close();
+  }
+});
diff --git a/ui/browser/web-applications-create.browser-spec.mjs b/ui/browser/web-applications-create.browser-spec.mjs
index e36c989..b60b88f 100644
--- a/ui/browser/web-applications-create.browser-spec.mjs
+++ b/ui/browser/web-applications-create.browser-spec.mjs
@@ -1,7 +1,7 @@
 /**
  * The Web application create form in a real browser, against the throwaway instance (Story 8.1).
  *
- * Five claims, each asserted on rendered DOM, on measured geometry or on the real URL rather than
+ * Seven claims, each asserted on rendered DOM, on measured geometry or on the real URL rather than
  * on store state:
  *
  * 1. **The form captures the field set in the classic order** (AC1), and the type control shows
@@ -22,6 +22,10 @@
  *    and a value longer than the field, with the token read off the document so the comparison
  *    cannot be 0 against 0. `ui/tools/design-tokens.test.mjs` is what makes the confirmed figures
  *    bind every later form.
+ * 6. **A Python application's directory is shown resolved under the instance's fixed root**
+ *    (AD-21, DW-1495), read-only, with the root read from the throwaway itself.
+ * 7. **Choosing Unauthenticated states its effect at the authentication field** (DW-1489), and only
+ *    while it is ticked.
  *
  * **It refuses the live container**, for the reason its siblings do: the throwaway is the instance
  * a browser run drives, and this spec creates web applications. It removes every one it created in
@@ -97,6 +101,23 @@ async function removeProbeApplications() {
   });
 }
 
+/**
+ * The fixed WSGI root, computed inside the throwaway the way AD-21 states it: `wsgi` under the
+ * Manager Directory, normalized. Read independently of OcuPilot, so the form's line is compared
+ * with the instance rather than with itself.
+ */
+function wsgiRoot() {
+  const result = spawnSync('docker', ['exec', '-i', config.container, 'iris', 'session', 'iris', '-U', '%SYS'], {
+    input: 'Write "OCU-ROOT-START:",##class(%File).NormalizeDirectory("wsgi", $System.Util.ManagerDirectory()),":OCU-ROOT-END",!\nHalt\n',
+    encoding: 'utf8',
+    timeout: 600000,
+  });
+  const output = `${result.stdout ?? ''}${result.stderr ?? ''}`;
+  const match = /OCU-ROOT-START:(.*?):OCU-ROOT-END/.exec(output);
+  assert.ok(match !== null && match[1] !== '', `the throwaway answered its WSGI root: ${output}`);
+  return match[1];
+}
+
 /** Whether the instance holds a web application called `name`, read inside the throwaway. */
 function applicationExists(name) {
   const result = spawnSync('docker', ['exec', '-i', config.container, 'iris', 'session', 'iris', '-U', '%SYS'], {
@@ -464,3 +485,55 @@ test('AC4: the form column is --ocu-form-max-width and its fields --ocu-field-ma
     await context.close();
   }
 });
+
+// AD-21, DW-1495. Mutation (Rule 19): drop the typed name from the store's `resolvedDirectory` ->
+// the resolved line reads the bare root and this goes red.
+test('a Python application\'s directory is shown resolved under the fixed root, read-only', async () => {
+  const root = wsgiRoot();
+  const { context, page } = await signedInAt(FORM_URL);
+  try {
+    await page.waitForSelector('#ocu-web-app-Name', { visible: true, timeout: config.navigationTimeoutMs });
+    await page.select('#ocu-web-app-Type', 'python');
+    await fill(page, 'ocu-web-app-WSGIAppLocation', 'probeapp');
+    await page.waitForFunction(
+      (expected) => document.querySelector('#ocu-web-app-WSGIAppLocation-resolved code')?.textContent?.trim() === expected,
+      { timeout: config.navigationTimeoutMs },
+      `${root}probeapp/`
+    );
+    const line = await page.$eval('#ocu-web-app-WSGIAppLocation-resolved', (node) => ({
+      text: node.textContent.trim(),
+      editable: node.querySelector('input, textarea, [contenteditable]') !== null,
+    }));
+    assert.ok(line.text.startsWith(STRINGS.webAppFormPythonDirectoryResolved), `the line is labelled: ${line.text}`);
+    assert.equal(line.editable, false, 'and it is display only');
+    const describedBy = await page.$eval('#ocu-web-app-WSGIAppLocation', (node) => node.getAttribute('aria-describedby') ?? '');
+    assert.ok(describedBy.split(' ').includes('ocu-web-app-WSGIAppLocation-resolved'), 'the field is described by it');
+  } finally {
+    await context.close();
+  }
+});
+
+// DW-1489. Mutation (Rule 19): drop the effect line from the authentication fieldset -> this goes
+// red on the ticked leg.
+test('ticking Unauthenticated states its effect at the authentication field, and unticking removes it', async () => {
+  const { context, page } = await signedInAt(FORM_URL);
+  try {
+    await page.waitForSelector('fieldset.ocu-form-authe', { visible: true, timeout: config.navigationTimeoutMs });
+    const box = '#ocu-web-app-AutheEnabled-64';
+    assert.ok(await page.$(box), 'the instance offers the Unauthenticated method (bit 64)');
+    const effect = '#ocu-web-app-AutheEnabled-effect';
+    if (await page.$eval(box, (node) => node.checked)) await page.click(box);
+    assert.equal(await page.$(effect), null, 'no effect is stated while Unauthenticated is not ticked');
+
+    await page.click(box);
+    await page.waitForSelector(effect, { visible: true, timeout: config.navigationTimeoutMs });
+    assert.equal(await page.$eval(effect, (node) => node.textContent.trim()), STRINGS.webAppUnauthenticatedEffect);
+    const describedBy = await page.$eval('fieldset.ocu-form-authe', (node) => node.getAttribute('aria-describedby') ?? '');
+    assert.equal(describedBy, 'ocu-web-app-AutheEnabled-effect', 'the group is described by the effect');
+
+    await page.click(box);
+    await page.waitForFunction((selector) => document.querySelector(selector) === null, { timeout: config.navigationTimeoutMs }, effect);
+  } finally {
+    await context.close();
+  }
+});
diff --git a/ui/src/app/app.ts b/ui/src/app/app.ts
index 7dbbc98..9a0440b 100644
--- a/ui/src/app/app.ts
+++ b/ui/src/app/app.ts
@@ -12,6 +12,8 @@ import { NavigationStart, Router, RouterOutlet } from '@angular/router';
 
 import { DefinitionActions } from './areas/agent/definition-actions';
 import { WebAppActions } from './areas/web-applications/web-app-actions';
+import { UserActions } from './areas/permissions/user-actions';
+import { UserCreateForm } from './areas/permissions/user-create-form.store';
 import { DefinitionForm } from './areas/agent/definition-form.store';
 import { AuditSearch } from './areas/logs/audit.store';
 import { ErrorLogDrill } from './areas/logs/error-log.store';
@@ -225,6 +227,10 @@ export class App {
   // pass, is what keeps `CommandBar`'s own signal write out of a pass that has already checked it
   // (`areas/web-applications/web-app-actions.ts`).
   private readonly webAppActions = inject(WebAppActions);
+  // Constructed for its own sake, the same way and for the same reason: the Users list's declared
+  // Create (`areas/permissions/user-actions.ts`).
+  private readonly userActions = inject(UserActions);
+  private readonly userCreateForm = inject(UserCreateForm);
   // Constructed for its own sake, the same way: there is no component whose job it is to act on
   // the agent's navigation directive, so injecting it here is what brings it into existence for
   // the life of the tab (`shell/agent-navigator.ts`).
@@ -490,6 +496,8 @@ export class App {
       // pasted API key that has not been stored yet (AD-35) -- and its dirty flag would otherwise
       // make the next principal's first navigation ask about work that is not theirs.
       this.definitionForm.reset();
+      // The create-a-user form holds a password THIS principal typed and has not saved (AD-35).
+      this.userCreateForm.reset();
       this.formDirty.reset();
       // The ninth: whether the instance holds an enabled definition is a read THIS principal
       // made, and the panel and the rail's dot pick an audience from it beside the navigation
diff --git a/ui/src/app/areas/permissions/user-actions.ts b/ui/src/app/areas/permissions/user-actions.ts
new file mode 100644
index 0000000..3cf99ca
--- /dev/null
+++ b/ui/src/app/areas/permissions/user-actions.ts
@@ -0,0 +1,45 @@
+import { Injectable, Injector, inject } from '@angular/core';
+import { Router } from '@angular/router';
+
+import { createFormFor, screenForRoute, withQuery } from '../../core/navigation';
+import { ScreenActions } from '../../core/screen-actions';
+
+/** The descriptor whose command bar this action appears on. */
+export const USER_LIST_DESCRIPTOR = 'OcuPilot.Screen.Descriptor.UserList';
+
+/** That descriptor's own route, which resolves its paired form through `createFormFor`. */
+export const USER_LIST_ROUTE = 'permissions/users';
+
+/** The declared primary action id: the command bar's Create, which opens the form. */
+export const CREATE_ACTION = 'create';
+
+/**
+ * The handler behind the Users list's declared Create (AD-5, AD-19).
+ *
+ * The Users list is served by the generic `ListPage`, so its action is registered here, once,
+ * against the descriptor's class name -- the key `ScreenActions` is indexed by. It is tab-scoped
+ * and constructed from the application root before the first change-detection pass, for the
+ * reason `web-app-actions.ts` gives (DW-246): a registration from a routed page's constructor
+ * would write `CommandBar`'s signal inside a pass that has already checked it.
+ *
+ * The route is resolved through `createFormFor`, never written, and `withQuery` carries the
+ * namespace.
+ */
+@Injectable({ providedIn: 'root' })
+export class UserActions {
+  private readonly injector = inject(Injector);
+
+  private readonly actions = inject(ScreenActions);
+
+  constructor() {
+    this.actions.register(USER_LIST_DESCRIPTOR, CREATE_ACTION, () => this.openCreate());
+  }
+
+  private openCreate(): void {
+    const list = screenForRoute(USER_LIST_ROUTE);
+    const editor = list === null ? null : createFormFor(list);
+    if (editor === null) return;
+    const router = this.injector.get(Router);
+    void router.navigateByUrl(withQuery(editor.route, router.url));
+  }
+}
diff --git a/ui/src/app/areas/permissions/user-create-form.page.ts b/ui/src/app/areas/permissions/user-create-form.page.ts
new file mode 100644
index 0000000..d88f4e7
--- /dev/null
+++ b/ui/src/app/areas/permissions/user-create-form.page.ts
@@ -0,0 +1,554 @@
+import {
+  ChangeDetectionStrategy,
+  Component,
+  DestroyRef,
+  ElementRef,
+  Injector,
+  afterNextRender,
+  inject,
+  signal,
+  viewChild,
+} from '@angular/core';
+import { Router } from '@angular/router';
+
+import { encodeEntityId } from '../../core/entity-id';
+import { FormDirty } from '../../core/form-dirty';
+import { NavigationService, formatDeniedAction, withQuery } from '../../core/navigation';
+import { STRINGS } from '../../core/strings';
+import { STATE_CONFLICT_CODE, type Violation } from '../../core/violations';
+import { Dialog } from '../../shell/dialog';
+import { PASSWORD_FIELD, ROLES_FIELD, type RoleOption, UserCreateForm } from './user-create-form.store';
+
+/** The list this form is reached from, which Cancel and the leave confirmation return to. */
+const LIST_ROUTE = 'permissions/users';
+
+/** This form's own route, the fallback when the mirror cannot resolve it from the URL. */
+const FORM_ROUTE = 'permissions/users/edit';
+
+/** The machine code a privilege denial carries (AD-39). Never the envelope's human reason. */
+const NO_PRIVILEGE_CODE = 'AUTH.NOPRIVILEGE';
+
+/** One field, resolved for drawing: its control id, its refusal and its described-by wiring. */
+interface FieldView {
+  readonly id: string;
+  readonly reason: string;
+  readonly invalid: boolean;
+  readonly describedBy: string | null;
+}
+
+/**
+ * The create-a-user form, a `form-page` over an account on the instance (AD-55, FR-30).
+ *
+ * **Field order is the acceptance criterion, and it is the classic editor's.** `%CSP.UI.Portal.User`
+ * draws name, full name, password, expiration date, startup namespace and startup routine; roles,
+ * which that page assigns on a tab after the save, come last here.
+ *
+ * **The password is masked, never pre-filled and never echoed** (AD-35). The input is bound to the
+ * store's value, which is empty until typed and emptied again by an accepted Save, and it asks the
+ * browser for a new password rather than a saved one. After a Save it carries the published
+ * stored-secret caption.
+ *
+ * **A role the server refuses to grant is drawn disabled, described by the server's own sentence**
+ * from the bootstrap read. The refusal itself is the server's, on either caller (AD-10, AD-39).
+ *
+ * It composes no payload and authors no field sentence; the unsaved-changes guard is the
+ * `form-page` route guard, answered here. Every control-flow condition is a paren-free member
+ * reference, for the reason `sign-in.ts` records.
+ */
+@Component({
+  selector: 'app-user-create-form-page',
+  changeDetection: ChangeDetectionStrategy.OnPush,
+  imports: [Dialog],
+  template: `<section class="ocu-form-page">
+    @if (hasSummary) {
+      <div #summary class="ocu-banner ocu-form-summary" role="alert" tabindex="-1">
+        <ul class="ocu-form-summary-list">
+          @for (entry of violations; track entry.field) {
+            <li>
+              <button type="button" class="ocu-button-text" (click)="focusField(entry.field)">
+                {{ entry.reason }}
+              </button>
+            </li>
+          }
+        </ul>
+      </div>
+    }
+    @if (hasReason) {
+      <p class="ocu-banner ocu-banner-warning" role="alert">{{ reason }}</p>
+    }
+
+    @if (loadedFlag) {
+    <p class="ocu-form-legend">{{ STRINGS.formRequiredFieldsLegend }}</p>
+    <div class="ocu-form-fields">
+      <div class="ocu-field">
+        <label class="ocu-field-label ocu-field-label-required" [attr.for]="nameField.id">{{ STRINGS.tableColumnName }}</label>
+        <div class="ocu-field-control">
+          <input
+            class="ocu-field-input"
+            type="text"
+            autocomplete="off"
+            [id]="nameField.id"
+            [value]="value('Name')"
+            aria-required="true"
+            [attr.maxlength]="maxLength('Name')"
+            [attr.aria-invalid]="nameField.invalid"
+            [attr.aria-describedby]="nameField.describedBy"
+            (input)="onText('Name', $event)"
+            (blur)="onBlur('Name')"
+          />
+        </div>
+        @if (nameField.invalid) {
+          <p class="ocu-form-error" [id]="nameField.id + '-reason'">{{ nameField.reason }}</p>
+        }
+      </div>
+
+      <div class="ocu-field">
+        <label class="ocu-field-label" [attr.for]="fullNameField.id">{{ STRINGS.userColumnFullName }}</label>
+        <div class="ocu-field-control">
+          <input
+            class="ocu-field-input"
+            type="text"
+            [id]="fullNameField.id"
+            [value]="value('FullName')"
+            [attr.maxlength]="maxLength('FullName')"
+            [attr.aria-invalid]="fullNameField.invalid"
+            [attr.aria-describedby]="fullNameField.describedBy"
+            (input)="onText('FullName', $event)"
+            (blur)="onBlur('FullName')"
+          />
+        </div>
+        @if (fullNameField.invalid) {
+          <p class="ocu-form-error" [id]="fullNameField.id + '-reason'">{{ fullNameField.reason }}</p>
+        }
+      </div>
+
+      <div class="ocu-field">
+        <label class="ocu-field-label ocu-field-label-required" [attr.for]="passwordField.id">{{ STRINGS.fieldPassword }}</label>
+        <div class="ocu-field-control">
+          <input
+            class="ocu-field-input"
+            [id]="passwordField.id"
+            [type]="passwordInputType"
+            autocomplete="new-password"
+            [value]="passwordValue"
+            aria-required="true"
+            [attr.aria-invalid]="passwordField.invalid"
+            [attr.aria-describedby]="passwordField.describedBy"
+            (input)="onPassword($event)"
+            (blur)="onBlur('Password')"
+          />
+          <button
+            type="button"
+            class="ocu-reveal-toggle"
+            [attr.aria-label]="revealLabel"
+            [attr.aria-pressed]="revealed()"
+            (click)="toggleReveal()"
+          >
+            <span aria-hidden="true">{{ revealGlyph }}</span>
+          </button>
+        </div>
+        @if (showStoredCaption) {
+          <p class="ocu-field-caption" [id]="passwordField.id + '-caption'">{{ STRINGS.formSecretStored }}</p>
+        }
+        @if (passwordField.invalid) {
+          <p class="ocu-form-error" [id]="passwordField.id + '-reason'">{{ passwordField.reason }}</p>
+        }
+      </div>
+
+      <div class="ocu-field">
+        <label class="ocu-field-label" [attr.for]="expiryField.id">{{ STRINGS.userFormExpiry }}</label>
+        <div class="ocu-field-control">
+          <input
+            class="ocu-field-input"
+            type="date"
+            [id]="expiryField.id"
+            [value]="value('ExpirationDate')"
+            [attr.aria-invalid]="expiryField.invalid"
+            [attr.aria-describedby]="expiryField.describedBy"
+            (input)="onText('ExpirationDate', $event)"
+            (blur)="onBlur('ExpirationDate')"
+          />
+        </div>
+        @if (expiryField.invalid) {
+          <p class="ocu-form-error" [id]="expiryField.id + '-reason'">{{ expiryField.reason }}</p>
+        }
+      </div>
+
+      <div class="ocu-field">
+        <label class="ocu-field-label" [attr.for]="namespaceField.id">{{ STRINGS.userFormNamespace }}</label>
+        <div class="ocu-field-control">
+          <input
+            class="ocu-field-input"
+            type="text"
+            [id]="namespaceField.id"
+            [value]="value('NameSpace')"
+            [attr.maxlength]="maxLength('NameSpace')"
+            [attr.aria-invalid]="namespaceField.invalid"
+            [attr.aria-describedby]="namespaceField.describedBy"
+            (input)="onText('NameSpace', $event)"
+            (blur)="onBlur('NameSpace')"
+          />
+        </div>
+        @if (namespaceField.invalid) {
+          <p class="ocu-form-error" [id]="namespaceField.id + '-reason'">{{ namespaceField.reason }}</p>
+        }
+      </div>
+
+      <div class="ocu-field">
+        <label class="ocu-field-label" [attr.for]="routineField.id">{{ STRINGS.userFormRoutine }}</label>
+        <div class="ocu-field-control">
+          <input
+            class="ocu-field-input"
+            type="text"
+            [id]="routineField.id"
+            [value]="value('Routine')"
+            [attr.maxlength]="maxLength('Routine')"
+            [attr.aria-invalid]="routineField.invalid"
+            [attr.aria-describedby]="routineField.describedBy"
+            (input)="onText('Routine', $event)"
+            (blur)="onBlur('Routine')"
+          />
+        </div>
+        @if (routineField.invalid) {
+          <p class="ocu-form-error" [id]="routineField.id + '-reason'">{{ routineField.reason }}</p>
+        }
+      </div>
+
+      <fieldset class="ocu-field ocu-form-authe" [attr.id]="rolesField.id" tabindex="-1">
+        <legend class="ocu-field-label">{{ STRINGS.userColumnRoles }}</legend>
+        @for (role of roleOptions; track role.name) {
+          <label class="ocu-field-checkbox">
+            <input
+              type="checkbox"
+              [id]="roleId(role.name)"
+              [checked]="roleChecked(role.name)"
+              [disabled]="role.privileged"
+              [attr.aria-describedby]="role.privileged ? rolesReasonId : null"
+              (change)="onRole(role.name, $event)"
+            />
+            <span>{{ role.name }}</span>
+          </label>
+        }
+        @if (hasPrivilegedRole) {
+          <p class="ocu-field-caption" [id]="rolesReasonId">{{ rolesReason }}</p>
+        }
+        @if (rolesField.invalid) {
+          <p class="ocu-form-error" [id]="rolesField.id + '-reason'">{{ rolesField.reason }}</p>
+        }
+      </fieldset>
+    </div>
+
+    <div class="ocu-form-bar">
+      <div class="ocu-form-bar-status">
+        @if (showSaved) {
+          <span role="status">{{ STRINGS.formSaved }}</span>
+        }
+      </div>
+      <div class="ocu-form-bar-actions">
+        <button type="button" class="ocu-button-text" (click)="cancel()">{{ STRINGS.actionCancel }}</button>
+        <button
+          type="button"
+          class="ocu-button-primary"
+          [attr.aria-disabled]="busyFlag"
+          (click)="onSave()"
+        >
+          {{ STRINGS.actionSave }}
+        </button>
+      </div>
+    </div>
+    }
+
+    @if (leavePending) {
+      <app-dialog
+        [heading]="STRINGS.formLeaveWithoutSaving"
+        [closeLabel]="STRINGS.actionCancel"
+        (closed)="answerLeave(false)"
+      >
+        <button dialogAction type="button" class="ocu-button-primary" (click)="answerLeave(true)">
+          {{ STRINGS.actionConfirm }}
+        </button>
+      </app-dialog>
+    }
+  </section>`,
+})
+export class UserCreateFormPage {
+  private readonly store = inject(UserCreateForm);
+  private readonly formDirty = inject(FormDirty);
+  private readonly router = inject(Router);
+  private readonly navigation = inject(NavigationService);
+  private readonly injector = inject(Injector);
+
+  protected readonly STRINGS = STRINGS;
+
+  /** Bumped by both stores, so the template re-reads them under `OnPush`. */
+  private readonly generation = signal(0);
+
+  /** Whether the password is shown in clear. Local to this page and never stored. */
+  protected readonly revealed = signal(false);
+
+  private readonly summary = viewChild<ElementRef<HTMLElement>>('summary');
+
+  /** Whether the summary has been focused for the refusal now on screen. */
+  private focusedSummary = false;
+
+  constructor() {
+    const stopStore = this.store.subscribe(() => this.generation.update((value) => value + 1));
+    const stopDirty = this.formDirty.subscribe(() => this.generation.update((value) => value + 1));
+    void this.store.open();
+    afterNextRender(() => this.focusRefusal(), { injector: this.injector });
+    inject(DestroyRef).onDestroy(() => {
+      stopStore();
+      stopDirty();
+      // Not torn down while the store is carried across the create's own route replacement,
+      // which destroys this component and builds it again over the same account.
+      if (!this.store.retaining()) this.store.reset();
+    });
+  }
+
+  // --- reads -----------------------------------------------------------------------------------
+
+  protected get loadedFlag(): boolean {
+    this.generation();
+    return this.store.loaded();
+  }
+
+  protected get busyFlag(): boolean {
+    this.generation();
+    return this.store.busy();
+  }
+
+  protected get violations(): readonly Violation[] {
+    this.generation();
+    return this.store.violations();
+  }
+
+  protected get hasSummary(): boolean {
+    return this.violations.length > 0;
+  }
+
+  /**
+   * What an envelope-level refusal reads (AD-8, AD-39): a privilege denial composed from the
+   * published sentence and the pair the envelope named, a stale save's published sentence, or the
+   * envelope's own reason.
+   */
+  protected get reason(): string {
+    this.generation();
+    const pair = this.store.refusalPair();
+    if (this.store.refusalCode() === NO_PRIVILEGE_CODE && pair !== '') {
+      return formatDeniedAction(STRINGS.privilegeDeniedAction, pair, STRINGS.userFormRefusedAction);
+    }
+    if (this.store.refusalCode() === STATE_CONFLICT_CODE) return STRINGS.formStaleSave;
+    return this.store.reason();
+  }
+
+  protected get hasReason(): boolean {
+    return this.reason !== '';
+  }
+
+  protected get passwordValue(): string {
+    this.generation();
+    return this.store.password();
+  }
+
+  protected get passwordInputType(): string {
+    return this.revealed() ? 'text' : 'password';
+  }
+
+  protected get revealGlyph(): string {
+    return this.revealed() ? '\u25CF' : '\u25CB';
+  }
+
+  /** The toggle's accessible name, which is what makes it labelled rather than an unnamed icon. */
+  protected get revealLabel(): string {
+    return this.revealed() ? STRINGS.accountHidePassword : STRINGS.accountShowPassword;
+  }
+
+  /**
+   * Whether the password field carries the published stored-secret caption: after an accepted
+   * Save, when the account holds the password that was sent and the field no longer does.
+   */
+  protected get showStoredCaption(): boolean {
+    this.generation();
+    return this.store.saved();
+  }
+
+  protected get roleOptions(): readonly RoleOption[] {
+    this.generation();
+    return this.store.rules().roles;
+  }
+
+  protected get hasPrivilegedRole(): boolean {
+    return this.rolesReason !== '' && this.roleOptions.some((role) => role.privileged);
+  }
+
+  /** The server's sentence for a role it refuses to grant, which each such checkbox is described by. */
+  protected get rolesReason(): string {
+    this.generation();
+    return this.store.rules().rolesReason;
+  }
+
+  protected get rolesReasonId(): string {
+    return `${this.controlId(ROLES_FIELD)}-privileged`;
+  }
+
+  protected get showSaved(): boolean {
+    this.generation();
+    return this.store.saved();
+  }
+
+  protected get leavePending(): boolean {
+    this.generation();
+    return this.formDirty.pending();
+  }
+
+  protected value(field: string): string {
+    this.generation();
+    return this.store.value(field);
+  }
+
+  protected roleChecked(name: string): boolean {
+    this.generation();
+    return this.store.roleChecked(name);
+  }
+
+  protected roleId(name: string): string {
+    return `${this.controlId(ROLES_FIELD)}-${name}`;
+  }
+
+  /** The length the instance stores for `field`, or `null` where it declares none. */
+  protected maxLength(field: string): number | null {
+    this.generation();
+    const bound = this.store.maxLength(field);
+    return bound > 0 ? bound : null;
+  }
+
+  protected get nameField(): FieldView {
+    return this.fieldView('Name');
+  }
+
+  protected get fullNameField(): FieldView {
+    return this.fieldView('FullName');
+  }
+
+  protected get passwordField(): FieldView {
+    return this.fieldView(PASSWORD_FIELD);
+  }
+
+  protected get expiryField(): FieldView {
+    return this.fieldView('ExpirationDate');
+  }
+
+  protected get namespaceField(): FieldView {
+    return this.fieldView('NameSpace');
+  }
+
+  protected get routineField(): FieldView {
+    return this.fieldView('Routine');
+  }
+
+  protected get rolesField(): FieldView {
+    return this.fieldView(ROLES_FIELD);
+  }
+
+  // --- intents ---------------------------------------------------------------------------------
+
+  protected onText(field: string, event: Event): void {
+    const target = event.target;
+    if (target instanceof HTMLInputElement) this.store.setValue(field, target.value);
+  }
+
+  protected onPassword(event: Event): void {
+    const target = event.target;
+    if (target instanceof HTMLInputElement) this.store.setPassword(target.value);
+  }
+
+  protected onRole(name: string, event: Event): void {
+    const target = event.target;
+    if (target instanceof HTMLInputElement) this.store.setRole(name, target.checked);
+  }
+
+  protected toggleReveal(): void {
+    this.revealed.update((value) => !value);
+  }
+
+  protected onBlur(field: string): void {
+    void this.store.onBlur(field);
+  }
+
+  protected async onSave(): Promise<void> {
+    if (this.store.busy()) return;
+    const saved = await this.store.save();
+    if (!saved) {
+      this.afterRefusal();
+      return;
+    }
+    // A create replaces the route with the new account's URL, so the address bar names the entity
+    // and Back goes to the list. The buffer on screen, less the password, is carried across that
+    // one navigation; nothing here reads the `:id`.
+    if (this.store.createdId() !== '') {
+      this.store.retainAcrossRouteReplacement();
+      void this.router.navigateByUrl(this.editorUrl(this.store.createdId()), { replaceUrl: true });
+    }
+  }
+
+  protected focusField(field: string): void {
+    document.getElementById(this.controlId(field))?.focus();
+  }
+
+  protected cancel(): void {
+    void this.router.navigateByUrl(withQuery(LIST_ROUTE, this.router.url));
+  }
+
+  protected answerLeave(leave: boolean): void {
+    this.formDirty.answer(leave);
+  }
+
+  // --- internals -------------------------------------------------------------------------------
+
+  /**
+   * After a refused Save: the error summary takes focus, then the first invalid field, in the
+   * order EXPERIENCE.md's `form-page` validation rule states. `afterNextRender`, because the
+   * summary is not in the DOM until this dirty component has painted.
+   */
+  private afterRefusal(): void {
+    if (this.store.violations()[0] === undefined) return;
+    this.focusedSummary = false;
+    afterNextRender(() => this.focusRefusal(), { injector: this.injector });
+  }
+
+  private focusRefusal(): void {
+    if (this.focusedSummary) return;
+    const first = this.store.violations()[0];
+    if (first === undefined) return;
+    this.focusedSummary = true;
+    this.summary()?.nativeElement.focus();
+    this.focusField(first.field);
+  }
+
+  private fieldView(field: string): FieldView {
+    this.generation();
+    const id = this.controlId(field);
+    const reason = this.store.violationFor(field);
+    const invalid = reason !== '';
+    const described: string[] = [];
+    if (field === PASSWORD_FIELD && this.store.saved()) described.push(`${id}-caption`);
+    if (invalid) described.push(`${id}-reason`);
+    return {
+      id,
+      reason,
+      invalid,
+      describedBy: described.length === 0 ? null : described.join(' '),
+    };
+  }
+
+  private controlId(field: string): string {
+    return `ocu-user-${field}`;
+  }
+
+  /** The new account's own URL. The id is `encodeEntityId`'s, never `encodeURIComponent`'s (AD-13). */
+  private editorUrl(id: string): string {
+    const editor = this.navigation.screenForUrl(this.router.url);
+    const route = editor === null ? FORM_ROUTE : editor.route;
+    return withQuery(`${route}/${encodeEntityId(id)}`, this.router.url);
+  }
+}
diff --git a/ui/src/app/areas/permissions/user-create-form.store.spec.ts b/ui/src/app/areas/permissions/user-create-form.store.spec.ts
new file mode 100644
index 0000000..7616e67
--- /dev/null
+++ b/ui/src/app/areas/permissions/user-create-form.store.spec.ts
@@ -0,0 +1,258 @@
+import { TestBed } from '@angular/core/testing';
+import { afterEach, describe, expect, it } from 'vitest';
+
+import { ApiService, type ApiRequestInit, type JsonResult } from '../../core/api';
+import { ChangeBus, type ChangeEvent } from '../../core/change-bus';
+import { entityRefKey } from '../../core/entity-ref';
+import { FormDirty } from '../../core/form-dirty';
+import {
+  NAME_TAKEN_CODE,
+  USERS_FORM_PATH,
+  USERS_NAME_PATH,
+  USERS_PATH,
+  USER_ENTITY,
+  USER_SCOPE,
+  UserCreateForm,
+} from './user-create-form.store';
+
+/**
+ * The create-a-user form's store (AD-14, AD-35, AD-39, AD-55).
+ *
+ * It pins what no browser leg can falsify: the change event a Save publishes (the form replaces
+ * its own route, and the list the browser then opens re-reads on arrival either way), that the
+ * password leaves the store on an accepted Save, on `reset()` and before the route replacement,
+ * and that the server's refusal of a privileged role lands on the Roles field. The bus is real and
+ * only the server's answers are stubbed.
+ */
+
+/** What `GET /users/form` answers, narrowed to what these tests read. */
+const RULES = {
+  requiredFields: ['Name', 'Password'],
+  maxLengths: { Name: 160, FullName: 2048, NameSpace: 64, Routine: 64 },
+  rules: [
+    { field: 'Password', code: 'USER.PASSWORD.REQUIRED', reason: 'Enter a password.' },
+    { field: 'Name', code: 'USER.NAME.REQUIRED', reason: 'Enter a name.' },
+  ],
+  roles: [
+    { name: '%Developer', privileged: false },
+    { name: '%All', privileged: true },
+    { name: '%SQL', privileged: false },
+  ],
+  rolesReason: 'OcuPilot cannot grant this role.',
+};
+
+const TAKEN_SENTENCE = 'This instance already has a user with that name.';
+
+async function settle(): Promise<void> {
+  for (let pass = 0; pass < 4; pass += 1) await new Promise((resolve) => setTimeout(resolve, 2));
+}
+
+function mount(
+  createAnswer: JsonResult<unknown> = { kind: 'ok', status: 201, body: { name: 'probeuser', user: {} } },
+  nameAnswer: JsonResult<unknown> = { kind: 'ok', status: 200, body: { name: 'probeuser', available: true, reason: '' } }
+) {
+  TestBed.resetTestingModule();
+  const calls: { path: string; method: string; body: string }[] = [];
+  const api = {
+    requestJson: async <T,>(path: string, init: ApiRequestInit = {}): Promise<JsonResult<T>> => {
+      calls.push({ path, method: init.method ?? 'GET', body: init.body ?? '' });
+      if (path === USERS_FORM_PATH) {
+        return { kind: 'ok', status: 200, body: RULES } as unknown as JsonResult<T>;
+      }
+      if (path.startsWith(USERS_NAME_PATH)) return nameAnswer as JsonResult<T>;
+      return createAnswer as JsonResult<T>;
+    },
+  };
+  const bus = new ChangeBus();
+  const events: ChangeEvent[] = [];
+  bus.subscribe((event) => events.push(event));
+  TestBed.configureTestingModule({
+    providers: [
+      { provide: ApiService, useValue: api as unknown as ApiService },
+      { provide: ChangeBus, useValue: bus },
+      { provide: FormDirty, useValue: new FormDirty() },
+    ],
+  });
+  return { store: TestBed.inject(UserCreateForm), calls, events };
+}
+
+function postedBody(calls: { path: string; method: string; body: string }[]): Record<string, unknown> {
+  const post = calls.filter((call) => call.method === 'POST').at(-1);
+  expect(post).toBeDefined();
+  expect(post!.path).toBe(USERS_PATH);
+  return JSON.parse(post!.body) as Record<string, unknown>;
+}
+
+afterEach(() => {
+  TestBed.resetTestingModule();
+});
+
+describe('the create-a-user form store', () => {
+  it('AC4, AD-14: a Save publishes one created event carrying the scoped triple', async () => {
+    const { store, events } = mount();
+    await store.open();
+    store.setValue('Name', 'probeuser');
+    store.setPassword('probe-secret-1');
+    expect(await store.save()).toBe(true);
+    await settle();
+
+    // Mutation (Rule 19): drop the `ChangeBus.publish` call from `save()` -> this goes red, and an
+    // open Users list never re-fetches the account the form just created.
+    expect(events).toEqual([
+      {
+        kind: 'changed',
+        type: USER_ENTITY,
+        scope: USER_SCOPE,
+        id: 'probeuser',
+        key: entityRefKey(USER_ENTITY, USER_SCOPE, 'probeuser'),
+        action: 'created',
+        proposalId: '',
+        expiresAt: 0,
+      },
+    ]);
+    expect(store.saved()).toBe(true);
+    expect(store.createdId()).toBe('probeuser');
+  });
+
+  it('AD-54: the body carries every create field, the password and the ticked roles in the order offered', async () => {
+    const { store, calls } = mount();
+    await store.open();
+    store.setValue('Name', 'probeuser');
+    store.setValue('FullName', 'Probe User');
+    store.setPassword('probe-secret-1');
+    store.setValue('ExpirationDate', '2027-01-31');
+    store.setValue('NameSpace', 'HSCUSTOM');
+    store.setValue('Routine', 'START^PROBE');
+    store.setRole('%SQL', true);
+    store.setRole('%Developer', true);
+    await store.save();
+
+    expect(postedBody(calls)).toEqual({
+      Name: 'probeuser',
+      Password: 'probe-secret-1',
+      FullName: 'Probe User',
+      ExpirationDate: '2027-01-31',
+      NameSpace: 'HSCUSTOM',
+      Routine: 'START^PROBE',
+      Roles: ['%Developer', '%SQL'],
+    });
+  });
+
+  it('AD-35: the password leaves the store on an accepted Save, on reset and before the route replacement', async () => {
+    const { store } = mount();
+    await store.open();
+    store.setValue('Name', 'probeuser');
+    store.setPassword('probe-secret-1');
+    await store.save();
+    // Mutation (Rule 19): drop the clear from `save()` -> this goes red, and the field would be
+    // drawn with the sent password in it after the save.
+    expect(store.password()).toBe('');
+
+    store.setPassword('typed-after-save');
+    store.retainAcrossRouteReplacement();
+    expect(store.password(), 'the retained buffer carries no password across the navigation').toBe('');
+    expect(store.value('Name'), 'but the rest of what was saved stays on screen').toBe('probeuser');
+
+    store.setPassword('typed-again');
+    store.reset();
+    expect(store.password()).toBe('');
+  });
+
+  it('a refused Save keeps what was typed, the password included, so the user corrects one field', async () => {
+    const { store } = mount({
+      kind: 'error',
+      status: 422,
+      error: 'validation_failed',
+      reason: 'The user was refused.',
+      code: 'USER.VALIDATION',
+      detail: {
+        violations: [{ field: 'NameSpace', code: 'USER.NAMESPACE.UNKNOWN', reason: 'Choose a namespace that exists.' }],
+      },
+    } as unknown as JsonResult<unknown>);
+    await store.open();
+    store.setValue('Name', 'probeuser');
+    store.setValue('NameSpace', 'NOPE');
+    store.setPassword('probe-secret-1');
+    expect(await store.save()).toBe(false);
+    expect(store.violationFor('NameSpace')).toBe('Choose a namespace that exists.');
+    expect(store.password()).toBe('probe-secret-1');
+    expect(store.saved()).toBe(false);
+  });
+
+  it('AC3: a privileged role refused by the server lands on the Roles field with the server sentence', async () => {
+    const { store, events } = mount({
+      kind: 'error',
+      status: 422,
+      error: 'validation_failed',
+      reason: 'The user was refused.',
+      code: 'USER.VALIDATION',
+      detail: {
+        violations: [
+          { field: 'Roles', code: 'PROHIBITED.PRIVILEGEGRANT', reason: 'OcuPilot cannot grant this role.' },
+        ],
+      },
+    } as unknown as JsonResult<unknown>);
+    await store.open();
+    store.setValue('Name', 'probeuser');
+    store.setPassword('probe-secret-1');
+    store.setRole('%Developer', true);
+    expect(await store.save()).toBe(false);
+
+    expect(store.violationFor('Roles')).toBe('OcuPilot cannot grant this role.');
+    expect(store.violations()[0]?.field).toBe('Roles');
+    expect(store.reason(), 'a field violation is not an envelope banner').toBe('');
+    expect(events).toEqual([]);
+    // Ticking a role clears the refusal it stood over.
+    store.setRole('%SQL', true);
+    expect(store.violationFor('Roles')).toBe('');
+  });
+
+  it('AC3: the picker marks a privileged role from the bootstrap read and never adds it to the body', async () => {
+    const { store, calls } = mount();
+    await store.open();
+    expect(store.rules().roles.find((role) => role.name === '%All')?.privileged).toBe(true);
+    expect(store.rules().rolesReason).toBe('OcuPilot cannot grant this role.');
+    store.setValue('Name', 'probeuser');
+    store.setPassword('probe-secret-1');
+    store.setRole('%All', true);
+    expect(store.roleChecked('%All')).toBe(false);
+    await store.save();
+    expect(postedBody(calls)['Roles']).toEqual([]);
+  });
+
+  it('the blur look-up marks a taken name with the server sentence, and leaves the field unmarked when it could not be made', async () => {
+    const taken = mount(undefined, {
+      kind: 'ok',
+      status: 200,
+      body: { name: 'probeuser', available: false, reason: TAKEN_SENTENCE },
+    } as unknown as JsonResult<unknown>);
+    await taken.store.open();
+    taken.store.setValue('Name', 'probeuser');
+    await taken.store.onBlur('Name');
+    expect(taken.store.violationFor('Name')).toBe(TAKEN_SENTENCE);
+    expect(taken.store.violations()[0]?.code).toBe(NAME_TAKEN_CODE);
+    expect(taken.calls.some((call) => call.path === `${USERS_NAME_PATH}?name=probeuser`)).toBe(true);
+
+    const refused = mount(undefined, {
+      kind: 'error',
+      status: 500,
+      error: 'server_error',
+      reason: 'An internal error occurred',
+      code: 'INTERNAL',
+      detail: null,
+    } as unknown as JsonResult<unknown>);
+    await refused.store.open();
+    refused.store.setValue('Name', 'probeuser');
+    await refused.store.onBlur('Name');
+    expect(refused.store.violations()).toEqual([]);
+  });
+
+  it('blurring an empty password renders the sentence the bootstrap read published for it', async () => {
+    const { store } = mount();
+    await store.open();
+    await store.onBlur('Password');
+    expect(store.violationFor('Password')).toBe('Enter a password.');
+    store.setPassword('x');
+    expect(store.violationFor('Password')).toBe('');
+  });
+});
diff --git a/ui/src/app/areas/permissions/user-create-form.store.ts b/ui/src/app/areas/permissions/user-create-form.store.ts
new file mode 100644
index 0000000..19c4e74
--- /dev/null
+++ b/ui/src/app/areas/permissions/user-create-form.store.ts
@@ -0,0 +1,512 @@
+import { Injectable, Injector, inject } from '@angular/core';
+
+import { ApiService, type JsonResult } from '../../core/api';
+import { ChangeBus } from '../../core/change-bus';
+import { FormDirty } from '../../core/form-dirty';
+import { reasonForField, type Violation, violationsOf } from '../../core/violations';
+
+/** The route the form creates through. */
+export const USERS_PATH = '/api/ocupilot/users';
+
+/** The form's own bootstrap read: the rules, their sentences and the roles the instance holds. */
+export const USERS_FORM_PATH = `${USERS_PATH}/form`;
+
+/** The blur check: whether a name is already taken. A read of the instance, not a validation. */
+export const USERS_NAME_PATH = `${USERS_PATH}/name`;
+
+/** The entity type and scope every change this form publishes carries (AD-13, AD-14). */
+export const USER_ENTITY = 'user';
+
+export const USER_SCOPE = 'instance';
+
+/** The one secret field. Held apart from the edit buffer, never snapshotted and never echoed. */
+export const PASSWORD_FIELD = 'Password';
+
+/** The roles field, whose value is a list of role names rather than text. */
+export const ROLES_FIELD = 'Roles';
+
+/** The text fields the form sets, in the order the classic editor draws them. */
+export const TEXT_FIELDS = ['Name', 'FullName', 'ExpirationDate', 'NameSpace', 'Routine'] as const;
+
+/** One role the instance holds, as `GET /users/form` projects it. */
+export interface RoleOption {
+  readonly name: string;
+  /** Whether the server refuses to grant it from OcuPilot (AD-10). The client decides nothing. */
+  readonly privileged: boolean;
+}
+
+/** One rule the server applies, with the sentence it refuses with (AD-39). */
+export interface FieldRule {
+  readonly field: string;
+  readonly code: string;
+  readonly reason: string;
+}
+
+/** The bootstrap read's whole answer. */
+export interface UserFormRules {
+  readonly requiredFields: readonly string[];
+  readonly maxLengths: Readonly<Record<string, number>>;
+  readonly rules: readonly FieldRule[];
+  readonly roles: readonly RoleOption[];
+  /** The server's sentence for a role it refuses to grant, which a privileged role is described by. */
+  readonly rolesReason: string;
+}
+
+/** The machine code the blur look-up reports a taken name under -- the server's own (AD-39). */
+export const NAME_TAKEN_CODE = 'USER.NAME.TAKEN';
+
+type TextBuffer = Record<string, string>;
+
+function textAt(source: unknown, key: string): string {
+  if (source === null || typeof source !== 'object') return '';
+  const value = (source as Record<string, unknown>)[key];
+  if (typeof value === 'string') return value;
+  if (typeof value === 'number') return String(value);
+  return '';
+}
+
+function arrayAt(source: unknown, key: string): readonly unknown[] {
+  if (source === null || typeof source !== 'object') return [];
+  const value = (source as Record<string, unknown>)[key];
+  return Array.isArray(value) ? value : [];
+}
+
+function emptyBuffer(): TextBuffer {
+  const out: TextBuffer = {};
+  for (const field of TEXT_FIELDS) out[field] = '';
+  return out;
+}
+
+function emptyRules(): UserFormRules {
+  return { requiredFields: [], maxLengths: {}, rules: [], roles: [], rolesReason: '' };
+}
+
+/**
+ * The create-a-user form's store (AD-19, AD-55).
+ *
+ * **It composes no payload of its own.** `POST /users` resolves the same tool class the agent's
+ * `permissions.users.create` does, so the screen's Save and the agent's confirm are two callers of
+ * one operation, and every field sentence -- including the refusal of a privileged role -- is the
+ * server's (AD-39).
+ *
+ * **The password is a secret end to end** (AD-3, AD-35). It is held in one private member apart
+ * from the edit buffer, sent once in the Save body, and cleared on an accepted Save, on `reset()`
+ * and before the store is carried across the create's own route replacement. It is never part of a
+ * refusal snapshot, so a refused Save leaves no copy of it behind.
+ */
+@Injectable({ providedIn: 'root' })
+export class UserCreateForm {
+  private readonly injector = inject(Injector);
+
+  private readonly formDirty = inject(FormDirty);
+
+  private readonly listeners = new Set<() => void>();
+
+  private generation = 0;
+
+  private buffer: TextBuffer = emptyBuffer();
+
+  private passwordValue = '';
+
+  private rolesValue: readonly string[] = [];
+
+  private rulesValue: UserFormRules = emptyRules();
+
+  private loadedValue = false;
+
+  private savingValue = false;
+
+  private violationList: readonly Violation[] = [];
+
+  private envelopeReason = '';
+
+  private refusalCodeValue = '';
+
+  private refusalPairValue = '';
+
+  private refusedValues: Record<string, string> = {};
+
+  private savedValue = false;
+
+  private createdIdValue = '';
+
+  private retainingValue = false;
+
+  subscribe(listener: () => void): () => void {
+    this.listeners.add(listener);
+    return () => {
+      this.listeners.delete(listener);
+    };
+  }
+
+  // --- reads -----------------------------------------------------------------------------------
+
+  loaded(): boolean {
+    return this.loadedValue;
+  }
+
+  busy(): boolean {
+    return this.savingValue;
+  }
+
+  value(field: string): string {
+    return this.buffer[field] ?? '';
+  }
+
+  /** What the password field holds now: what was typed since the last clear, or `''`. */
+  password(): string {
+    return this.passwordValue;
+  }
+
+  rules(): UserFormRules {
+    return this.rulesValue;
+  }
+
+  /** Whether role `name` is ticked. */
+  roleChecked(name: string): boolean {
+    return this.rolesValue.includes(name);
+  }
+
+  violations(): readonly Violation[] {
+    return this.violationList;
+  }
+
+  violationFor(field: string): string {
+    return reasonForField(this.violationList, field);
+  }
+
+  reason(): string {
+    return this.envelopeReason;
+  }
+
+  refusalCode(): string {
+    return this.refusalCodeValue;
+  }
+
+  refusalPair(): string {
+    return this.refusalPairValue;
+  }
+
+  /** Whether the last Save was accepted, which the sticky bar and the password caption read. */
+  saved(): boolean {
+    return this.savedValue;
+  }
+
+  /** The created account's id, or `''`. */
+  createdId(): string {
+    return this.createdIdValue;
+  }
+
+  dirty(): boolean {
+    return this.formDirty.dirty();
+  }
+
+  /** Whether this store is being carried across the create's own route replacement. */
+  retaining(): boolean {
+    return this.retainingValue;
+  }
+
+  /** Keep this form's state across the one navigation that is not a departure, less the password. */
+  retainAcrossRouteReplacement(): void {
+    this.passwordValue = '';
+    this.retainingValue = true;
+  }
+
+  /** Whether `field` is one a Save must carry. */
+  required(field: string): boolean {
+    return this.rulesValue.requiredFields.includes(field);
+  }
+
+  /** The length `field` stores on this instance, or `0` when the instance declares none. */
+  maxLength(field: string): number {
+    const held = this.rulesValue.maxLengths[field];
+    return typeof held === 'number' ? held : 0;
+  }
+
+  // --- writes ----------------------------------------------------------------------------------
+
+  /** Forget everything, the password first: from the sign-out teardown and when the form is left. */
+  reset(): void {
+    this.passwordValue = '';
+    this.generation += 1;
+    this.buffer = emptyBuffer();
+    this.rolesValue = [];
+    this.loadedValue = false;
+    this.savingValue = false;
+    this.violationList = [];
+    this.clearRefusal();
+    this.savedValue = false;
+    this.createdIdValue = '';
+    this.retainingValue = false;
+    this.formDirty.reset();
+    this.notify();
+  }
+
+  /** Open the form: read the rules and roles the server applies, on every open rather than cached. */
+  async open(): Promise<void> {
+    // A create that has replaced its own route with the new account's URL is not an arrival at
+    // another form: the state on screen is that account's.
+    if (this.retainingValue) {
+      this.retainingValue = false;
+      return;
+    }
+    this.reset();
+    const generation = this.generation;
+    const result = await this.api().requestJson<unknown>(USERS_FORM_PATH);
+    if (generation !== this.generation) return;
+    if (result.kind === 'ok') {
+      this.rulesValue = absorbRules(result.body);
+    } else {
+      this.envelopeReason = result.kind === 'error' ? (result.reason ?? '') : '';
+      this.rememberRefusal(result, {});
+    }
+    this.loadedValue = true;
+    this.notify();
+  }
+
+  setValue(field: string, value: string): void {
+    if (this.buffer[field] === value) return;
+    this.buffer = { ...this.buffer, [field]: value };
+    this.clearFieldViolation(field);
+    this.markDirty();
+    this.notify();
+  }
+
+  setPassword(value: string): void {
+    if (this.passwordValue === value) return;
+    this.passwordValue = value;
+    this.clearFieldViolation(PASSWORD_FIELD);
+    this.markDirty();
+    this.notify();
+  }
+
+  /**
+   * Tick or untick one role. A role the server marked privileged is never added: its checkbox is
+   * drawn disabled, and this keeps a stray event from putting it in the body anyway. The server's
+   * refusal of it is the authority either way (AD-10).
+   */
+  setRole(name: string, on: boolean): void {
+    if (this.roleChecked(name) === on) return;
+    if (on && this.rulesValue.roles.some((role) => role.name === name && role.privileged)) return;
+    this.rolesValue = on
+      ? this.rulesValue.roles.map((role) => role.name).filter((role) => role === name || this.roleChecked(role))
+      : this.rolesValue.filter((entry) => entry !== name);
+    this.clearFieldViolation(ROLES_FIELD);
+    this.markDirty();
+    this.notify();
+  }
+
+  /**
+   * On blur: drop a refusal that no longer describes what the field holds, render the server's
+   * required-field sentence on an empty required field, and -- for the name alone -- ask the
+   * instance whether it is still free. A look-up that could not be made leaves the field unmarked.
+   */
+  async onBlur(field: string): Promise<void> {
+    this.dropStaleViolation(field);
+    this.markEmptyRequired(field);
+    if (field !== 'Name') return;
+    const name = this.value('Name');
+    if (name === '') return;
+    const generation = this.generation;
+    const result = await this.api().requestJson<unknown>(
+      `${USERS_NAME_PATH}?name=${encodeURIComponent(name)}`
+    );
+    if (generation !== this.generation) return;
+    if (result.kind !== 'ok') return;
+    if (this.value('Name') !== name) return;
+    const body = result.body;
+    const available = body !== null && typeof body === 'object'
+      ? (body as Record<string, unknown>)['available'] === true
+      : true;
+    if (available) return;
+    const reason = textAt(body, 'reason');
+    if (reason === '') return;
+    this.violationList = [
+      ...this.violationList.filter((entry) => entry.field !== 'Name'),
+      { field: 'Name', code: NAME_TAKEN_CODE, reason },
+    ];
+    this.notify();
+  }
+
+  /** Drop a refusal that no longer describes what the field holds, compared with what was refused. */
+  dropStaleViolation(field: string): void {
+    const refused = this.refusedValues[field];
+    if (refused === undefined) return;
+    if (this.currentText(field) === refused) return;
+    if (this.violationList.every((entry) => entry.field !== field)) return;
+    this.clearFieldViolation(field);
+    this.notify();
+  }
+
+  /**
+   * Save: post the field set and report whether the instance accepted it.
+   *
+   * An accepted Save clears the password before anything is published, and publishes one change
+   * event with `created` (AD-14). A refused one keeps what was typed, the password included, so the
+   * user can correct the field the server named without typing the secret again.
+   */
+  async save(): Promise<boolean> {
+    if (this.busy()) return false;
+    const generation = this.generation;
+    this.savingValue = true;
+    this.violationList = [];
+    this.clearRefusal();
+    this.savedValue = false;
+    this.notify();
+
+    const sent = this.snapshotValues();
+    const result = await this.api().requestJson<unknown>(USERS_PATH, {
+      method: 'POST',
+      headers: { 'Content-Type': 'application/json' },
+      body: JSON.stringify(this.body()),
+    });
+    if (generation !== this.generation) return false;
+    this.savingValue = false;
+    if (result.kind !== 'ok') {
+      this.violationList = violationsOf(result);
+      this.envelopeReason =
+        this.violationList.length === 0 && result.kind === 'error' ? (result.reason ?? '') : '';
+      this.rememberRefusal(result, sent);
+      this.notify();
+      return false;
+    }
+    this.passwordValue = '';
+    this.createdIdValue = textAt(result.body, 'name');
+    this.savedValue = true;
+    this.formDirty.setDirty(false);
+    this.publishCreated();
+    this.notify();
+    return true;
+  }
+
+  // --- internals ------------------------------------------------------------------------------
+
+  private api(): ApiService {
+    return this.injector.get(ApiService);
+  }
+
+  private markDirty(): void {
+    this.formDirty.setDirty(true);
+  }
+
+  private clearFieldViolation(field: string): void {
+    if (this.violationList.every((entry) => entry.field !== field)) return;
+    this.violationList = this.violationList.filter((entry) => entry.field !== field);
+  }
+
+  /**
+   * Render the bootstrap read's first rule for an empty required field -- the server's
+   * required-ness sentence, listed first per field. A field the read published no rule for
+   * renders nothing. The name is left to the look-up and to Save.
+   */
+  private markEmptyRequired(field: string): void {
+    if (field === 'Name') return;
+    if (!this.required(field)) return;
+    if (this.currentText(field) !== '') return;
+    const rule = this.rulesValue.rules.find((entry) => entry.field === field);
+    if (rule === undefined) return;
+    if (this.violationList.some((entry) => entry.field === field)) return;
+    this.violationList = [...this.violationList, { field, code: rule.code, reason: rule.reason }];
+    this.notify();
+  }
+
+  /** The body: every field the create sends, typed as the wire expects (AD-4, AD-54). */
+  private body(): Record<string, unknown> {
+    const out: Record<string, unknown> = {};
+    out['Name'] = this.value('Name');
+    out[PASSWORD_FIELD] = this.passwordValue;
+    for (const field of TEXT_FIELDS) {
+      if (field !== 'Name') out[field] = this.value(field);
+    }
+    out[ROLES_FIELD] = [...this.rolesValue];
+    return out;
+  }
+
+  private currentText(field: string): string {
+    if (field === PASSWORD_FIELD) return this.passwordValue;
+    if (field === ROLES_FIELD) return this.rolesValue.join(',');
+    return this.value(field);
+  }
+
+  /** What a refusal is compared against later. Never the password (AD-35). */
+  private snapshotValues(): Record<string, string> {
+    const out: Record<string, string> = {};
+    for (const field of TEXT_FIELDS) out[field] = this.value(field);
+    out[ROLES_FIELD] = this.rolesValue.join(',');
+    return out;
+  }
+
+  private rememberRefusal(result: JsonResult<unknown>, sent: Record<string, string>): void {
+    if (result.kind !== 'error') {
+      this.clearRefusal();
+      return;
+    }
+    this.refusalCodeValue = result.code ?? '';
+    const pair = result.detail === null ? undefined : result.detail['failedPair'];
+    this.refusalPairValue = typeof pair === 'string' ? pair : '';
+    this.refusedValues = sent;
+  }
+
+  private clearRefusal(): void {
+    this.envelopeReason = '';
+    this.refusalCodeValue = '';
+    this.refusalPairValue = '';
+    this.refusedValues = {};
+  }
+
+  private publishCreated(): void {
+    if (this.createdIdValue === '') return;
+    this.injector.get(ChangeBus).publish({
+      kind: 'changed',
+      type: USER_ENTITY,
+      scope: USER_SCOPE,
+      id: this.createdIdValue,
+      action: 'created',
+    });
+  }
+
+  private notify(): void {
+    for (const listener of [...this.listeners]) listener();
+  }
+}
+
+/** The bootstrap read's body, narrowed. A member the server did not send reads as empty. */
+function absorbRules(body: unknown): UserFormRules {
+  const lengths: Record<string, number> = {};
+  const rawLengths = body !== null && typeof body === 'object'
+    ? (body as Record<string, unknown>)['maxLengths']
+    : null;
+  if (rawLengths !== null && typeof rawLengths === 'object' && !Array.isArray(rawLengths)) {
+    for (const [field, value] of Object.entries(rawLengths as Record<string, unknown>)) {
+      if (typeof value === 'number') lengths[field] = value;
+    }
+  }
+  const rules: FieldRule[] = [];
+  for (const entry of arrayAt(body, 'rules')) {
+    if (entry === null || typeof entry !== 'object') continue;
+    const field = textAt(entry, 'field');
+    const code = textAt(entry, 'code');
+    const reason = textAt(entry, 'reason');
+    if (field === '' || code === '' || reason === '') continue;
+    rules.push({ field, code, reason });
+  }
+  const roles: RoleOption[] = [];
+  for (const entry of arrayAt(body, 'roles')) {
+    if (entry === null || typeof entry !== 'object') continue;
+    const name = textAt(entry, 'name');
+    if (name === '' || roles.some((role) => role.name === name)) continue;
+    // Fail closed: a role whose flag the server did not send as false is drawn unavailable.
+    const flag = (entry as Record<string, unknown>)['privileged'];
+    roles.push({ name, privileged: !(flag === false || flag === 0) });
+  }
+  const required = arrayAt(body, 'requiredFields').filter(
+    (entry): entry is string => typeof entry === 'string'
+  );
+  return {
+    requiredFields: required,
+    maxLengths: lengths,
+    rules,
+    roles,
+    rolesReason: textAt(body, 'rolesReason'),
+  };
+}
diff --git a/ui/src/app/areas/web-applications/create-form.page.ts b/ui/src/app/areas/web-applications/create-form.page.ts
index 046c22c..a2273bb 100644
--- a/ui/src/app/areas/web-applications/create-form.page.ts
+++ b/ui/src/app/areas/web-applications/create-form.page.ts
@@ -257,11 +257,16 @@ interface FieldView {
               aria-required="true"
               [attr.maxlength]="maxLength('WSGIAppLocation')"
               [attr.aria-invalid]="directoryField.invalid"
-              [attr.aria-describedby]="directoryField.describedBy"
+              [attr.aria-describedby]="directoryDescribedBy"
               (input)="onText('WSGIAppLocation', $event)"
               (blur)="onBlur('WSGIAppLocation')"
             />
           </div>
+          @if (hasResolvedDirectory) {
+            <p class="ocu-field-caption" [id]="directoryField.id + '-resolved'">
+              {{ STRINGS.webAppFormPythonDirectoryResolved }} <code>{{ resolvedDirectory }}</code>
+            </p>
+          }
           @if (directoryField.invalid) {
             <p class="ocu-form-error" [id]="directoryField.id + '-reason'">{{ directoryField.reason }}</p>
           }
@@ -311,7 +316,12 @@ interface FieldView {
         }
       </div>
 
-      <fieldset class="ocu-field ocu-form-authe" [attr.id]="autheField.id" tabindex="-1">
+      <fieldset
+        class="ocu-field ocu-form-authe"
+        [attr.id]="autheField.id"
+        tabindex="-1"
+        [attr.aria-describedby]="unauthenticatedFlag ? autheField.id + '-effect' : null"
+      >
         <legend class="ocu-field-label ocu-field-label-required">{{ STRINGS.serviceColumnAuthentication }}</legend>
         @for (method of autheMethods; track method.bit) {
           <label class="ocu-field-checkbox">
@@ -324,6 +334,9 @@ interface FieldView {
             <span>{{ method.label }}</span>
           </label>
         }
+        @if (unauthenticatedFlag) {
+          <p class="ocu-field-caption" [id]="autheField.id + '-effect'">{{ STRINGS.webAppUnauthenticatedEffect }}</p>
+        }
         @if (autheField.invalid) {
           <p class="ocu-form-error" [id]="autheField.id + '-reason'">{{ autheField.reason }}</p>
         }
@@ -480,6 +493,31 @@ export class WebAppCreateFormPage {
     return this.store.rules().authenticationMethods;
   }
 
+  /** Whether the Unauthenticated method is ticked, which is when its effect is stated (DW-1489). */
+  protected get unauthenticatedFlag(): boolean {
+    this.generation();
+    return this.store.unauthenticated();
+  }
+
+  /** The directory the typed name resolves under on this instance, shown read-only (AD-21). */
+  protected get resolvedDirectory(): string {
+    this.generation();
+    return this.store.resolvedDirectory();
+  }
+
+  protected get hasResolvedDirectory(): boolean {
+    return this.resolvedDirectory !== '';
+  }
+
+  /** The directory field's described-by: its resolved line, then its refusal. */
+  protected get directoryDescribedBy(): string | null {
+    const field = this.directoryField;
+    const described: string[] = [];
+    if (this.hasResolvedDirectory) described.push(`${field.id}-resolved`);
+    if (field.describedBy !== null) described.push(field.describedBy);
+    return described.length === 0 ? null : described.join(' ');
+  }
+
   protected get showSaved(): boolean {
     this.generation();
     return this.store.saved();
diff --git a/ui/src/app/areas/web-applications/create-form.store.spec.ts b/ui/src/app/areas/web-applications/create-form.store.spec.ts
index 0bdf935..c302052 100644
--- a/ui/src/app/areas/web-applications/create-form.store.spec.ts
+++ b/ui/src/app/areas/web-applications/create-form.store.spec.ts
@@ -42,6 +42,7 @@ const RULES = {
   conditionalFields: { rest: ['DispatchClass', 'WSGICallable'], wsgi: ['WSGIAppName', 'WSGIAppLocation', 'WSGIType'] },
   wsgiTypes: ['WSGI', 'ASGI'],
   maxLengths: { Name: 64, Description: 256 },
+  wsgiRoot: '/durable/iris/mgr/wsgi/',
   rules: [
     { field: 'NameSpace', code: 'WEBAPP.NAMESPACE.REQUIRED', reason: 'Choose the namespace.' },
     { field: 'WSGIAppName', code: 'WEBAPP.WSGI.APPNAME', reason: 'Name the file.' },
@@ -233,6 +234,32 @@ describe('the Web application create form store', () => {
     expect('WSGIAppName' in rest).toBe(false);
   });
 
+  it('AD-21: the resolved directory is the read\'s root plus the typed name, displayed and never sent', async () => {
+    const { store, calls } = mount();
+    await store.open();
+    store.setType(TYPE_PYTHON);
+    expect(store.resolvedDirectory()).toBe('/durable/iris/mgr/wsgi/');
+    store.setValue('WSGIAppLocation', 'probeapp');
+    expect(store.resolvedDirectory()).toBe('/durable/iris/mgr/wsgi/probeapp/');
+    store.setValue('Name', '/csp/probe');
+    store.setValue('NameSpace', 'HSCUSTOM');
+    await store.save();
+    // The body sends the name as typed; the server resolves it under the root at the call.
+    const python = postedBody(calls);
+    expect(python['WSGIAppLocation']).toBe('probeapp');
+    expect('Path' in python).toBe(false);
+  });
+
+  it('DW-1489: the unauthenticated effect follows the Unauthenticated method being ticked', async () => {
+    const { store } = mount();
+    await store.open();
+    expect(store.unauthenticated()).toBe(false);
+    store.setAuthe(64, true);
+    expect(store.unauthenticated()).toBe(true);
+    store.setAuthe(64, false);
+    expect(store.unauthenticated()).toBe(false);
+  });
+
   it('the blur look-up marks a taken name with the server sentence, and leaves the field unmarked when it could not be made', async () => {
     const taken = mount(undefined, {
       kind: 'ok',
diff --git a/ui/src/app/areas/web-applications/create-form.store.ts b/ui/src/app/areas/web-applications/create-form.store.ts
index 321550f..06e742f 100644
--- a/ui/src/app/areas/web-applications/create-form.store.ts
+++ b/ui/src/app/areas/web-applications/create-form.store.ts
@@ -40,6 +40,12 @@ export const WRITABLE_FIELDS = [
   'AutheEnabled',
 ] as const;
 
+/**
+ * The vendor's `AutheEnabled` bit for the Unauthenticated method. The form states that method's
+ * effect while it is ticked (DW-1489); the kernel marks a proposal's `consequence` on the same bit.
+ */
+export const UNAUTHENTICATED_BIT = 64;
+
 /** The fields whose value is a JSON boolean on the wire. */
 const BOOLEAN_FIELDS: readonly string[] = ['Enabled', 'Recurse'];
 
@@ -85,6 +91,12 @@ export interface FormRules {
   readonly wsgiTypes: readonly string[];
   readonly maxLengths: Readonly<Record<string, number>>;
   readonly rules: readonly FieldRule[];
+  /**
+   * The fixed directory a Python application's directory name resolves under, as the server
+   * computed it for this read (AD-21), or `''`. Display only: the server resolves the name again
+   * at the call and sends the resolved directory itself.
+   */
+  readonly wsgiRoot: string;
 }
 
 /** The edit buffer: text for an input, a flag for a checkbox. */
@@ -162,6 +174,7 @@ export class WebAppCreateForm {
     wsgiTypes: [],
     maxLengths: {},
     rules: [],
+    wsgiRoot: '',
   };
 
   private loadedValue = false;
@@ -224,6 +237,23 @@ export class WebAppCreateForm {
     return this.autheBits.includes(bit);
   }
 
+  /** Whether the Unauthenticated method is ticked, which the form states the effect of (DW-1489). */
+  unauthenticated(): boolean {
+    return this.autheChecked(UNAUTHENTICATED_BIT);
+  }
+
+  /**
+   * The directory the typed application directory name resolves to on this instance, for display:
+   * the bootstrap read's `wsgiRoot` followed by the name and a separator, or `''` while the read
+   * published no root. It is not validated here and never sent; the server resolves the name.
+   */
+  resolvedDirectory(): string {
+    const root = this.rulesValue.wsgiRoot;
+    if (root === '') return '';
+    const name = this.value('WSGIAppLocation');
+    return name === '' ? root : `${root}${name}/`;
+  }
+
   /** The mask the ticked methods add up to, which is what the body sends. */
   autheMask(): number {
     return this.autheBits.reduce((total, bit) => total + bit, 0);
@@ -638,5 +668,6 @@ function absorbRules(body: unknown): FormRules {
     wsgiTypes: stringsAt(body, 'wsgiTypes'),
     maxLengths: lengths,
     rules,
+    wsgiRoot: textAt(body, 'wsgiRoot'),
   };
 }
diff --git a/ui/src/app/core/navigation.ts b/ui/src/app/core/navigation.ts
index 80ff834..13fadc7 100644
--- a/ui/src/app/core/navigation.ts
+++ b/ui/src/app/core/navigation.ts
@@ -194,10 +194,14 @@ export function createFormFor(screen: ScreenDeclaration): ScreenDeclaration | nu
 /**
  * Paired forms that create and never open an existing entity: their `:id` route exists so a Save
  * can replace the URL with the new entity's, but the page reads no id, so neither a row's name cell
- * nor a change toast may open one. The Web application form (Story 8.1) is the create half of that
- * list's editor; Epic 9's editor reads the id, and its story removes the entry.
- */
-export const CREATE_ONLY_FORMS: ReadonlySet<string> = new Set(['OcuPilot.Screen.Descriptor.WebAppForm']);
+ * nor a change toast may open one. The Web application form (Story 8.1) and the create-a-user form
+ * (Story 8.2) are the create halves of their lists' editors; Epic 9's editors read the id, and
+ * their stories remove the entries.
+ */
+export const CREATE_ONLY_FORMS: ReadonlySet<string> = new Set([
+  'OcuPilot.Screen.Descriptor.WebAppForm',
+  'OcuPilot.Screen.Descriptor.UserForm',
+]);
 
 /**
  * The route segment a list's own document viewer is declared under, appended to the list's route.
diff --git a/ui/src/app/core/proposal-view.ts b/ui/src/app/core/proposal-view.ts
index a1e0c3e..34ea314 100644
--- a/ui/src/app/core/proposal-view.ts
+++ b/ui/src/app/core/proposal-view.ts
@@ -17,9 +17,8 @@
  *
  * **The screen's two facts arrive as parameters, not as a module-level lookup.** The singular
  * entity noun and the declared secret argument names both come from the screen mirror, and
- * reading `screens.generated.ts` here would make the masked field untestable until the first real
- * secret ships: no descriptor declares one yet (`Kernel/Proposal/Mint.cls` refuses a secret
- * argument outright), so a test has to be able to supply a screen record as data.
+ * reading `screens.generated.ts` here would tie the masked field's tests to whichever descriptor
+ * declares a secret, so a test supplies a screen record as data.
  */
 
 import { STRINGS } from './strings.ts';
@@ -99,6 +98,23 @@ export interface ProposalCardView {
    * copy and invents no phase -- the row is still live, and a phase is a terminal state.
    */
   readonly refusalReason?: string;
+  /**
+   * The kernel's code for what the write does beyond its diff, or `''`; the wire's own value,
+   * projected the way `auditWarning` is. The card reads it through `consequenceSentence`.
+   */
+  readonly consequence?: string;
+}
+
+/** The consequence the kernel marks a web-application create that admits unauthenticated access with. */
+export const CONSEQUENCE_UNAUTHENTICATED = 'WEBAPP.UNAUTHENTICATED';
+
+/**
+ * The published sentence for a proposal's `consequence` code, or `''` for no code or one this
+ * client publishes nothing for. The sentence is `STRINGS`'; the code is the kernel's (AD-39).
+ */
+export function consequenceSentence(code: string | undefined): string {
+  if (code === CONSEQUENCE_UNAUTHENTICATED) return STRINGS.webAppUnauthenticatedEffect;
+  return '';
 }
 
 /**
@@ -285,7 +301,8 @@ function maskedRow(row: ProposalDiffRow): ProposalDiffRow {
  * declares, `secretArguments` the names that screen declares secret, and everything else is
  * `proposal`'s own -- the instance-computed diff with every declared secret masked on both sides,
  * the unchanged count and the instance's own already-masked unchanged rows, the agent's two
- * blocks, the reversal, the expiry, the audit warning and the tool's destructive declaration.
+ * blocks, the reversal, the expiry, the audit warning, the tool's destructive declaration and the
+ * kernel's consequence code.
  * `maskedFields` is `secretArguments` narrowed to the names this proposal's own payload carries
  * (`payloadSecrets`).
  * `refusalReason` is the envelope's own sentence for a decision the instance refused on a row it
@@ -293,8 +310,7 @@ function maskedRow(row: ProposalDiffRow): ProposalDiffRow {
  * caller's to hold, and nothing here writes it.
  *
  * Both of the screen's facts are parameters rather than a lookup of `screens.generated.ts` here,
- * for the reason the header gives: no shipped descriptor declares a secret argument yet, so a test
- * has to be able to supply one as data.
+ * for the reason the header gives.
  *
  * Nothing here writes a proposal value down (AD-6).
  */
@@ -319,6 +335,7 @@ export function toCardView(
     maskedFields: payloadSecrets(proposal, secretArguments),
     auditWarning: proposal.auditWarning,
     destructive: proposal.destructive,
+    consequence: proposal.consequence,
     refusalReason,
   };
 }
@@ -334,9 +351,9 @@ export function toCardView(
  * restriction rather than a consequence of the merge: `Confirm.WithSecrets` calls `%Set` for every
  * declared name the body supplied, which **adds** one the payload does not carry.
  * The payload's own field names are its diff rows and its unchanged rows -- together the
- * projection of the stored body the instance published (AD-4); a secret is never a diff row,
- * because the mint refuses a secret argument outright, so in practice it is the unchanged half
- * that names one.
+ * projection of the stored body the instance published (AD-4). The mint refuses a secret argument
+ * outright, so an update names a secret in its unchanged half; a create names one as the diff row
+ * the kernel composes for it, masked on both sides.
  */
 function payloadSecrets(
   proposal: TurnProposal,
diff --git a/ui/src/app/core/screens.generated.ts b/ui/src/app/core/screens.generated.ts
index 1741de1..ac431e0 100644
--- a/ui/src/app/core/screens.generated.ts
+++ b/ui/src/app/core/screens.generated.ts
@@ -5782,6 +5782,64 @@ export const SCREENS: readonly ScreenDeclaration[] = [
     "fingerprintExcludes": [],
     "entityLabelKey": ""
   },
+  {
+    "descriptor": "OcuPilot.Screen.Descriptor.UserForm",
+    "route": "permissions/users/edit",
+    "area": "permissions",
+    "labelKey": "userFormLabel",
+    "sideBarPosition": 0,
+    "archetype": "form-page",
+    "built": true,
+    "refreshes": false,
+    "refreshRates": [],
+    "privileges": [
+      {
+        "resource": "%Admin_Secure",
+        "permission": "USE"
+      },
+      {
+        "resource": "%DB_IRISSYS",
+        "permission": "READ"
+      }
+    ],
+    "entityType": "user",
+    "secondaryEntityTypes": [],
+    "scope": "instance",
+    "parentScope": "",
+    "id": {
+      "kind": "single",
+      "parts": []
+    },
+    "primaryAction": {
+      "id": "",
+      "selfProtection": ""
+    },
+    "rowActions": [],
+    "context": {
+      "fields": [],
+      "secretFields": [
+        "Password"
+      ]
+    },
+    "secretArguments": [],
+    "fingerprintExcludes": [],
+    "emptyStateKey": "",
+    "commandAliases": [],
+    "classicPage": "%CSP.UI.Portal.User",
+    "classicLinkExemption": {
+      "exempt": false,
+      "reason": "",
+      "label": "",
+      "href": ""
+    },
+    "toolIdentifier": "permissions.userform",
+    "read": null,
+    "table": null,
+    "banner": null,
+    "tab": null,
+    "rowTarget": null,
+    "entityLabelKey": ""
+  },
   {
     "descriptor": "OcuPilot.Screen.Descriptor.UserList",
     "route": "permissions/users",
@@ -5811,7 +5869,7 @@ export const SCREENS: readonly ScreenDeclaration[] = [
       "parts": []
     },
     "primaryAction": {
-      "id": "",
+      "id": "create",
       "selfProtection": ""
     },
     "rowActions": [],
@@ -5827,6 +5885,9 @@ export const SCREENS: readonly ScreenDeclaration[] = [
       ],
       "secretFields": []
     },
+    "secretArguments": [
+      "Password"
+    ],
     "emptyStateKey": "userListEmpty",
     "commandAliases": [
       "accounts"
@@ -5919,14 +5980,13 @@ export const SCREENS: readonly ScreenDeclaration[] = [
           "kind": "identifier"
         }
       ],
-      "emptyNextKey": "tableReadOnlyEmptyNext",
-      "emptyAgentKey": ""
+      "emptyNextKey": "",
+      "emptyAgentKey": "userListEmptyAgent"
     },
     "toolIdentifier": "permissions.users",
     "banner": null,
     "tab": null,
     "rowTarget": null,
-    "secretArguments": [],
     "fingerprintExcludes": [],
     "entityLabelKey": ""
   },
diff --git a/ui/src/app/core/strings.ts b/ui/src/app/core/strings.ts
index f60b74f..3744bcd 100644
--- a/ui/src/app/core/strings.ts
+++ b/ui/src/app/core/strings.ts
@@ -1345,6 +1345,30 @@ export const STRINGS = {
   /** EXPERIENCE.md:396 */
   webAppFormRefusedAction: 'create a web application',
 
+  // Story 8.2. The Users list becomes write-capable, so its empty state invites the agent, and the
+  // create-a-user form publishes the labels the classic editor carries that no existing key holds.
+  // Name, full name, password and roles are keys that already exist (`tableColumnName`,
+  // `userColumnFullName`, `fieldPassword`, `userColumnRoles`). The last two are the web-application
+  // form's resolved Python directory line and the unauthenticated effect, which the proposal card
+  // reads too.
+  /** EXPERIENCE.md:397 */
+  userListEmptyAgent: 'create a user account',
+  /** EXPERIENCE.md:398 */
+  userFormLabel: 'New user',
+  /** EXPERIENCE.md:398 */
+  userFormExpiry: 'Account expiration date',
+  /** EXPERIENCE.md:398 */
+  userFormNamespace: 'Startup namespace',
+  /** EXPERIENCE.md:398 */
+  userFormRoutine: 'Startup tag^routine',
+  /** EXPERIENCE.md:398 */
+  userFormRefusedAction: 'create a user',
+  /** EXPERIENCE.md:399 */
+  webAppFormPythonDirectoryResolved: 'Resolved directory on this instance',
+  /** EXPERIENCE.md:400 */
+  webAppUnauthenticatedEffect:
+    'Anyone who can reach this address can use the application without signing in.',
+
 } as const;
 
 /**
diff --git a/ui/src/app/core/turn.ts b/ui/src/app/core/turn.ts
index de7f1e1..7dadc2e 100644
--- a/ui/src/app/core/turn.ts
+++ b/ui/src/app/core/turn.ts
@@ -229,6 +229,11 @@ export interface TurnProposal {
    * fall out of step.
    */
   readonly destructive: boolean;
+  /**
+   * The kernel's code for what the write does beyond its diff (`WEBAPP.UNAUTHENTICATED`), or `''`.
+   * A code, not a sentence: the card resolves it to the published string (`consequenceSentence`).
+   */
+  readonly consequence: string;
 }
 
 /**
@@ -488,6 +493,7 @@ function parseProposal(value: unknown): TurnProposal | null {
     confirmedAt: textAt(row, 'confirmedAt'),
     auditWarning: boolAt(row, 'auditWarning'),
     destructive: boolAt(row, 'destructive'),
+    consequence: textAt(row, 'consequence'),
   };
 }
 
diff --git a/ui/src/app/shell/proposal-card.spec.ts b/ui/src/app/shell/proposal-card.spec.ts
index c4654bb..b7e143b 100644
--- a/ui/src/app/shell/proposal-card.spec.ts
+++ b/ui/src/app/shell/proposal-card.spec.ts
@@ -621,6 +621,22 @@ describe('the proposal card', () => {
     expect(mount(liveView(), { phase: 'live' }).card.querySelector('.ocu-proposal-card-warning')).toBeNull();
   });
 
+  it('DW-1489: a proposal the kernel marks with the unauthenticated consequence states it on the card', () => {
+    // The code is the kernel's and the sentence the string source's; the create is still offered.
+    //
+    // Mutation (Rule 19): drop the consequence block from the template, or make
+    // `consequenceSentence` answer '' -> this goes red.
+    const { card } = mount(liveView({ consequence: 'WEBAPP.UNAUTHENTICATED' }), { phase: 'live' });
+    const line = card.querySelector('[data-slot="consequence"]') as HTMLElement;
+    expect(line).not.toBeNull();
+    expect(line.textContent).toContain(STRINGS.webAppUnauthenticatedEffect);
+    expect(line.getAttribute('role')).toBe('status');
+    expect(card.querySelector('.ocu-proposal-card-confirm')).not.toBeNull();
+
+    expect(mount(liveView({ consequence: '' }), { phase: 'live' }).card.querySelector('[data-slot="consequence"]')).toBeNull();
+    expect(mount(EXAMPLE_PROPOSAL).card.querySelector('[data-slot="consequence"]')).toBeNull();
+  });
+
   it('the in-card warning is a status region, the convention for an advisory', () => {
     // DW-1246, corrected: four warning banners in this shell are already `role="status"`. The
     // convention is `alert` for a fault or a refusal and `status` for an advisory, and this is an
diff --git a/ui/src/app/shell/proposal-card.ts b/ui/src/app/shell/proposal-card.ts
index 692fb8c..2529a51 100644
--- a/ui/src/app/shell/proposal-card.ts
+++ b/ui/src/app/shell/proposal-card.ts
@@ -11,6 +11,7 @@ import {
 
 import {
   type ProposalPhase,
+  consequenceSentence,
   countdownPhase,
   countdownRemaining,
   formatCountdown,
@@ -216,6 +217,17 @@ export interface ProposalConfirmRequest {
       </p>
     }
 
+    @if (consequenceVisible) {
+      <p
+        class="ocu-banner ocu-banner-warning ocu-proposal-card-warning"
+        role="status"
+        data-slot="consequence"
+      >
+        <span class="ocu-banner-glyph" aria-hidden="true">{{ bannerGlyph }}</span>
+        <span class="ocu-banner-message">{{ consequenceText }}</span>
+      </p>
+    }
+
     @if (refusalVisible) {
       <p
         class="ocu-banner ocu-banner-warning ocu-proposal-card-warning"
@@ -555,6 +567,18 @@ export class ProposalCard {
     return this.phase() !== null && this.view().auditWarning === true;
   }
 
+  /**
+   * The published sentence for the kernel's `consequence` code, an advisory beside the audit
+   * warning: the write is still offered (DW-1489). `''` where the code has no sentence.
+   */
+  protected get consequenceText(): string {
+    return consequenceSentence(this.view().consequence);
+  }
+
+  protected get consequenceVisible(): boolean {
+    return this.phase() !== null && this.consequenceText !== '';
+  }
+
   /**
    * Whether the tool declared this write destructive, which turns the card's left-edge bar and its
    * Confirm to the destructive treatment (DESIGN.md `:1176`, `:1243`).
diff --git a/ui/src/app/shell/screen-outlet.ts b/ui/src/app/shell/screen-outlet.ts
index 788d669..b65051b 100644
--- a/ui/src/app/shell/screen-outlet.ts
+++ b/ui/src/app/shell/screen-outlet.ts
@@ -24,6 +24,7 @@ import { HomePage } from '../areas/home/home.page';
 import { AuditPage } from '../areas/logs/audit.page';
 import { ErrorLogPage } from '../areas/logs/error-log.page';
 import { LogViewerPage } from '../areas/logs/log-viewer.page';
+import { UserCreateFormPage } from '../areas/permissions/user-create-form.page';
 import { WebAppCreateFormPage } from '../areas/web-applications/create-form.page';
 import { OpenApiViewerPage } from '../areas/web-applications/openapi-viewer.page';
 import { decodeEntityId } from '../core/entity-id';
@@ -93,6 +94,7 @@ export const DESCRIPTOR_PAGES: Readonly<Record<string, Type<unknown>>> = {
   'OcuPilot.Screen.Descriptor.DatabaseFreeSpace': DatabasesPage,
   'OcuPilot.Screen.Descriptor.DatabaseDetails': DatabaseDetailsPage,
   'OcuPilot.Screen.Descriptor.WebAppForm': WebAppCreateFormPage,
+  'OcuPilot.Screen.Descriptor.UserForm': UserCreateFormPage,
 };
 
 /**
diff --git a/ui/tools/field-lists.mjs b/ui/tools/field-lists.mjs
index e801e7a..00772ce 100644
--- a/ui/tools/field-lists.mjs
+++ b/ui/tools/field-lists.mjs
@@ -17,6 +17,12 @@
  * segment matches the credential pattern (Conventions, Secrets) classified anything but `secret`
  * is refused; a boolean or number never is.
  *
+ * **An entry may author a wrapper field** (`authored`, AD-3): a top-level name the endpoint's
+ * request wrapper carries and its template does not -- `Security.User`'s POST `Password` -- mapped
+ * to `secret`, the only class accepted. It is emitted as its own `secret` literal row marked
+ * `authored: true`. A name that is not one top-level segment, or that collides with a derived
+ * path, is refused.
+ *
  * Usage: `node tools/field-lists.mjs` writes `ToolFields.cls`; `--check` reports and exits 1 on
  * a refusal or when the committed file differs from what would be written.
  */
@@ -46,7 +52,13 @@ export const CLASSES = ['ordinary', 'secret', 'opaque'];
 export const DEFAULT_CLASS = 'secret';
 
 /** The keys an entry may carry. */
-export const ENTRY_KEYS = ['fieldList', 'classification', 'required', 'enum', 'description'];
+export const ENTRY_KEYS = ['fieldList', 'classification', 'authored', 'required', 'enum', 'description'];
+
+/** The one class an authored wrapper field may carry. */
+export const AUTHORED_CLASS = 'secret';
+
+/** An authored field's name: one top-level segment, no member and no element. */
+const AUTHORED_NAME_RE = /^[A-Za-z][A-Za-z0-9]*$/;
 
 /** The keys reserved for the semantic half of a schema; until that half is defined, only empty. */
 export const RESERVED_KEYS = ['required', 'enum', 'description'];
@@ -239,6 +251,27 @@ export function classify(lists, entries) {
       }
       return { ...row, class: value };
     });
+    if (entry.authored !== undefined) {
+      if (!isObject(entry.authored)) {
+        refuse('authored is not a JSON object of name to class');
+      } else {
+        for (const [name, value] of Object.entries(entry.authored)) {
+          if (!AUTHORED_NAME_RE.test(name)) {
+            refuse(`authored name ${JSON.stringify(name)} is not one top-level field name`);
+            continue;
+          }
+          if (rows.some((row) => row.path === name || extends_(row.path, name))) {
+            refuse(`authored name ${name} collides with a derived path of list ${entry.fieldList}`);
+            continue;
+          }
+          if (value !== AUTHORED_CLASS) {
+            refuse(`authored name ${name} is classified ${JSON.stringify(value)}; an authored field may only be ${AUTHORED_CLASS}`);
+            continue;
+          }
+          fields.push({ path: name, shape: 'literal', templateType: 'string', class: AUTHORED_CLASS, authored: true });
+        }
+      }
+    }
     if (problems.length === before) tools[tool] = { fieldList: entry.fieldList, fields };
   }
   return { tools, problems };
@@ -258,13 +291,23 @@ export function buildToolFields(tools) {
   const entries = names.map((name) => {
     const { fieldList, fields } = tools[name];
     const rows = fields.map((field) =>
-      JSON.stringify({
-        path: field.path,
-        shape: field.shape,
-        templateType: field.templateType,
-        itemType: field.itemType,
-        class: field.class,
-      })
+      JSON.stringify(
+        field.authored === true
+          ? {
+              path: field.path,
+              shape: field.shape,
+              templateType: field.templateType,
+              class: field.class,
+              authored: true,
+            }
+          : {
+              path: field.path,
+              shape: field.shape,
+              templateType: field.templateType,
+              itemType: field.itemType,
+              class: field.class,
+            }
+      )
     );
     const head = `${JSON.stringify(name)}: {"fieldList":${JSON.stringify(fieldList)},"fields":[`;
     return rows.length === 0 ? `${head}]}` : `${head}\n  ${rows.join(',\n  ')}\n]}`;
diff --git a/ui/tools/field-lists.test.mjs b/ui/tools/field-lists.test.mjs
index 3cdd940..3e0f9d7 100644
--- a/ui/tools/field-lists.test.mjs
+++ b/ui/tools/field-lists.test.mjs
@@ -311,3 +311,42 @@ test('Story 8.1: the create form writes exactly the fields its tool admits, plus
     'the form writes exactly the tool\'s admitted fields plus the identifying name'
   );
 });
+
+// Story 8.2, AD-3: an entry may author a wrapper field the endpoint's template does not carry, as
+// `secret` and nothing else, and it is emitted as its own secret literal row marked `authored`.
+//
+// Mutation (Rule 19): drop the `fields.push(...)` of an authored row in classify() -> the emitted
+// row assertions go red; accept any class for an authored name -> the 'ordinary' refusal goes red.
+test('an authored wrapper field is emitted secret and marked, and every other shape is refused', () => {
+  const entry = (authored) => ({
+    'permissions.users.create': { fieldList: 'Security.User', classification: { Roles: 'opaque', EscalationRoles: 'opaque' }, authored },
+  });
+  const accepted = classify(lists, entry({ Password: 'secret' }));
+  assert.deepEqual(accepted.problems, [], 'an authored secret is accepted');
+  assert.deepEqual(
+    fieldOf(accepted, 'permissions.users.create', 'Password'),
+    { path: 'Password', shape: 'literal', templateType: 'string', class: 'secret', authored: true },
+    'and emitted as a secret literal row marked authored'
+  );
+  const text = generateFrom({ lists, entries: entry({ Password: 'secret' }) }).text;
+  assert.match(text, /\{"path":"Password","shape":"literal","templateType":"string","class":"secret","authored":true\}/, 'ToolFields carries it');
+
+  const refusals = [
+    [{ Password: 'ordinary' }, /authored name Password is classified "ordinary"/],
+    [{ Password: 'opaque' }, /authored name Password is classified "opaque"/],
+    [{ FullName: 'secret' }, /authored name FullName collides with a derived path/],
+    [{ Roles: 'secret' }, /authored name Roles collides with a derived path/],
+    [{ 'User.Password': 'secret' }, /authored name "User.Password" is not one top-level field name/],
+    [{ 'Pass[]': 'secret' }, /authored name "Pass\[\]" is not one top-level field name/],
+  ];
+  for (const [authored, pattern] of refusals) {
+    const refused = generateFrom({ lists, entries: entry(authored) });
+    assert.equal(refused.text, null, `${JSON.stringify(authored)} emits nothing`);
+    assert.ok(refused.problems.some((problem) => pattern.test(problem)), `${JSON.stringify(authored)} is refused: ${refused.problems.join('; ')}`);
+  }
+  const notAnObject = generateFrom({ lists, entries: entry(['Password']) });
+  assert.ok(notAnObject.problems.some((problem) => /authored is not a JSON object/.test(problem)), 'an authored list that is not an object is refused');
+
+  // The committed entry is the one the Users list's create tool carries.
+  assert.deepEqual(committedEntries['permissions.users.create']?.authored, { Password: 'secret' }, 'the committed create entry authors Password');
+});
diff --git a/ui/tools/navigation.test.mjs b/ui/tools/navigation.test.mjs
index c89946f..4e6764c 100644
--- a/ui/tools/navigation.test.mjs
+++ b/ui/tools/navigation.test.mjs
@@ -151,6 +151,7 @@ test('a side bar lists only built screens, in side-bar order', () => {
       'tasks/on-demand',
       'tasks/upcoming',
       'tasks/history',
+      'permissions/users/edit',
       'permissions/users',
       'permissions/roles',
       'permissions/resources',
@@ -173,7 +174,7 @@ test('a side bar lists only built screens, in side-bar order', () => {
       'agent/definitions',
       'agent/switches',
     ],
-    'the built screens are Home, at the application root, then the alerts.log viewer, the application error log and the audit database, the unlisted Database details, Free-space view and Volume files, process details, processes, Locks, System usage, Databases, the unlisted task details and per-task history, task schedule, on-demand tasks, upcoming tasks, task history, users, roles, resources, services, OpenAPI document viewer, the unlisted web-application form, web applications, REST API explorer, the four unlisted OAuth 2.0 tabs, Secrets, SSL/TLS, X.509, LDAP / Kerberos, Wallet and OAuth 2.0 screens, and the Agent co-pilot area\'s Definition form, Definitions list and Switches, in area rail order'
+    'the built screens are Home, at the application root, then the alerts.log viewer, the application error log and the audit database, the unlisted Database details, Free-space view and Volume files, process details, processes, Locks, System usage, Databases, the unlisted task details and per-task history, task schedule, on-demand tasks, upcoming tasks, task history, the unlisted user form, users, roles, resources, services, OpenAPI document viewer, the unlisted web-application form, web applications, REST API explorer, the four unlisted OAuth 2.0 tabs, Secrets, SSL/TLS, X.509, LDAP / Kerberos, Wallet and OAuth 2.0 screens, and the Agent co-pilot area\'s Definition form, Definitions list and Switches, in area rail order'
   );
 });
 
@@ -254,6 +255,13 @@ test('documentScreenFor resolves the REST API explorer to its unlisted, id-keyed
   assert.equal(isListedScreen(screenForRoute('web-applications/list/edit')), false, 'which takes no side-bar position');
   assert.equal(editorScreenFor(webApps), null, 'but a row name opens the list\'s own id route');
   assert.equal(createFormFor(screenForRoute('agent/definitions')).route, 'agent/definitions/edit', 'and a form that reads its id is both');
+  // Story 8.2: the Users list pairs with its create form the same way.
+  // Mutation (Rule 19): drop UserForm from `CREATE_ONLY_FORMS` -> the editor assertion below and
+  // the user leg of the screenForChange test go red.
+  const users = screenForRoute('permissions/users');
+  assert.equal(createFormFor(users).route, 'permissions/users/edit', 'the Users list\'s Create opens its own form');
+  assert.equal(isListedScreen(screenForRoute('permissions/users/edit')), false, 'which takes no side-bar position');
+  assert.equal(editorScreenFor(users), null, 'and a row name does not open it, because it reads no id');
   assert.deepEqual(
     listedScreensForArea('web-applications').map((screen) => screen.route),
     ['web-applications/list', 'web-applications/rest-apis'],
@@ -936,6 +944,7 @@ test('screenForChange resolves the screen a change opens and the route that name
   // still wins, so a task's toast opens the task's own details rather than a list.
   assert.equal(screenForEntityType('task').route, 'tasks/schedule/details', 'a task change opens its details');
   assert.equal(screenForEntityType('agent-definition').route, 'agent/definitions/edit', 'a definition change opens its form');
+  assert.equal(screenForEntityType('user').route, 'permissions/users', 'a user change opens the Users list, not the create form');
   assert.ok(!target.route.includes('/csp/myapp'), 'the id is one encoded segment, never raw path (AD-13)');
 
   // A type no built screen shows is not a fault: the toast still says what changed, with nothing
diff --git a/ui/tools/proposal-view.test.mjs b/ui/tools/proposal-view.test.mjs
index 47fe7d6..c2506d4 100644
--- a/ui/tools/proposal-view.test.mjs
+++ b/ui/tools/proposal-view.test.mjs
@@ -28,12 +28,14 @@ const uiRoot = join(dirname(fileURLToPath(import.meta.url)), '..');
 const corePath = (name) => join(uiRoot, 'src', 'app', 'core', name);
 
 const {
+  CONSEQUENCE_UNAUTHENTICATED,
   COUNTDOWN_PLACEHOLDER,
   COUNTDOWN_WARNING_MS,
   CONFIRMED_TIME_PLACEHOLDER,
   MASKED_VALUE,
   RESIDUE_COUNT_PLACEHOLDER,
   USER_NAME_PLACEHOLDER,
+  consequenceSentence,
   countdownPhase,
   countdownRemaining,
   formatCountdown,
@@ -49,6 +51,7 @@ const {
 const { PROPOSAL_EXPIRED_STATE, PROPOSAL_LIVE_STATE, parseProposals, restoredProposals } =
   await import(corePath('turn.ts'));
 const { STRINGS } = await import(corePath('strings.ts'));
+const { screenForToolName } = await import(corePath('navigation.ts'));
 
 const PROPOSE_CLS = join(uiRoot, '..', 'src', 'OcuPilot', 'Kernel', 'State', 'Propose.cls');
 const DISCLOSURE_CLS = join(uiRoot, '..', 'src', 'OcuPilot', 'Kernel', 'Proposal', 'Disclosure.cls');
@@ -165,6 +168,47 @@ test('the audit warning travels off the wire rather than being decided here', ()
   assert.equal(toCardView(parsedProposal({ auditWarning: false }), 'x').auditWarning, false);
 });
 
+// DW-1489: the kernel marks a web-application create that admits unauthenticated access with a
+// consequence code, and the card states the published sentence for it. The code travels off the
+// wire; the sentence is the string source's, resolved here and nowhere else.
+//
+// Mutation (Rule 19): drop `consequence` from `toCardView`'s result, or from `parseProposal` ->
+// the first assertion goes red.
+test('the consequence code travels off the wire and resolves to the published sentence', () => {
+  const marked = toCardView(parsedProposal({ consequence: 'WEBAPP.UNAUTHENTICATED' }), 'x');
+  assert.equal(marked.consequence, CONSEQUENCE_UNAUTHENTICATED);
+  assert.equal(consequenceSentence(marked.consequence), STRINGS.webAppUnauthenticatedEffect);
+  const plain = toCardView(parsedProposal(), 'x');
+  assert.equal(plain.consequence, '', 'a wire row that omits it carries no consequence');
+  assert.equal(consequenceSentence(plain.consequence), '');
+  assert.equal(consequenceSentence('SOMETHING.ELSE'), '', 'and a code this client publishes nothing for reads as none');
+  assert.equal(consequenceSentence(undefined), '');
+});
+
+// AD-3, AD-35: a user create's diff carries one Password row the kernel composes with both values
+// empty, and the Users screen declares the name secret. The card reads the mask on both sides and
+// asks for it at confirm -- with the screen resolved the way the panel resolves it, by tool name.
+test("a user create's composed Password row is masked on both sides and asked for at confirm", () => {
+  const screen = screenForToolName('permissions.users.create');
+  assert.ok(screen, 'the create tool resolves to its screen');
+  assert.ok(screen.secretArguments.includes('Password'), 'which declares Password secret');
+  const view = toCardView(
+    parsedProposal({
+      target: { type: 'user', scope: 'instance', id: 'probe' },
+      tool: 'permissions.users.create',
+      changed: [
+        { field: 'Name', before: '', after: 'probe' },
+        { field: 'Password', before: '', after: '' },
+      ],
+      unchangedCount: 0,
+    }),
+    'x',
+    screen.secretArguments
+  );
+  assert.deepEqual(view.changed[1], { field: 'Password', before: MASKED_VALUE, after: MASKED_VALUE, removed: false });
+  assert.deepEqual([...view.maskedFields], ['Password'], 'and the confirm asks for it');
+});
+
 // AC1 (Story 5.10): the declaration is the write tool's own and reaches the card on the wire, so no
 // list of destructive tool names exists in this client to fall out of step with the tool set.
 //
diff --git a/ui/tools/proposal.test.mjs b/ui/tools/proposal.test.mjs
index 373eced..1297f02 100644
--- a/ui/tools/proposal.test.mjs
+++ b/ui/tools/proposal.test.mjs
@@ -430,8 +430,10 @@ test('nothing shipped in the client authors a proposal value, and nothing posts
   // from the client rather than from the tool, which is the safety signal the declaration exists
   // to keep on the instance, so it is in the alternation with `auditWarning` and admits the two
   // boolean spellings for the same reason.
+  // `consequence` is the kernel's code for what a write does beyond its diff, and the card states
+  // its sentence as a warning -- a shipped literal there would author or suppress that warning.
   const authoring =
-    /\b(before|after|unchanged|unchangedCount|rationale|expectedImpact|reverse|fingerprint|auditWarning|destructive)\s*:\s*('|"|\d|`|true\b|false\b)/;
+    /\b(before|after|unchanged|unchangedCount|rationale|expectedImpact|reverse|fingerprint|auditWarning|destructive|consequence)\s*:\s*('|"|\d|`|true\b|false\b)/;
   // Since Story 5.3 the client does POST to a proposal route -- the id in the path and, in the
   // body, only the fields the target screen declares secret-typed. What it still may not do is
   // post a proposal's own content, so the scan is for a request body that names one.
diff --git a/ui/tools/screen-mirror.mjs b/ui/tools/screen-mirror.mjs
index 7457dca..2471703 100644
--- a/ui/tools/screen-mirror.mjs
+++ b/ui/tools/screen-mirror.mjs
@@ -444,6 +444,11 @@ export function entityLabelProblem(declaration) {
  * pattern and is absent from `secretArguments` is refused (DW-1121): it is a secret the confirm
  * channel would otherwise carry in clear.
  *
+ * A `secretArguments` name also qualifies when it is a top-level `secret` literal row of one of
+ * the screen's write tools (`secretRowNames`) -- a derived credential such as X.509's
+ * `PrivateKeyPassword`, or an authored wrapper field such as `Security.User`'s POST `Password`.
+ * That is additive: every name the two sources above admit is still admitted.
+ *
  * `toolFields` is the generated `ToolFields.cls` block; a caller that supplies none (every fixture
  * in `screen-mirror.test.mjs`) is read as "this screen owns no write tool", which is what a
  * descriptor with no entry means anyway.
@@ -460,8 +465,9 @@ export function confirmChannelProblem(declaration, toolFields = {}) {
       return `fingerprintExcludes names '${path}', which is neither a field of this screen's write tool nor one its read declares (AD-6)`;
     }
   }
+  const secretRows = secretRowNames(declaration.toolIdentifier, toolFields);
   for (const name of secrets) {
-    if (!names.settable.includes(name) && !names.read.includes(name)) {
+    if (!names.settable.includes(name) && !names.read.includes(name) && !secretRows.includes(name)) {
       return `secretArguments names '${name}', which is neither a settable field of this screen's write tool nor one its read declares (AD-6)`;
     }
   }
@@ -634,6 +640,27 @@ function toolFieldRows(identifier, toolFields) {
   return rows;
 }
 
+/**
+ * The top-level `secret` literal rows of the write tools `identifier` owns, by name -- the third
+ * source a `secretArguments` entry may name (`OcuPilot.Screen.Registry.SecretRowNames`). A nested
+ * path, an array element and a row of any other class contribute nothing.
+ */
+export function secretRowNames(identifier, toolFields) {
+  const names = [];
+  if (typeof identifier !== 'string' || identifier === '' || !isObject(toolFields)) return names;
+  for (const [name, entry] of Object.entries(toolFields)) {
+    if (name.split('.').slice(0, 2).join('.') !== identifier) continue;
+    if (!isObject(entry) || !Array.isArray(entry.fields)) continue;
+    for (const field of entry.fields) {
+      if (!isObject(field) || typeof field.path !== 'string') continue;
+      if (field.class !== 'secret' || field.shape !== 'literal') continue;
+      if (field.path.includes('.') || field.path.includes('[')) continue;
+      if (!names.includes(field.path)) names.push(field.path);
+    }
+  }
+  return names;
+}
+
 /** The first name matching the credential pattern that `secrets` does not declare, as a refusal. */
 function credentialNameProblem(names, secrets, where) {
   for (const name of names) {
diff --git a/ui/tools/screen-mirror.test.mjs b/ui/tools/screen-mirror.test.mjs
index 8498361..a5f8012 100644
--- a/ui/tools/screen-mirror.test.mjs
+++ b/ui/tools/screen-mirror.test.mjs
@@ -2068,3 +2068,57 @@ test('entityLabelProblem returns the instance-side sentences, and every shipped
     /entityLabelKey names the singular noun/
   );
 });
+
+// AD-3, AD-6: a `secretArguments` name also qualifies when it is a top-level `secret` literal row
+// of the screen's write tools -- a derived credential, or an authored wrapper field such as
+// `Security.User`'s POST `Password` (Story 8.2). The widening is additive, so both directions are
+// pinned: a top-level secret row passes, and every name that qualified before still qualifies
+// while an unknown name, a nested secret path and an array element are still refused.
+//
+// Mutation (Rule 19): drop the `secretRows` clause from `confirmChannelProblem` -> the Users list
+// and the synthetic secret row below are refused; make `secretRowNames` admit nested paths -> the
+// `MatchRoles[].MatchRole` assertion goes red.
+test('a secretArguments entry may name a top-level secret row, and nothing that qualified stops qualifying', async () => {
+  const { secretRowNames } = await import('./screen-mirror.mjs');
+  const { screens, toolFields } = readSources();
+  const users = screens.find((screen) => screen.className === 'OcuPilot.Screen.Descriptor.UserList');
+  assert.ok(users !== undefined, 'the users list is among the descriptors');
+  assert.deepEqual(users.declaration.secretArguments, ['Password'], 'it declares the authored Password');
+  assert.ok(secretRowNames('permissions.users', toolFields).includes('Password'), "the create tool's authored row is a top-level secret");
+  assert.equal(confirmChannelProblem(users.declaration, toolFields), null, 'the declaration passes');
+  assert.equal(
+    confirmChannelProblem({ ...users.declaration, secretArguments: ['Pasword'] }, toolFields),
+    "secretArguments names 'Pasword', which is neither a settable field of this screen's write " +
+      'tool nor one its read declares (AD-6)',
+    'a name no source carries is still refused'
+  );
+
+  const webApp = screens.find((screen) => screen.className === 'OcuPilot.Screen.Descriptor.WebAppList');
+  const synthetic = {
+    'webapp.list.probe': {
+      fieldList: 'WebApp.App',
+      fields: [
+        { path: 'ProbeSecret', shape: 'literal', templateType: 'string', class: 'secret', authored: true },
+        { path: 'ProbeNested.Key', shape: 'literal', templateType: 'string', itemType: '', class: 'secret' },
+        { path: 'ProbeList[]', shape: 'literal', templateType: 'string', itemType: '', class: 'secret' },
+        { path: 'ProbeOpaque', shape: 'literal', templateType: 'string', itemType: '', class: 'opaque' },
+      ],
+    },
+  };
+  const widened = { ...toolFields, ...synthetic };
+  const of = (secretArguments) => ({ ...webApp.declaration, secretArguments });
+  assert.equal(confirmChannelProblem(of(['ProbeSecret']), widened), null, 'a top-level secret row qualifies');
+  assert.equal(confirmChannelProblem(of(['ProbeSecret']), toolFields), confirmChannelProblem(of(['ProbeSecret']), {}), 'and only while the row exists');
+  assert.notEqual(confirmChannelProblem(of(['ProbeSecret']), toolFields), null, 'absent the row it is refused');
+  for (const name of ['ProbeNested.Key', 'ProbeNested', 'ProbeList', 'ProbeList[]', 'ProbeOpaque']) {
+    assert.notEqual(confirmChannelProblem(of([name]), widened), null, `${name} does not qualify`);
+  }
+  for (const name of ['Timeout', 'AutoCompile', 'CorsAllowlist', webApp.declaration.read.fields[0]]) {
+    assert.equal(confirmChannelProblem(of([name]), widened), null, `${name} still qualifies`);
+  }
+  assert.notEqual(
+    confirmChannelProblem(of(['MatchRoles[].MatchRole']), widened),
+    null,
+    'a nested secret path of the shipped list is not admitted'
+  );
+});
diff --git a/ui/tools/strings.test.mjs b/ui/tools/strings.test.mjs
index d0eeb28..34ef499 100644
--- a/ui/tools/strings.test.mjs
+++ b/ui/tools/strings.test.mjs
@@ -599,13 +599,15 @@ test('Story 1.11 adds no string: the switch and its refusal are named by keys th
   // application error log publishes for a namespace this log does not carry -- so on this screen
   // there IS now a sentence for an unknown namespace, which supersedes the claim this roster
   // carried for DW-126: the shell is silent for a namespace only where no screen publishes copy.
+  // `userFormNamespace` is a field label, not a sentence about a namespace: the create-a-user
+  // form's startup namespace, which the account enters on sign-in.
   const namespaceSentences = Object.entries(stringsValues).filter(([key]) =>
     key.toLowerCase().includes('namespace')
   );
   assert.deepEqual(
     namespaceSentences.map(([key]) => key).sort(),
-    ['errorLogEmptyNamespace', 'errorLogRefusedNamespace', 'headerNamespaceLabel'],
-    'the namespace-named keys are the switch\'s accessible name, one drilled-scope empty state and one named refusal'
+    ['errorLogEmptyNamespace', 'errorLogRefusedNamespace', 'headerNamespaceLabel', 'userFormNamespace'],
+    'the namespace-named keys are the switch\'s accessible name, one drilled-scope empty state, one named refusal and one field label'
   );
 });
 
