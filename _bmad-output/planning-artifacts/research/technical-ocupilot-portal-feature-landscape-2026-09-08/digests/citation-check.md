# Semantic citation check — 2026-09-08

Tool calls used: 15 (3 to read the report, 1 to load WebFetch, 6 Bash probes, 4 WebFetch, 1 to write this file)

Method: 12 load-bearing citations sampled per the brief. Web sources fetched directly; local sources grepped in the export folders and sibling repositories; live-instance claims probed with `curl -u _SYSTEM:SYS` (GET only). A digest was accepted as support only where it cites a path, URL or quoted line. Verdict scale: SUPPORTED / PARTIAL / NOT SUPPORTED / UNCHECKABLE.

## Results

| ref | report sentence (trimmed) | verdict | decisive evidence (quote or path:line) | note |
| --- | --- | --- | --- | --- |
| [2] | D1: "submissions close 2026-09-27 23:59 EST, and improvements are explicitly allowed through the voting week ending 2026-10-04"; task statement verbatim with six areas; approval "based on the criteria of complexity and usefulness", final and not subject to appeal | SUPPORTED | DC announcement: "September 27, 2026 (23:59 EST): Deadline for submissions"; "October 4, 2026 (23:59 EST): Voting ends"; "Create a GUI powered by InterSystems IRIS management APIs for the following Management Portal tasks:" + the six bullets; "Our experts will have the final say on whether the application is approved for the contest, based on the criteria of complexity and usefulness. This decision is final and not subject to appeal."; "You can continue improving your application throughout both the submission and voting periods."; prizes $5,000/$2,500/$1,000/$500/$300/$100 and $600/$400/$100 | The fetch summary returned six bullets; the seventh line in the report's block quote ("Feel free to add any other screens...") was not echoed back, so it is unconfirmed by this pass, not contradicted. |
| [21] | D4: "hidden, undocumented `/api/admin` service (`%Api.Admin` with dispatch classes v1 and v2, 84 Hidden classes) that already covers namespaces and mappings, databases including mount, dismount, compact, truncate, defragment and integrity check, journal ..., locks, processes, tasks, license, ECP, devices, external language servers, web applications and sessions, DocDB, wallet secrets, system-usage and dashboard monitors, and every security object ... Sixteen probed GET routes returned 200" | PARTIAL | Live: `GET /api/admin/info` -> `{"apiVersion":2,"username":"_SYSTEM","serverVersion":"IRIS for UNIX ... 2026.2 (Build 221U)"...}`; `GET /api/mgmnt/v1/%25SYS/spec/api/admin` -> 305 paths incl. `/v2/namespace`, `/v2/database-dir/compact|truncate|defragment|integrity-check|mount|dismount`, `/v2/journal/file`, `/v2/lock(s)`, `/v1/process`, `/v1/task` (19 paths), `/v1/license/key`, `/v2/ecp/...`, `/v1/device`, `/v1/web-session`, `/v1/wallet`, `/v1/monitor/system-usage`, `/v1/monitor/dashboard/...`, `/v1/security/user|role|service|ssl-configuration|x509-credential|oauth2/... (38)|ldap/configuration|audit/...|encryption/...|superserver`; live 200 on `/api/admin/v2/namespaces`, `v2/processes`, `v2/tasks`, `v2/databases`, `v2/locks`. Class count: digests/api-coverage-r1-verify.md:12 (`SELECT Name, Hidden FROM %Dictionary.ClassDefinition WHERE Name LIKE '%Api.Admin%'` -> `"rowCount":84`, every row Hidden) and :24 ("two of them are `%SYS.Api.Admin.Util.AsyncTask` and `%SYS.Api.Admin.Util...`") | Existence, hidden status, v1/v2 dispatch and broad coverage are supported live. Two parts not confirmed by this pass: (a) the verifier's own note says 2 of the 84 rows are `%SYS.Api.Admin.Util.*`, so "84 Hidden classes" in `%Api.Admin` is a slight overcount; (b) no spec path matched "webapp", "language", "docdb", "sqlpriv" or "webauth" — the routes may exist under other names, but web applications, external language servers, DocDB, SQL privileges and web-authentication settings were not confirmed here. |
| [22] | D4: "every `/api/*` application is password-only (AutheEnabled 32) except `/api/monitor`, which is unauthenticated with the `%DB_IRISSYS` role; `/api/atelier` requires the `%Development` resource ... No REST application sets a CSRF token ... Session cookies are scoped per application path with SameSite Strict ... `GroupById=%ISCMgtPortal` on the portal, `/ui/interop`, `/api/interop-editors` and `/api/admin`, and the latter two also enable JWT authentication with `UseSession=0`" | SUPPORTED | digests/api-coverage-r1-1.md:129 "`/api/admin` ... AutheEnabled=32 (password), `JWTAuthEnabled=1`, `GroupById=%ISCMgtPortal`, `Resource=""`, `CookiePath=/api/admin/`, `CSRFToken=0`" (source: `Security.Applications.Get` in %SYS, verify row C1); :229 "Bit meanings (Documatic `Security.Applications.AutheEnabled`): bit 5 = `AuthePassword` (32), bit 6 = `AutheUnauthenticated` (64) ... every `/api/*` app is **password-only**"; :231 "`CSRFToken=0` on every `/api/*` app; `1` only on `/csp/sys`, `/ui/interop`, `/csp/broker`"; :248 "session cookies are scoped to each app's Session Cookie Path (`/api/atelier/` observed, SameSite Strict, httpOnly)"; :255 "`/api/admin` and `/api/interop-editors` have `JWTAuthEnabled=1` and `UseSession=0`". Live consistency: `GET /api/monitor/metrics` with no credentials -> 200; `GET /api/atelier/` with no credentials -> 401. | The Security.Applications values could not be re-read over GET; accepted on the digest's cited MCP probes plus the two live consistency checks. The "Do not use" docs line (GSA_manage_applications.md:793, cited at digest :254) was not re-opened. |
| [24] | D5: portal "navigates the top window to `/ui/interop/<app>/index.html?...`"; "bundles derive their API root from a regular expression that requires `/ui/interop` in their own URL, use `<base href="index.html">` and hash routes"; "`VSCODE=1` hides the top bar, suppresses the automatic login, honours `READONLY=1`, posts `compatible`, `saved`, `compiled` and `badrule`, `baddtl` or `badbpl` events to the parent, and accepts inbound `auth` (username and password), `compile`, `revert`, `undo` and `redo` messages, with no origin check" | SUPPORTED | irisui bundles: `RegExp(/(.*?)(?=\/ui\/interop)/)` x6; `<base href="index.html"` x6; `useHash:!0` x10; VSCODE in `ui/interop/{rule-editor,dtl-editor,bpl-editor,interop-editor}/main.*.js`: `a.get("VSCODE")?(e.vscodeMode=!0,localStorage.setItem("vscodeMode",...),window.parent.postMessage({direction:"vscode",type:"compatible"},"*"),window.onmessage=p=>{"auth"==p.data.type&&e.appAuthService.doLogin(p.data.`; outbound `type:"badrule"` x6, `"baddtl"` x6, `"badbpl"` x4, `"compiled"` x10, `"saved"` x10, `"compatible"` x6; inbound `"auth"|"compile"|"revert"|"undo"|"redo"==x.data.type` all present. Launch sites: irislib/EnsPortal/BPLEditor.cls:1342 `var href = prefix + '/ui/interop/bpl-editor/index.html?';`, DTLEditor.cls:3912 same for dtl-editor; 0 lines in irislib/EnsPortal combine `ui/interop` with `iframe`. | `READONLY` as a query parameter was not found by grep (only an unrelated i18n string), so "honours `READONLY=1`" is unconfirmed by this pass. The VSCODE string also appears in the interop-editor (production config) bundle; the report scopes the mode to the three document editors — not contradicted if that occurrence only clears the flag, as the digest notes for the message viewer. |
| [25] | D5: "Nothing blocks same-origin framing. The `/ui/interop` static files are served unauthenticated with no X-Frame-Options and no Content-Security-Policy; only the classic portal page sends X-Frame-Options SAMEORIGIN"; "opening the Rule Editor logged in silently: a null-body login POST returned 200 and the editor rendered without a login form. Which cookie carried that login was not isolated; forcing the portal session cookie or the IRIS session token through curl returned 401" | SUPPORTED | Live: `curl -I http://localhost:52774/ui/interop/rule-editor/index.html` (no credentials) -> `HTTP/1.1 200 OK` with no X-Frame-Options, CSP or Set-Cookie header; `curl -I -u _SYSTEM:SYS .../csp/sys/UtilHome.csp` -> `X-FRAME-OPTIONS: SAMEORIGIN`. Silent login: digests/irisui-embed-r1-1.md:62 "**Silent login with an existing SMP session works in the browser (live).** With tab 1 logged into `/csp/sys/%25CSP.Portal.Home.zen?$NAMESPACE=HSCUSTOM`, opening `http://localhost:52774/ui/interop/rule-editor/index.html?$NAMESPACE=HSCUSTOM` in tab 2 produc..."; :119 "null-body `POST /login` -> 200 followed by `/info`, `mgmt-url`, `system-mode`, `sourcecontrol/enabled` all 200"; :137 "In Chrome it succeeded with only cookies available (`IRISSessionToken`, `CSPWSERVERID`, possibly `CSPBrowserId`...); in curl, `IRISSessionToken` alone against a GET returned 401" | Header claims re-verified live. The silent-login sequence is a recorded Chrome DevTools observation with URLs; it was not re-run (GET-only budget), so it rests on the digest's cited record. |
| [10] | D6a: "a provider abstraction with four working LLM adapters"; providers table `openai`, `anthropic`, `gemini`, `openai-compatible`; "Not present: Azure OpenAI, Bedrock, Vertex, Cohere, Mistral. Provider selection is an If/ElseIf chain"; "the loop is fully synchronous and non-streaming, one blocking call per turn with a 90-second provider timeout and a README instruction to raise the Web Gateway timeout to 300 seconds" | SUPPORTED | iris-session-agent (module.xml:6 `<Version>1.0.4</Version>`, last commit 2026-06-10): literal counts in src/SessionAgent `"openai"` 82, `"anthropic"` 39, `"gemini"` 29, `"openai-compatible"` 27, zero for azure/bedrock/vertex/cohere/mistral; src/SessionAgent/Agent/AgentLoop.cls:679 `If pConfig.Provider = "openai" {` / :682 `ElseIf pConfig.Provider = "anthropic" {`; src/SessionAgent/LLM/GeminiProvider.cls:34 "FR29 90s timeout. Sets `%Net.HttpRequest.Timeout = 90`"; README.md:163 "Server Response Timeout:  60   ->  change to  ->  300", :167 "The 300s value gives a 90s per-call cap x 3-tool-call agent turn"; no `event-stream` anywhere in src/SessionAgent (the only `"stream"` hits are a body render_strategy value); README.md:402 "Four bundled LLM providers (OpenAI, Anthropic, Google Gemini, OpenAI-compatible ...)", :498 "add a fifth (e.g., Cohere, AWS Bedrock, Vertex AI) by implementing one new sub[class]" | Default model names and the 28-tool count were not re-counted in this pass. |
| [27] | D6b: "ships 104 package tools plus one framework tool, confirmed against the registrations in code: dev 28, admin 26, interop 22, ops 21, data 7 ... Eighty of them depend on the suite's own IRIS-side REST application `/api/executemcp/v2` (package `ExecuteMCPv2`, 29 classes, a dispatcher extending `%Atelier.REST` with about 85 routes); admin, interop and ops are entirely custom, 19 dev tools are Atelier-only" | PARTIAL | iris-execute-mcp-v2 (last commit 2026-08-19): distinct `name: "iris_..."` strings under packages/<pkg>/src: iris-dev-mcp 28, iris-admin-mcp 26, iris-interop-mcp 22, iris-data-mcp 7 (all match) but iris-ops-mcp **25** (report: 21). tool_support.md ~L390: "19 of the 28 dev tools hit Atelier directly"; "### Four servers are fully dependent on the custom REST handlers — `@iris-mcp/admin`, `@iris-mcp/interop`, `@iris-mcp/ops` ... depend entirely on the ExecuteMCPv2 handlers"; "`@iris-mcp/data` is the outlier — multi-API". `grep -rlE 'Extends %Atelier.REST' src/ExecuteMCPv2` -> REST/Dispatch.cls, REST/Base.cls; `<Route ` occurrences in src/ExecuteMCPv2: 95; .cls files in src/ExecuteMCPv2: 68 (includes ExecuteMCPv2.Tests.*). Digest harvest-execute-mcp-r1-1.md:11 and :342 cite `packages/*/src/tools/*.ts` and tool_support.md L58/L105/L180/L220/L279 for 104 and 80. | Not resolved: ops registers 25 distinct `iris_*` names by this grep versus 21 claimed (the extra four may be non-tool `name:` fields or the shared framework tool, but that was not checked). The 80-of-104 figure is arithmetic (104 - 19 Atelier-only dev - 5 data) consistent with tool_support.md; the literal "80" line (digest cites L392-394) was not seen in the lines sampled. "About 85 routes" versus 95 `<Route` hits and "29 classes" versus 68 .cls files are plausibly explained by test classes and comments but were not reconciled. |
| [28] | D7: "Avoid `<CSPApplication>`, deprecated since IPM 0.9.0 even though both InterSystems templates still use it"; "0.10.10 is in beta as of 2026-08-26 (two sources)"; pitfalls "deprecated `CSPApplication` and `${dbrole}` (use `${globalsDbRole}`)" | SUPPORTED | ipm wiki page 03: "`<CSPApplication/>` has been deprecated in IPM 0.9.0 and later. If you have this in your module.xml, consider changing to `<WebApplication/>` instead."; "${dbrole} - the current namespace's globals database role - deprecated in favor of ${globalsDbRole}"; FileCopy "Copies the specified directory or file ... during the `Activate` phase"; Invoke "indicates which Class Method should be called at a specified phase"; SourcesRoot "This element is required". GitHub releases API: `v0.10.10-beta.6 prerelease 2026-08-26T16:45:17Z` (beta.1 2026-08-13 through beta.6). | Not checked in this pass: "latest stable 0.10.8" (that is [32]) and "since 0.9.0 IPM is no longer mapped across namespaces"; whether the two templates still use CSPApplication is [29]. |
| [18] | D3a: "90 menu leaves (Configuration 54, Security 30, Licensing 2, Encryption 4), generated in code by three methods of the portal application class"; "Eleven leaves point at seven legacy CSP pages absent from the export, so SQL Gateway, all five Database Backup entries, Web Gateway Management, Task Manager Email, Enable and Disable Auditing, and Security Advisor are a blind spot" | SUPPORTED | irissys/%CSP/UI/Portal/Application.cls: `ClassMethod GetSystemConfigArray` (:82), `GetConfigurationArray` (:134), `GetSecurityArray` (:277) — three methods spanning the cited 82-339 range, 111 `$LB(` nodes (leaves plus group nodes). `.csp` references in lines 82-339: `/csp/sys/mgr/UtilSqlGateways.csp` 1, `UtilSysBackup.csp` 4, `UtilSysBackupDatabases.csp` 1, `UtilSysCSPGateway.csp` 1, `UtilSysTaskMail.csp` 1, `/csp/sys/sec/UtilSecAction.csp` 2, `UtilSysSecurityAdvisor.csp` 1 = 11 references to 7 files; `find irissys -iname <file>` -> 0 for all seven. digests/smp-sysadmin-r1-1.md:122 "Leaf count: 90 menu entries (Configuration 54 incl. 3 Sharding + 6 Mirror, Security 30, Licensing 2, Encryption 4). Of these, 11 entries (7 distinct files) resolve to legacy non-Zen `.csp` pages" | The blind-spot claim is fully verified (11 references, 7 files, none in the export). The 90-leaf total was not independently recounted; 111 `$LB` nodes including group headers is consistent with it, and the breakdown rests on the digest's count of the same lines. |
| [11] | D3b: "Long-running work goes through `%CSP.UI.System.BackgroundTask.RunTask`, which validates the task name against a 45-entry whitelist, JOBs a worker, and reports progress in `^IRIS.Temp.MgtPortalTask`" | PARTIAL | irissys/%CSP/UI/System/BackgroundTask.cls:38 `If (..#TASKS '[ (","_pTask_",")) {` ... :40 `Set tSC = $$$ERROR($$$GeneralError,"TaskNotDefined")`; :56 `Job ##class(%CSP.UI.System.BackgroundTask).RunBackgroundTask(pTask,pNS,tParmList,tIdx)::10`; `Parameter TASKS = ",AuditCopy,AuditExport,AuditPurge,Backup,CompactDBSpace,CopyNamespaceMappings,CreateDatabase,Compile,DatabaseIntegrityCheck,DataMigration,DefragmentDB,Delete,EnableEnsNamespace,Export,ExternalLangServers,FileMan,Import,SQLExport,SQLImport,SQLExportStatement,SQLImportStatement,QueryExport,JournalIntegrityCheck,LinkTable,LinkProcedure,MirrorActivateCatchupDB,MirrorDownloadDB,MirrorRemoveDB,MirrorMountDB,MirrorAddDatabases,ModifyDatabaseSize,RebuildIndices,TuneTable,TuneTables,PurgeAllCachedQueries,JobShowPlan,JobSaveQuery,JobPossiblePlans,JobComparePlans,ShardActivate,ShardAssign,ShardVerify,ShardRebalance,Task1";` — **44** names by comma split and by hand count. | Mechanism (whitelist check, JOB, temp global) is supported. The count is 44, not 45 (digests/smp-sysop-r1-1.md:124 also says 45; the digest is off by one). The progress global `^IRIS.Temp.MgtPortalTask` was not in the 70 lines read (only `^|"%SYS"|IRIS.Temp(tIdx)` for parameters) — unconfirmed, not contradicted. |
| [39] | D8: "Microsoft Copilot in Azure runs agents in a sidecar panel that can act on the user's behalf, with no action performed without approval and no privilege beyond the user's, but logs actions under the user's identity with no agent marker" | SUPPORTED | learn.microsoft.com/en-us/azure/copilot/agents (updated 2026-08-10): "An agent in a conversation has the ability to take action on your behalf. You can review what the agent proposes, and confirm if you want to proceed. No actions are performed without approval. Azure Copilot agents can also produce artifacts such as scripts that you can deploy"; "in the navigation bar in full screen mode, or in the dropdown menu in the sidecar mode"; "Administrators can enable or disable access to each specific agent if desired." | The sampled clause (approval, sidecar, admin controls) is verbatim on this page. The "no privilege beyond the user's" and "Activity Log ... no agent marker" clauses are attributed by the appendix to the second [39] source (modernendpoint governance guide), which was not fetched; the agents page itself says nothing about RBAC or the Activity Log. |
| [41] | D8: "Gemini Cloud Assist shares page context by default, drafts snippets and verification queries, and launches multi-step investigations"; recurring pattern "side panel with full-screen toggle; page-context sharing on by default and toggleable" | SUPPORTED | docs.cloud.google.com/cloud-assist/chat-panel: "By default, **Page context sharing** is enabled to improve chat quality by letting Gemini Cloud Assist understand the context of your current page in the Google Cloud console."; "In the **Cloud Assist** panel, click more_vert **More actions**. Click **Page context sharing** to toggle the setting."; "To expand the **Cloud Assist** panel to the full width of your browser window, click open_in_new **Open in full page**."; "Gemini Cloud Assist replies with a snippet of code that you can copy into your shell or IaC repository."; "If Gemini Cloud Assist determines an investigation would be useful, a **Create investigation** button appears in the chat." | "Verification queries" specifically was not in the returned extract (only code snippets); "multi-step" for investigations is from the blog/InfoQ sources bundled under [41], not this page. Minor. |

Counts: SUPPORTED 9, PARTIAL 3 ([21], [27], [11]), NOT SUPPORTED 0, UNCHECKABLE 0.

## Mismatches

- **[21] `/api/admin` class count and sub-area list.** The report should say "84 Hidden classes match `%Api.Admin%` on the instance, of which two are `%SYS.Api.Admin.Util.*` helpers" (or "82 `%Api.Admin` classes"), per the verifier's own note at digests/api-coverage-r1-verify.md:24. The coverage list is supported for every area whose route name I could match in the 305-path spec; web applications, external language servers, DocDB, SQL privileges and web-authentication settings did not match by keyword and should either be tied to their actual route names in the digest inventory or flagged as unconfirmed in the report. Everything else in the sentence (hidden, undocumented, v1/v2 dispatch, `apiVersion` 2, live 200s) stands.
- **[27] MCP tool count for ops.** The report's "ops 21" was not reproduced: `grep -rhoE 'name: *"iris_[a-z0-9_]+"' packages/iris-ops-mcp/src --include='*.ts' | sort -u` returns 25 distinct names, while dev 28, admin 26, interop 22 and data 7 match exactly. Until the four extra names are classified (non-tool `name:` fields, aliases, or real tools), the report should present ops as "21 per tool_support.md (25 `iris_*` name registrations in source)" and the total as "104 per tool_support.md". The dependent figure "80 of 104" is arithmetic from the 19 Atelier-only dev tools and 5 data tools and is consistent with tool_support.md; "about 85 routes" and "29 classes" should be qualified as non-test counts (95 `<Route` occurrences and 68 `.cls` files including `ExecuteMCPv2.Tests.*` are what the tree holds).
- **[11] Background-task whitelist size.** `Parameter TASKS` in irissys/%CSP/UI/System/BackgroundTask.cls lists 44 names, not 45; the report (and digests/smp-sysop-r1-1.md:124) should say "44-entry whitelist". The mechanism description is correct as written. The progress-global name `^IRIS.Temp.MgtPortalTask` was not verified in this pass and should keep its citation to the specific lines of BackgroundTask.cls that set it, if the digest has them.

Unconfirmed-but-not-contradicted details noted in the table (no wording change required unless the author wants to hedge): the seventh "Feel free to add any other screens" line of the [2] quote; `READONLY=1` handling in the [24] bundles; the RBAC and Activity-Log clauses attributed to the second [39] source; "verification queries" and "multi-step" under [41]; the 90-leaf recount under [18]; the [28] per-namespace-mapping change and the 0.10.8 stable line ([32]).
