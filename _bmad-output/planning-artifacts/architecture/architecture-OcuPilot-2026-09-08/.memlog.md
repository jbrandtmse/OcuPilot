---
scope: OcuPilot full-parity architecture: Release 1 contest build (119 P0 rows, deadline 2026-09-27) binding, Stages 2-6 decided where gated decisions are decidable
purpose: Build substrate for bmad-create-epics-and-stories and bmad-build; plus a contest-facing architecture page, a C4 diagram set, and a file-level sibling harvest plan
altitude: initiative -> features
updated: 2026-09-23T10:24
---

- (decision) OQ17 RESOLVED (probed live on ocupilot container 2026-09-09): agent tools execute IN-PROCESS, not over HTTP. Proof: %Api.Admin.Endpoint is a plain %RegisteredObject with Run(ByRef sc, requestBody As %DynamicObject) As %DynamicObject; %Api.Admin.Dispatch.v1:Main() (line 1485, readable) is a thin wrapper = ResourcesOR() + $SYSTEM.Security.Check(res,'U') + ValidateRequest + ValidateSemantics + Run + RenderResponseBody. Endpoint sets IsRunningAsync=1 to neutralize every %response/%request touch; query params seeded via SaveOneQueryParam(). Verified by instantiating %Api.Admin.Endpoints.WebApp.App type=0 outside any CSP request: returned 42 web applications, resource gate %Admin_Secure evaluated in-process, allowed=1. Binds: every write tool, the token question (FR-18), the progress channel. Prevents: the 60s access-token vs 90s turn mismatch entirely - no token is needed for a tool call because the process already runs as the user.
- (decision) Admin API write payloads are NOT unobserved: every endpoint publishes RequestBodySchema() As %DynamicObject. Probed %Api.Admin.Endpoints.WebApp.App PUT -> 45 fields with types (AutheEnabled, CSRFToken, DispatchClass, GroupById, JWTAuthEnabled, MatchRoles[{MatchRole,TargetRoles[]}], ServeFiles, WSGI*, ...). Binds: every write tool schema in 5.5-5.10. Prevents: hand-writing ~40 write payloads against an unverified contract and the per-story 'exercise the payload first' task the PRD addendum sec.3 mandates. Write-tool JSON schemas are GENERATED from RequestBodySchema(), not authored.
- (constraint) Admin API PUT semantics are get-merge-put: %Api.Admin.Endpoints.WebApp.App:RunPut calls MergeJsonAndProperties(requestBody,.Properties) then Security.Applications.Modify(..Name,.Properties). So a partial body IS honored - it merges over existing properties. Resolves the EXPERIENCE.md [NOTE FOR ARCHITECTURE] at Agent Write Lifecycle step 2 ('where the API accepts a partial update, send only the changed fields'): it does; write tools send only changed fields.
- (constraint) FR-29 escalation containment PROVEN on the instance: 'New $ROLES' + 'Set $ROLES=...' confines an escalation to the calling frame. Probe OcuPilotProbe.Admin:RolesContainment returned before=[%All] inside=[%All,%Manager] after=[%All] restored=1. Combined with $SYSTEM.Security.AddRoles(ApplicationName,Hash) ('Add the extra roles associated with the named application') and Security.Datatype.ApplicationType's $$$AppTypePrivRoutine ('Routine' application type), the FR-29 mechanism is a PRIVILEGED ROUTINE APPLICATION: OcuPilot's storage classes call AddRoles inside a New $ROLES frame, so the OcuPilot state role exists only for the duration of a storage call and is gone before tool, admin-API or provider code runs.
- (version) Angular 22 is current stable (22.1.5, 2026-09-02; v22.0.0 released 2026-06-03). Angular Material + CDK v22 (22.0.4). Zoneless change detection stable since 20.2 and the DEFAULT for new projects since v21 - a v22 scaffold is zoneless with OnPush out of the box. Signals stable; Signal Forms stable in v22. Verified via web search 2026-09-09; endoflife.date confirms v22 active support to 2027-06-30.
- (decision) OQ5 RESOLVED (probed live): install runs at CONTAINER START, idempotently, not at image build. Proof from iris_database_list on the running ocupilot container: IRISLIB (/usr/irissys/mgr/irislib/), ENSLIB, HSLIB stay in the IMAGE; IRISSYS (/durable/iris/mgr/), IRISSECURITY, HSCUSTOM, USER, IRISLOCALDATA, IRISAUDIT all live on the DURABLE VOLUME. OcuPilot's classes compile into HSCUSTOM and its web apps/roles/resources into IRISSECURITY - both durable. A build-time install therefore writes into the image's copies, which are SUPERSEDED by the pre-existing durable copies on every 'docker compose up' against an existing ./iris-data. Build-time install is invisible on the upgrade path (up with a newer image), which is exactly the case FR-67 must survive. Binds: FR-64, FR-66, FR-67, the Dockerfile, the IPM module. Prevents: a Dockerfile that appears to work on a clean clone and silently ships no OcuPilot on every subsequent start.
- (constraint) iris-session-agent's agent loop is SYNCHRONOUS and BLOCKING: AgentLoop.RunTurn is called straight from a Zen ZenMethod, no streaming, no JOB, no queue; the browser blocks for the whole multi-iteration turn. It also has NO context-window trimming at all (grep for trim/truncate/prune/window/MaxHistory returns nothing) - full history is replayed every iteration and grows until the provider rejects it. Both are gaps OcuPilot must FILL, not harvest. Binds: FR-12 progress, NFR-2, the progress channel.
- (decision) Tool bodies come from TWO sources, in this precedence: (1) where an admin API route exists, the tool calls the vendor's own %Api.Admin.Endpoints.* object in-process - no OcuPilot code to write or maintain, schemas generated from RequestBodySchema(); (2) only where no route exists (messages.log, application error log, alerts history tail, and Stage 5 parity) does OcuPilot harvest an ExecuteMCPv2.REST.* handler body. Binds: every read and write tool. Prevents: reimplementing 70 vendor endpoints, and the opposite error of harvesting nothing and hand-writing 119 rows of payload code.
- (constraint) The ExecuteMCPv2 handler harvest has exactly ONE coupling seam to %Atelier.REST: every handler's only envelope contact is 'Do ..RenderResponseBody(tSC, , tResult)' - 738 call sites across 15 handlers, all identical. An OcuPilot base class with the same signature (pStatus As %Status, pMsgPart As %DynamicArray, pResPart As %DynamicObject) As %Status replaces the {status,console,result} envelope with ZERO call-site edits. ExecuteMCPv2.REST.Base already demonstrates the technique by wrapping ##super() for pre-flight. Binds: any harvested handler body. Prevents: the assumption that dropping the Atelier envelope means rewriting 39 handlers.
- (constraint) PRD Open Question 4 (X.509 backing class) RESOLVED: %SYS.X509Credentials, confirmed twice - ExecuteMCPv2.REST.Security.cls calls it directly, and %Api.Admin.Endpoints.Security.X509Credential exists on the instance as one of the 70 endpoint classes. Wallet is also covered: %Api.Admin.Endpoints.Wallet.Collection and .Wallet.Secret both exist. Remaining OQ4 items: messages.log and application error log.
- (constraint) iris-execute-mcp-v2's governance model is 100% TypeScript (~2000 lines in @iris-mcp/shared, zero IRIS dependency). OcuPilot's governance must run ON THE INSTANCE, so this is a port of the MODEL, not the code: the tool/tool:action key scheme, the frozen baseline (membership => pre-existing => enabled; absent => new => disabled iff mutates=write; never regenerate to grow), the four-layer cascade using ?? not || so an explicit false is honored, the read-only preset that never consults defaultEnabled and fails SAFE on an unclassifiable key, the call-time gate placed AFTER profile resolution and BEFORE opening a connection, the GOVERNANCE_DISABLED structured result with presetCaused, and the audit log that governance cannot disable.
- (constraint) Installer trap from iris-execute-mcp-v2 README: Security.Applications.Create() does NOT notify the CSP Gateway, so a newly created web application may return 404 until an SMP Save or a Gateway restart. The auth spike saw this too ('did not occur in the spike'). OcuPilot's installer must handle or at least report it. Second trap, from the same repo: its ipm/module.xml drifted to 13 classes while the bootstrap embedded 29, because the project rule listing required rosters omitted the manifest - an IPM install would fail to compile. OcuPilot's IPM manifest must be GENERATED from the same source roster, never hand-kept.
- (constraint) IPM/ZPM IS ABSENT from intersystems/irishealth-community:latest-cd (IRIS 2026.2, the image this repo's docker-compose.yml uses). Probed three ways: 0 rows for '%ZPM%' or '%PackageManager%' in %Dictionary.CompiledClass in both %SYS and HSCUSTOM, and nothing named zpm/ipm on the container filesystem. The PRD addendum sec.6 listed this as unverified; it is now verified false for the OFFICIAL intersystems/* images (the community-contributed intersystemsdc/iris-community images are the ones that preinstall it). Consequence: the Docker path (FR-67) CANNOT call 'zpm install'. Install logic must live in one ObjectScript Installer class with TWO entry points - iris.script / a start hook for the Docker path, and an IPM <Invoke> for the Open Exchange path. Binds: FR-64, FR-66, FR-67.
- (constraint) Log-file backing (PRD OQ4 remainder): messages.log and alerts.log are plain files in the durable manager directory, /durable/iris/mgr/messages.log and alerts.log, located at runtime via $System.Util.ManagerDirectory() or $ZU(12,"") - no class, no route, so a custom OcuPilot endpoint that tails them with byte-offset paging is correct and unavoidable. The application error log is the %CSP.ErrorLog class. Binds: FR-60, FR-62, FR-63, LG-01. NFR-4 applies: those endpoints accept NO caller-supplied path - the directory comes from ManagerDirectory() and the file name is a fixed enum.
- (decision) Progress channel (the second half of OQ17) RESOLVED: a turn runs in a BACKGROUND JOB that inherits the user's identity; the panel polls a cheap progress endpoint. Evidence: JOB inherits $USERNAME and $ROLES including login and added roles (InterSystems Security Reference, ROARS_iam_advanced; RCOS_vroles; RCOS_vusername), and probed live - parent=[_SYSTEM|%All] child=[_SYSTEM|%All]. The jobbed process has no %request/%session, which is exactly the mode the admin API endpoint objects already support via IsRunningAsync=1. Benefits: removes the Web Gateway 300s timeout from the install prerequisites entirely (no long-held request), makes Stop implementable as a flag the job checks between steps, and survives a browser reload mid-turn. Rejected: running the turn in the request process and polling alongside it - works, but keeps the gateway-timeout prerequisite and loses reload survival.
- (decision) The WRITE does not happen in the turn job. Per FR-17 the Confirm is a separate authenticated request from the browser, so: the turn job only PROPOSES (LLM calls + read tools + a server-computed diff), and the write executes in the short foreground confirm request as the user. This keeps the write path a normal authenticated request with the user's own roles, and means the background job never performs a mutation. Binds: FR-17, FR-18, the proposal store.
- (constraint) An agent write produces TWO audit records, and they must be correlatable. Probed on the instance: the vendor's own %System/%Security events are enabled by default and fire on the underlying call - ApplicationChange, UserChange, RoleChange, ResourceChange, SSLConfigChange, X509CredentialsChange, WalletSecretChange, AuditChange all show enabled=Yes with writes recorded. OcuPilot's agent marker (FR-22) is a SEPARATE event under its own Source, emitted alongside. The marker must therefore carry the proposal id and the target identity so the pair can be found from either side; NFR-7 ('recoverable by the agent marker without correlating across systems by hand') is about that pairing.
- (decision) Reconcile against EXPERIENCE.md found two of its nine [NOTE FOR ARCHITECTURE] asks unaddressed; both became ADs. AD-24 caps screen-context rows (the UX asked for a cap and a count on the read tool-call card) - it binds the per-screen context serializer AD-5 already requires, and it is simultaneously a cost control (sec 7.3), a latency control (NFR-1) and an injection-surface control (NFR-6), so two slices choosing different caps is a real divergence. AD-25 makes the FR-69 /csp/myapp demo fixture opt-in via this repo's own docker-compose.yml only, never on an operator's instance - the UX asked for the decision and it has a safety edge (creating a web application the operator did not request).
- (decision) Currency reviewer caught three real errors, all verified and corrected. (1) TypeScript: Angular 22 requires >=6.0.0 <6.1.0, NOT 5.9.x - TS 5.9 is a v22 breaking change and TS 7 (current stable) is refused by @angular/compiler-cli. Node is ^22.22.3||^24.15.0||^26.0.0. (2) My first endpoint probe UNDERCOUNTED because 3 stub classes failed GetTextAsString and I treated a failed read as 'no match'. Re-probed: 7 endpoints have an async path (not 4) and 5 touch %request/%response (not 0). Crucially ShouldRunAsync is evaluated PER REQUEST TYPE, not per class. Exactly two Release 1 paths are async - the audit record LIST (FR-61) and Database.SysCRUD TYPEINFO free-space (FR-58) - both of which the PRD addendum sec.3 and EXPERIENCE.md had already flagged. Became AD-26. (3) The %Api.Admin.* dependency on [Hidden] undocumented classes had no containment rule or fallback anywhere in the spine; became AD-27, which also pins the image tag off the floating latest-cd.
- (decision) Feasibility reviewer found the spine never stated OcuPilot's OWN authentication - 'Bearer' appeared zero times, 'CSPBrowserId' zero times. That is step-0 work due in the 2026-09-14 listing build and AD-7's polling, the confirm request and the dev loop all depend on it. Added AD-28 from the auth spike's settled Design A: two web apps (unauthenticated static /ocupilot, JWT-enabled password /api/ocupilot in the vendor's GroupById group), silent-first empty-body POST /login, Bearer-only authorization with per-tab token storage, refresh token in the JSON body not as a Bearer, logout carrying both Bearer and cookie, and a dev loop proxied through the IRIS origin because the browser-id cookie is SameSite=Strict. Also noted: because a turn can outlive a 60s access token, refresh is the API service's background concern, never a screen's.
- (decision) AD-3 corrected after the feasibility reviewer challenged it and a re-probe confirmed. My original claim rested on a substring count of 38 that silently merged TWO method names. Truth: the body-template method has FOUR names across the 70 classes - RequestBodySchema (21), PutRequestBodySchema (17), PutAndPostSchema (1), Schema (1) - is absent from the base class, and 16 mutating endpoints publish none at all, five of them Release 1 (Wallet.Secret FR-46 and Security.Audit.Event FR-47 are field-bearing; Process, Lock and Task.Manager are action-style and need no template). What the method returns is a PROTOTYPE OF PLACEHOLDER VALUES ('' for string, true for boolean, [''] for list), not JSON Schema: no required, no enum, no description. So the derivation covers field names and types only; the semantic half is authored once per tool. Saving is real but roughly half what the first draft implied.
- (decision) AD-2 CORRECTED after the rubric reviewer's critical finding, verified by direct probe. SetRespStatus() is a NO-OP under IsRunningAsync=1 (the base class guards it with 'If '..IsRunningAsync'), so every 400/404/409/422 the vendor's endpoints signal would be SILENTLY DISCARDED in-process. Probed a GET for a non-existent web application: with IsRunningAsync=0 plus a stub %response the status read back [404 Not Found]; with IsRunningAsync=1 it read [200 OK] with an empty body. Fix: AdminPort supplies STUB %request and %response objects (both instantiate standalone - ##class(%CSP.Response).%New() and ##class(%CSP.Request).%New() both returned objects) and runs with IsRunningAsync=0, then reads %response.Status as the outcome. This also makes the 5 %request-touching endpoint classes work unchanged. The sequence must also include ValidateQueryParams() - my own probe failed with 'Invalid Application name' until I added it, because that is what sets ..Name - plus ApiVersion, BeginCaptureOutput/EndCaptureOutput, and the <PROTECT>-to-403 mapping.
- (decision) AD-4 INVERTED after the rubric reviewer challenged it and a probe settled it. Get-merge-put is NOT a property of the admin API: of 47 RunPut implementations only 19 call MergeJsonAndProperties. The 28 that do not include most Release 1 editors - Security.User (FR-35), Task.CRUD (FR-53), Security.Resource (FR-40), Security.X509Credential (FR-43), Wallet.Secret (FR-46), Security.Audit.Event (FR-47), all four Security.OAuth2.* (FR-44), Database.ConfigCRUD, Journal.Settings, License.Key. Sending only changed fields to those ERASES every field omitted - the exact accident AD-4 existed to prevent, inverted. Also probed: ValidateRequest enforces required fields on a partial PUT ('Field NameSpace is required'), and Wallet.Secret/Security.Audit.Event PUTs are UPSERTS, so a partial body against a deleted target silently creates a stub. Correct rule: OcuPilot performs the merge ITSELF - fresh read, apply the diff, send the COMPLETE body - which is uniform across both idioms and is what FR-17 already describes ('the unchanged fields that the payload still sends'). The first draft contradicted the UX; the UX was right.
- (event) Reviewer gate complete: 5 lenses (rubric, currency, adversarial, security, feasibility) plus a deterministic lint. 12 critical findings across the reviews, all closed. Spine went from 25 to 38 ADs. Corrections to ADOPTED decisions: AD-2 (stub %request/%response with IsRunningAsync=0, because SetRespStatus is a no-op under the async flag and silently discards every 404/409/422 - probed), AD-4 (inverted: OcuPilot computes the merge and sends a complete body, because only 19 of 47 RunPut implementations merge - probed), AD-3 (four method names not one, 16 mutating endpoints publish none, and the templates carry password fields), AD-9 (globals only, not code, because database READ is routine-execution permission), AD-8 (privileges are a SET of resource/permission pairs), AD-10 (prohibited set defined by effect not verb - privilege grants and web-service disabling were both reachable). New: AD-26 async paths, AD-27 undocumented-internals containment, AD-28 authentication, AD-29 per-port gates, AD-30 switch evaluation, AD-31 job identity revalidation, AD-32 outbound TLS, AD-33 progress channel, AD-34 atomic confirm, AD-35 secrets vs the error log, AD-36 read contract, AD-37 state lifecycle, AD-38 install-before-serve. Plus an Operational Envelope section covering the dimensions the draft left silent.
- (event) Owner decisions folded in: (1) descriptors are HAND-WRITTEN classes and only the write-tool field lists are generated - drops ~4-5 days of generator machinery from step 0/1 and keeps every build step publishable; (2) AD-9 protected state stays in the contest build (globals only, ~1 day, it is the FR-29 acceptance test and retrofitting after 60 screens write to it is far worse), AD-22 governance moves to polish week with its shape fixed now and two non-retrofittable hooks built into the floor; (3) ALL remaining reviewer findings folded into the spine rather than left in reviews - added AD-39 through AD-47 covering the dual-consumer error envelope, confirm-not-callable-from-a-tool plus write-side gating, turn and ledger resource bounds, provider egress allow-list, the live-data framework and its proposal-pause channel, classic-resource route mapping and namespace as data scope, the smoke path and readiness endpoint, OcuPilot's own records being visible in its own screens, and the static origin as hostile ground; (4) AD-24's context cap set to 200 rows, operator-configurable, and extended to bound total content and per-field size rather than row count alone. Owner also asked for an explicit rule that builders read the spine in its entirety - added as the opening section. Spine final at 47 ADs.
- (event) spine finalized
- (reference) Contest-facing architecture page published as an Artifact: https://claude.ai/code/artifact/a48174c0-adfd-49fb-96de-7be88015ff01 . Source kept at architecture-page.html in this workspace. Built on OcuPilot's own Lantern/Bridge tokens from DESIGN.md (navy #0F3A5F shell, teal #0B7080 as the only action color, agent yellow #6B5E00/#FFF6C8 used ONLY on agent-authored elements per the design system's own reservation rule), Inter + JetBrains Mono per DESIGN.md typography, both themes token-driven. Reusable in the Open Exchange description and the Developer Community article.
- (event) Update pass 2026-09-08, forwarded from bmad-spec after live probes recorded in _bmad-output/specs/spec-OcuPilot/.memlog.md. AD IDs held stable; one new AD, three Rules amended in place, two Deferred rows retired.
- (decision) AD-48 (new): the application error log is a port-owned GLOBAL and its delete is an ordinary write. Probe established ^ERRORS is held per namespace, written by ^%ETN, retention via Config.Startup.ErrorPurge - not a file, and no admin API endpoint backs it. The spine had a genuine hole: LogFilePort was scoped to 'the manager directory', AD-21's log rule was framed entirely around file paths under $System.Util.ManagerDirectory(), and AD-36's read contract named no global source, so under the absolute boundary rule FR-63's read had NO OWNER and AD-16's namespace save/restore had nobody assigned. The memlog shows the port's file-only scope was never reasoned about - it was assumed from the start, so this is an unexamined gap rather than a considered choice.
- (decision) Owner chose, from three shown alternatives: RENAME LogFilePort to LogSourcePort and broaden it to own every log source the admin API does not back - the manager directory's files AND the ^ERRORS global, including the namespace switch and the delete. Rationale: a port is defined by what sits outside it, not by transport (AdminPort already spans 70 endpoint classes); the Logs area keeps ONE owner; AD-29's per-port authorization gate then covers the error-log delete for free; port count stays at five. Rejected: keeping the name LogFilePort with a widened rule, because the name would then lie in a document whose whole job is preventing divergence; and a separate ErrorLogPort, because it adds a sixth port for one feature and the next global-backed screen re-opens the question or tempts a general GlobalPort, a far worse boundary.
- (constraint) The sharp half of AD-48, and the divergence it actually prevents: the error-log DELETE is an ordinary write. It mints a proposal, shows a server-computed diff of the scope to be removed, waits for confirmation (AD-6, AD-34), runs under the caller's own privileges through the port gate (AD-29, AD-8) and emits the agent marker (AD-15). The absence of an admin API endpoint changes NOTHING about the write invariants - a builder reading 'no AdminPort call' as 'no real write' would produce exactly the unaudited deletion this spine exists to prevent. messages.log is unaffected: it IS a plain file under the manager directory and LogSourcePort handles it exactly as LogFilePort already did.
- (decision) AD-21 Rule amended in place: the fixed-enum discipline now covers global-backed log sources as well as files - the namespace is chosen from the set the user can read, never taken as a caller string (AD-48). Previously the rule only spoke of file names and $System.Util.ManagerDirectory().
- (decision) Deferred row 'Streaming replies' RETIRED - its stated revisit condition ('Stage 2 planning, or if PRD Open Question 14 is decided earlier') fired. Owner decided 2026-09-08: streaming ships in the POLISH WEEK, conditional on build step 7 finishing and ranked after FR-70/FR-71. AD-7 and AD-33 are unchanged in contract - the turn still runs in a background job and the panel still polls progress - but the panel's render path gains an incremental-append mode, which is why the work is sequenced after the step-7 hardening rather than beside it.
- (decision) Deferred row 'The Atelier port's authentication' UPDATED - its blocker is gone. Probed: /api/atelier has JWTAuthEnabled=false and GroupById='' (outside %ISCMgtPortal). Enabling JWT would work mechanically but means modifying a VENDOR web application, which this project does not do on an operator's instance, so the 'JWT-on-Atelier test' is settled as a no. The choice narrows to explicit Basic header versus OcuPilot API pass-through, still a Stage 3 call but no longer waiting on evidence.
- (decision) AD-3 Rule amended in place: the semantic half authored once per tool now has a NAMED source for the task tools - %SYS.Task's 66 documented property descriptions, which carry the legal-value vocabularies the body templates omit (TimePeriod 0-5, DailyFrequency 0/1 with its increment/start/end triple, the expiry offsets, the %Admin_Secure:Use gate on RunAsUser). Recorded so the epics do not re-derive it.
- (event) Reviewer gate on the update: deterministic lint (1 low, a false positive - '{id}' at line 163 is a real URL path parameter) plus the two configured lenses as parallel subagents scoped to the changed material. Both returned criticals. Reviews in reviews/review-adversarial-ad48.md and reviews/review-currency-ad48.md.
- (decision) AD-48 REWRITTEN after the currency lens found SYS.ApplicationError, and I verified it myself. It is a supported %SYS API - NamespaceList/DateList/ErrorList/ErrorDetail queries plus DeleteByNamespace, DeleteByDate and DeleteByError - and EVERY member takes the namespace as a parameter. Probed directly: the class does NOT exist in HSCUSTOM (%ExistsId=0) but does in %SYS (=1) and the ResultSet builds there. So the port switches ONCE to %SYS by AD-16 and passes the target namespace as an argument, rather than switching into each target namespace as the first draft said. My 'there is no class API' claim was WRONG and had propagated into SPEC.md and PRD FR-63; both corrected.
- (constraint) AD-48 CRITICAL 1 closed - one namespace source. Two existed: the route's ?ns= (AD-44/AD-13) and the drill's own namespace level (FR-63/UX). A fully compliant delete could therefore purge a different namespace than the one the user drilled into, with a matching fingerprint and a correct-looking audit marker. The rule now says the drill's descriptor state is the only source and ?ns= does not reach this port.
- (constraint) AD-48 CRITICAL 2 closed - the captured payload is sensitive and has no schema. An ^ERRORS entry holds every local variable at every stack level plus $ROLES and $USERNAME for whichever application faulted; AD-3's derived classification cannot reach it because there is no template to classify against, and AD-35 covers only the converse case of OcuPilot's own credentials landing in the log. On IRIS for Health those variable tables can hold patient data. Rule: the error DETAIL payload is secret-by-default - never in screen context (AD-24), never sent to the model, and the read tool returns summary fields only. The user reads the full table on screen; the agent does not. AD-35's Prevents line now points at AD-48 for the larger half.
- (constraint) AD-48 CRITICAL 3 closed - the gate resolves per namespace. ^ERRORS is unmapped and lives in each namespace's own globals database, so the required permission is a function of the selected namespace and a single static descriptor resource cannot express it. The classic portal's own keys are %Admin_Operate plus read/write on the target namespace's globals database.
- (constraint) AD-48 HIGH closed - the fingerprint is the enumerated id set, never a count or a live re-query. A count livelocks on an instance still logging errors; re-running the selection at confirm would delete rows the user never saw. A by-namespace or by-date proposal enumerates the specific ids it will remove and confirm deletes exactly those; residue from errors logged in between is correct and the card says so.
- (change) Companion fixes from the gate: HARVEST-PLAN.md line 82 still routed application error log access to Area/Log/ via %CSP.ErrorLog - a slice, contradicting AD-48, and %CSP.ErrorLog is the default CSP error PAGE, not a store; rerouted to SYS.ApplicationError into Port/LogSourcePort. C4.md's port node relabelled.
- (decision) AD-3 corrected on three counts the currency lens caught, two of which would have reached code: the DailyFrequency group is a QUADRUPLE not a triple - DailyFrequencyTime selects minutes versus hours and DailyIncrement is 120-fold ambiguous without it; only 49 of the 66 compiled properties carry documentation and all of them are declared on %SYS.TaskSuper, not %SYS.Task; and ExpiresDays/Hours/Minutes have NO descriptions, so citing 'the expiry offsets' as a documented source was wrong. Both gaps now named as things the epics fill by hand and test, rather than cite.
- (constraint) AD-21 gained a file-half clause from the currency lens: the manager directory is not a constant (messages.log's path is operator-settable through the console configuration) and the instance rotates the file at its configured maximum size, which invalidates any byte offset a paging viewer holds. FR-62's tail resolves the path at call time and revalidates its offset rather than serving from a stale position.
- (event) Update complete and re-linted: 48 ADs, no duplicate ids, lint's only finding is the pre-existing '{id}' false positive (a real URL path parameter in AD-7). Frontmatter 'updated' left at 2026-09-09 rather than regressed to today's 2026-09-08 - the architecture artifacts were dated a day ahead of the rest of the planning set from the start. Spine finalized.

- 2026-09-09 — `/epic-cycle` epic 1 start, Rule 20 writes from owner-approved deferred decisions D2/D4/D5/D7: AD-30 gains the blocked-result contract and names Story 3.7 as the single read-only enforcement point; AD-44 gains the `classicLinkExemption` list-vs-detail predicate; Conventions gains "REST route ordering" and "Classic-portal link-out". Sources: epics-review-deferred.md D2/D4/D5, harvest/iris-couch.md (three UrlMap invariants). PRD FR-9/FR-44 reconciled in the same commit.

- 2026-09-09 — `/epic-cycle` Story 1.2 plan gate, Rule 20: the spine named no home for global stylesheets, vendored fonts or the string source, and did not pin SCSS. Added the "Client asset homes" Consistency Conventions row (`ui/src/styles/`, `ui/src/assets/fonts/`, `ui/src/app/core/strings.ts`, SCSS via `mat.theme()` fixing the `--mat-sys-*` prefix, `\uXXXX` escapes for non-ASCII). Raised by the plan stage as a convention later stories would drift on. Also corrected `epics.md`, which spelled the canonical string "done · audit not marked" with a hyphen in 6 lines; EXPERIENCE.md is canonical.

- 2026-09-09 — `/epic-cycle` Story 1.3 plan gate, Rule 20: the plan stage probed the live instance and found five security-object behaviors that bind every later story touching databases, resources or roles (the `%DB_` naming requirement, resource-before-database ordering, the auto-created implicit role, `Security.Users.Create` silently accepting a non-existent role, `%Operator` as a self-escalation primitive, and `New $ROLES` frame scoping). Recorded as the "IRIS security objects" Consistency Conventions row so Epic 7 and Story 3.8 inherit them rather than re-probing.
- (decision) AD-17 amended (DW-73, owner 2026-09-11): _SYSTEM is unexpired only from the container start path, on a genuinely first install, by name; the IPM <Invoke> never unexpires. Prevents a package install undoing an operator's deliberate expiry.
- (decision) AD-38 amended (DW-72, owner 2026-09-11): each container start marks an installed stamp installing before it recompiles, and the container health check reports healthy only once this start's install recorded success. Prevents --wait and the API gate trusting the previous start's stamp on a same-version restart.
- (decision) AD-25 clarified (DW-74, lead 2026-09-11): 'namespaced' is met by the OcuPilotDemo prefix where names are free, and by behaviour for the literal /csp/myapp -- created only when absent, never modified, enabled, granted, inventoried or removed when install did not create it. Prevents a slice renaming a path downstream ACs pin.
- (decision) AD-38 and AD-17 wording corrected (DW-89, cr round 4, lead 2026-09-11): the start-scoped health check, not the mark, is what stops a same-version restart reading healthy; the mark is best effort (skipped when the prior installer lacks it, never fails a start) and leaves failed, installing and absent stamps alone. AD-17's 'first install' is AC14's: no version row, or only a failed one at schema 0, so again after an uninstall.
- (decision) AD-21: the unauthenticated static application carries exactly one purpose-built read-only matching role (read on the install namespace's database, nothing else), because database READ is routine-execution permission (AD-9); prevents the anonymous 500 <PROTECT> that also leaks the database directory. Owner decision, Story 1.5 intent gap.
- (decision) AD-45: readiness is hosted by a third unauthenticated web application under /api/ocupilot/ with its own dispatch class and the shell's privilege floor; prevents Story 1.17 re-deciding it or widening the password-authenticated API application. Owner decision, Story 1.5.
- (decision) AD-13: the entity-id wire contract is encode-twice / decode-once (UTF-8 then percent-encode twice; decode once per id); prevents DW-31's Latin-1 codec failing to round-trip a browser-encoded id. Settled by live probe in Story 1.5.
- (decision) AD-12: one carve-out for the static handler streaming file bytes to the response device (a file is not an envelope; the writer is JSON-shaped). Every static-handler FAILURE still renders through Error.Render. Prevents later reviews re-finding correct code as an AD-12 violation. Owner decision at the 2026-09-11 check-in, DW-95.
- (decision) AD-28: corrected the sign-out sentence. Bearer+cookie logout ends the browser-level login across the whole %ISCMgtPortal group; Bearer alone leaves it intact ONLY for a superseded session, because the cookie resolves to the most recently minted session. Prevents a later story offering a 'sign out this tab only' that silently signs the user out of the instance. Measured at Story 1.7 implement.
- (decision) AD-28: tightened the sign-out sentence to separate observation from inference (DW-117). Observed: the credentialled logout deletes the group session node ^%cspSession(-3,'%iscmgtportal:<browserId>') and the cookie then minted nothing at two of the group's nine JWT applications. Inferred and labelled: it therefore ends the login for every application in the group. Prevents a one-probe generalization hardening into a contract.
- (decision) Conventions/ObjectScript naming: scoped the 29-character class-name cap to classes that get a data global (%Persistent). Its evidence is the natural ^<Class>D global, which a storage-free class never has. Prevents the cap contradicting the descriptor layout the same spine mandates - OcuPilot.Screen.Descriptor. alone spends 27 of 29. Story 1.9.
- (decision) AD-44: corrected the key. The classic portal keys custom page resources by normalized CLASS NAME, not page URL - %SYS.Portal.Resources' IdKey Page is a class name and NormalizePage turns a link into one. Prevents a later descriptor author declaring a URL as the key and silently reading no resource. Found at Story 1.9.
- (decision) Conventions/Screen archetype: the descriptor archetype is a closed vocabulary declared in Screen/Archetype.cls and classified list/detail/none; archetype-dependent rules fail closed. Prevents AD-44's detail-view-only exemption rule from silently passing a typo, which it did until Story 1.15 - the field was free text and three readers ignored it.
- (decision) AD-18: corrected the evidence clause. The pinned image ships no LOADED IPM (zero %ZPM*/IPM.* classes across three namespaces, re-verified 2026-09-13) but does carry an unloaded offline installer at /usr/irissys/dist/install/misc/zpm.xml. The rule is unchanged - nothing in the install path may assume IPM - but 'nothing on the filesystem' was false and would have misled anyone checking. Found at Story 1.16.
- (decision by lead) AD-5 amended: two things are generated from a screen descriptor, the write-tool field lists and the client screen mirror (screens.generated.ts). Decision sheet DW-136; the sentence was narrower than what Story 1.9 shipped and than epic-1-context.md already authorized. No implementation change.
- (decision by lead) AD-19 amended: the screen store is framework-free and components mirror it into signals. core/ imports no @angular/core so it runs under node --test. Decision sheet DW-177; the alternative was a real signal store at the cost of the fastest suite in the project.
- (decision by lead) AD-27 bullet 4 corrected: every screen declares its relationship to the classic page, which is AD-44's list-versus-detail rule, not a blanket outbound link. Decision sheet DW-187, an AD-vs-AD conflict; AD-44 and prd.md :298 win, and the code was already right against them.
- (decision by lead) AD-38 amended: the install-gate mark is best effort on every path, not only the container's. The IPM path marks nothing and the window is accepted for Release 1 because IPM is a distribution channel while the container is the shipped install path (AD-18). Decision sheet DW-206.
- (decision by lead) AD-43 corrected from ten auto-refreshing screens to six, carrying EXPERIENCE.md :561's roster verbatim: Processes, Databases, Database details, Task schedule, Task details, System usage. Decision sheet DW-175; an enumeration naming every member beats a count with no list behind it.
- (decision by lead) AD-38 amended: a fifth gate answer, unreadable, for a state read that fails outright (a revoked SQL grant raises PROTECT). It is not a kind of installing - installing and a refused downgrade both mean keep waiting, this means waiting never helps. Install reads the grant back; the schema name is derived. DW-96, owner's design over the decision sheet's.
- (decision by lead) AD-26 amended (Story 2.1 plan intent gap): async is one path with two entries - ShouldRunAsync() handoff through AsyncTaskEndpoint, or a Run() that self-queues and answers 202 - converging on one AsyncResult poll, a bounded wait failing PORT.TIMEOUT; audit record LIST is self-queued (probed ShouldRunAsync()=0 for LIST); inventory fixture records both entries. Owner-delegated decision.
- (decision by lead) AD-2 corrected against %Api.Admin.Dispatch.v1:Main() on 2026.2: gate precedes query params; params seeded into %request.Data then SaveQueryParams() (SaveOneQueryParam alone ignored maxRows: 45 rows for 2); %session stub with Username; sequence runs in %SYS per AD-16; missing ValidateQueryParams reads ERROR #5813 Null oid.
- (decision by lead) AD-3 amended (Story 2.2 plan intent gap, owner-delegated): derivation reads template methods only; Security.User Password/NewPassword live in ValidateRequest wrappers and are authored secret by the tool; classification is a reviewed per-tool entry ordinary|secret|opaque, no entry => secret; field shape (scalar/object/array) is contract, placeholder scalar type informational (WebApp.App GET returns numbers for 5 string placeholders); 'mutating' = class defines RunPut/Post/Delete/Patch; Wallet.Secret one list per Type; Conventions Secrets gains suffix pattern (password|passwd|pwd|secret|apikey|privatekey|token)$ applied at build to string-placeholder fields.
- (decision by lead) Conventions Secrets credential pattern extended (Story 2.2 implement deferral, owner-delegated): suffixes gain secret64, plus the exact name Key (License.Key); ReturnRefreshToken stays secret until a tool needs it shown.
- (decision by lead) Conventions REST route ordering: the three invariants are enforced structurally by check-objectscript.py (Story 2.3, DW-32); only the first two are observable by routing tests, since a :param compiles to ([^/]+).
- (decision by lead) Conventions Screen archetype: a list descriptor with a read declares table (columns label key + kind, two empty-state keys), refused by both engines; write-capable = declares a primary or row action (Story 2.4 plan). NFR-1 end-to-end at 1,000 rows routed to Story 2.10.
- (decision by lead) Conventions Angular naming: a screen served by its archetype page (ListPage over a declared read and table) needs only its ObjectScript descriptor and the generated mirror; per-screen page/store/descriptor files only when the archetype page does not serve it (Story 2.5 plan). epics Story 1.9: web apps use a single slash-carrying id, not a composite.
- (decision by lead) AD-36 amended (Story 2.6 plan intent gap, owner-delegated, option A): a declared read may name one per-row detail call source.rowGet, run after the cap and merged into the row; 404 drops the row, other row faults fail the read. Users LIST lacks Roles and expiry; expired = ExpirationDate set and earlier than today on the instance clock.
- (decision by lead) AD-36: rowGet may declare derived fields from a closed rule set computed on the instance (first rule beforeToday). Story 2.6 Account expired column is kind text, not status (green disc on Yes would mislead).
- (decision by lead) Conventions Dates scoped: the UTC/no-Horolog rule is for emitted timestamps; a comparison against the instance's local calendar date (AD-36 beforeToday) uses +$Horolog (Story 2.6 DW-265).
- (decision by lead) AD-36: a rowGet declares its detail type (GET or INFO) - Task.CRUD LIST coerces Suspended to false while INFO answers truthfully (Story 2.8 probe). epics 2.8 AC2: the banner ships without the Resume control, which is Epic 7's (FR-51).
- (decision by lead) AD-29 amended (Story 2.9 intent gap, owner-delegated): an admin endpoint's ResourcesOR() is a lower bound; %SYS.ProcessQuery requires %Admin_Manage:USE or IRISSYS write beyond the endpoint's %Admin_Operate, so a screen's pair set is established by running the read as a real least-privileged principal on a throwaway. Story 2.9 declares three pairs; the port's 500-for-a-privilege-refusal is filed separately.
- (correction by lead) Correcting entry 80: AD-29's amendment has two halves, and the earlier entry recorded only one. A screen's pair set is established by reading the backing query or class's own privilege check in irislib/ (that is where %Admin_Manage:USE was found) AND running the read as a real least-privileged principal on a throwaway. The spine at AD-29 is the contract.
- (decision by lead) AD-7 amended (Story 3.3 plan inference, owner-delegated): "the job never mutates" scopes to the instance the agent acts on, not to OcuPilot's own protected state (AD-9), which the job necessarily writes - AD-33's progress records, and the bookkeeping a failed call leaves behind. DW-22's flag (clearing a definition's verification when its credential reference no longer resolves) is that second kind and needs no confirm. The plan labeled this an inference; it is settled in the spine instead so it does not cross three specs unlabeled.
- (decision by lead) Three spine amendments at Story 3.5's plan gate (owner-delegated), each blocking a named AC: AD-39 gains the per-violation pair - detail.violations[] carries {field, code, reason}, the refusal copy written once on the server beside the envelope reasons, which is what gives all 22 field-level codes a sentence (DW-339, DW-366); AD-36 gains a second read-source kind - OcuPilot's own protected state resolved against a kernel store's guarded list, changing where the rows come from and nothing else, so the Definitions list and Story 3.7's Switches stay declared reads rather than bespoke pages; AD-11 rule 3 gains a refusal path - the departing screen may refuse an agent navigation, the refusal reaching the turn as an ordinary tool result, because an announcement does not stop the agent discarding unsaved work. AD-5 deliberately NOT amended: the descriptor already declares the read, and only the read's source kind is new.
- (decision by lead) EXPERIENCE.md Fixed strings gains three rows for the Definitions list and form (Story 3.5 plan gate). None of those literals was published anywhere, and ui/tools/strings.test.mjs admits only what the table, seven named extractors and three named extras authorize, so the rows were the only route to an authorized label. The screen descriptions at :149 and :150 carry the labels unquoted and lower-case, which no extractor can lift.
- (decision by lead) Story 3.5 sized at two stories by its plan (seam between the Definitions list and the Definition form; the list is the first screen over OcuPilot's own protected state and takes three of the four blockers). NOT split: the epic's structure is a /bmad-correct-course after the merge gate, per the owner. Recorded here so the correct-course has the seam and the reasoning.
- (decision by lead) Conventions row added (Story 3.5 plan gate): a built screen declares sideBarPosition 0 when it is routable but takes no side-bar position. Chosen over a new `listed` declaration key, which would have touched DECLARATIONKEYS, the mirror's DECLARATION_KEYS and its hand-written TS interface, DeclarationCorpus, ReadTool, screen-mirror.test.mjs, Descriptor.cls and 22 hardcoded ScreensFor strings; the sentinel touches none. Note the footgun the plan found: nothing validated sideBarPosition before this, and Base.cls's +..Field() already made an absent key read 0, so the story gives the key its first validation rule (a built screen must declare it, integer >= 0).
- (correction by lead) AD-21's closing clause was false against the shipped tree and is corrected at Story 3.8's plan gate: it said the static application's matching role is the only application or matching role "either" OcuPilot application carries, written when there were two applications. Story 1.17 added readiness, so there are three and two carry a purpose-built matching role - Test/WebApp already had three separate methods saying so. The invariant restated: no OcuPilot application carries an application role at all, and no application outside the roster carries an OcuPilot role, asserted in both directions.
- (decision by lead) Two spine amendments at Story 3.9's plan gate (the Epic 3 burn-down, owner-delegated). AD-37 gains one bounded deletion exception: OcuPilot removes a credential entry it created when the last definition naming it is deleted - bounded on both sides, since an entry OcuPilot did not create is never touched and one a surviving definition still names is never touched. The reference stays weak; the deletion is a lifecycle rule about a secret OcuPilot put there. A Consistency Conventions row settles optimistic concurrency on OcuPilot's own stores: every guarded save is conditional on the row version the caller read, a zero row count is a refusal (STATE.CONFLICT, 409) rather than a retry or a silent overwrite, scoped to the within-request window. Chosen as a Conventions row rather than a new AD because AD-34 already gives the confirm path the same idiom and this applies it to the stores; every store on Kernel/State/Base inherits it, so Epics 4 and 5 do not answer it again.
- (decision by lead) EXPERIENCE.md Fixed strings gains one row at Story 3.9's plan gate: the stale-save refusal sentence. Appended after the table's last row so none of the 246 existing /** EXPERIENCE.md:n */ comments need renumbering - the step where this has drifted before.
- (decision by owner) AD-27 re-titled and its Rule's first sentence replaced at the Epic 3 merge gate (correct-course 2026-09-16): the admin API is experimental, named as the contest's intended API at the 2026-09-14 kick-off, specified in the published mainspec_v2.json whose 185 v2 paths match the 2026.2 instance path for path, and final in IRIS 2027.1. The containment mechanisms are unchanged; only the reason they exist is now dated. The old 'hidden, undocumented, no stability contract' wording is replaced, not annotated, at every origin: the spine, PRD sections 2, 8, 10.3, 11 and 12, the research open-question row, and epics.md (Epic 18, Story 17.4).
- (decision) AD-37 and AD-42 amended at Story 4.0's plan gate (runner, Epic 4), recording four Epic 3 merge-gate decisions where later stories read them: a key is never stored into a credential entry OcuPilot did not create (DW-342); the proxy host is judged like the endpoint and https tunnels through it (DW-337); a stored credential goes only to the stored endpoint (DW-357); a missing credential-store privilege is a named refusal, never an install grant (DW-349); moving the default marker is a security change (DW-404).
- (decision) AD-31 amended at Story 4.1's plan gate (orchestrator decision under owner autonomy): the job re-checks existence and privilege from current grants, since a least-privileged job cannot read its enabled flag without a second elevation AD-8 forbids; a 120 s poll lease covers disablement and abandoned tabs; OcuPilot's own sign-out abandons turns through the instance because a token logout fires no session event; limits are named constants (600 s, iterations capped at 100, 500,000 tokens, 15 min progress retention, 16,000-character message).
- (decision) AD-11 rule 1 amended at Story 4.1's plan gate (orchestrator decision, DW-333): a definition's system prompt override is administrator-written audited configuration, so it replaces the built-in prompt whole and is never joined to runtime-read text.
- (correction) AD-31's disablement sentence corrected at Story 4.1's re-plan (the orchestrator's pre-authorized fallback): probed on the slot-A throwaway, a disabled account's access token keeps answering and /refresh keeps minting pairs, so the poll lease does not cover disablement and the turn is bounded by the wall-clock limit alone. The earlier clause that a write still fails at the authenticated confirm is removed as unsupported by the probe.
- (correction) AD-31's disablement and lease sentences rewritten as one statement at Story 4.1's dev_complete: the Rule had said both that the lease covers disablement and that it does not. It now says a disabled account's turn is bounded by the wall-clock limit alone, and the lease bounds a turn nobody is watching. Story 4.1's AC 4 in epics.md corrected the same way.
- (decision) Conventions Tool naming row amended at Story 4.2's plan gate (runner): the dotted tool name stays canonical, and a provider request carries it with dots replaced by underscores, because Anthropic's tool-name grammar (^[a-zA-Z0-9_-]{1,64}$) refuses dots; reversible because canonical names hold no underscore.
- (decision) Structural Seed source tree corrected at Story 4.2's first review (runner): tool dispatch lives in Kernel/Agent/ (it needs the governance gate and restraint, and the registry never depends on the kernel), Screen/Tool/ holds the tool base, schemas and registry, and a Kernel/Shell/ package holds the shell reads that the API handlers and their read tools share, so shell tools add no Registry to API edge.
- (decision) AD-24 amended at Story 4.4's plan halt (orchestrator decision under owner autonomy): the kernel itself bounds every context payload and read tool result, the registry refuses a context field declared above the default, and the numbers are fixed: operator row cap 1-1,000 default 200 on Switches, 65,536 characters total cut by whole rows, 1,000 per field unless declared lower, cut mark U+2026, rowsSent/rowsAvailable/truncated/truncatedFields; sharing remembered per user with the instance default as fallback.
- (decision) AD-11 rule 1 amended at Story 4.4's plan halt: screen context enters as a synthetic screen.context tool call and result before the user's message, in the canonical message shape, never advertised; a model-issued call with that name is refused as unknown.
- (decision) Conventions Secrets row reconciled at Story 4.4's plan halt (DW-399): the backstop list adds the credential suffix and the exact name CredentialName, which the server redactor already carried, and one list is held by both redactor and client pattern under a test.
- (decision) AD-24 amended at Story 4.5's plan gate (runner): a conversation's earlier turns reach the model as message and final reply only, capped together at 65,536 characters oldest first dropped, and earlier tool results and screen context are never replayed, so the conversation lock has a history worth protecting without widening AD-11's untrusted surface.
- (amendment by epic-runner-4) AD-11 gains a mechanism paragraph for a tool the browser fulfils, written while planning Story 4.7: a tool declares instance or client fulfilment, the registry refuses client on a write-kind tool, dispatch runs its whole chain (wire name, identity, gate, pairs, arguments) before recording a directive instead of invoking a body, the directive rides the progress poll that already renews the lease, the browser answers once on one route in a closed outcome vocabulary so it can never author model-visible text, the wait is bounded by its own limit and by every turn boundary check on each pass, and a lapse is an ordinary tool result saying the navigation did not happen with the announcement withdrawn. Rule 3's departing-screen refusal is only reachable this way; AD-39 and AD-31 are unchanged, only who runs the body is new.
- (decision by runner-6, orchestrator-delegated) AD-7 amended (Story 6.1 plan gate, option a): the read-triggered-write exception now names exactly two shapes. The second is a derived cache a vendor read path keeps for itself - %Api.Mgmnt.v2.impl.GetRESTApps reaches %SYS.REST.ListRESTApplications, which rebuilds ^%SYS("REST","Application",ns) when the namespace's class index changed (irislib/%SYS/REST.cls:196-216). OcuPilot writes none of it and no operator-created object changes meaning; cost: the first discovery read after a class change rewrites that global in %SYS. Refused alternative: a hand-copied %Dictionary discovery, which is the vendor-internals trade AD-7 already refuses and would drift from /api/mgmnt.
- (decision by runner-6) AD-36 amended (Story 6.1 code review, DW-1008): the max-rows cap bounds rows; a read whose rows derive from one named vendor object may return that object beside them (the OpenAPI viewer's document) - bounded by the vendor's answer for that one object, never in the tool's view, never in screen context (AD-24). All three stages (plan, implement, review) read AD-36 this way; the sentence makes the reading explicit so a later viewer does not re-litigate it.
- (decision by runner-6) Conventions/Screen archetype: a table column may declare an optional emptyKey, the word an empty cell reads instead of (none). Story 6.2 spec gate: an empty Security.Service AllowedConnections is the vendor's encoding of unrestricted, and (none) would tell a security administrator no address may connect. Refused alternatives: rewriting [] in the port (the service editor round-trips the field) and leaving (none) (misstates the security posture).
- (decision by runner-6) AD-36 amended (Story 6.3 spec gate): a declared rowGet detail type may also be CERTINFO where only that type carries the fields - Security.X509Credential LIST has no subject, issuer or validity; CERTINFO answers all three. The descriptor declares it as rowGet.type (GET default, INFO, CERTINFO).
- (decision by runner-6, orchestrator-delegated) Story 6.4 spec gate (orchestrator option a): AD-5 gains a tabbed-screen bullet - one descriptor per tab grouped by a declared tab, each tab with its own primary entity type, read, tool and privilege set, the listed member the side-bar entry, grouping declared never hand-routed; its multi-entity bullet keeps secondary types in AD-14 routing with the OAuth example narrowed to one tab. AD-36 names three source shapes: a single-object GET whose 404 reads as zero rows, a forEach per-parent list bounded by the row cap and by cap+1 parents listed (so at most that many child calls) that reports truncation, and <object>.<member> fields. AD-44: the one Release 1 exemption (the OAuth 2.0 tabs) is declared by five descriptors and the check reports the five under one exemption, so SM-C1 counts one. Refused: one multi-read descriptor (needs Screen/Tool/** changes Epic 4 owns, and would hold Epic 6's merge behind Epic 4).
- (decision by runner-6) AD-36 amended (Story 6.5 spec gate): a read may name a list-shaped admin request type other than LIST (Task.CRUD UPCOMING, one row per scheduled run) and fixed query parameters no caller can change or remove (source.query, the vendor's onDemand=1). Epics 6.5 AC amended: no inert Run control; Story 7.5 declares Run with its handler (2.8 Resume precedent).
- (decision by runner-6) Story 6.6 spec gate (DW-1020): AD-5's sub-resource bullet now says a parent-scoped screen's route id identifies an entity of the parent screen's primary entity type, resolved through the parent declaration and never declared twice, while its rows keep their own type (Secrets -> wallet-collection, a task's history -> task). AD-36 names HISTORY beside UPCOMING and a criterion vendorParam (a value sent under the vendor's name when that name is reserved for the read tool's own filter). EXPERIENCE.md: the one-task history is a plain list, its only criterion coming from the route.
- (decision by runner-6) AD-36 amended (Story 6.7 spec gate): a single-object GET under a parent declaration takes its id from the route as its one criterion and may name one detail call keyed by it (Task.CRUD GET for properties and schedule, INFO for last/next run and the truthful Suspended); a route-id criterion does not bar auto-refresh because it reads on open, so AD-43's refusal of refresh-with-criteria keeps its purpose for search forms. Also recorded: the agent-navigation AC is verified by a deep link carrying the scoped identity (orchestrator pre-answer); UJ-6 replay is Story 7.6.
- (decision by runner-6, orchestrator-delegated) AD-43 amended (Story 6.8 spec gate): the auto-refresh roster is seven - the owner-decided six plus Process details, whose classic page refreshes (catalog OS-08, reconcile G-13). EXPERIENCE.md's Auto-refresh controls row and Process details screen row updated; the meter archetype row no longer names Process details (the classic page has no threshold meters, verified in irissys ProcessDetails.cls). epics.md Story 6.8 AC1 narrowed: the current SQL statement becomes whether a cached SQL query is executing, named by its routine; statement text is Story 19.10's (no port reaches INFORMATION_SCHEMA.CURRENT_STATEMENTS).
- (correction by runner-6) AD-43 Binds and Prevents lines corrected from six to seven screens to match the roster amendment of Story 6.8 (DW-1050); AD-29's %SYS.ProcessQuery wording checked and left, since VariableByPid is that class's method.
- (decision by runner-6, orchestrator-delegated) Story 6.9 spec gate: AD-36 gains single-object parts (at most 3 {type, as}) merged into one row as <as>.<member> fields, with AdminPort mapping request types that lack the TYPE prefix (Monitor SYSTEMUSAGE, SYSTEMUSAGESHM, DASHBOARDMAIN). epics.md 6.9 AC2 and prd.md FR-56 amended: meter names come from the admin Monitor answers, CPU goes to FR-76. DESIGN.md: success fill, 85/95 labeled as borrowed from the vendor lock-table cut-off (not a memory threshold), dashboard state words, em dash while pending; EXPERIENCE.md meter row state words Normal/Warning/Troubled.
- (decision by runner-6) Story 6.9 spec gate: AD-36 parts clause says the member projection composes (Dashboard.Performance.CacheEfficiency), still one bounded row of scalars; epics.md 6.9 threshold AC marked amended to the 85/95 borrowed assumption.
- (decision by runner-6) Story 6.10 spec gate (orchestrator-delegated): AD-5 gains a cross-screen row target - a list may name another screen's route and the row field holding its id, validated in both engines and resolved ahead of the paired-surface chain, because a lock's owner is a process while the lock's own key is its removal id (Q3 option a). Locks is instance-scoped, not namespace-scoped: the same read answers identical rows from HSCUSTOM and USER, a lock's scope marker is a database directory, and the vendor's View Locks page is pinned to %SYS (AUTONS=0); epics.md 6.10 AC1, EXPERIENCE.md :94 and its :408 example, and prd.md FR-57 amended (Q1 option a). The in-transaction column is dropped: FR-57 makes it a removal concern, so epics.md 6.10 AC3 now points at Story 16.12's own 409 refusal and no AD-36 derived-field grammar is added (Q2 option c, departing from the runner's recommendation).
- (correction by runner-6) AD-29 corrected at its origin (orchestrator-approved): %SYS.ProcessQuery.AllowToOpen admits %Admin_Manage:USE, or IRISSYS write, or IRISSYS read, or the caller's own pid (ProcessQuery.cls:425), while VariableByPid requires %Admin_Manage:USE outright; Story 6.8's pair set stands and DW-1050 carries the note.
- (decision by runner-6) Story 6.11 spec gate (orchestrator-delegated): AD-26's Release-1 async paragraph corrected - the database free-space figures arrive together from one read with a rowGet per row, not per figure, because Read.cls answers one envelope and fourteen databases cost 0.852 s against NFR-1's 2 s (Q1 option a); epics.md 6.11 AC2 and EXPERIENCE.md :496 and :625 amended with it. Two descriptors, DatabaseList listed and DatabaseFreeSpace unlisted, because SysCRUD.ResourcesOR answers Manage only for LIST while the AsyncResult poll needs Operate and a descriptor's gate requires all its pairs (Q2 option a, the 6.4 per-tab shape). Database details ships properties and volume files; the background tasks section is chartered to the owner's decision sheet, since %SYS.BackgroundTask:RunningInDatabase is a Final Internal class query no %Api.Admin class references (Q3 option a), and epics.md 6.11 AC3 amended. Database details reads GET plus TYPEINFO as parts, with an added AC that an INFO fault keeps the last values and shows the refusal strip (Q6). Classic key is %CSP.UI.Portal.OpDatabases, which AD-44 unions into the gate.
- (decision by runner-6) Story 6.11 spec gate, lead-applied under Rule 20: AD-5 gains a bullet that a page may issue another built screen's declared read through the ordinary read route, so that screen's gate, cap and field set apply unchanged - Database details reads the volume-files screen's read for its second collection, and the Free-space view reads the Databases list's read so rows can render with skeleton cells before its own rowGet-per-row read resolves, which is what makes the amended AC2 satisfiable by one envelope. Not a way to widen a read. VOLUMELIST needs no spine edit: the spine never enumerates the list source types, so adding it to READSOURCETYPES, AdminPort's TYPESUFFIXES and Read.Execute is additive implementation work on the UPCOMING and HISTORY precedent. EXPERIENCE.md :98 corrected with the AC3 amendment, and the Databases Fixed strings row applied at :365.
- (correction by runner-5) Story 5.1 spec gate, lead-applied under Rule 20: AD-7's Rule corrected at its origin (DW-445). It read "It writes per-step progress to a temp global keyed by turn id"; the shipped `Kernel.State.Turn` and `Kernel.State.Step` tables and AD-33 have contradicted that since Epic 4, so the clause now reads "OcuPilot's own protected storage (AD-33), keyed by turn id and owned by the user who started the turn". An amendment to an existing Rule, so no `AD-n` was claimed. Correcting it here rather than in the story's Design Notes is the point: the wrong sentence was the one the next builder would have read.
- (decision by runner-10) Story 10.1 spec gate, lead-applied under Rule 20: AD-42's "every provider call carries a bounded timeout" now names the bound - one call's worst case, its attempts and their backoff together, is at most the 300 s attempt budget, and a stored per-call timeout or attempt count that would exceed it is clamped at the point of use rather than refused. DW-1104 reported that the deadline only gates when a further attempt may start, so `State.Egress.TimeoutSeconds` and `MaxAttempts` were both unbounded and the previous worst case was the budget plus the stored timeout; the owner decided the direction at the Epic 4 merge gate. The clamp lands in `Kernel/Provider/Retry.cls` and is applied in `Base.cls` at the two read sites, not in `Kernel/State/Egress.cls`, which is Epic 5's footprint and is not where a caller-supplied settings array would be bounded anyway. `Kernel/Agent/Limits.cls:91-101` still carries the superseded sentence and is Epic 5's footprint, so it is left for that epic; DW-1104 keeps the residual at Story 10.1's adjudication.
- (correction by runner-10) Story 10.1 code review, lead-applied: AD-42's bound corrected at its origin the same day it was written. The clamp bounds what OcuPilot configures, not the wall clock, because `%Net.HttpRequest.Timeout` re-arms on each socket read (`irislib/%Net/HttpRequest.cls:2075`, read by the lead), so a provider that drips its response runs past 300 s and is bounded by AD-31's turn limits instead. DW-1179 carries the open product question -- whether to bound an attempt at the transport -- to the decision sheet; the clamp arithmetic Story 10.1 shipped is unaffected and its pins stand.
- (decision by runner-10, orchestrator-directed) Story 10.3 spec gate, lead-applied under Rule 20: AD-42 now says a cloud instance-metadata endpoint is refused in every address family and that the marked-local flag licenses none. DW-1214: `Egress.IsLinkLocal`'s IPv6 arm is 0xFE80-0xFEBF while `IsPrivate`'s is 0xFC00-0xFDFF, so AWS's `fd00:ec2::254` and GCP's `fd20:ce::254` read private rather than link-local and `IsPermitted` admitted them to a marked-local definition, while the IPv4 `169.254.169.254` was refused with no escape. The orchestrator ruled against redefining unique-local as non-local, because `fc00::/7` is how private IPv6 LANs are addressed and a local model legitimately sits there -- refusing the range would trade the use case Story 10.3 exists to serve for a narrow risk. The asymmetry removed is the escape, not the range. The AD records the denylist's incompleteness rather than claiming coverage it does not have; the address arithmetic stays in the code and its test. Azure and Oracle use the IPv4 address and Alibaba's 100.100.100.200 fails every `IsPrivate` octet test, so the hole was specifically IPv6 unique-local.
- (correction by runner-10) Story 10.3 code review, lead-applied under Rule 20: AD-42 now records that a marked-local endpoint bypasses the proxy entirely and is therefore not judged against it. The owner decided this at the Epic 4 merge gate (DW-441) and Story 10.3 ships it in `ProviderPort.Dispatch` and `Egress.LeavesInstance`, but the same story amended AD-42 for DW-1214 without recording it -- a Rule 20 miss the review caught (DW-1218). It matters beyond bookkeeping because Epic 11's egress line and streaming plan against this AD's text, and because AD-42's own "cannot disagree" clause is what makes the bypass and the chip one computation rather than two.
- (decision by runner-5) Story 5.4 spec gate, lead-applied under Rule 20: AD-8 and AD-9 amended to name a SECOND privileged routine application, for one identity read. AD-8's Rule said "no elevation anywhere on the request path"; refusing a disabled account at authentication (DW-444, decided by the owner at Epic 4's merge gate) needs that account's `Enabled` flag, which an unescalated caller cannot read. The amendment is deliberately narrow: one method, its own application, one flag for the authenticating user, and no access to OcuPilot's own data - so the identity application cannot become a general elevation. Recorded here because the alternative was shipping a hole the owner had already refused, and because a later reader finding two applications needs to know the second was decided rather than accreted.
- (decision by runner-15) Story 15.1 spec gate, lead-applied under Rule 20: new **AD-49** - a self-service account action is the user's own write and sits outside the agent write path, so it mints no proposal, takes no confirm token, is never a tool, and calls the documented `%SYSTEM.*` method in the caller's own process. The plan stage proposed `$System.Security.ChangePassword($Username, new, old, .tSC)` against the epic context's mapping to the admin API's `POST /security/user/password`, and the admin route is wrong three ways: it needs `%Admin_Secure` (the elevation AD-8 forbids), it takes no old password so it is an administrator's reset rather than a self-service change, and `AdminPort`'s `TYPESUFFIXES` admits read types only. Precedent for the direct call is broad - 23 classes outside `Port/` already call `$System.Security.Check`, `CheckUserPermission`, `Audit`, `AddRoles` and `GetGlobalPermission` in the calling process, as AD-8 and AD-9 require - but every one of those is a read, an audit emission or an escalation into OcuPilot's own state, so this is the first direct **mutation** of an IRIS security object outside a port and the seam was worth naming before 15.6's theme choice and Epic 16's account extras answer it differently. The AD also records why no OcuPilot audit marker is emitted: IRIS's own `%System`/`%Security`/`UserChange` is registered and enabled on this build and fires on failure as well as success (measured on slot C), AD-15 binds agent writes, and AD-46 already puts the vendor row on the audit screen - so an operation for which IRIS emits no event is the case that needs a marker, and a later story that finds one amends this AD.
- (decision by runner-15) Story 15.2 spec gate, lead-applied under Rule 20: new **AD-50** - per-user preferences are one `Kind`-discriminated store in the protected database, read and written through a caller-own shell-chrome route rather than AD-36's declared read. The plan stage settled the shape and flagged it as architectural rather than writing the spine itself, which is the correct split. Two forces fix it: the Conventions row's 29-character cap on a `%Persistent` class name leaves 7 after `OcuPilot.Kernel.State.`, so `Favorite` and `Bookmark` (30 each) cannot exist while `Pref` (26) can; and AD-36's `state` source kind resolves through `GuardedScreenRows(Output pRows, pMaxRows)`, which takes no caller argument, so a per-user read cannot use the declared-read contract without giving that contract an argument every other read in the product would then carry. The AD also closes the door the shape opens: a preference is never screen context and never a tool's view, and the store is not a licence to satisfy a survives-a-sign-out criterion with `localStorage`. Story 15.5 extends it by adding `Kind` values, not `State` subclasses.
- (decision) AD-27: the vendored admin API spec matches the 2026.2 instance on 184 of 185 v2 paths, not all 185; method sets identical on all 184. Prevents a story asserting an exact path-for-path match that fails on /v2/security/oauth2/revoke.
- (decision by epic-5 runner) AD-8 amended (DW-1208, owner-decided at the merge gate 2026-09-21): a write tool's pair set is the screen's own declared set, and an administrative resource on it is required at USE, never WRITE. IRIS's built-in %Admin_*/%Service_*/%Development resources are system resources carrying no WRITE -- verified on ocupilot-slot-a, where every shipped role grants them :U and only %DB_* carry :R/:RW -- so a WRITE pair is a gate only %All can pass. USE is also exactly what the vendor's editor checks. An OcuPilot-owned resource carrying its own WRITE was considered and rejected as a second authorization model that must be kept in step with the vendor's. Amended rather than given a new AD id because AD-8 already owns the pair-set rule; no id claimed. Caught by the Story 5.8 reviewer as DW-1432: the decision had been recorded only in WebAppUpdate.cls's doc comment and the ledger trailer, which is nowhere the next plan stage reads.
- (decision by epic-5 runner) AD-15 amended (Story 5.10 intent gap, orchestrator-authorised option (a) 2026-09-21): a write that CLOSES the audit channel cannot be marked at all - an impossibility, not a relaxation. Measured: Audit() returns 1 on, 0 in the same process with AuditEnabled=0, 1 restored, with Events.Exists=1 throughout so the 0 is the closed channel. Marker-before-write considered and REJECTED as strictly worse (it would record an agent write that may then fail). Correlation is one-directional for this one operation: ledger to audit survives by timestamp via the vendor's registered and enabled AuditChange/SystemChange rows; audit to ledger is lost because those rows carry no proposal id. Scoped to that operation only, never general licence.
- (decision by epic-5 runner) Story 5.11 spec gate, lead-applied under Rule 20: new **AD-51** - an action-style write declares its own admin request type, admits no settable fields, sends no body, and fingerprints the fresh read rather than the sent body. The plan stage settled the shape, argued at length in Design Notes that it needed no HALT, and did not write the spine; the lead agreed with the argument and wrote the AD, which is the correct split. Why it is a gap and not a conflict: AD-3 already names `Task.Manager`, `Process` and `Lock` as "action-style with trivial or empty bodies", so the spine contemplates such endpoints - it had simply never said what the proposal, fingerprint and confirm path do with one. Measured on ocupilot-slot-a: `Task.CRUD` exposes resume only as bodyless `TYPERESUME = 14`, `NeedsRequestBody()` excludes it, and `MergeJsonAndObj` omits `Suspended`, so no PUT can resume a task and the tree's three hardcoded PUT sites (`AdminPort.MUTATINGTYPES`, `Confirm.WRITETYPE`, `Mint.READTYPE`) had to become per-tool declarations. The two invariant readings the AD fixes: AD-4's Prevents ("two fields silently erasing the other forty") has NO SUBJECT for a write that sends nothing, so the merge idiom is not being dodged; and AD-6's fingerprint wording says "the property set the write will send" only because AD-4 makes that a copy of the fresh read for a merge write - taken literally for a bodyless write it would mean an EMPTY fingerprint and no protection at all, so the fresh read is the set. Written now rather than left in the spec because 5.12 (process suspend/resume) and 5.13 (`SYS.ApplicationError` deletes) are the second and third action-style writes and Story 7.6 adds Run, Suspend and Delete on the same descriptor - four consumers that read the spine, not Epic 5's spec.
- (decision by epic-5 runner, orchestrator-approved) Story 5.12 spec gate, lead-applied under Rule 20: **AD-51 gains a declared fingerprint subject for action-style writes, and AD-6's selection sentence is corrected at its origin** so the two do not disagree. The ground is not that AD-6 is unsatisfiable for `Process` but that its mechanism answers the WRONG QUESTION for an action write: a digest over a live process's whole 50-key read refuses the confirm whenever the target has executed a command between mint and confirm, so it fires hardest for exactly the processes the action exists to act on -- a runaway process is the one whose counters move fastest -- and never for an idle one. Measured by the lead on ocupilot-ci: across two reads of a WORKING process `CommandsExecuted` moved 1,678 to 6,004,249 and `CPUTime` 0 to 90; the idle CONTROL daemon held still over 20 s, which REFUTES the plan stage's own supporting claim that CPUTime moves on an idle daemon and makes the gap sharper rather than weaker. Two of the lead's own probes were discarded before any result was banked: one compared object-valued keys by OREF and read the always-different answer as drift, and its correction reported zero movement and could not be explained. AD-6's purpose survives intact -- it prevents a write against state that MOVED UNDER THE DIFF, and for an action write there is no payload, so the reviewed intent is "this target, in this state, this verb" and a counter advancing is the process doing its job. Adequacy is structural, not a review convention: the subject must carry the scoped target identity (AD-13) and every field the action's precondition reads (`State` for suspend/resume), because a subject of `{Pid}` alone would let a confirm succeed against a process somebody else had already suspended. Validated by the SAME builder that validates `fingerprintExcludes`, through one extraction consumed by both -- a second validator beside the first is the DW-1206 defect wearing a new hat, fixed three stories earlier by extracting the union once. Amending two existing ADs, so no id was claimed and the counter was not touched.
- (decision by epic-5 runner, orchestrator-approved) Story 5.13 spec gate, lead-applied under Rule 20: new **AD-52** - a write tool declares the port it reaches its target through, defaulting to `AdminPort`. The plan stage flagged it and the orchestrator nearly declined it as premature, then measured: `AdminPort` is returned as a CONSTANT from `Confirm.cls:57`, `Mint.cls:45` and `Prohibited.cls:252`, while AD-48 routes the application error log through `LogSourcePort`, which has no delete methods at all. So the write path **could not express Story 5.13's write at all** - a seam earning itself on first contact rather than a generalisation with no consumer. Scoped deliberately: which port answers is the only thing that varies; the proposal, the diff, the fingerprint (AD-6, and AD-51's declared subject where the write is action-style), AD-34's single atomic transition, the caller's own privileges through that port's own gate (AD-29, AD-8) and the agent marker (AD-15) are identical whichever port answers, so a port is where the target lives and never a lighter path to it. Declared per TOOL rather than per screen because the port is a property of the target: a screen may read through one port and write through another. Also recorded at the same gate, as a Rule 5 tier-1 amendment rather than a spine change: `epics.md:3791` named the right scope and the wrong method - `DeleteByNamespace` removes every error for every date, contradicting the story's own preceding criteria that confirm deletes EXACTLY the enumerated ids and that residue is correct - so it now reads `DeleteByError(ns, date, <ids>)` once per enumerated date, and AD-48 is untouched.
- (correction by epic-5 runner, orchestrator-approved) Story 5.13 spec gate: **AD-51's adequacy clause corrected at its origin.** It had required the subject to carry the scoped target identity AND every precondition field; it now requires **the precondition fields the tool's own read type answers**, with identity explicitly NOT required. The clause was wrong because it conflated two questions: the fingerprint asks "has the state I reviewed moved?" while "which object is this?" is already answered by the proposal's `TargetRef` (AD-13), so re-carrying identity in the subject adds no protection. Its cost was concrete rather than theoretical: `Task.CRUD`'s `INFO` answers no `Id` while `Process`'s `GET` answers `Pid`, so the clause bound one shipped tool and not its sibling purely by an accident of what each vendor read returns - and enforcing DW-1476 under it made `TaskResume` unregistrable, reddening six methods across two classes. Reverted rather than loosened, which was the right instinct: a rule that makes shipped, correct code unregistrable is a defect in the rule. The corrected clause makes DW-1476 enforceable, which was its point. Recorded with the *reason* in the AD so the next reader does not re-tighten it. Root cause named by the orchestrator: the constraint was written from the shape the artifact was assumed to have.
- (decision by epic-5 runner, orchestrator-approved) Story 5.13 spec gate: **AC2's process-ownership prohibition narrowed, reversing the Story 5.12 merge-gate ruling.** It now refuses only the process serving the confirm request (`$JOB`) and any OcuPilot turn job (AD-7), whoever owns it; every other process the user owns stays proposable. Two things drove the reversal. First, the fixture evidence: `Install/Fixture.cls` creates no process at all - its single `JOB` is inside a doc comment at line 56 - so the "make the demo fixture create its target under another account" resolution was unavailable, and `JOB` inherits the spawner's username so only a `Login` or a task `RunAsUser` would change it, a durable side effect AD-25 forbids the fixture to leave. Second, and more important, the broad reading's stated authority did not hold: AD-10's self-protection items are **accounts** (the current user, the last `%All` holder, `_SYSTEM`), the serving path, and terminating IRIS system processes - **it nowhere names process ownership as a harm** - so the broad prohibition was a new rule borrowing AD-10's authority rather than an application of it. Its cost was not only the undemonstrable demo: it prohibited the feature's most likely legitimate use, an administrator suspending their own runaway background job. `$JOB` at confirm is the certain irrecoverable harm (it kills the write inside AD-34's transition); suspending your own portal session is recoverable by signing in again. The turn-job half closes DW-1477's cross-user vector inside the SAME predicate rather than as a second one to keep in step (DW-1206's lesson). The demo then works unchanged, its target being a second portal session's process.
- (decision by epic-7 runner, orchestrator-approved) Story 7.2 spec gate, Rule 20: new **AD-56** - an action write may send a body made only of its declared secrets (CHANGEPWD takes exactly {NewPassword}, measured), and a screen action accepts values only under names its tool declares, a list field changed by a server-side delta; secrets travel under the existing secretArguments declaration AD-55 also uses, never a second channel.
- (decision by epic-7 runner, orchestrator-approved) Story 7.2 spec gate, Rule 20, DW-1486: **AD-10 amended** - the account predicates (current user, _SYSTEM, service account, last %All holder) are evaluated by effect for delete, disable and a Roles delta stripping %All; the plan measured all four firing on disable only, so a bodyless delete passed them.
- (correction by epic-7 runner) Story 7.2 implement measurement: **AD-56 (ii) corrected at origin** - a CHANGEPWD clears ChangePassword, so the change-on-login flag is sent first (a refusal stops everything) AND again after the password lands; two agent proposals need the flag confirmed after the password.
- (decision by epic-7 runner) Story 7.4 spec gate, Rule 20: **AD-53 amended** - a write that opens or closes the audit channel records the observed marking fact the banner reads; the screen's caller re-reads the auditing flag and OcuPilot's marker event through the port with its own privileges (MOVESMARKING opt-in). OcuPilot's own state, not a marker.
- (decision by epic-7 runner) Story 7.5 spec gate, Rule 20: **AD-51 amended** - a vendor type requiring a fixed body no caller supplies (Task.CRUD RUN's {RunNow:true}) takes it from the port keyed by endpoint and type; the tool still sends no body and the ledger records none.
- (decision by epic-7 runner) Story 7.8 spec gate, Rule 20: **AD-52 amended** - a tool-declared port may carry out a write the admin API cannot express through the documented %SYS class the vendor endpoint itself calls, reproducing its gate, not-found and guard first (ProcessPort: SYS.Process.Terminate with SendError for FR-55's error-to-job flag).
- (decision by epic-7 runner) Story 7.10 spec gate, Rule 20: **AD-48 amended** - the three application-error scopes are composite-id prefixes, each enumerated at proposal time and deleted through DeleteByError; DeleteByDate and DeleteByNamespace are never called (measured: DeleteByDate deletes an error logged after the enumeration; emptying a date restarts its numbering, so entries carry Time).
