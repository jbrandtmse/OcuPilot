---
topic: OcuPilot portal UX: DESIGN.md and EXPERIENCE.md
updated: 2026-09-25T20:26
---

- (event) workspace created; DESIGN.md and EXPERIENCE.md stubbed frontmatter-only; intent Create, no prior UX runs
- (decision) sources inherited by reference: PRD+addendum (prds/prd-OcuPilot-2026-09-08), research report + feature catalog + auth spike handoff (research/technical-ocupilot-portal-feature-landscape-2026-09-08), product brief+addendum (briefs/brief-OcuPilot-2026-09-08), logo renders (logo/) as brand import
- (decision) working mode: Coaching path (user overrode the Fast-path recommendation); creative tools woven in as decisions come up
- (decision) stakes: internal ops tool, contest-judged; IRIS administrators at a keyboard, community voters judge a demo; WCAG AA floor, keyboard-first
- (decision) brain dump approach: seed from sources via subagent extracts, then batch the gaps to the user
- (event) import: logo/OcuPilot-Logo-web.png copied to imports/OcuPilot-Logo-web.png (user-supplied brand asset)
- (event) import: logo/OcuPilot-Logo-full.png copied to imports/OcuPilot-Logo-full.png (user-supplied brand asset)
- (event) import: logo/OcuPilot-Logo.jpeg copied to imports/OcuPilot-Logo.jpeg (user-supplied brand asset)
- (event) extracts written to .working/: extract-prd.md (6 journeys, 87 surface rows, 35 states, 22 owner decisions), extract-research.md (14 auth states, classic-portal pain points), extract-catalog.md (119 P0 rows, ~63 surfaces, 26 destructive P0 actions), extract-brief.md (8 decisions, no look-and-feel statements)
- (event) import inspected: logo shows an eye with a globe iris (deep navy to teal gradient), a winged left edge, and a docked panel on the right holding a yellow robot; wordmark OcuPilot in a bold geometric sans, deep navy. The mark literally mirrors the product: eye = watching the instance, right-docked panel = the co-pilot
- (decision) UI system: Angular Material (Material 3) + Angular CDK. Both spines inherit; DESIGN.md specifies the brand-layer delta over Material tokens, EXPERIENCE.md the behavioral delta. Rationale: ARIA-complete components and CDK overlay/table/virtual-scroll on a 19-day build; same Material-token language as the vendor's Angular editors
- (decision) register: Bold and branded. The logo's navy-to-teal and robot yellow carried through the shell; unmistakably OcuPilot in a screenshot. Directions will be rendered around this register
- (decision) dark mode: light and dark tokens defined and contrast-checked in DESIGN.md from day one; the polish-week theme toggle (FR-73 / SH-22) becomes a flag flip
- (event) anything-else probe: user said the extracts cover it; proceed to open decisions
- (assumption) form-factor resolved from sources: single-surface desktop web (Chrome), Angular SPA at /ocupilot; below ~900px behavior still to decide
- (assumption) audit database viewer lives in the Logs area (LG-02); the Security area's auditing configuration screen cross-links to it (SS-14 duplicate) — to confirm at triage
- (decision) navigation model (user's words): primary navigation is the agent in the right panel; otherwise follow VS Code conventions: a left rail of icons (user will generate the icons once their meaning and the color scheme are fixed; placeholders until then), a slide-out menu from the rail icons listing the area's screens (behavior to confirm), and a top-center command box like VS Code's command palette
- (decision) area order: daily-use order — Logs, OS management, Tasks, Permissions, Web applications and REST API explorer, Security and secrets
- (decision) agent administration lives in a seventh rail entry ('Agent co-pilot'): Definitions, Switches; polish week adds Governance policy, Agent audit ledger, Transcripts. Satisfies FR-20 (reachable without the agent)
- (decision) Home is panel-forward: the panel widens on Home, the six areas are a compact row, Home is where you talk to the agent; polish-week System Information panel, favorites and recents fit above/beside the row
- (event) renders dispatched: design directions (hero = Web applications list at the UJ-3 climax with the VS Code-shaped shell; secondary = panel-forward Home) and IA diagram (excalidraw)
- (decision) slide-out: VS Code primary side bar semantics — click a rail icon to open a side bar listing that area's screens, click the same icon to collapse; stays open across routes until collapsed; open/closed state and width remembered
- (decision) command box (top center): screens and actions — typing filters every screen the user may open plus the current screen's command-bar actions; Enter navigates or runs. Not a channel to the agent
- (decision) panel width: default 400 px, minimum 320 px, remembered per browser; full-screen toggle; on Home the panel widens to about half the viewport
- (decision) narrow viewport (<~900 px): squeeze like VS Code — the panel keeps its minimum width, content shrinks to its own minimum then scrolls horizontally; no overlay or bottom-sheet mode
- (event) user shared a VS Code screenshot mid-turn as the shell example: activity bar left (icon stack, account + gear pinned bottom, badge counts on icons), command center top-center with back/forward arrows, editor area with keyboard-shortcut hints when empty, right-docked panel with a tabbed header row plus full-screen and close controls, status bar bottom (branch, errors/warnings, language mode, notifications bell). Note: OcuPilot's panel has no close control in Release 1
- (event) color themes rendered: .working/color-themes-1.html — V1 Deck (navy chrome, teal actions), V2 Iris (teal chrome, yellow for the agent), V3 Lantern (light neutral chrome, gradient on header + panel edge), V4 Signal (gradient everywhere), V5 Nightwatch (dark-first). All five pass AA in light and dark; logo teal #2090A0 is a non-text gradient stop only; light-mode agent accent is dark gold #6B5E00, robot yellow #F2D64B returns in dark mode; server flags: Live red (classic precedent), Test amber, Failover violet, Development green; added shell/on-shell/shell-edge and focus-ring-inner roles
- (decision) color theme: V3 Lantern — light neutral chrome, the navy-to-teal gradient confined to the header and the panel's edge, yellow (dark gold in light mode) marks the agent and the just-changed row. Tokens lifted from .working/color-themes-1.html V3 block
- (decision) logo: the provided OcuPilot logo (imports/OcuPilot-Logo-web.png) sits in the upper-left corner of the header
- (decision) default mode: light on first load; dark is the polish-week toggle (both token sets defined now)
- (decision) header/status split: header holds the logo, the command box (top center) and the namespace switch; a VS Code-style status bar along the bottom holds server, instance, user, licensed-to, the server-flag badge, the auto-refresh stamp and the connection state
- (decision) rail badges: only an attention dot on the pinned Agent co-pilot entry (unconfigured, kill switch on, definition needs attention); no counts on other rail entries
- (assumption) rail icon meanings fixed for the user's icon work: Home, Logs, OS management, Tasks, Permissions, Web applications and REST API explorer, Security and secrets, Agent co-pilot (pinned bottom)
- (decision) lists: fetch up to a max-rows cap (default 1,000, persisted per screen with sort and filter), CDK virtual scroll, client-side sort and filter; server-side criteria only where the API searches (audit viewer, task history); the classic page-size control is dropped
- (decision) rows: one click selects the row and it becomes the panel's selected entity; the name cell links to the detail or editor route; a row overflow menu and the command bar both act on the selection
- (decision) editors: every create and edit form is a full-page route with a sticky Save/Cancel bar; large editors (user, web application, role, service, SSL/TLS, LDAP) use Material tabs mirroring the classic tabs; a reduced form ends with a 'More in the classic portal' link card; dialogs only for set password, resource grant, terminate process, remove locks and delete confirmations
- (decision) log viewers: lines parsed into time, pid, severity, text with severity chips; sticky search with highlight; jump to top/bottom; 'Load newer' for the tail; a Raw toggle shows the bounded monospace file view on the code surface. Application errors keep the namespaces → dates → errors drill-down; audit viewer keeps its criteria form + detail dialog
- (assumption) task wizard: vertical Material stepper (Basics · Task type and settings · Schedule · Options and notifications); Edit task reuses the same form in tabs with current values — field lists arrive when the legacy CSP source is pulled (OQ7)
- (assumption) toasts: off-screen change toasts appear bottom-right above the status bar, stack at most three, persist until dismissed or 10 s, and carry the 'Open in <screen>' link; change highlight fades over 2 s and clears on the next interaction with the row or field
- (assumption) progress channel (OQ17, due 2026-09-09): tool-call cards appear in order during the turn whether the channel is polling or server-sent events; the spine specifies card behavior once and notes the channel as open
- (decision) keyboard model: VS Code-shaped — Ctrl/Cmd+K opens the command box, Ctrl/Cmd+I focuses the panel input, Ctrl/Cmd+B toggles the side bar, Escape returns focus to the screen; Enter sends, Shift+Enter newline; tables: arrow keys move selection, Enter opens the selected row, the row overflow menu opens with Shift+F10 or the menu key
- (decision) agent mark: the logo's robot is the agent's avatar beside its messages and cards; rationale and expected impact sit on the agent-container tint under the headings 'Agent's rationale' and 'Expected impact'; the human's messages carry no avatar
- (decision) tone: UI copy plain and direct, second person, sentences not labels; every error says what happened and what to do next. The agent speaks in first person, never claims success it did not verify, and names the screen it means
- (decision) citations in Release 1: replies name the rows they used in plain text and offer to select them; the agent's navigation tool does the selecting; polish week (FR-71) turns names into click-through chips. UJ-1's climax is recorded in this Release 1 form
- (assumption) Home suggested view (panel, UJ-6): attention lines — tasks suspended after an error, application errors today per namespace, new alerts.log entries, agent status (definition, read-only, kill switch); each line is a prompt the user can send
- (event) IA diagram rendered: .working/ia-2026-09-08.excalidraw (292 elements; auth stack, rail with eight area bands, P0 solid / P1 dashed / dialogs rounded, cross-cutting band, panel column with context and navigation arrows); indices validated
- (event) design directions rendered: direction-bridge.html (A, light, navy chrome, dense 13px/36px rows, 6px radii), direction-stripe.html (B, light, white header + gradient stripe, airy 14px/48px rows, 12px radii), direction-nightwatch.html (C, dark, VS Code-Dark layering, 13px/38px, monospace identifiers), direction-beacon.html (D, light, full-bleed gradient header, tonal cards, roomy 14px/44px, 16px cards). Each shows the UJ-3 hero, a panel-forward Home and one state variant
- (decision) design direction: A Bridge — solid navy rail and header framing a light instrument surface, teal the sole action color, gradient rationed to hairline indicators, yellow only for the agent and the changed row; dense: 13px body, 36px rows, 6px radii, M3 pill buttons, Inter-style sans
- (override) user on Bridge: 'the logo and robot avatar don't look right. Need to go back to the original logo and capture the robot part' — the renderer's inline-SVG stand-in is rejected; the header uses the real logo PNG and the agent avatar is cropped from the original artwork's robot
- (event) IA map confirmed complete by the user: every stated need has a surface and every surface has a way in
- (decision) proposal expiry: 'Expires in m:ss' countdown caption on a live card; an expired card dims, its buttons go, and a 'Re-propose' action asks the agent for a fresh one; a card restored from a reloaded transcript is always shown expired
- (decision) reviewer gate at Finalize: run three lenses — rubric walker, accessibility (WCAG 2.1 AA + keyboard), judge's eye (contest Usability and Developer Experience as a voter would score them)
- (decision) chrome reconciliation: Lantern's shell role (#0F3A5F light mode, #0B2440 dark) is the logo navy, so Bridge's navy rail and header are that role applied to both, with shell-edge as the header gradient end; side bar, content and panel use Lantern's light surfaces. Lantern supplies every color; Bridge supplies density, type, radii and where the brand sits
- (event) import: imports/robot-avatar.png (256px) and imports/robot-avatar-64.png cropped from the original logo's docked-panel robot (box 851,116,1111,376 of OcuPilot-Logo-full.png); the agent's avatar per the agent-mark decision
- (event) Finalize started: distill subagents dispatched for DESIGN.md and EXPERIENCE.md with a shared token and component contract
- (event) avatar re-cropped to box 893,136,1109,352 (robot centred on its teal ground, panel stripe removed); imports/robot-avatar.png 256px and robot-avatar-64.png
- (event) EXPERIENCE.md distilled (556 lines): 78 IA surfaces, 42 component patterns, 27+24+14 state rows plus a 12-archetype matrix, 6 key flows; 14 distinct [ASSUMPTION] tags (sign-out home, OAuth as one screen with five tabs, unbuilt P1 screens hidden, unsaved-changes guard, silent-probe retry before form, auto-refresh rates 5/10/30/60 s, toast timings, wizard steps, etc.); three source contradictions resolved toward the memlog (instance fields in status bar, PRD marker wording, page-size control dropped)
- (event) DESIGN.md distilled (1136 lines): 64 color roles light + 64 dark from Lantern, typography 6 (caption added), rounded 5, spacing 26, components 42/42; 99 token references resolve. 10 [ASSUMPTION] tags: Inter and JetBrains Mono vendored; status bar 24px on shell; content-min-width 640px; log-row-height 28px; logo on a white plate because the PNGs are opaque; stacked lockup too small at 48px header; form page 720/480px; meter thresholds 80/95%. Resolved: buttons/links/selection use secondary (teal), primary (deep blue) keeps focus ring and titles; chrome controls use focus-ring-dark with a shell halo; header-height lifted as 48px with panel-header-height 44px
- (event) import: imports/OcuPilot-Logo-transparent.png (1182x774) and imports/OcuPilot-Mark-transparent.png (950x479) generated from the full logo by knocking out the white ground with a soft luminance edge (200-245); a vector or transparent export from the owner would supersede them
- (event) reconcile-vscode-reference.md written: 10 adopted, 9 adapted, 12 dropped or not carried (5 by decision, 7 never discussed: back/forward arrows, chord text display, history icon, new-conversation icon, notifications bell, stop button, errors/warnings segment); flagged gap: no state or string for a step-7 turn-limit refusal
- (event) reviewer gate dispatched: rubric walker, accessibility, judge's eye (parallel); logo reconcile dispatched
- (decision) stop a running turn: the Send button becomes Stop while a turn is busy; the turn halts at its next step and a card reads 'Stopped by you at <step>'; the lock banner stays for a second message
- (decision) new conversation: a 'New conversation' control in the panel header starts a fresh conversation on the instance; earlier conversations stay in the stored transcript; no reopening in Release 1 (the polish-week Transcripts screen is the history view)
- (decision) header lockup: the mark only (eye and panel, transparent cutout) at 32 px plus the wordmark reversed in on-shell beside it; no white plate; the stacked PNG is not used in the header
- (decision) typefaces: Inter (text) and JetBrains Mono (code), OFL, vendored as woff2 in the bundle; system-ui and monospace fallbacks named
- (assumption) shortcut discovery: Ctrl/Cmd+K shown in the command-box placeholder, Ctrl/Cmd+I as the panel input caption, Ctrl/Cmd+B in the rail tooltip; status-bar user segment is the account home (sign-out, change password P1, theme toggle P1)
- (event) reconcile-logo.md written: 12 adopted, 13 dropped/diverged; wordmark navy #123C5D on shell #0F3A5F is 1.02:1 so a plate or a reversed lockup is structural; first cutout hollowed the sclera whites; favicon has no source; eye/globe motif unused
- (event) reviews written: review-rubric.md (0 critical / 4 high / 11 medium / 13 low; flow and token coverage strong; the two spines contradict on ~12 points), review-accessibility.md (2 critical / 4 high / 8 medium / 6 low; focus destinations and expired-card contrast), review-judge.md (1 critical / 8 high / 12 medium / 12 low; auditing-off container, Home at 1,280 px, typing cancels a proposal, payload field list, fresh-container first five minutes)
- (event) import: flood-fill logo assets generated in imports/: OcuPilot-Mark-transparent.png (949x485, interior whites kept), OcuPilot-Wordmark-transparent.png, OcuPilot-Wordmark-reversed.png (on-shell #EAF2F7 fill of the artwork's letterforms), OcuPilot-Lockup-horizontal-reversed.png (623x128, for the navy header), OcuPilot-Lockup-horizontal.png (for light grounds: README, About), OcuPilot-Favicon-source.png (globe crop, 256px); preview .working/logo-assets-preview.png. A vector export from the owner supersedes them
- (decision) header lockup realized with imports/OcuPilot-Lockup-horizontal-reversed.png at 32 px tall (mark + reversed wordmark from the artwork's own letterforms); mark alone where only the mark fits; favicon from imports/OcuPilot-Favicon-source.png; the stacked PNG on a plate is dropped
- (decision) proposal expiry: 10 minutes; countdown caption on the live card; announced to assistive tech once at 1:00 remaining
- (decision) side bar: fixed 240 px, no drag handle; open/closed state remembered; the panel is the only resizable edge
- (decision) new-turn rule kept as the PRD writes it (a new turn cancels open proposals) with guard rails: caption on the live card 'Confirm here; sending a message cancels this proposal', Send demoted to secondary while a card is live, the agent's reply ends with 'Press Confirm on the card to apply it.'
- (decision) no-key softening: the configuration-empty state shows a static, labeled example proposal card ('This is what a proposal looks like') under the who-can-configure line; when the suggested view has nothing to report it offers three starter prompts instead of zeros
- (decision) facilitator resolutions of reviewer findings (within decided scope): tool-call-card is an expandable disclosure (running card expanded, completed cards collapsed, Enter/Space toggles); expired card uses restrained roles not opacity; focus destinations specified (Send→Stop keeps focus, Confirm/Cancel/expiry move focus to the card's status line); row overflow menu opens by Alt/Option+Down and the contextmenu event with an APG grid model and aria-activedescendant under virtual scroll; privilege gating uses aria-disabled + focusable + tooltip on focus + inline reason; aria-live for agent reply, Test connection result, Saved, sign-in failure, unreachable/403, search counts, never for auto-refresh; Home tiles wrap and panel-home = min(50vw, viewport minus rail minus content-min-width) with yield order side bar collapses first, then panel to minimum, then content scrolls; proposal card shows changed fields first and unchanged fields collapsed under 'N unchanged fields'; the not-being-marked banner links to Auditing configuration with a Turn auditing on action for admins; namespace eyebrow on-shell at full opacity off the gradient end
- (assumption) [NOTE FOR ARCHITECTURE] the installer should enable auditing and register OcuPilot's audit events so the agent marker works on a fresh Community container; consider unexpiring _SYSTEM's password at install so the first login does not bounce to the classic portal
- (decision) key-screen mocks to render at 1:1 with final tokens and real assets: (1) Web applications list + proposal card at the UJ-3 climax, (2) Home panel-forward with suggested view and starter-prompt column, (3) Form login + first-login gate, (4) proposal card state sheet (live, confirmed, canceled, expired with Re-propose, destructive typed-name, blocked by read-only)
- (decision) promote from .working/: direction-bridge.html and color-themes-1.html to mockups/, ia-2026-09-08.excalidraw to wireframes/, logo-assets-preview.png to mockups/; the three unchosen directions, extracts and crops stay in .working/ as the audit trail
- (event) promoted: mockups/direction-bridge.html, mockups/color-themes-1.html, mockups/logo-assets-preview.png, wireframes/ia-2026-09-08.excalidraw (copies; .working/ keeps the originals)
- (event) validation-report.html and validation-report.md written: 75 deduplicated findings (3 critical, 16 high, 30 medium, 26 low) from 81 raw across the three lenses; extra-reviewer verdicts Accessibility thin, Judge adequate. Most consequential for a story-dev: the missing focus-destination rule, installer-created instance state, and the shell layout math at 1,280 px
- (event) note on the judge's fresh-container finding: PRD FR-69 already specifies a demo fixture option that seeds a disabled web application, a task suspended after an error and application errors, so the gap is whether that option is on by default in the Compose flow, not that fixtures are missing
- (event) both fix passes hit the Fable rate limit after their edits landed but before validating: DESIGN.md 1136 to 1239 lines, EXPERIENCE.md 556 to 624 lines; neither wrote its disposition table
- (event) facilitator validation of both spines (parent, Opus 5): frontmatter parses in both; 128 color tokens (64 light + 64 dark, no light without a dark pair), typography 6, rounded 5, spacing 28, components 42; 89 distinct token references, 0 unresolved; 42/42 component patterns present in EXPERIENCE.md with real rules; the four rubric highs fixed (button-primary fill secondary, attention-dot agent-accent-dark, tool-call-card expandable disclosure, header lockup asset); focus-destination rule, APG grid model, 10-minute expiry, 240px side bar, panel-home formula, yield order with width table at 1280/1440/1920, and the promoted artifact links all present; toast timing split 10 s without an action and 30 s with one (accessibility refinement over the earlier 10 s)
- (event) fixes-applied.md written: 75 findings dispositioned — 65 applied, 5 applied differently, 3 skipped, 2 noted; both rubric owner-items resolved (10-minute expiry, fixed 240px side bar)
- (decision) facilitator closed five remainders in EXPERIENCE.md: Home's rail-item navigates to Home and has no side bar (Home has no screen list); the side-bar row says absent on Home; with no enabled definition the panel keeps its Home width so the example card fills it for a visitor without a key; a new state 'Idle, no messages yet' carries the greeting "I'm ready. Ask about this screen, or try one of these." plus the hint "Click a row to select it; click its name to open it."; the Definition form puts name, provider, model, endpoint, key and Test connection above the fold and collapses max tokens, temperature, max iterations, system-prompt override and retention under a closed 'Advanced' group
- (event) H4 checked and closed: EXPERIENCE.md references exactly the eight import files its frontmatter lists; the two transparent lockup sources are referenced and listed by DESIGN.md, so no import is an orphan. H12 checked: the expired-password state already carries the classic-portal and README links, which is as far as UX can go without a password API
- (assumption) L22 avatar crop nit (the left edge still shows a sliver of the panel outline) deferred until the key-screen renderer finishes embedding the file, to avoid a torn read
- (event) key-screen mocks rendered and promoted to mockups/: key-webapps-proposal.html (the shell at 1440px at the UJ-3 climax, now the composition reference), key-proposal-states.html (six card states with measured ratios), key-home.html (lived-in and fresh-container variants), key-signin-gate.html (form login, expired password, gate with Test connection passing and refusing); each self-contained with assets as data URIs, 384 KB total
- (decision) cross-spine contradictions the renderer surfaced, now resolved: the command-box chord is stated once as the kbd chip at the field's right edge and removed from the placeholder string; the proposal-card footer order is 'Runs as <user>' then Confirm/Cancel then the guard caption (DESIGN.md now matches EXPERIENCE.md); the changed-row tag is sentence case 'Changed' everywhere
- (decision) three details the spines had not specified, now written: a delete proposal shows the target's identifying fields as 'field · value → (removed)' and carries no Reverse line; the context chip is absent in the configuration-empty state because there is no provider or endpoint to name; the countdown disappears together with the buttons when a card resolves
- (event) avatar re-cropped tighter (box 901,136,1105,340) closing the L22 crop nit; the promoted mocks embed the previous crop as data URIs, an eight-pixel difference
- (event) mocks linked inline: DESIGN.md visual-reference table carries all four with what each illustrates and the note that the hero supersedes direction-bridge.html; EXPERIENCE.md links the state sheet at Agent Write Lifecycle, the sign-in mock at Session and Sign-in, Home in the IA table, and the hero as the Foundation composition reference
- (event) avatar finalized at box 915,145,1075,305: a head-and-shoulders crop of the robot on its teal ground with the panel's edge stripes outside the frame; a dedicated transparent robot export from the owner would still be better
- (event) doc standards run: bmad-review with the structure and prose lenses on both spines (structure first, prose on top of its findings). DESIGN.md 13 structure + 25 prose findings; EXPERIENCE.md 17 structure + 23 prose findings, each with a minor tail. Reported in review-editorial.md; triage accepted the correctness and findability fixes and declined the word-count cuts that would remove guard rails (the panel and proposal-card entries, the inline contrast ratios, three statements of the wordmark rule, and the PRD build-step column)
- (event) DESIGN.md editorial polish applied: 1243 to 1278 lines; all 11 structural items and 44 of 46 wording fixes; two notation sweeps left partial (ratio and padding style) with reasons; frontmatter byte-identical, 42 components intact, 101 token references all resolving. Facilitator fixed the one mismatch it reported: rule 7's expired-card range now reads 2.77-4.38:1, matching the Contrast table it cites
- (event) EXPERIENCE.md editorial polish applied: 630 to 708 lines; all 14 structural items and every wording fix, 0 skipped; Agent Write Lifecycle and Privilege Gating moved ahead of Voice and Tone, six oversized cells promoted to subsections, the ten proposal state rows merged into the lifecycle, Screen Synchronization made authoritative, an Open items section added; two facts from cut bullets relocated rather than lost. Facilitator then closed four leftovers: header 48px and status-bar 24px stated in Responsive & Platform, area-tile explains six tiles for seven areas, confirm-dialog names its source section, and the matrix legend admits the grouped form row
- (event) final verification: both frontmatters parse; 89 token references resolve; 64 light and 64 dark colors fully paired; 42 components in the DESIGN frontmatter, 42 DESIGN headings, 42 EXPERIENCE pattern rows; DESIGN section order canonical; EXPERIENCE carries all eight required sections plus two triggered and five product-specific; six flows with protagonists, climaxes and edge paths; 12 and 21 assumption markers, 8 and 20 architecture notes
- (event) spines finalized
- (decision) Story 15.10: header lockup is the navy-wordmark file on a fixed white rounded tile (both themes); supersedes the reversed-file-on-the-shell decision
