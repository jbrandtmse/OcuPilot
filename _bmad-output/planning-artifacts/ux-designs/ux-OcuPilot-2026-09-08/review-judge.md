# Judge's Eye Review — OcuPilot spines

Lens: contest **Usability** and **Developer Experience** as Priya (thirty minutes, a laptop, a fresh Community container) and an expert-panel judge would score them. Read: EXPERIENCE.md in full; DESIGN.md Brand & Style, Layout & Spacing, Components, Do's and Don'ts; .memlog.md; `.working/direction-bridge.html`; research extract §1–2; PRD extract §2, §4, §5; the reconcile note. Scope decisions in the memlog (Anthropic-only floor, polish-week items, no hosted demo) are taken as given. Section tags (§1–§7) refer to the seven questions of the brief.

## Verdict

As specified, the propose → review → confirm card, the CHANGED row and the navy / teal / yellow frame will read as a distinctive, carefully reasoned product to a voter and to an expert judge: Usability lands adequate-to-strong and Developer Experience strong once the install path holds. But the spine is written around Dana's lived-in instance, and Priya's container is fresh — an expired `_SYSTEM` password on the first screen, an empty suggested view on Home, five empty Security lists, and, unless the installer turns auditing on, "Agent writes are not being marked" as the panel's first words and no audit event at the end of UJ-3. The single biggest risk is that fresh-container reality: the trust story's payoff and the panel's first impression both depend on instance state the spine assumes rather than creates.

## Scorecard

| Criterion | Predicted impression | Why |
|---|---|---|
| Usability | **adequate** (strong if the high findings land) | The card, the row highlight and the error copy are the best in the field; the first five minutes, rail-click semantics and the Home squeeze at 1,280 px are stumbles a voter feels before she reaches them. |
| Developer Experience | **strong** in-product, **adequate** for extension | Archetype × state matrix, fixed strings and enumerated dialogs make sixty screens behave alike; the keyboard chords are invisible and there is no written per-screen contract for adding one. |
| Clarity of what the agent does | **strong** with a key, **thin** without | Labeled model text, computed before/after, "Confirmed by", "done · audit marked" say exactly what happened; the no-key voter sees an empty panel and never learns what a proposal looks like. |
| Distinctiveness | **strong** if the assets land, **thin** if placeholders ship | The navy frame with one yellow-barred card is unmistakable; initial-in-a-circle rail icons and a 40 px stacked lockup on a white plate read as a wireframe at README width. |

## Findings

### Critical

- **[critical]** The trust story's last beat depends on auditing being on, and the spine does not create that state (§4, §1). "done · audit marked" on the tool-call card and the event under Priya's own name in Logs › Audit database need instance auditing enabled plus OcuPilot's user events registered. IRIS enables auditing only at the Locked Down security level; a Normal-level Community container ships with it off. This project's own container reports auditing on — with four `AuditChange` records, i.e. switched on by hand. Left alone, the panel's first words on every README screenshot are "Agent writes are not being marked. Auditing is off on this instance." and UJ-3 step 6 ends in "No events match." *Fix:* the installer enables auditing and registers OcuPilot's events (owner-consented, itself audited); the auditing-off banner uses its optional link — "Turn auditing on" → Auditing configuration — so the state is one click from recovered; the README names it in the install steps; the smoke script asserts the marked audit row exists after the demo write.

### High

- **[high]** Home does not fit on a 1,280 px window, and the 900–1,279 band has no rule (§7). Rail 48 + side bar 224 + content-min 640 + panel-min 320 = 1,232 px, so 1,280 fits only with the panel squeezed to 368; the remembered 400 does not fit with the side bar open (1,312). On Home the panel is 50vw: content stays at 640 only above 1,376 px with the side bar closed and 1,824 with it open. At 1,280 (1080p at 150 %, the common Windows laptop) the tile row gets 592 px, or 368 with the side bar open, and scrolls horizontally; a 1,440 px MacBook Air fails the moment the side bar is open (448 px). The responsive table says "≥ 1,280: panel at its remembered width" and "900–1,279: nothing changes", while the resize-handle rule caps the panel where content reaches its minimum; DESIGN says "the side bar collapses first", EXPERIENCE says "no panel auto-collapse" and squeezes only below ~900. *Fix:* exempt Home from content-min and let the six tiles wrap (grid, 168 px minimum); `panel-home = clamp(panel-default, 50vw, viewport − rail − side bar − content-min)`; write one yield order for 900–1,279 in both spines (side bar collapses first, then the panel to its minimum, then content scrolls).

- **[high]** Answering the proposal in chat cancels it (§2). UJ-1 and UJ-6 teach the user to answer "Shall I…?" by typing "yes"; at the card, "Sending a new message cancels every live proposal", so the same reflex destroys the diff under review and the agent then answers "yes" with nothing to act on. *Fix:* while a proposal is live the composer caption reads "A proposal is waiting — use Confirm or Cancel on the card. Sending a message cancels it."; the agent's message that accompanies a proposal ends "Press Confirm on the card to apply it."; Send drops its filled treatment while a card is live (which also removes the two-primaries-in-one-view violation: Send and Confirm are both filled teal today).

- **[high]** The unchanged-payload rows push Confirm below the fold (§2). The card lists "every field the payload will send"; a web application update carries dozens (the classic editor is 1,423 lines), so in a 400 px panel two changed rows are followed by a screen of muted rows before the buttons. The mock's legibility comes from omitting them. *Fix:* "Also sent, unchanged (38)" is a collapsed disclosure and the diff shows changed rows by default; where the API accepts a partial update, the write tool sends only the changed fields so the list is what the user asked for.

- **[high]** The first screen sends the voter away (§1). On a fresh Community container `_SYSTEM`'s password is expired; the form's expired state says "Change it in the classic portal, or run the command in the README" — the voter's first interaction bounces her out of the product she came to score. *Fix:* the compose/installer path unexpires `_SYSTEM` so the README path never reaches this state (the project's own CLAUDE.md carries the one-liner); keep the state for other users but add an inline "Set a new password" (current, new, confirm) so the fix stays inside the form; name the user in the message ("The password for _SYSTEM has expired.").

- **[high]** The no-key voter sees nothing of the agent (§1). Unconfigured, the panel is an empty-state with the avatar and "who can configure it" — 400 px on every screen and half the viewport on Home — and the voter never sees a proposal. *Fix:* the unconfigured empty-state renders a static, labeled example ("Example — what a proposal looks like") of the UJ-3 card in its live state, non-interactive, plus three sentences (reads with your privileges; proposes; you confirm; every write is marked in the audit database); on Home the unconfigured panel stays at panel-default, not 50vw; the README leads with the same card.

- **[high]** Home's suggested view is empty on a fresh instance (§1). The lines are suspended tasks, application errors today, new alerts.log entries — all zero on a container that just started — so the panel-forward Home shows three "0" lines and the agent status; the video's first frame is thin. *Fix:* when every attention line is zero, the block shows three starter prompts instead ("Which web applications are disabled?", "Which users can't sign in, and why?", "What is using the most CPU right now?") — Release 1's cheap version of FR-70; after the first definition is saved the panel posts a first-run message ("I'm ready. Try one of these.") that repeats them.

- **[high]** "Visible rows" is undefined and uncounted (§7, §4). With virtual scroll a 1,280 × 800 window shows about sixteen rows of a 1,000-row fetch; the spine never says whether the sixteen or the thousand travel with each turn. A thousand rows is on the order of 100 k tokens a turn — cost, first progress past the 10 s target, and the exact privacy question a judge asks at the "leaves the instance" pill. *Fix:* define screen context as the rows in the viewport plus the active filter and sort (so the read tool can reproduce the set), capped (50); the context-chip shows the count ("Web applications, HSCUSTOM · 6 rows") and the read tool-call card shows rows returned.

- **[high]** Placeholder icons will be in the screenshots (§5). Initials in outlined circles on the rail, the tiles and the empty-states are wireframe furniture; if the owner's icons slip, every README image and the first video frame ship with them. *Fix:* vendor a Material Symbols subset now as the interim set (allowed under NFR-10); the owner's icons replace it file for file.

### Medium

- **[medium]** Rail click opens a menu; tile click navigates (§3). A rail item "navigates nothing by itself": the voter clicks Logs and the Web applications list stays on screen, while Home's tiles open the area's first screen. Only VS Code users expect the first behavior, and it costs one extra click per area on a thirty-minute clock. *Fix:* a rail click opens the side bar and, when the current route is outside that area, navigates to the area's first screen; clicking the active item still collapses.

- **[medium]** The gate lands without orientation (§1). The redirect opens the Definition form — twelve-plus fields and no sentence saying why she is here; the panel's reminder banner tells her to go where she already is; after Save the editor stays open with "Saved" and nothing points onward. *Fix:* an info banner above the form on the gate landing ("OcuPilot needs one agent definition before the panel can help. Anthropic is selected — paste a key and press Test connection. You can skip this and browse."); provider, key and Test connection above the fold with the rest under "Advanced"; on the first successful Save the sticky bar offers "Go to Home" and the panel posts its first-run message.

- **[medium]** Security and secrets is five empty lists on a fresh container (§3, §1). SSL/TLS, X.509, LDAP, Wallet and OAuth are all empty, so the contest's most detailed bullet meets "No SSL/TLS configurations." five times; UJ-5's "at least one list backed by live data" holds only for Auditing, last in the side bar. *Fix:* empty-states on write-capable lists gain a second line inviting the agent ("Or ask the agent: create an SSL/TLS configuration for outbound HTTPS."); the README walkthrough uses one of these as its second write; consider seeding a demo SSL/TLS configuration and a self-signed X.509 credential at install.

- **[medium]** Ctrl/Cmd+K, +I and +B are invisible (§6, §3). Only the ⌘K hint in the command box exists; the keyboard-first stake earns nothing a voter can see, and the no-close panel plus rail toggle are VS Code conventions with no on-screen explanation. *Fix:* the reconcile note's defaults — caption "Enter to send · Ctrl+I to focus", Ctrl+B in the rail tooltips, kbd chips in empty-states.

- **[medium]** Trust lines the screen never says (§4). Nothing before Confirm states that the write runs as the user; the Done reply does not lead to the audit event; the "leaves the instance" pill has no explanation and can read as an error. *Fix:* card footer caption "Confirm runs this as Dana Okafor, with your privileges."; the Done reply pattern ends "Shall I show you the audit entry?" and the navigation tool opens Audit database with the agent-marker filter applied; the pill's tooltip reads "Screen context is sent to api.anthropic.com".

- **[medium]** The header lockup at README width (§5). A stacked 480 × 322 PNG at 40 px on a white plate in the navy band: the wordmark is about 11 px on screen and 7 px in a README image, and the plate reads as a broken-image patch. *Fix:* the eye-and-panel mark (the new transparent mark import) at 32 px plus a typeset "OcuPilot" in the vendored brand face; no plate.

- **[medium]** No Stop, no new conversation (§4, §7). A mistyped prompt during the recorded demo runs to completion (60–90 s); the only clean slate is a new tab, and a reload restores the messy transcript with its cards expired. A judge probing restraint will ask "can I stop it?". *Fix:* Send becomes Stop while Busy (cancels at the next tool boundary; card "Stopped by you at <step>"); a "New conversation" button-text in the panel header — both per the reconcile note.

- **[medium]** The name cell does not look like a link (§7). Identifier columns are `code` in on-surface; the row hover and the name link share the same cell, so the voter who clicks the name to select lands in the editor and needs Back. *Fix:* name cells take `secondary` with underline on hover and a pointer cursor; row hover stays on the row; the first-run message says once "click a row to select it, its name to open it".

- **[medium]** The side bar on Home shows the previous area (§1). It "stays open across routes", so Home reads "Home" in the locator beside a PERMISSIONS list, and it eats 224 px of the tile row. *Fix:* navigating to Home collapses the side bar; the remembered state returns on the next area click.

- **[medium]** Area tiles say the name, not the contents (§3). A judge ticking "Operating system management (processes, disks, CPU, memory, devices)" sees "OS management" and a circle. The area names already match the six bullets closely. *Fix:* each tile carries its side-bar entries as a caption ("Processes · Locks · System usage · Databases · Devices") so the contest parentheticals are visible on Home.

- **[medium]** Adding a screen is not fully derivable from the spine (§6). The archetype matrix carries the states, but nothing says how an entity id becomes a route segment (`/csp/myapp` → `csp-myapp`; a task name with spaces; `_SYSTEM`), nor what a screen must register for the panel: entity-type key for change events, allow-listed route id, context serializer, highlight target. *Fix:* a ten-line "Screen contract" in EXPERIENCE.md — route pattern and id encoding, side-bar entry, archetype, privilege resource, entity-type key, context serializer, primary action, row-menu items, empty-state sentence, fixed strings.

- **[medium]** Test connection failure breaks the error rule (§1, §6). "The provider's error text verbatim" is an Anthropic JSON body with no next step; Save without a passing test leaves a disabled definition signaled only by the attention dot. *Fix:* "The provider refused the request. Check the key and try again. Provider said: <text>"; the sticky bar shows "Saved — disabled until Test connection passes." on that save.

### Low

- **[low]** Countdown pressure (§2). A 4:59 ticker in the card header reads as a timer to a first-time voter. *Fix:* "Valid until 10:47" until the last minute, then the ticking "Expires in 0:59" in warning; tooltip "Proposals expire so a stale diff is never applied."
- **[low]** Highlight timing conflict (§2). EXPERIENCE "fades over 2 s and clears on the next interaction" vs DESIGN "settles over 2 s and holds". *Fix:* settle and hold — the screenshot needs it.
- **[low]** Read-only line conflict (§4). DESIGN and the mock always show "Read-only: off"; EXPERIENCE assumes it only while a read-only state applies. *Fix:* always show it — a judge learns the mode exists.
- **[low]** Cancel variant conflict (§2). button-secondary (EXPERIENCE) vs button-text (DESIGN). *Fix:* text.
- **[low]** Command-box vocabulary (§3). "web apps", "REST", "x509", "certificates", "CPU", "disks" should hit. *Fix:* an alias list per screen drawn from the contest wording.
- **[low]** Sign-out hunt (§6). The status-bar user segment has no visible menu affordance. *Fix:* a ▾ glyph like the namespace switch.
- **[low]** Avatar crop (§5). The robot fills about half the 64 px box; at 24 px it is a yellow dot on teal. *Fix:* a tighter crop (robot about 80 % of the box).
- **[low]** Composer caption (§7). *Fix:* "Enter to send · Shift+Enter for a new line".
- **[low]** Table Tab stop (§6). The grid is not stated to be one Tab stop; without it a keyboard user tabs through rows to reach the panel. *Fix:* state it.
- **[low]** Panel width jump (§7). 400 ↔ 50vw on every Home transition with no transition specified. *Fix:* 120 ms width transition; none under reduced motion.
- **[low]** Toast overflow (§7). Beyond three is unspecified. *Fix:* the oldest drops; the row highlight and the ledger keep the record.
- **[low]** Demo prerequisite (§2). `/csp/myapp` does not exist on a fresh container. *Fix:* the installer seeds it disabled with no resource, or the README first has the voter disable one from the row menu (which shows the screen's own action).

Counts: critical 1 · high 8 · medium 12 · low 12.

## What to show first

- **The UJ-3 card, live, cropped to the panel** — diff rows, "Agent's rationale" and "Expected impact" on the agent tint, "Reverse:", Confirm with the countdown — then the same moment at 1,280 px with the CHANGED row beside the confirmed footer and "done · audit marked". The single most distinctive frame the spine produces; it needs the unchanged rows collapsed (high finding) to stay legible.
- **Home after the first Save** — rail, tile row with captions, panel at half width carrying the first-run message and three starter prompts. The video's opening frame after login; it needs the starter-prompt fallback (high finding) or it is three zeros.
- **The restraint frame** — the context-chip with "leaves the instance", Switches with enforced read-only on, and the panel's "blocked by read-only mode" reply with no card (Release 1 can stage UJ-4's climax without the local model), followed by Logs › Audit database filtered on the agent marker showing the write under the user's own name. This is the frame that earns the expert panel's "the agent never holds a privilege the user does not" — and it exists only if the critical finding is fixed.
