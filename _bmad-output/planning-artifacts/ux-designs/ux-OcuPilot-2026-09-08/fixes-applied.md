# Reviewer findings — disposition

Reconstructed by inspection after both fix passes hit the Fable rate limit with their edits landed
but their disposition tables unwritten (memlog, 18:24). Every entry below was verified against the
current `DESIGN.md` (1,239 lines) and `EXPERIENCE.md` (624 lines), the owner's decisions in
`.memlog.md` after the "reviews written" event, and `reconcile-logo.md` /
`reconcile-vscode-reference.md`. Line numbers are the *current* files.

## Summary

| Severity | applied | applied differently | skipped | noted | total |
|---|---|---|---|---|---|
| Critical | 2 | 0 | 0 | 1 | 3 |
| High | 13 | 3 | 0 | 0 | 16 |
| Medium | 28 | 0 | 2 | 0 | 30 |
| Low | 22 | 2 | 1 | 1 | 26 |
| **Total** | **65** | **5** | **3** | **2** | **75** |

The two passes changed the pair most by turning it from two separately distilled documents into one
contract: every cross-spine contradiction the rubric found (button fill, attention dot, tool-call
card, typed-name feedback, button variants, banner variant, focus-ring semantics, rail-item size,
status-bar interactivity, diff colours, highlight timing) now reads the same in both files, and
`DESIGN.md` has given up the UI strings it was carrying. The second change is that three states the
tables never listed became first-class rules: a **Focus destinations** paragraph in the Accessibility
Floor, an **expired card dimmed by role rather than opacity** (with the retired 60 % measurements kept
in the contrast table as the reason), and a **width yield order with a worked table at 1,280 / 1,440 /
1,920 px** so Home fits the common Windows laptop.

## Critical

| # | Reviewer | Finding (short) | Disposition | Where it landed / why not |
|---|---|---|---|---|
| C1 | Accessibility | Focus dropped by disabling or removing the focused control (composer on every Send, Confirm at the UJ-3 climax, buttons at expiry, Ctrl/Cmd+B in the side bar, deleted row) | applied | `EXPERIENCE.md` Accessibility Floor › **Focus destinations** (l.513) states the rule in one sentence and names all five destinations. Carried into the panel Busy/Locked rows (l.351–352: composer stays focusable and editable, `aria-disabled`), every proposal-card terminal state (l.284, 357–361, 365: buttons replaced by a status line that receives focus), Interaction Primitives › Banned everywhere (l.453) and the Send/Stop rule (l.435). `DESIGN.md` `panel` › The Send control (l.1085) "keeps focus through each change and is never removed or natively disabled"; `proposal-card.footer-status-focus` (l.469) and the Do's/Don'ts row (l.1222). |
| C2 | Accessibility | Expired proposal card at 60 % opacity fails AA in light (2.6–4.4:1) — the resting state of every restored transcript | applied | `DESIGN.md` Colors rule **7. Dim by role, never by opacity** (l.796); `proposal-card` Expired row (l.1156) "**No opacity anywhere**" with `restrained` 6.55/9.64 and `on-surface-variant` 7.63/10.30 cited; frontmatter `expired-text` / `bar-inactive` (l.471–472). The retired 60 % measurements are kept as a contrast-table row (l.790) so nobody re-introduces them. `EXPERIENCE.md` l.359 "restrained treatment (roles, never opacity — the diff stays readable at AA)". |
| C3 | Judge | The trust story's last beat needs instance auditing on; a Normal-level Community container ships with it off | noted for architecture | The UX half is applied — the auditing-off banner now carries the link for every user and the **"Turn auditing on"** action for administrators (`EXPERIENCE.md` l.209, 286, 366), and the write path through the screen's own toggle or propose→confirm is spelled out (Lifecycle step 9, l.467). The instance state itself is outside the UX remit and is carried as `[NOTE FOR ARCHITECTURE]` in `EXPERIENCE.md` l.286 and `DESIGN.md` l.1096 (`panel` › Auditing off), matching the memlog assumption. The README step and the smoke-script assertion the judge asked for are **unrecorded** in either spine. |

## High

| # | Reviewer | Finding (short) | Disposition | Where it landed / why not |
|---|---|---|---|---|
| H1 | Component coverage | `button-primary` fill: `primary` in EXPERIENCE vs `secondary` in DESIGN | applied | `EXPERIENCE.md` l.291 now "Filled `{colors.secondary}`", matching `DESIGN.md` l.1197 and memlog 63. |
| H2 | Component coverage | `attention-dot` token is 1.80:1 on the rail in EXPERIENCE | applied | `EXPERIENCE.md` l.266 → `{colors.agent-accent-dark}` "in both modes (it sits on the chrome)". `DESIGN.md` frontmatter l.246, `attention-dot` prose l.968, rule 1, and a Do's/Don'ts row. |
| H3 | Component coverage · Accessibility | `tool-call-card`: expands on click (EXPERIENCE) vs not interactive in Release 1 (DESIGN) | applied differently | Neither side's position won. The facilitator/owner resolution (memlog: "tool-call-card is an expandable disclosure") made it a disclosure in **both** spines — `<button aria-expanded>`, running card expanded, completed cards collapsed, Enter/Space toggles (`EXPERIENCE.md` l.283, Interaction Primitives l.429; `DESIGN.md` l.1126–1133 with `expanded-default` in the frontmatter). FR-12 is satisfied differently from the review's recommendation: the status is required to sit on the *collapsed* line, never only in the body. |
| H4 | Visual reference · Judge | The transparent logo exports are orphans; the header lockup is still an opaque PNG on a white plate | applied differently | The owner's header-lockup decision (memlog) superseded the fix: rather than pointing at the stacked transparent cutout, a new asset set was generated and `logo-lockup.asset` is now `imports/OcuPilot-Lockup-horizontal-reversed.png` at 32 px with `plate: none` (`DESIGN.md` l.267–275, l.987–990, Do's/Don'ts l.1224). `DESIGN.md`'s frontmatter `imports:` lists all twelve files and the Visual references table (l.709–723) gives every one a row plus the "an owner export supersedes them" note. **Partial:** `EXPERIENCE.md`'s frontmatter (l.18–26) still lists only the eight files it references — the two original orphans (`OcuPilot-Logo-transparent.png`, `OcuPilot-Mark-transparent.png`) are not in it. |
| H5 | Accessibility | "Namespace" eyebrow sits on the gradient's failing end (3.60:1 / 4.17:1) | applied | `DESIGN.md` `header.eyebrow` frontmatter (l.265) and prose (l.984): `on-shell` at **100 %**, "no text in the header is ever drawn below 100 %". Both pairs are now contrast-table rows — the passing 5.35/6.43 and the failing 72 % pair marked "**never drawn**" (l.786–787). |
| H6 | Accessibility · Judge | Row actions have no keyboard path on macOS; grid model incomplete; table not stated to be one Tab stop | applied | `EXPERIENCE.md` `data-table` (l.275) specifies the APG grid in full: `role="grid"`, one Tab stop, focus on the container with `aria-activedescendant` (explicitly so virtual scroll cannot drop it), Up/Down/Home/End/PageUp/PageDown, Right/Left into cells, Enter opens, **Alt/Option+Down or the `contextmenu` event** (Shift+F10, menu key, VO+Shift+M, right-click), Tab leaves; deleting the focused row has a destination. Repeated in Interaction Primitives (l.425–431), the Floor › Tables (l.516) and `row-overflow-menu` (l.276). The chord shows in empty-state kbd chips (l.301). |
| H7 | Accessibility | Status messages (4.1.3) incomplete — the agent's reply is not on the list | applied | `EXPERIENCE.md` Floor › **Status messages** (l.514): the transcript is `role="log"` (polite, labelled, `tabindex="0"`, also in the `panel` row l.277) so replies, cards and proposals announce by construction; the full `role="status"` list (Test connection, "Saved", search "n of N", filter and command-box counts, toast region, change highlight, lock banner, connection state on transitions only) and the `role="alert"` list (sign-in failure, unreachable, generic error, 403, form error summary); the stamp and countdown declared explicitly *not* live. |
| H8 | Accessibility | Disabled-with-tooltip conflicts with Material defaults in menus, the command box and the command bar | applied | `EXPERIENCE.md` Privilege Gating › **Mechanism** (l.494): `aria-disabled="true"` never the `disabled` attribute, `skipPredicate` overridden, inline reason as part of the accessible name in menus and listboxes, tooltips only where DOM focus lands; "Select a row first" uses the same mechanism (`command-bar` l.274). Component rows updated (`command-box` l.270, `row-overflow-menu` l.276). `DESIGN.md` rule 5 (l.794) and the `restrained` gated-reason values, plus the requested `restrained` / `secondary-container` contrast row — which fails at 4.497:1 in dark, so the keyboard-active row draws the caption in `on-secondary-container` (l.788). |
| H9 | Judge | Home does not fit a 1,280 px window; the 900–1,279 band has no rule | applied | `spacing.panel-home` is now the formula `min(50vw, 100vw − rail − content-min-width)` (`DESIGN.md` l.222); tiles wrap in an auto-fit grid with `tile-min-width` 168 px (`area-tile`, l.1078; Layout › Home, l.891). One **yield order** is written identically in both spines (side bar collapses → panel to minimum → content scrolls) with a worked width table at 1,920 / 1,440 / 1,280 / 1,024 / 900 (`DESIGN.md` l.893–900) and a per-route table on the EXPERIENCE side (l.531–546). |
| H10 | Judge | Answering the proposal in chat cancels it | applied | The owner kept the PRD's rule and took the guard rails instead (memlog). Card caption "Confirm here; sending a message cancels this proposal" (`EXPERIENCE.md` l.221, 284, 356); Send drops to `button-secondary` while a card is live (l.277, 291, `DESIGN.md` `panel.send-proposal-live`); the agent's message ends "Press Confirm on the card to apply it." (l.222); the Voice table adds the "Reply yes to apply" anti-pattern and the post-cancel reply (l.197–198); Lifecycle step 4 (l.462) states the whole loop. |
| H11 | Judge | The unchanged-payload rows push Confirm below the fold | applied | Changed rows first, the rest under a collapsed "N unchanged fields" disclosure closed by default (`DESIGN.md` `proposal-card` anatomy 2, l.1143, and `diff-row`; `EXPERIENCE.md` l.224, 284, Lifecycle step 2). "Two changed rows and one disclosure line is the card's default height, whatever the payload carries." The partial-update half is a `[NOTE FOR ARCHITECTURE]` at `EXPERIENCE.md` l.460. |
| H12 | Judge | The first screen sends the voter away (expired `_SYSTEM` password) | applied differently | The message now names the user (`"The password for <user> has expired…"`, l.245, 317) and the installer fix is carried as `[NOTE FOR ARCHITECTURE]` in both spines (`EXPERIENCE.md` l.317, UJ-5 edge l.613; `DESIGN.md` Form login l.1037) rather than as a UX change. **Not applied:** the inline "Set a new password" (current / new / confirm) the judge asked for, so the state still routes the user out to the classic portal or the README — no reason recorded. |
| H13 | Judge | The no-key voter sees nothing of the agent | applied | The `proposal-card` **Example** variant is a full component state (`DESIGN.md` l.1160: identical to Live, a "Example — this is what a proposal looks like" band, nothing focusable) shown under the who-can-configure line with the three sentences the judge asked for (`EXPERIENCE.md` l.347–348, string l.225, `panel` Unconfigured l.1094, UJ-5 step 2). **Not applied:** the "on Home the unconfigured panel stays at panel-default, not 50vw" rule — `panel-home` still applies on Home in every panel state. |
| H14 | Judge · State coverage | Home's suggested view is empty on a fresh instance | applied | When every attention line would be zero the block shows three starter prompts instead (`EXPERIENCE.md` l.350, verbatim strings l.254, matrix `home` / empty l.408, `DESIGN.md` Home suggested view l.1101, UJ-2 step 5). Owner decision ("no-key softening"). **Not applied:** the first-run panel message ("I'm ready. Try one of these.") — absent from both spines. |
| H15 | Judge | "Visible rows" is undefined and uncounted | applied | `EXPERIENCE.md` l.60 defines screen context as route, namespace, selection **and the viewport's rows with the active sort and filter (so the read tool can reproduce the set)**, with `[NOTE FOR ARCHITECTURE]` capping it ("a viewport holds about sixteen; 50 is a safe ceiling"). The chip pattern gains the count (`"Users, HSCUSTOM · 6 rows"`, l.212, 279) and the read card shows rows returned (l.283). `DESIGN.md` `context-chip` carries the matching `[NOTE FOR PRD]` (l.1108). |
| H16 | Judge | Placeholder icons will be in the screenshots | applied | The interim set is now a **vendored Material Symbols Outlined** subset, replaced file for file — stated in the Components intro (`DESIGN.md` l.960, "never an initial in a circle, which reads as a wireframe in a screenshot") and repeated on `rail`, `empty-state` and `banner`; `EXPERIENCE.md` `rail` l.264. |

## Medium

| # | Reviewer | Finding (short) | Disposition | Where it landed / why not |
|---|---|---|---|---|
| M1 | Flow coverage | The proposal-expiry interval is decided only by example (4:59) | applied | Owner decision: **10 minutes**. The countdown runs "from 10:00" everywhere (`EXPERIENCE.md` l.220, 284, 356, Lifecycle step 5 l.463; `DESIGN.md` l.1139) and UJ-3 shows "Expires in 9:59" (l.587) derived from it. |
| M2 | Component coverage · Accessibility | `typed-name-field` mismatch: silent vs "Does not match" on blur | applied | Resolved toward the accessible side in both: `EXPERIENCE.md` l.241, 289 (mismatch on blur shows "Does not match", `aria-invalid`, message via `aria-describedby`); `DESIGN.md` `typed-name-field` l.1189 "reported, never silent, per the accessibility review". |
| M3 | Component coverage · Judge | Button variants disagree in four places (Cancel, Re-propose, Load newer, Raw, Filter) | applied | DESIGN's mapping kept. `EXPERIENCE.md` l.292 (secondary: View, Back, Re-propose, Load newer, Raw, classic-link-card, Send while a proposal is live), l.293 (text: Cancel on the card, sticky bar and dialogs, Open in, Jump to, Clear filter, New conversation, "Open ›"), and the filter is a field, not a button (`command-bar` l.274). |
| M4 | Component coverage | Administrator reminder banner on `agent-container` breaks DESIGN's rule 4 | applied | `EXPERIENCE.md` l.286 lists it as "administrator reminder (**info**)"; the `agent-container` tint is now stated to appear only on the proposal card's two agent blocks (`DESIGN.md` l.1145). |
| M5 | Component coverage | `focus-ring` "never suppressed for mouse users" vs `:focus-visible` | applied | `EXPERIENCE.md` l.303: "`:focus-visible` semantics — shown for keyboard and programmatic focus, never removed with `outline: none`, never replaced by a colour change alone" — which is what the original wording was protecting. |
| M6 | Component coverage | Form login, the two notices, Home's suggested view and the OpenAPI browser have no row on either side | applied | Four compositions added to `DESIGN.md`: **Form login** (l.1037), **Version-mismatch and no-administrative-privileges notices** (l.1039), **OpenAPI path-and-verb browser** (l.1057), **Home suggested view** (l.1101). Behaviour rows on the EXPERIENCE side at l.301 (notices map to `empty-state` + banner), l.350 and the IA rows. |
| M7 | State coverage | The IA Archetype column keys the matrix but only 7 of 31 values appear as rows | applied | The column is normalised to the twelve matrix keys plus `shell` / `external` (`EXPERIENCE.md` l.64), the matrix carries the line "`shell` takes its states from the Shell and Session tables; `external` … has none here" (l.394), and the compound and one-off cases (OAuth tabs, Auditing configuration, confirm dialogs) are in the Exceptions paragraph (l.411). |
| M8 | Visual reference | DESIGN's frontmatter `imports:` omits the two avatar files it references | applied | `DESIGN.md` l.27–28 now lists `imports/robot-avatar.png` and `imports/robot-avatar-64.png` (the list grew from 3 to 12 entries). |
| M9 | Bloat · Judge | DESIGN carries UI copy EXPERIENCE owns, and the two have drifted | applied | Every drifted string is gone from `DESIGN.md` — no "refreshing token", "Taking you to…", "Agent disabled:", or "Read-only: off"; each component now points at EXPERIENCE (`status-bar` l.998, `data-table` Refresh-paused l.1027, `message-agent` l.1116, `proposal-card` l.1139). `EXPERIENCE.md` reconciles to one navigation phrasing (l.229). The read-only footer line follows the judge's resolution — always shown (l.236; `DESIGN.md` l.1083 "always present, so the mode is learnable"). |
| M10 | Bloat · Shape fit | The sign-in story is told three times with drifting strings | applied | The Session table is declared the authority (`EXPERIENCE.md` l.309); the Shell table keeps only the five states with a distinct visual (l.315–319); *Session and Sign-in* (l.480–488) is cut to what the table does not carry — the silent retry, the lost-values toast, the card composition, sign-out and the gate. |
| M11 | Inheritance discipline | The side bar's width is "remembered" but nothing can change it | applied | Owner decision: **fixed 240 px, no drag**. Only the open/closed state is remembered (`EXPERIENCE.md` l.47, 267; `DESIGN.md` `spacing.side-bar-width` comment l.215, `side-bar.resize-handle: none`, l.880, l.972, and a Do's/Don'ts row). |
| M12 | Accessibility | Rail active indicator is below 3:1 for most of its length | applied | Solid `{colors.secondary-dark}` (6.72:1 light / 8.97:1 dark) replaces the gradient (`DESIGN.md` frontmatter l.249, `rail-item` Active l.966, rule 2), the retired gradient's 1.94–3.10 measurements are kept as the reason (l.789), and `aria-current="page"` is on the rail-item, side-bar entry and locator segment (`EXPERIENCE.md` l.264–267, 273, 519). |
| M13 | Accessibility | Proposal expiry is a time limit with no stated basis (2.2.1) | applied | `EXPERIENCE.md` Floor › **Time limits (WCAG 2.2.1)** (l.520): the essential-exception argument, Re-propose as the accommodation, 2.2.2 covering the countdown and spinner, the countdown explicitly not live, one polite announcement at 1:00 (string l.220), and a `[NOTE FOR ARCHITECTURE]` making the interval a server-side constant with a per-instance setting as a polish-week Switches candidate. |
| M14 | Accessibility | Off-screen toast: a 10 s timed control last in the focus order | applied | Split by whether the toast carries an action: 30 s with, 10 s without, timer paused while hovered **or focused**, dismiss button on every toast, and "the agent's reply names the same change, so nothing is lost when a toast expires" (`EXPERIENCE.md` l.287, 334, 474, Floor l.520). Kept `role="status"`. |
| M15 | Accessibility | Diff-row direction is carried by glyph, strike-through and colour | applied | `EXPERIENCE.md` `diff-row` (l.285): "`<field>`: was `<before>`, now `<after>`" with visually hidden "was"/"now" and the arrow `aria-hidden`; unchanged rows read "…, unchanged". The colour contradiction is resolved to `destructive` / `success` in both, with "the `on-surface-variant` / `on-surface` pairing is retired" stated in `DESIGN.md` l.1163. |
| M16 | Accessibility | Five cross-spine contradictions with accessibility consequences | applied | (a) typed-name → M2; (b) tool-call-card → H3; (c) `status-bar` — the stamp is now "a readout, not a control — the command-bar chip is the control" (`EXPERIENCE.md` l.271) and `DESIGN.md` l.998 agrees nothing else is interactive; (d) rail-item is 48 × 48 in both (`EXPERIENCE.md` l.523); (e) composer → C1. |
| M17 | Accessibility | Keyboard chords are not scoped; the navigation announcement races the route change | applied | Chords are inert while a dialog or the command-box overlay is open (`EXPERIENCE.md` l.419–421, `confirm-dialog` l.288, Floor l.513). The navigation tool commits the message first and changes route "after the announcement has been dispatched (about 1 s)", and the new heading announces "`<title>` — opened by the agent; Back returns" (l.229, 369, 514, UJ-6 step 3). |
| M18 | Accessibility | The command box has no named ARIA pattern | applied | `role="combobox"` with `aria-expanded` / `aria-controls` / `aria-activedescendant`, results a `role="listbox"` grouped by `role="group"` + `aria-label`, the polite "`<n>` screens, `<m>` actions" count and "No screen or action matches." (`EXPERIENCE.md` l.248, 270, Floor l.517); gated results per M-H8. |
| M19 | Accessibility | Forms: error association and focus after a failed Save unspecified | applied | `EXPERIENCE.md` `form-page` (l.295) and Floor › **Forms** (l.518): the summary banner receives focus with `role="alert"` and a link per field, server errors set `aria-invalid` + `aria-describedby`, the first invalid field is focused and its tab opened, custom fields replicate `mat-form-field`'s wiring, `aria-required` plus an asterisk legend, "Saved" as `role="status"`. `DESIGN.md` `form-page` gives the banner its focus ring (l.1035). |
| M20 | Judge | Rail click opens a menu; tile click navigates | **skipped** | `EXPERIENCE.md` l.265 still reads "Click opens the side-bar listing the area's screens and **navigates nothing by itself**". The behaviour is the owner's own VS Code slide-out decision (memlog: "click a rail icon to open a side bar listing that area's screens, click the same icon to collapse") and was not revisited in the facilitator's resolutions. Partly mitigated: the area-tile and the locator bar's area segment both open the area's first screen (l.273, 305). |
| M21 | Judge | The gate lands without orientation | applied | Gate landing banner above the form with its verbatim string (`EXPERIENCE.md` l.232, 286, state row l.338, Session l.488); provider, model, key and Test connection first (`form-page` l.295); "Go to Home" on the first successful Save (l.244, 338, UJ-2 step 5). **Not applied:** the "rest under Advanced" grouping — the form's remaining fields have no collapse rule. |
| M22 | Judge | Security and secrets is five empty lists on a fresh container | applied | Write-capable empty-states gain the second line "Or ask the agent: `<a write it could propose here>`." (`EXPERIENCE.md` l.253, 301, screen contract item 8; `DESIGN.md` `empty-state` l.1065). The seeding half is a `[NOTE FOR ARCHITECTURE]` on the same component (a demo SSL/TLS configuration, a self-signed X.509 credential and `/csp/myapp`). |
| M23 | Judge | Ctrl/Cmd+K, +I and +B are invisible | applied | All three are now on screen: command-box placeholder "Search screens and commands · Ctrl+K" (l.248), composer caption "Enter to send · Shift+Enter for a new line · Ctrl+I to focus" (l.249), rail tooltip "`<Area>` · Ctrl+B toggles the side bar" (l.250, 264), plus kbd chips in empty-states (l.301) — the reconcile note's defaults. |
| M24 | Judge | Trust lines the screen never says | applied | Card footer caption "Runs as `<user name>`, with your privileges." (l.221, 284, Lifecycle step 2); the Done reply ends "Shall I show you the audit entry?" and the navigation tool opens Audit database with the agent-marker filter (l.230, Lifecycle step 3, UJ-3 step 6); the egress pill gains the tooltip "Screen context is sent to `<host>`" (l.211, 279). |
| M25 | Judge | No Stop, no new conversation | applied | Both are owner decisions and are now components: Send becomes **Stop** while busy, halting at the next step with "Stopped by you at `<step>`" (l.217, 226, 277, 283, 351, 353, 435; `DESIGN.md` `panel.stop-busy`, tool-call-card Stopped status), and **New conversation** is a panel-header control (l.217, 277, 354, 437; `DESIGN.md` `panel.header-icon-buttons`). |
| M26 | Judge | The name cell does not look like a link | applied | `DESIGN.md` `data-table` (l.1014): "the **name cell is a link and looks like one**" — `{colors.secondary}`, underlined on hover and focus, pointer cursor, "so that clicking the name (open) and clicking the row (select) read as different gestures", with the three marginal ratios cited. **Not applied:** the one-off first-run line "click a row to select it, its name to open it". |
| M27 | Judge | The side bar on Home shows the previous area | **skipped** | Neither spine collapses the side bar on navigating to Home; it still "stays open across routes until collapsed" (`EXPERIENCE.md` l.47, 267). No reason recorded. Partly mitigated by H9's yield order, which auto-collapses it on Home at 1,280 px and 1,440 px (l.543–544) — but not at 1,920 px, where the judge's locator/PERMISSIONS mismatch survives. |
| M28 | Judge | Area tiles say the name, not the contents | applied | Each tile carries its side-bar entries as a caption (`DESIGN.md` `area-tile.screens-caption` l.652 and l.1078 with the worked example; `EXPERIENCE.md` l.305) "so the contest's parentheticals are visible on Home". |
| M29 | Judge | Adding a screen is not fully derivable from the spine | applied | New section **Adding a screen (the screen contract)** — ten registrations covering route and id encoding, side-bar entry, archetype, privilege resource, entity-type key, context serializer, primary action and row-menu items, empty-state sentence, fixed strings and command-box aliases (`EXPERIENCE.md` l.168–180), with the id-encoding round-trip flagged `[NOTE FOR ARCHITECTURE]`. |
| M30 | Judge | Test connection failure breaks the error rule | applied | "The provider refused the request. Check the key and try again. Provider said: `<text>`" replaces the verbatim JSON (l.199, 243, 340, UJ-2 edge l.581), and Save before a passing test shows "Saved — disabled until Test connection passes." |

## Low

| # | Reviewer | Finding (short) | Disposition | Where it landed / why not |
|---|---|---|---|---|
| L1 | Flow coverage | "Confirmed by `<user>`" — login name or full name unspecified | applied | One rule in Voice and Tone (l.201): "`<user name>` in any string is the login name, as the audit database records it." Every string uses `<user name>` (l.221, 223, 357, 461) and UJ-3/UJ-6 render "`_SYSTEM`". |
| L2 | Token completeness | `destructive-container` / `on-destructive-container` defined but unused | applied | Noted as reserved the way `tertiary` is: "The container pair is reserved (no Release 1 component uses it)" (`DESIGN.md` l.757). |
| L3 | Token completeness | The user bubble's radius is the only shape not expressed through a token | applied | `'{rounded.lg} {rounded.lg} 2px {rounded.lg}'` in the frontmatter (l.410) and in prose (l.1112). |
| L4 | Component coverage | Naming drift: "error card" vs a `banner` (error) | applied | `EXPERIENCE.md` `message-agent` (l.281) "A turn-ending error is rendered as a `banner` (error) beside the avatar"; `DESIGN.md` l.1116 names it "EXPERIENCE.md's *error card*" so either term finds the other. |
| L5 | Component coverage | `empty-state`: "No illustration" vs a 32 px placeholder icon | applied | Both now say the same thing: "A 32 px icon per `DESIGN.md`, no illustration" (`EXPERIENCE.md` l.301) and "an icon, not an illustration" (`DESIGN.md` `empty-state.icon-note` l.632, l.1065). |
| L6 | Component coverage · Accessibility | Row-overflow trigger 24 vs 28 px; rail-item 48 × 46 vs 48 × 48 | applied | The Floor cites DESIGN's values: "rail-items 48 × 48; the row-overflow-menu trigger 28 (per `DESIGN.md`)" (`EXPERIENCE.md` l.523). The 48 × 46 typo is gone. |
| L7 | State coverage | No state for Home's suggested view when nothing needs attention, nor for a suspended Task Manager | applied | Home's zero case is the starter-prompt state (l.350, matrix `home`/empty l.408); the Task Manager gets its own Shell state row (l.339) plus a banner kind (l.286) and a matrix exception (l.411). |
| L8 | Visual reference | The frontmatter comment cites a working file that is not promoted | applied | The `.working/theme-lantern-tokens.md` citation is gone; the colors comment now cites "the V3 block of `mockups/color-themes-1.html`" (`DESIGN.md` l.31–34) and no `.working/` path appears in either spine. |
| L9 | Bloat | Session rows 11, 12, 14 have no user-facing state; Privilege Gating restates existing rows | applied differently | The three rows are dropped — the table's heading now reads "auth spike states 1–10 and 13; 11, 12 and 14 have no user-facing state in Release 1" (l.376). The section was **not** deleted as the fix asked: Privilege Gating was kept and rewritten as the accessibility **Mechanism** contract that H8 required (l.490–505), which the component rows now point at. |
| L10 | Bloat | Decorative asides that carry no decision | applied | Both cut — no "(12.5 px in Bridge; 12 px here)" and no vendor-Noto-Sans aside anywhere in `DESIGN.md`. |
| L11 | Inheritance discipline | The Resource editor is a dialog beyond memlog 41's five cases; DESIGN's dialog count is stale | applied | The IA row is tagged `[ASSUMPTION: a small three-field form, so a dialog rather than a route]` (l.130) and the Dialogs paragraph (l.163) enumerates the full set. `DESIGN.md` `confirm-dialog` (l.1185) no longer counts — it points at "the dialog set EXPERIENCE.md › Information Architecture › Dialogs enumerates" and lists it. |
| L12 | Inheritance · Judge | The changed-row highlight "fades and clears" vs "settles and holds" | applied | "settles to its resting tint over 2 s and holds until the next interaction" in `EXPERIENCE.md` l.333 and l.473, matching `DESIGN.md` l.937 and the `data-table` Changed row. |
| L13 | Accessibility | Zoom and reflow | applied | `EXPERIENCE.md` Floor › **Zoom and reflow** (l.524) and `DESIGN.md` **Reflow** (l.902): every height is a `min-height` outside virtualised lists, form pages / banners / transcript / proposal card reflow at any width, only data tables, the raw log and code blocks claim the 1.4.10 exception, and the panel's full-screen toggle is named as the zoom accommodation with the 720 px CSS-viewport math worked. |
| L14 | Accessibility | Browser shortcut conflicts | applied | Floor › **Keyboard shortcuts** (l.521): 2.1.4 non-applicability recorded, per-browser bindings named (Chrome macOS none, Windows/Linux Ctrl+K overridable, Firefox binds all three and is best effort per NFR-11), and the capture-while-focused justification. "Shift+Enter for a new line" is in the visible caption (l.249). |
| L15 | Accessibility | Names, roles and glyphs | applied | Floor › **Names, roles, glyphs** (l.519) carries the whole list — rail `aria-label`, `aria-current` in three places, `nav aria-label="Breadcrumb"` (l.273), the resize handle as `role="separator"` with `aria-valuenow/min/max` (l.278), `aria-expanded` + `inert` on full screen, `aria-hidden` skeletons in an `aria-busy` region, `aria-hidden` separators, suggested-view lines as buttons distinct from "Open ›", the labelled max-rows field. The Skip link is the first Tab stop (l.511, 512; `DESIGN.md` `focus-ring.skip-link`). |
| L16 | Accessibility | Tab error dot is colour-and-shape only | applied | "a tab holding errors adds ", N errors" to its accessible name" (`EXPERIENCE.md` l.296, 518) and the stepper's error step "names it in text (Material's `errorMessage`), never by its circle alone" (l.297). |
| L17 | Accessibility | Dark-mode cosmetics and two marginal passes | applied | `server-flag-badge.edge-dark` — a 1 px `on-shell` at 20 % edge in dark mode, with the 1.15:1 reason stated (`DESIGN.md` l.313, l.1002). Both marginal pairs are contrast-table rows tagged "guard with a test": the toast link at 4.64:1 dark and the selected-row name link at 4.56:1 light (l.780, 792). |
| L18 | Accessibility | Running text below the spine's own floor | applied | `DESIGN.md` `proposal-card` (l.1139): "Labels and captions are `{typography.caption}`; the agent's text is `{typography.body}` — running prose never drops below 13 px", and `agent-text-typography` in the frontmatter. |
| L19 | Judge | Countdown pressure | applied differently | The tooltip "Proposals expire so a stale diff is never applied." landed (l.220, 284) and the countdown turns `{colors.warning}` only in the last minute (`DESIGN.md` l.1142). The "Valid until 10:47" clock form was **not** taken — the owner's 10-minute window (twice the reviewed length) plus the single polite announcement at 1:00 was treated as covering the pressure the finding described. |
| L20 | Judge | Command-box vocabulary | applied | An alias list per screen drawn from the contest wording ("web apps", "REST", "x509", "certificates", "CPU", "disks") in the `command-box` row (l.270) and as item 10 of the screen contract (l.180). |
| L21 | Judge | Sign-out hunt | applied | A ▾ glyph on the status-bar user segment, "the bar's one interactive segment" (`DESIGN.md` `status-bar.user-segment-glyph` l.306, l.998; `EXPERIENCE.md` l.50, 271). |
| L22 | Judge | Avatar crop — the robot fills about half the 64 px box | **skipped** | `imports/robot-avatar.png` and `-64.png` are unchanged since 16:40, before the reviews were written (17:08); no re-crop was made and no reason is recorded. `DESIGN.md` l.722 documents a *different* cosmetic (the aqua sliver in the left eight columns), not the crop tightness. |
| L23 | Judge · Accessibility | Composer caption does not name the new-line chord | applied | One caption carries all three: "Enter to send · Shift+Enter for a new line · Ctrl+I to focus" (⌘I on macOS) — l.249, 277; `DESIGN.md` `panel` footer. |
| L24 | Judge | Panel width jump on Home transitions | applied | `panel.width-transition: '120ms ease; none under prefers-reduced-motion'` (`DESIGN.md` l.384, l.891, l.1083; `EXPERIENCE.md` l.277, 539, Floor › Reduced motion l.522). |
| L25 | Judge | Toast overflow beyond three | applied | "Stack of at most three, newest on top; **a fourth drops the oldest** (the row highlight and the transcript keep the record)" (`EXPERIENCE.md` l.287, 474). |
| L26 | Judge | `/csp/myapp` does not exist on a fresh container | noted for architecture | `[NOTE FOR ARCHITECTURE]` at UJ-3 step 1 (`EXPERIENCE.md` l.585) and on `DESIGN.md` `empty-state` (l.1065). The memlog records that PRD FR-69 already specifies a demo fixture seeding it, so the open question is whether that option is on by default in the Compose flow — an installer decision, not a UX one. |

## Still open

Three findings were skipped and two carry unrecorded remainders. A downstream reader should know:

- **M20 — a rail click still navigates nothing** (`EXPERIENCE.md` l.265). Clicking *Logs* leaves the previous screen on the content column; only VS Code users expect that, and it costs an extra click per area on the judge's thirty-minute clock. This contradicts a judge high-adjacent finding but honours the owner's own slide-out decision, so it needs an owner call, not an edit.
- **M27 — the side bar is not collapsed on navigating to Home** (`EXPERIENCE.md` l.47, 267). At 1,920 px Home reads "Home" in the locator beside the previous area's screen list, and the tile row loses 240 px. Below 1,440 px the yield order hides the symptom; at large widths it does not. No reason recorded.
- **L22 — the avatar was not re-cropped.** `imports/robot-avatar-64.png` predates the reviews; at 24 px the robot is still about half the box, so the transcript's agent mark reads as a yellow dot on teal in every screenshot.
- **H12 remainder — no inline password change.** The expired-password state still sends the user to the classic portal or the README. It is covered only by a `[NOTE FOR ARCHITECTURE]` asking the installer to unexpire `_SYSTEM`; if the installer does not, Priya's first interaction still bounces her out of the product.
- **H13 / H14 remainders — the panel is 50 vw on Home even when unconfigured**, and there is **no first-run panel message** ("I'm ready. Try one of these.", "click a row to select it, its name to open it"). A no-key voter therefore gets half the Home viewport of static example card, and the row-select-vs-name-open gesture is taught only by the link styling.
- **C3 remainder — the README install step and the smoke-script assertion** the judge asked for are recorded nowhere in the UX pair; the auditing state depends entirely on the installer honouring the `[NOTE FOR ARCHITECTURE]`.

The two items the rubric said "require a new decision from the owner" are **both resolved**: the
proposal-expiry interval is **10 minutes** (memlog decision; derived through `EXPERIENCE.md` l.220,
284, 356, 463), and the side bar is **fixed at 240 px with no drag handle**, with only its open/closed
state remembered (memlog decision; `DESIGN.md` l.215, 880, 972 and `EXPERIENCE.md` l.47, 267).
